# Session Handoff — for Codex review

> Rolling file, overwritten at each session close. Past handoffs: `git log -p docs/HANDOFF.md`.
> Written by Claude Code so Codex can review changes and reasoning at full project context.

**Session date:** 2026-09-03 afternoon (11:36 → ~16:00 ET, kept open through the ZS 16:05 ET live print). Focus: live print v2 **slice B** built subagent-driven in a sibling worktree, plus three session-start quick items; the ZS print watched in-session on the DEPLOYED (slice A) app.

## 1. Goal + exact files changed

**Branch `print-v2-slice-b` (sibling worktree `../vanguard-skin-print-v2-b`, 34 commits over `main` `18c3188`, PUSHED UNMERGED — final HEAD in §4).** Plan: `docs/superpowers/plans/2026-09-02-live-print-v2-slice-b.md`; spec §4.2. New: `lib/db/code-migrations.ts` (static registry of `.ts` migrations), `lib/db/migrations/089_print_watch_document_identity.ts` (content-identity rebuild + five sidecar tables), `scripts/migrate-089-document-identity.ts` (`--rehearse` / `--live` runner with hardened gates), `lib/print-watch/{ssrf,url-fetch,gate,delivery,pdf,roads,ir-page-adapter,ir-baseline-step,register,merge-handler,candidate-fate}.ts`, `app/api/print-watch/sources/route.ts`, tests under `tests/db/`, `tests/print-watch/`, `tests/api/`. Modified: `lib/db/migrate.ts`, `lib/print-watch/{hardened-fetch,types,store,watcher,extract}.ts`, `app/api/print-watch/{drop,status}/route.ts`, `lib/earnings/registry-bootstrap.ts` (the one slice-A file the plan's integration task permits: enables `registerPrintWatch()`), `app/dashboard/today/PrintWatchPanel.tsx` (two outcome branches + the `ir` ladder rung only), `docs/DECISIONS.md` (PDF-pair pre-registered gate; slice B deviations), `docs/reference/earnings-pipeline.md` §Print-watch (v2 roads, identity, 089 cutover order), `tests/earnings/registry-bootstrap.test.ts` (four steps → five).

**On `main` this session:** `4809db6` `qa/nightly-qa-cron.sh` — the 2 AM smoke runs once per night (archived report = per-day marker; it had run twice nightly since 2026-06-01 because the 10-minute ET gate admits two 5-minute launchd ticks) and its dead `claude -p` auto-fix block is removed (dead since 2026-05-30 — `~/.local/bin` never on that PATH; had it run it would have switched the MAIN checkout to a `qa-fixes` branch at 2 AM; the 02:45 deep-QA chain owns auto-fix). `ce36267` `docs/plans/TODO.md` reconciled. This handoff.

Live-data actions (no code): the orphan RBRK `earnings_worksheet_flags` row on superseded event 1510 deleted (cloud_outbox generation unchanged — the expected no-op); ten QA-ledger rows flipped `branch-unpushed` → `merged` for PR #62's commits (ledger is gitignored; backup beside it).

## 2. Tests / E2E / deploy result

- Branch, per task: 13 task-scoped reviews (opus for transactions/migration/watcher, sonnet for pure modules) + 8 fix rounds; controller verification at every wave HEAD. Whole-branch review (session model): no Critical, 6 Important → ONE fix wave (7 commits) → scoped re-review: all 20 findings addressed; one new Important — a literal NUL byte typed as the slot separator in the new `lib/print-watch/candidate-fate.ts` made git treat the file as binary (invisible in diffs) — fixed as a one-byte commit by the controller after the re-reviewer ruled no semantic re-review was needed.
- Final HEAD: `npx vitest run` 663 files / **7,932 passed** / 3 fixture-gated skips / 0 failed (worktree has no `.env.local`; a dummy `ANTHROPIC_API_KEY` satisfies the three key-presence tests); scoped `tsc --noEmit` clean; `npx next build` clean with the 089 code migration present in the standalone server chunks.
- Migration: `--rehearse` on a fresh `VACUUM INTO` copy of the live DB → exit 0; documents 7 → 7 (0 merged), 79 candidates kept / 0 archived, no line changes, no missing bytes, no unreadable contracts. **Cold-start proof:** the BUILT standalone server (`node .next/standalone/server.js`, `env -i`, dummy `CRON_SHARED_SECRET`/`ELECTRON_SERVICE_CRED` — the trust-boundary instrumentation fails closed on blanks) on a copy lacking 089 applied it at first open (3 → 8 `print_watch_%` tables, `schema_migrations` row recorded).
- Sandbox E2E (worktree dev server on :3095, VACUUM copy, real Anthropic key only, no outbound mail/push, TWS never contacted): 8/8 — HTML re-drop → duplicate with a second road; PDF drop → new document, both readings weak, no new green; binary drop → 400 with the reason shown; local / http / secret-bearing URLs handled with no secret in any body; `PUT /sources` valid / SSRF / clear; GET read-only; console clean.
- **Deploy: NONE.** Not merged, no Electron rebuild, Worker untouched. The live app is the 10:41 ET build (slice A). Deliberate — see §3.

## 3. Open concerns / rejected approaches / decisions

- **089 cutover order is a hard gate (whole-branch review Important 3, verified by the cold-start proof):** `lib/db.ts` runs `runMigrations` at module load, so the packaged app applies 089 implicitly on its first launch after a rebuild — without the runner's backup / holder / missing-bytes gates. Next session: merge → quit the desktop app → `lsof` clean → `VACUUM INTO data/backups/pre-089-<stamp>.db` + `PRAGMA integrity_check` → `--rehearse` on a copy → `--live` → THEN rebuild/relaunch. Never in the other order. Recorded in `earnings-pipeline.md` and DECISIONS.md.
- **R-B7 → R-B7b (ruling revised by the whole-branch review):** the remapped-candidate fate in 089 and the merge handler is keyed on (survivor document, REPRESENTATION), not document alone. `reconcile.ts::independent` treats two representations of one document as v1's legitimate repA/repB pair; the doc-only rule would have silently downgraded those pairs to `single_source` on every future date-correction merge. One shared pure helper (`lib/print-watch/candidate-fate.ts`) now serves both.
- **R-B1 — no registry shim.** Slice A was already on `main`, so B imports the real registries and enables the bootstrap call itself; plan Task 16 folded into Task 13. The slice-A DECISIONS sentence about a shim is superseded by B's entry.
- **R-B15 — `/Encrypt` is advisory; poppler decides encryption.** The raw-byte pre-check refused owner-password-only PDFs that `pdftotext` reads fine.
- Other rulings (R-B3 route tests in a new file, R-B10/R-B16 minimal Today-panel edits, R-B11/R-B14 task splits so no file ever had two writers, R-B12 v1 HTML-sniff parity, R-B17 bootstrap test count) are in the SDD ledger (copied to `docs/private/2026-09-03-live-print-v2-slice-b-sdd/`).
- **Rejected:** a lane-wide IR refusal budget (one 403'd anchor would retire the whole road on print night — per-link stands); building the shim "because the plan says so"; enabling the smoke's dormant auto-fix by fixing its PATH (it would branch-switch the shared checkout).
- **Process incident:** an implementer ran `git reset --soft` "to probe pathspec-commit behaviour" and un-committed a sibling's commit (recovered before any loss). The worktree rules now enumerate the ONLY allowed git commands.
- **Known flake, now named:** `tests/print-watch/watcher.test.ts › IR page lane › "with a step-recorded baseline…"` fails ~1 in 3–4 block runs at both the fix-wave base and HEAD (`waitUntil: the condition never became true`); the fix belongs in the shared `waitUntil`/`flushIo` test helper. Filed in TODO.
- **Deferred minors** from the whole-branch review are filed as one TODO item (watcher.ts extraction, drop-route response-shape normalisation, archive-reason alignment, SIGKILL escalation, …).
- **ZS live print (16:05 ET, deployed slice A app):** result appended in §6 below when the watch closes.

## 4. Uncommitted changes / live-process state

- `main` clean; pushed through this handoff. Branch `print-v2-slice-b` pushed to origin at HEAD `702baaf` (unmerged; no PR opened — merge is a next-session decision after the ZS review). Worktrees: `../vanguard-skin-print-v2-b` (slice B, keep until merged), `../vanguard-skin-qa-fix` (the nightly fixer's parked worktree, leave alone).
- No dev servers running (the :3095 E2E sandbox was stopped by PID). No stray Chrome/Playwright. Electron app on :3099 = the 10:41 ET build; Worker unchanged; KV `armed-events` generation 1.
- SDD ledger/briefs/reports: `../vanguard-skin-print-v2-b/.superpowers/sdd/2026-09-02-live-print-v2-slice-b/` (git-ignored scratch) — copied to `docs/private/2026-09-03-live-print-v2-slice-b-sdd/` before the worktree is removed.

## 5. Claude session link

https://claude.ai/code/session_01GvaNmmYtnpzjprfCjuTWcL

## 6. ZS live watch (appended after the print)

**Third live run — 2026-09-03 ZS (AMC 16:05 ET) — FULL SUCCESS on the deployed slice A app, no drop-zone recovery needed.** Window opened 15:55 ET with all lanes healthy (DJ wire on with the backfilled contract id, EDGAR polling, RSS idle for the symbol). 16:05 ET: the DJ wire delivered the press release; five lines went to the provisional flash lane. 16:06 ET: dual-parse complete, all five lines `agreed` from that single document's two-reading pair (the exact v1 pair the R-B7b ruling protects). 16:08 ET: all five lines accepted on Today. 16:09 ET: the EDGAR lane delivered the EX-99 exhibit four minutes after the wire — the 9/2 acceptance-time fix held on its first live test — and by 16:10 ET it had parsed with every line still `accepted`: each line now carries four value candidates across two documents with exactly one distinct value (wire and filing unanimous; nothing superseded, no conflict, the per-candidate accept path was not needed). Promote stamped manual actuals at 2026-09-03 UTC; enrichment completed by 16:12 ET; the recap email was claimed by the sweep at 16:13 ET and SENT at 16:18 ET with no error (thirteen minutes after the wire print, six after Promote); the TWS reaction snapshot had not yet landed at send time (it fills on a later tick — check tomorrow that it did and that the recap's reaction line was not blank). Observations for the backlog: the in-process watcher writes nothing to the packaged app's `server.log` (acquisition timing is visible only in the DB / status route); the sheet still has no line for the metric the name trades on (product-revenue guidance) because lines derive from bogey rows — both already in TODO.

## 7. Session continuation — slices C + D built in parallel (2026-09-03 evening → 2026-09-04 ~00:45 ET)

After the ZS close-out the user chose "C and D in parallel". Both plans were written (D by a forked pass), Codex-reviewed once each (C: 18 findings folded; D: 29 findings → 25 folded, 1 scoped, 2 PARTIAL disputes recorded for the user: Codex #20 — bogeys/actuals/deltas render as public market data with sheet parity, not masked; #26 — no fake-model seam in shipped code, one live-model sandbox smoke instead), committed on main (`b386fb2`+`5355024` C, `85d5713` D) with the TODO reconciled (`15dc0b2`). Two sibling worktrees stacked on `print-v2-slice-b` @ `702baaf`.

**Cutover order CORRECTED (Codex D #23 + the C Task 1 review):** `scripts/migrate-089-document-identity.ts --live` refuses while any migration above 089 is pending, so the order is merge B → 089 cutover from a checkout holding nothing above 089 → merge C → merge D (rebased onto C) → rebuild. Never merge C or D before `--live`.

**Slice C — DONE, `print-v2-slice-c` @ `c37ed1e`, PUSHED UNMERGED** (25 commits; worktree `../vanguard-skin-print-v2-c`). 9 tasks in 4 parallel-safe waves (one owner per file), 9 task reviews + 8 fix rounds, whole-branch review (session model): 1 Critical — a go press that ARMED the event inside the live window ran the `ir_baseline` prepare step and recorded tonight's release link as baseline, filtering it out forever; 2 Important — a press/extend from another process waited for the owner's next reconcile (up to 15 min), and a disarm after a press was ignored for the forced window; → one fix wave (two rounds), scoped re-reviews clean. Rulings R-C4..R-C19 (highlights: R-C4 the window pools min/max per spec §4.3 — the plan TEST had the typo; R-C5 `hardenedFetchText` cancels abandoned bodies on all eight early exits and the scheduler releases a slot at headers for status ≥ 300 — two ordinary EDGAR 404 polls would otherwise have held the SEC lane 120 s at the print minute; R-C7 C's merge handler registers BEFORE B's because go rows have no cascade; R-C8 a settled pass aborts its signal; R-C12 a road's report settles at DELIVERY, the 15 s road timer covers acquisition only, parse runs outside the race; R-C15 `ir_baseline` is a no-op once the window is open; R-C16 the dispatcher reconciles unplaceable work; R-C17 the user's disarm wins). Evidence: suite 8,073 passed / 0 failed at the final head (3 timeouts in `tests/digest/send-digest-race.test.ts` under concurrent load — solo 5/5 ×3, no branch commit touches digest), scoped `tsc` clean, `next build` clean with the pre-existing Turbopack tracer warning removed (`lib/print-watch/storage-path.ts`), 090 applied by the BUILT standalone server cold-starting on a copy at 089 (both columns + the go-requests table, 0 non-NULL, FK/integrity clean), sandbox E2E on :3095 6/7 exact — Go → `window_open` within 2 s with per-road outcomes (DJ skipped: TWS offline), Extend ×2 = +60 min, repeat Go = new request / same stamp / same end, HTML drop → `user-drop` document, console clean, status GET idempotent; the seventh step: the built code refuses a secret-bearing link BEFORE any fetch (stricter than the plan's post-fetch expectation) and the secret reached no payload. Ledger/briefs/reports archived to `docs/private/2026-09-03-live-print-v2-slice-c-sdd/`.

**Slice D — controller run (fable general-purpose agent) in `../vanguard-skin-print-v2-d`, branch `print-v2-slice-d`.** All 12 tasks built (17 commits at `98b3a64`) and the whole-branch review completed before the account spend limit killed the first controller mid-verification (~22:40 ET; the user raised the limit 09-04). Review verdict "with fixes, one wave": C1 the `"use client"` first-pass component imported server modules (node/DB/AI) → `next build` failed — ruled R-D20 (client-safe `first-pass-format.ts`); I1 slice B's exhaustive print-watch table-list test needs the two 091 tables; I3 no read trigger on accept (facts are accepted-only, so the parse hook always found no facts) → R-D21 option (a): B's accept route calls `scheduleFirstPassRead` post-commit — the first read follows the desk's first accept within seconds (flag for the user); I4 D imported the recap composer's module → R-D22 lazy seams + local intel fields, never import `lib/digest`; R-D23 no personal path in the rehearsal script; R-D24 unit agreement at callout eligibility; R-D25 a merge note when a donor accept is dropped. A fresh controller is running that fix wave, its re-review, and Task 12 (tsc, build, full suite, 091 rehearsal on a copy, sandbox E2E on :3094 with one live-model read). Final numbers are appended below when it reports; the branch is pushed unmerged at the end.

**Process notes:** background subagents' completion notices were routed to the main session instead of the delegated D controller, which ended its turn waiting — the restarted controller blocks on `TaskOutput` and the main session forwards stray notices. The TODO-gate hook fired twice on doc heredocs naming the deploy script; both times the precondition was satisfied (TODO reconciled) rather than evaded. Real ticker/ids from the sandbox copy appear only in gitignored reports.

### 7b. Slice D — final (2026-09-04 ~15:45 ET)

**`print-v2-slice-d` @ `4c33e361`, PUSHED UNMERGED** (29 commits over `702baaf`). The delegated controller died TWICE on the account spend limit (once mid-verification 09-03, once mid-E2E 09-04); the session controller took the build over directly after the second death — no task was re-dispatched, the worktree ledger carried the state.

Fix wave (four rounds, every re-review clean): C1 the plan's own `"use client"` imports pulled `node:child_process`/`@stoqey/ib` into the Today bundle and broke `next build` → R-D20 client-safe `first-pass-format.ts` plus a static boundary guard; I1 slice B's exhaustive print-watch table list gained the two 091 tables; I3 → **R-D21: B's accept route now schedules the first-pass read**, so the first read follows the desk's FIRST ACCEPT within seconds (facts stay accepted-only per Codex #2 — this is the one ruling that changes product behaviour and wants the user's confirmation before the next live print); I4 → R-D22 lazy scheduler seams + three intel fields re-implemented locally, so print-watch never imports `lib/digest`; R-D23 the rehearsal script derives its refused path from the repo's own resolver instead of a hardcoded personal path; R-D24 unit agreement at callout eligibility; R-D25 a merge note when a donor accept is dropped; R-D33/34/35 the number gate checks the union scoreboard, bogey and delta cells mask, the panel note clears; F2 the attempt cap keeps the causal error code; F10 `activeRead` means live work only; **R-D36** — the live sandbox had failed ELEVEN consecutive reads because validation demanded exactly three surviving call-watch lines: those lines are forward-looking and cite nothing, so cites are optional there, 0–3 survivors finalise `done`, and a zero-survivor read carries a caveat; R-D37 the note clears on a failed attempt too.

Evidence at HEAD: suite 677 files / **8,064 passed / 0 failed**; `tsc` clean for every D-owned file (20 errors remain in four files the branch never touches — `tests/api/service-auth-consolidation`, `tests/gmail/empty-enrichment-guard`, `tests/http/apiFetch`, `tests/mutations/securities`, 0 commits in range touch them); `next build` clean; `scripts/rehearse-additive-migrations.ts` PASS on a fresh 089 copy (row counts, `sqlite_sequence`, indexes, additive DDL, exact `schema_migrations` gain, FK check, integrity); sandbox E2E on :3094 — **phase A3 7/7 with one REAL model call** (read id 12, 9 read lines, 3 call-watch lines, 4 caveats, 2 callouts proposed; accept → accepted → un-accept → proposed; privacy masked prose/call-watch/caveats/callout labels/`vs_bogey_text` and the bogey + delta cells while actual + verdict stayed readable; canaries 0/0, no `notes` key) and **phase B 4/4** (read id and `generated_at` identical across a server restart, `activeRead` null, callouts unchanged, and after 75 s the reconcile did not regenerate an unchanged fingerprint). Ledger, briefs, reports, E2E report and screenshots archived to `docs/private/2026-09-03-live-print-v2-slice-d-sdd/`.

**Next session, in order:** (1) merge `print-v2-slice-b`; (2) the 089 cutover from a checkout holding nothing above 089 — quit the desktop app, `lsof` clean, `VACUUM INTO data/backups/pre-089-<stamp>.db` + `PRAGMA integrity_check`, `--rehearse` on a copy, then `--live`; (3) merge `print-v2-slice-c`; (4) rebase `print-v2-slice-d` onto C and merge — expect small textual conflicts in `lib/print-watch/register.ts` (keep BOTH C's and D's registrations above B's), the status route's field map, and `PrintWatchPanel.tsx` (props/imports/mount); (5) rerun on the merged tree: the 090→091 rehearsal, `next build`, and the E2E; (6) rebuild the desktop app. Owed decisions: R-D21's accept-triggered read, and the two recorded Codex disputes (bogeys/actuals/deltas render as market data with sheet parity; no fake-model seam in shipped code).
