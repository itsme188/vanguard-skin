# Session Handoff — for Codex review

> Rolling file, overwritten at each session close. Past handoffs: `git log -p docs/HANDOFF.md`.
> Written by Claude Code so Codex can review changes and reasoning at full project context.

**Session date:** 2026-09-04 17:30 ET → late evening. Focus: plan, build, review and land the last two live-print-v2 slices — E (outputs are buttons: the post-print paper sheet, the canonical send service, the send-recap gate) and F (Today layout, the Hub live controller, the in-place print expansion, desk-defined extra metric lines) — in parallel, with the session model used for orchestration and review only. Both are MERGED on local `main` (`d7ea64dc`), verified on `main`, NOT pushed and NOT deployed.

## 1. Goal + exact files changed

Process: the session wrote a cross-slice contract and two briefs; Opus writers produced the plans (the writers corrected four wrong code assumptions in the briefs); one Codex round each (16 + 18 findings) plus the session's own reviews were ruled on and folded by Opus; two Opus controllers ran SDD in sibling worktrees; the session took both whole-branch review seats and merged.

Commits on `main` since `31d0e84f` (all local):

- `96ab07eb` — the E/F contract (`docs/superpowers/plans/2026-09-04-live-print-v2-outputs-contract.md`) and the F plan; `a2c647f5` — the E plan.
- `0bbe79e9` — **merge slice E** (20 commits, 63 files, +10,429/−802). Creates `lib/earnings/{email-states,send-service,recap-nudge-gate,print-outputs,post-print-sheet,print-ladder}.ts`, `lib/digest/print-watch-read-block.ts`, `lib/db/migrations/092_earnings_email_delivery_states.sql`, `app/api/print-watch/{print-sheet,send-recap}/route.ts`; rewrites the claim section of `lib/digest/send-earnings-email.ts`; the sweep loop, the debrief, the reporter recap, every `error`-sentinel reader (Mac + Worker), the status route (`outputs`), the manual email route (`markDelivered`), a nominal brand on `DirectionSafeFacts`.
- `d7ea64dc` — **merge slice F** (27 commits, 59+ files, +9.4k/−2.4k). Creates `app/dashboard/today/{EarningsHubLive,LivePrintRow}.tsx`, `app/dashboard/today/{hub-live,live-print}/*`, `lib/print-watch/{extra-metrics,recompile}.ts`; DELETES `EarningsCockpit.tsx` and `PrintWatchPanel.tsx`; rewrites `page.tsx`, `EarningsHub.tsx`, `EarningsRowChips.tsx`, `BogeysEditModal.tsx`; extends the bogeys route/mutation, the cockpit query, `earnings-intel`'s `allCockpitRows`, the sources route (+GET), `contracts.ts`, one function in `watcher.ts` (`writeLines` in one immediate transaction), and adds a retired-line guard to the accept route. Two doc conflicts on the merge (DECISIONS, TODO) resolved keep-both.

## 2. Tests / build / deploy result

| Check (on `main` @ `d7ea64dc`, from the MAIN checkout) | Result |
|---|---|
| Full suite | **717 files passed, exit 0**; the live DB's mtime/size identical before and after (`1788561150 168931328`) |
| Worker suite | 36 files passed |
| `tsc --noEmit` | 20 errors, the documented baseline (same four untouched test files) |
| `npm run build` | compiled successfully, exit 0 (live DB untouched) |
| Migration 092 rehearsal on a fresh `VACUUM INTO` copy | **PASS** — `column-append: earnings_emails`, seq/indexes/FK/integrity all pass |
| Slice E sandbox E2E (worktree, :3095) | parts A and B every step PASS — a REAL printed sheet, ONE real recap email to the user's own address, real KV markers via a local Worker |
| Slice F sandbox E2E (worktree, :3094) | browser 9/9 PASS with privacy mode on at 390/768/1280-rail/1440 + the curl half |
| Integration E2E on `main` (:3095, sandbox copy, secretless, privacy mode on) | **every cross-slice step PASS** — `outputs` present on the status payload; `Print sheet` → 200 (one real sheet printed, 2 pages); `Send recap now` → 200 `failed — 401 … API key is invalid` rendered verbatim, claim released, state still `unsent`; a value-less print shows both buttons disabled with both reason sentences as text; the `delivery unknown` chip is warn/`?`/clickable and the viewer GET carries `deliveryState`; masked Δ neutral (control: unmasked = green); mobile expansion under the card; hidden tab 87 s → zero requests on all four streams, one each on return; 092 also applied cold in-process on the copy. Live DB stat identical before/after. **Two defects found and fixed on `main` afterwards** (see §3): the viewer dropped `deliveryState`, and `IrPageField`'s labels overflowed 24 px at 390. |
| Deploy (session-end, 2026-09-05 evening ET) | **Worker deployed first** (version `0f5d9149`, the `fallback-earnings.ts` live-claim filter), then **`npm run electron:deploy` exit 0** — notarization successful, `verify-bundle: OK`, installed and relaunched 20:46 ET. Post-deploy: `schema_migrations` top = `092_earnings_email_delivery_states.sql`, both new columns present; Today **200** with a minted QA session (revoked after; 401 confirmed); `GET /api/print-watch/status` `success:true` with zero live prints (nothing armed until ORCL 2026-09-07 16:15 — the `outputs` field is first observable then). `main` pushed to `origin` at `1603561e` before the deploy. |

## 3. Open concerns / rejected approaches / decisions

- **Codex disagreements recorded for the user (E):** R-E1 the first-pass read's prose lines DO feed the recap block (callouts' figures do not); R-E6 KV markers stay fail-open (no load-bearing acknowledgement, no Mac/Worker reservation protocol); R-E8 no delivery-attempts table — `earnings_emails` stays one current-state row per (event, phase). Each reverts in a few lines.
- **User question (F, R-F25):** under privacy mode the masked Δ column also renders NEUTRAL (no green/red). Kept because the colour is the delta's sign; one ternary to restore the direction colour.
- **Design facts that changed on contact with the code (both recorded in DECISIONS):** at 1280 with the chat rail open the Hub is ALREADY the mobile layout (`globals.css` 768–1535 band); there is no grid to span, the expansion is a block sibling; the status route carries no prepare state, so the prepare line reads the worksheet route; `PUT /api/print-watch/sources` had no UI until F; retirement of a line is a RENAME because the PK is `(print_id, metric_id)`.
- **Defects the process caught before merge (worth knowing they were real):** the planned `@ts-expect-error` boundary was structural and would have failed `tsc` (fixed by a nominal brand); the reaper's flip had an ABA race (token+timestamp CAS now); a rejected manual refire destroyed the delivered row's provider evidence (restore now covers all four columns); the rehearsal script's column-append classifier was wrong for a column added before a trailing table constraint (`earnings_emails` ends in `UNIQUE(...)`); a retired line's candidate pool leaked onto its replacement (pool re-tagged on rename); the accept route could accept a retired line's candidate (guarded); a `","` typed as a bogey became `$0` (strict parser); the expansion reducer defeated its own print-id rule (fixed); the cockpit stream's first-run logic was inverted (trigger-keyed now); the stage-chip lane overprinted the Δ column on 4 of 5 rows at 1440 and 1920 (measured, fixed).
- **Two cross-slice defects the integration E2E found on `main`, fixed in two follow-up commits `d1e7288f` and `15e6ea24` (TDD, `react-dom/server` render tests + source pins):** (1) `app/dashboard/components/EarningsEmailViewer.tsx` never read the additive `deliveryState` the email-content route returns, so a `delivery_unknown` email opened with the header "Sent … to …" and no caveat — the viewer is owned by neither slice, which is exactly why neither sandbox could see it; it now says "Delivery unknown" with the reconciliation sentence. (2) `IrPageField`'s two flex-column labels lacked `min-w-0`, giving a 24 px horizontal page scroll at 390 whenever a row was expanded.
- **Known flake (not this work):** `tests/print-watch/watcher.test.ts` fails a different case under full parallel load, passes 100/100 in isolation; filed since slice A.
- **Deferred, in TODO:** `lib/calendar/cloud-reconcile.ts` can overwrite a locally promoted `actual_value` (no `manual_actuals_at` guard); the alerts Emails tab does not surface `delivery_unknown`; a self-booked unknown in the 07:45 debrief fires no Pushover; the ambient-notes FAB covers a Hub card's delete control at 390 (pre-existing); the orphan "live prints outside this week" block names no date; `merge-handler.ts`'s donor path re-tagging (proven unreachable today).
- **Rejected:** a delivery-attempts table (schema beyond 092's reservation); load-bearing KV markers (contradicts the Mac-is-truth architecture); rebasing F's 27 commits (merged instead — the overlap was four doc files).

## 4. Uncommitted changes / live-process state

`main` = `origin/main` (this handoff commit on top of `1603561e`); tracked tree clean; untracked `docs/CODEX-CLAUDE-COORDINATION.md` belongs to a concurrent Codex session the user authorized (its worktree `/private/tmp/portfolio-desk-astra-2026-09-04`, branch `codex/shabbos-reliability-2026-09-04`, port 3093, non-earnings files only; both landings are noted in that file for it to rebase onto). Worktrees `../vanguard-skin-print-v2-e` (`print-v2-slice-e` @ `25c5d665`) and `../vanguard-skin-print-v2-f` (`print-v2-slice-f` @ `1f59d85c`) are clean and fully merged — safe to remove with their branches when the user says so (not auto-removed per the checklist); their ledgers are archived under `docs/private/2026-09-04-live-print-v2-slice-{e,f}-sdd/` and the integration run under `docs/private/2026-09-04-live-print-v2-integration-e2e/` (all gitignored). `../vanguard-skin-qa-fix` is the nightly fixer's (now at `1603561e` detached) — untouched. The desktop app on :3099 runs the 2026-09-05 20:46 ET build of `main` @ `1603561e`; the live DB is at migration 092 (applied by the packaged app on first launch, exactly as rehearsed); server log clean since the relaunch. Open PRs #64, #65 and the new #66 (nightly QA) are unreviewed. Nothing is armed for earnings until ORCL on 2026-09-07 16:15 — the first live print on the complete v2 pipeline.

## 5. Claude session link

https://claude.ai/code/session_01SHUM6GCPDui3xocDhHhzTQ
