# Session Handoff — for Codex review

> Rolling file, overwritten at each session close. Past handoffs: `git log -p docs/HANDOFF.md`.
> Written by Claude Code so Codex can review changes and reasoning at full project context.

**Session date:** 2026-09-02 night → 2026-09-03 early morning. Focus: build live print v2 **slice A** end to end from the reviewed plan, in the sibling worktree `../vanguard-skin-v2-a` on branch `live-print-v2-a` (subagent-driven development: one fresh implementer per task, task-scoped review + fix rounds, one whole-branch review, one fix wave).

## 1. Goal + exact files changed

**MERGED to main locally at `1b53b15` (merge commit, 31 branch commits, 87 files, +9408 / −559) on the user's instruction; NOT pushed, NOT deployed (see §4).** Merged-result verification: Worker suite 530/530 and Worker `tsc` clean; the main suite passed 7515 with ONE failing test in each of two full runs, a different test each time, both in `tests/print-watch/watcher.test.ts`, a file this branch never touched. The same content passed four full runs in the clean worktree; the file passes 49/49 in isolation in both checkouts and under a 119-file partial load. Cause: the main checkout's `data/print-watch` fixtures enable three env-gated print-watch tests that only run there, and they interleave with the watcher's timing assertions under full parallel load. Filed as a TODO follow-up (pre-existing flake), not a merge defect.

What slice A delivers (spec `docs/superpowers/specs/2026-09-02-live-print-v2-design.md` §4.1, §5 088, §6, §7, §8 A-line; plan `docs/superpowers/plans/2026-09-02-live-print-v2-slice-a.md`):

- **Armed is an event fact.** `isEventArmed` / `getArmedEventIds` / `getArmedSymbolsInHorizon` (`lib/queries/earnings-worksheet-flags.ts`); `coveredForEvents` / `coveredForEvent` (`lib/queries/briefing-symbols.ts`) = held/watchlist family-aware OR the event (or an unsuperseded same-symbol, same-date twin — ruling R11) carries a worksheet flag. `SymbolStatus` gains a DISPLAY-ONLY `"armed"`. Eight selection consumers rewired (`enrichment-runner`, `extract-newsletter-bogeys`, `bogeys-reminder`, `verify-earnings-dates`, `wire-probe`, `wrap`, `debrief`, `earnings-cockpit`); the three push gates untouched; a repo guard (`tests/repo/symbol-status-consumers.test.ts`) classifies every call site. Chips: `app/dashboard/today/status-chip.ts` (pure; from `EarningsHub.tsx`), `EarningsCockpit.tsx` (accepted deviation D3b), `lib/digest/todays-reporters.ts`, Worker mirror.
- **ET day math** in the four UTC consumers (TODO 68 closed).
- **Migration 088**: `earnings_prepare_steps`, `earnings_bogey_scans`, `cloud_outbox`; `earnings_bogeys` rebuilt with `'finnhub'` source, `eps_consensus_vendor`, `extra_metrics_json` (D1: vendor EPS never enters `eps_consensus`); `sqlite_sequence` carried exactly.
- **Cloud parity**: `lib/earnings/armed-events-projection.ts` (full armed list within a 14-day lookback — R23 — plus D7 tombstones), `lib/earnings/cloud-outbox.ts` (in-transaction writer, D10 no-op, generation-ordered drain, 2 s whole-wait post-commit cap R9, wedge detection), the sweep-tick reconcile write (R8), tombstones on both delete paths, snapshot v11 (`scripts/snapshot-state-to-r2.ts`, one read transaction), Worker `POST`/`GET /internal/armed-events` behind the shared `/internal/*` gate, resolver `workers/cron/src/armed-events.ts` (strictly-greater apply, projection-owned-field merge, degraded-v10), consumers on the effective collection (D5: push gate untouched), parity tests for the projection key set and the chip.
- **Merge registry** `lib/earnings/event-merge.ts` (cross-slice contract; five built-in rules; preview plausibility gate on every path — R15) wired into `correctEarningsEventDate`, `reconcileEarningsDates`' apply, and BOTH delete hand-backs (R12/R12b); repo guard pins the bogey merge column lists against the live schema (R17).
- **Prepare registry + runner** `lib/earnings/prepare-armed-event.ts` (cross-slice contract + additive `signal: AbortSignal` — R13; CAS claims; 5-attempt cap incl. takeovers — R14; 4-min per-step deadline; fingerprint-first loop so drift revives capped rows; 5-min pass budget — R26), `lib/earnings/registry-bootstrap.ts` (lazy composition root), four steps under `lib/earnings/prepare-steps/` (consensus_row with a tri-state vendor read, intel with a post-condition — R19, con_id with TWS-down = pending, newsletter_rescan with a claim ledger, already-extracted skip — R20, per-pass budget — R22); worksheet route arm enqueues + kicks; the sweep runs the pass AFTER the Mac-aliveness marker (R26).
- **Newsletter path**: shared `callAndStore` helper for the global scan and the per-event path; issue-dated labels [C-3]; bogey reads ordered by issue date (R21).
- **Docs**: `docs/DECISIONS.md` (one 2026-09-03 entry), spec pointers, contract doc (`signal`, constants, slice B shim rule), `docs/reference/earnings-pipeline.md` (new subsection), `docs/reference/cron-and-workers.md` (§15), `docs/plans/TODO.md` (slice A → implemented, pending merge; item 68 closed; follow-ups).

Every controller ruling (R1–R26) with its "cost if wrong" is in the gitignored ledger copy `docs/private/2026-09-03-live-print-v2-slice-a-sdd-ledger.md` (copied into the main checkout's gitignored `docs/private/`; the worktree and the SDD workspace were removed after the merge) and summarised in the session's final message; the merge-relevant ones are in `docs/DECISIONS.md`.

## 2. Tests / E2E / deploy result

- **Ladder at HEAD `eaf6948`**: main suite 633 files / 7515 passed / 3 skipped / 9 todo (0 failures; was 7292 at `bc1ec51`); Worker 36 files / 530 passed; Worker `tsc` clean; main `tsc` errors only in the 4 test files that already fail on main; `next build` compiled (one warning: the output tracer touched a `workers/cron/.wrangler` local-KV blob deleted mid-build — see §3).
- **Migration 088 rehearsal** on a VACUUM copy of the live DB: orphan pre-check 0; per-row, all-18-column digest identical before/after the rebuild; `sqlite_sequence` unchanged; all three indexes present; `PRAGMA foreign_key_check` empty; `integrity_check` ok. The rehearsed SQL is the final SQL.
- **Sandbox E2E** (secretless `:3095` from the worktree, DB copy, outbox → local `wrangler dev` with a sandbox secret): arm an unheld manual event → four prepare rows (con_id `pending` "TWS offline", attempts 0 across two sweep ticks); outbox drained; Worker GET twin shows the generation; date correction carried the flag, the step rows and a tombstone; Worker down → unsent row with `send_error`; Worker poisoned to a higher generation → `send_error` "… worker holds generation X > local Y — KV key armed-events needs a reset" and the drain stopped; after the KV reset it drained; the 14-day horizon cut the payload from 22 live entries to 8; Today renders the ARMED chip (browser check, zero console errors); `research_articles.bogeys_scanned_at` untouched by the per-event path. What the secretless sandbox cannot prove: the newsletter model path (unit tests + one supervised arm after deploy, plan [C-18]) and a real R2 upload.
- **Whole-branch review** (most capable model): no Critical; five Important findings, all fixed in one wave and re-reviewed (see the ledger for the verdict line).
- **Deploy: NOT done.** Nothing on main changed. The running Electron app (`:3099`) and the deployed Worker are the 2026-09-02 19:43 ET build.

## 3. Open concerns / rejected approaches / decisions

- **Post-merge deploy order (cron-and-workers §15):** deploy the Worker → run `scripts/snapshot-state-to-r2.ts` once by hand so a v11 snapshot exists (until then the Worker degrades to held+watchlist) → the Electron deploy chain → arm one real upcoming event and confirm `cloud_outbox.sent_at` is stamped with no `send_error`, and the Worker's GET twin shows that generation. The `consensus_row` fingerprint version was bumped, so every existing prepare row re-runs once (idempotent upsert).
- **The live DB carries ~20 August worksheets that were never disarmed.** R23 keeps them out of the cloud payload; on the Mac they still show as armed on their (past) rows. A one-time user-run disarm of past worksheets is a TODO follow-up.
- **Worker digest subrequest budget** now sits at 48 + Resend = 49 of 50 (R25: the KV read is v11-only and `MAX_ARTICLES_PER_RUN` gives one slot of headroom). Recount if sources are added. CAVEAT from the fix-wave re-review: that arithmetic (pre-existing) counts neither the snapshot's R2 read nor the Gmail token KV read / refresh POST — if Cloudflare counts binding calls as subrequests, the real headroom is at or below zero (the wave still reduced the count by one). Recount before adding anything to that path.
- **Bundle gate follow-up:** `next build`'s output tracer copies `workers/cron/.wrangler/state/**` into `.next/standalone` when that directory exists (I ran `wrangler dev` in the worktree). Add `workers/cron/.wrangler` to `scripts/verify-bundle.js`'s leak list / the electron-builder filter, and never run `wrangler dev` in the main checkout before a deploy.
- **Data-quality observation (backlog):** `securities` holds IBKR ForecastEx prediction contracts typed `'Stock'` whose symbol is the contract question text (spaces, punctuation); one rendered on Today as a multi-word "ticker" in the sandbox. Worth a symbol-shape guard.
- **Behaviour change to know about:** an armed, unheld name that also has a read-through pair now leaves the pre-open reporter road and waits for the recap road (armed = held shape). Documented in earnings-pipeline; watch one live run.
- **Rejected:** flipping the bogey write order to fix the email's issue ordering (would break `compileContracts`' rowid rule — the READ was reordered instead, R21); a bare-label backfill migration for pre-[C-3] newsletter rows (UNIQUE-key rewrite; needs its own review — TODO); single-sourcing the merge's bogey column list with the upsert's (`CONTENT_COLUMNS` drives upsert semantics — slice F's call; a live-schema guard test pins both instead).
- **Slice B integration (whichever merges second):** swap `lib/print-watch/registry-shim.ts` imports to `@/lib/earnings/event-merge` / `@/lib/earnings/prepare-armed-event`, enable `registerPrintWatch()` inside `bootstrapEarningsRegistries()` — B's `register.ts` must perform NO registration at module top level (TDZ on the cycle) — and land the cross-slice test. `PrepareStepContext` now carries `signal` (additive; B's `{ now }`-typed steps stay assignable).
- **Hook note:** the TODO-gate PreToolUse hook blocked one docs heredoc that mentioned the deploy script name; complied via the file editor.

## 4. Uncommitted changes / live-process state

- Main checkout at the merge commit `1b53b15` (+ this handoff amendment); working tree clean; NOTHING PUSHED (origin/main is still `bc1ec51`). Worktree `../vanguard-skin-v2-a` removed and branch `live-print-v2-a` deleted after the merge. The nightly fixer's `../vanguard-skin-qa-fix` worktree remains as before.
- The sandbox dev server (`:3095`) and the local `wrangler dev` (`:8787`) started for the E2E are killed at session end; the sandbox DB copy and screenshots live only in the session scratchpad.
- Electron app running unchanged (`:3099`); nightly chain unchanged.

## 5. Claude session link

https://claude.ai/code/session_01J4HumdJ9ZHaUaydh1FkaKY
