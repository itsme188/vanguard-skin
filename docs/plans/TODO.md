# Vanguard Skin — TODO

> **In-repo shortlist — open items only.** Trimmed 2026-07-08: everything closed through that date (all session close-out blocks, ✅ entries, and the stacked prior headers from April–July 2026) moved verbatim to [archive/TODO-closed-2026-07-08.md](archive/TODO-closed-2026-07-08.md). Session-end reconciliation keeps working as before: close items in place / append a "Closed this session" block at the bottom; migrate those blocks to a fresh archive snapshot at the next trim.
>
> - Closed archive (Apr–Jul 2026): [archive/TODO-closed-2026-07-08.md](archive/TODO-closed-2026-07-08.md)
> - v2 build log (Mar–Apr 2026): [archive/TODO-v2-complete-2026-03-30.md](archive/TODO-v2-complete-2026-03-30.md)
> - Master roadmap (off-repo, session-driven): `~/.claude/plans/last-session-session-summary-eventual-ripple.md`
> - Live topic index: the Claude memory at `MEMORY.md` (per-project auto-memory)

---

## User roadmap (added 2026-07-03)

> R1/R1b (away-from-home refresh), R2 (long-email ingestion), R3 (Defense/Hedging tab), and the earnings skip-touch fix all shipped — see archive.

- [ ] **[R4] Stock donation tracking (donor-advised fund)** — user donates appreciated stock from Vanguard to a DAF; on statements these appear as share TRANSFERS OUT, never sales — and per user, an outbound security transfer is ALWAYS a donation (clean classification signal). Build: recognize/tag outbound share transfers as donations at import, then a monthly-scale tracking surface (which stock, shares, FMV at transfer date, cost basis, embedded gain avoided, per-month + YTD totals — the tax-relevant numbers). Decide: new transaction type vs tag on TRANSFER_OUT; where the surface lives (Accounts? own tab? Analysis sub-view). Feature — brainstorm + design first.
- [ ] **[R6] Full icon rebrand** (2026-07-15) — new Portfolio Desk mark applied everywhere: Electron app/DMG icon (`build/icon.icns`), menu-bar tray template (`public/tray-iconTemplate*.png`), favicon, iOS home-screen touch icon. Needs a design decision first (user has no asset yet). Small-medium once the mark exists.
- [ ] **[R7] ATH hedge reminders** (2026-07-15) — "next time X is at an all-time high, remind me to hedge with Y" (e.g. SMH ATH → short/puts via NEOS). Natural fit: new `ath` `price_source` in the existing security-levels system, resolved from `ohlcv_bars` like MA levels; the user's hedge instruction becomes the alert's action text. Brainstorm-first (ATH definition window, re-arm semantics after a hit, watchlist coverage).
- [ ] **[R8] Desearch MCP (Twitter/X research)** (2026-07-15) — scope TBD: research-feed peer to Gmail newsletters vs chat-side tool vs both. Scoping session first.
- [ ] **[R5] Trade thesis at entry — capture, store, resurface at close, grade against it** — today the thesis is only collected at trade-review time (after close), which is retrospective and dishonest to grade. Want: when a NEW position is opened (detected at import/TWS sync — same detection family as `detectNewTradeReviewPeriods`), prompt the user for the entry thesis in that month; store it keyed to the position/lot; when the position closes (possibly years later), pull the original thesis back up, collect the exit rationale, and have the trade-review grader evaluate the round-trip against the ORIGINAL thesis. Existing hooks: `trade_thesis` note type, trade-review Q&A phase, GroupedTrade abstraction. Feature — brainstorm + design first (schema: thesis ↔ open-lot linkage surviving partial closes).

## Earnings (season-timed — banks kick off 2026-07-14)

- [x] ✅ **Earnings feedback backlog (user review 2026-08-02) — ALL ITEMS SHIPPED** — from the full-pipeline walkthrough session; #1 (date verification), #2 (PRLB pair), #4 (morning debrief) shipped same day; #8 (drop share counts) 8/02 evening; #7 (fix-date chip), #5 (sheet expected-move priority), #3 (reporter recap), #6 (printable worksheet) all 8/03. See the closed-session blocks.
- [x] ✅ **Worker parity follow-ups from the 8/02 build — SHIPPED 2026-08-02 evening** (`edad759`, batched with feedback #8 per spec `docs/superpowers/specs/2026-08-02-outbound-privacy-parity-design.md`): (a) Worker wrap is suppress-but-never-send (skip reason `wrap-suppressed-for-debrief`, no markers — heavy-night names roll into the Mac morning debrief; `runSlotWrapSend` deleted, cluster build is membership-only/no KV probes); (b) Worker `parseSourceKey` routes `manual:SYM:DATE:earnings` down the Finnhub road (mirrored test); (c) debrief recipient resolution moved above the day-key stamp (`skippedReason: "no-recipient"`, day retries once env fixed).
- [x] ✅ **FIFO negative-holding-period audit + FULL IBKR LEDGER REBUILD — EXECUTED 2026-08-03 evening** (user chose option (a) full rebuild; branch `ibkr-ledger-rebuild`, plan `docs/superpowers/plans/2026-08-03-ibkr-ledger-rebuild.md`). Canonical batch 17 retired; 2024 annual + 2025 annual + Jan/Feb/Mar/Jul 2026 re-imported via the native parser (new capabilities: Transfers-section ACATS parsing, TRANSFER_IN lot creation, dup source_key ordinals); 9 worksheet-exact ACATS lots repaired in (tombstoned autos). Results: options properly typed (580 BTO/549 STC vs 48/45), fees 60→387, interest 20→110; ledger-vs-broker audit **115/119 clean** (3 intraday round-trip audit false-positives + CRWD split, below); negative-hpd rows 626→426, remaining = genuine broker-confirmed shorts (QCOM repro now a tight -1..-29d short-cover set); RECONCILE_CLOSE 21→2; TWR gate 132/133 (lone outlier = pre-existing Vanguard Dec-2022 artifact); suite 4,173 green. Backup: `data/backups/pre-ibkr-rebuild-2026-08-03.db`. Audit verdict recorded: engine exonerated, pairing is 1099-B-consistent for shorts.
- [ ] **Corporate-action (split) support in the IBKR ledger — follow-up from the rebuild** (2026-08-03): the parser ignores the statement `Corporate Actions` section, so **CRWD's 4:1 split (2026-07-01, +120 shares)** is the one remaining ledger-vs-broker gap (statement-verified; ledger −90 vs broker +30 trajectory reconciles exactly through the split). Same defect family as the VGT price-series split repair the nightly fixer is scripting. Proper fix = split-adjust existing open lots (qty ×, per-share basis ÷) rather than a zero-cost share injection; parser reads the Corporate Actions section going forward. Also minor: audit tool's same-day rule can't represent intraday buy→snapshot→sell interleaving (TQQQ/PLTR/SAP false positives — all verified correct in ledger). Session-scale; brainstorm the lot-adjustment semantics first.
- [ ] **Trade-review lot snapshots predate the ledger rebuild** (2026-08-03, found in post-rebuild E2E): `trade_roundtrips` is a frozen per-review snapshot from generation time, so pre-rebuild reviews (e.g. March-2026 IBKR, generated 7/06) display stale lot pairings (QCOM shows two phantom Oct-2025 lots) while `tax_lot_sales` underneath is correct. Same class as the 7/04 option-dollar repair — decide: (a) metrics-only recompute of `trade_roundtrips` from current lots keeping AI prose frozen (precedent-consistent), (b) full regeneration of affected reviews (AI cost), or (c) extend the stale-narrative banner to cover the 2026-08-03 rebuild epoch. Decision + ~1-2h.
- [x] **Negative holding periods: display as "short" — SHIPPED 2026-08-05 evening** (wire-time batch Task 6, `487eaed`+`b59100c`): shared `<HoldingPeriodBadge>` (`app/dashboard/components/HoldingPeriodBadge.tsx`, `lib/format.ts::formatHoldingPeriod` unit-tested) renders a `short` info chip on FOUR surfaces — Tax Lots closed-sales Days column, Trade Reviews lot breakdown + trade header (header truthful via client-side `allLotsShort` over the unclamped per-lot values — the API's clamped average can never go negative), and the Trade Grades table (`{tg.holding_days}` raw render, found in review — the snake-case-without-"period" name had dodged the coverage grep). 8949 CSV/TXF exports deliberately stay raw/signed.
- [ ] **Foreign-trade broker-USD realized capture** (2026-08-05, layer 2 of the KRW tax-lots decision): `ibkr-activity` parser reads the statement's `Realized & Unrealized Performance Summary` per-symbol **base-currency (USD) realized P&L** for foreign-currency names and stores it; Tax Lots surface shows the broker-reported USD figure when available (labeled per-broker). Verified against the July 2026 statement: 402340.KS realized −$2,684.28 USD (vs the engine's native −₩3,980,000 price-only / broker's −₩4,021,876 incl. commissions — the USD figure also carries real buy→sell FX movement). Layer 1 (exclude non-USD from USD headline totals + disclosure) is DECIDED on the ledger for the nightly fixer. Design questions: allocation to per-lot rows on partial closes; where to store (statement-sourced annotation, not engine-computed). Small-medium.
- [x] **FIFO audit verdict (2026-08-03, archived — rebuild executed same day, see above)** — verdict: **engine exonerated** (all 626 rows re-tested against the sale transaction's TOTAL quantity: every one is a ledger-level oversell — zero cases where FIFO skipped an eligible on-or-before lot; the future-lot pairing at oversell points mirrors 1099-B short-sale convention, acquired=cover/sold=short, with correct economics — AAL −1000 short round-trip broker-verified). Root cause is **incomplete data in the ONE canonical-csv backfill (batch 17, `dashboard_IBKR_transactions.csv`, 2,493 txns spanning 2024-01→2026-03)**: (1) the Jan-2024 Robinhood ACATS **in-kind position legs were never imported** — statement Transfers section shows SQQQ 2,500 / TQQQ 150 / TTD 150 / UCO 350 arriving 2024-01-05, DB has only the cash legs (SQQQ's first ledger event is SELL 2,500 four days later); (2) at least one real fill missing (QQQ +642 @427.41 on 2026-05-02, present in the statement MTM details ≈ the constant ~682-742 QQQ broker-vs-ledger gap); (3) ZERO EXPIRED/ASSIGNED/EXERCISED rows in the whole IBKR ledger across 2.5 years of options (the QQQ 424P exercise's stock leg made it in as a plain SELL, siblings likely dropped). Broker-vs-ledger gaps are CONSTANT since the TWS era (QQQ ~700, UCO ~4,390, TQQQ ~1,650) → holes are historical, the native ibkr-activity path (2026-04+) is clean. Second population: genuine shorts (broker-confirmed AAL/MSFT/PINS/SNAP/RKT/QCOM/IGV…) — correct data, negative hpd is the engine's faithful short representation. Tax exposure bounded: all 626 rows correctly short-term (negative days can't cross 365); 2024/2025 filed from IBKR's own worksheets; 2026 has 204 rows netting −$249. **Repair inputs verified on disk**: `~/Desktop/Trading - Local/IBKR 2024 activity.csv` (real activity statement: 640 Trades rows + the 5 ACATS Transfers rows) + monthly 2025 activity CSVs in `Trading/`. Remediation options to decide: (a) rebuild 2024-01→2026-03 from the real activity statements via the native parser (requires retiring canonical batch 17 first — destructive, user-run); (b) minimal patch: import just the 4 ACATS position legs + spot-fix known missing fills; (c) leave historical years as-is (filed from broker docs anyway) + display-side "short" labeling instead of "-Nd". Audit scripts in scratchpad `fifo-audit.py` / `fifo-audit-results.json`.
- [x] **VGT split repair — EXECUTED 2026-08-05 evening** (user pre-authorized via the session-focus selection). Backup `data/backups/pre-vgt-split-repair-2026-08-05.db` taken first; dry-run confirmed 8:1 at 2026-04-21 (809.96 → 101.57; 11 prices rows, 1 holdings row); `--apply` clean — healing-window delta $4,384.38 (expected), outside-window $0.00, idempotent re-run reports nothing to do; `refresh-vanguard-betas` re-run on the clean series (67 computed, 0 errors). The script itself was hardened at landing time: residual verification moved INSIDE the transaction (tripped check now rolls back instead of leaving a half-adjusted DB).
- [x] **Raise `DEFAULT_VERIFICATION_LIMIT` 8→12 — DECIDED + SHIPPED 2026-08-05 evening** (user decision on live data: the pass hit the cap of 8 on 3 of its first 4 days — 8/02: 8, 8/03: 8, 8/04: 7, 8/05: 8 — earnings-season saturation with daily candidate deferral). One-line constant in `lib/calendar/verify-earnings-dates.ts` + rationale comment; costs at most 4 extra web-search calls on saturated days.
- [x] **Wrap suppression vs BMO clusters — DECIDED + SHIPPED 2026-08-04** (user decision: exempt BMO). Only AMC clusters suppress now — the defer-to-morning-debrief rationale was AMC-specific; a BMO cluster's individual recaps land the same morning the user is following the prints (the 8/04 DOCN/XMTR/WIX cluster had to be recapped manually). Mac: `lib/calendar/email-sweep.ts` suppression branch gates on `slot === "AMC"`; Worker: `fallback-earnings.ts` builds the suppression cluster for AMC only. Both sides TDD'd. *(Original: today's BMO cluster tripped wrap suppression (≥3 same-slot) and deferred recaps to the NEXT-DAY 07:45 debrief.)*
- [x] **Worksheet auto-arm for held reporters — DECIDED 2026-08-04: keep opt-in, no code change.** User chose to keep per-event ⎙ chip arming (auto-arm would print paper for every held reporter; the XMTR miss was a one-off arming lapse, not a workflow gap). Closed without implementation.
- [x] **Wire-time tracking — SHIPPED 2026-08-04**: spec `docs/superpowers/specs/2026-08-04-earnings-wire-time-tracking-design.md`, plan `docs/superpowers/plans/2026-08-04-wire-time-tracking.md`, migration 076, pre-release probe + cascade + EarningsWhispers jump-start + chip override editor. *(Original: record observed print times per symbol/quarter (XMTR hit the wire ~07:05 vs the 08:00 BMO default; call 08:30). Constraint: enrichment only polls AFTER the recorded release_time, so first-actual-capture can't observe early wires — needs a pre-release probe window (e.g. Finnhub probe from T−90m for BMO names) feeding `SYMBOL_RELEASE_TIMES_ET` or a new table. Design-first, next-season prep.)*
- [x] **MERGE PR #32 (rich worksheet print) + DMG rebuild — DONE 2026-08-05 pm** — merged locally `c1d8773` + pushed (the two pending QA-fixer commits rode along), worktree + both branches deleted, full suite 4,311 green on merged main, DMG rebuilt/notarized/installed/relaunched. First live rich auto-print same hour: LFMD 14:02 ET.
- [ ] **Worksheet rich-print deferred minors** (2026-08-05, all triaged acceptable-to-defer by the final review — batch when next touching `lib/earnings/worksheet-rich.ts`): (a) `stripInline` keeps single-asterisk `*italic*` markers (past-prints footnote prints literal asterisks; fix needs a PAIRED-italic regex, not a bare `\*` strip); (b) hard-cap slice can bisect a table row at the 185-line boundary (cosmetic, cap holds); (c) `loadRichWorksheetInputs` runs twice per auto-printed event (gate + compose — cheap SELECTs); (d) no test for the real-table-after-next-heading branch in `extractPreviewSections` (hand-verified); (e) content BEFORE the bogies heading is silently dropped from paper (spec-compliant, prompt mandates bogies-first); (f) `renderMonospaceTable` last column has no drawn right-edge rule. Small. **Superseded 2026-08-06, confirmed shipped 2026-08-07**: the rich monospace sheet (`worksheet-rich.ts` → `printViaLp`) is now the FALLBACK-ONLY road, live in production — `printWorksheetNow` prefers the email-identical PDF road (`lib/earnings/print-sheet.ts` + `lib/earnings/print-pdf.ts`, merged `c026c4e`+`56ad89f`) whenever a local preview exists, so these minors only bite when Chrome/the PDF render fails. Still worth a pass eventually, lower urgency.
- [ ] **Earnings workflow — next improvement round (user request 2026-08-06)** — four asks from live use during the season; (a)–(c) SHIPPED 2026-08-07 (branch `earnings-print-prose-round`, squash-merged `c026c4e` + same-day user-feedback follow-up `56ad89f`; spec `docs/superpowers/specs/2026-08-06-earnings-print-prose-round-design.md` — carries the 2026-08-07 addendum — plan `docs/superpowers/plans/2026-08-06-earnings-print-prose-round.md`). (d) remains OPEN — its own brainstorm, deliberately excluded from this round's spec:
  - **(a) Preview printout = ONE sheet, email-exact bogeys — SHIPPED 2026-08-07 (`c026c4e` + `56ad89f`).** New `lib/earnings/print-sheet.ts` (`composePrintSheetHtml` + verbatim `extractBogiesTableMarkdown`) renders the LOCAL preview email's own HTML (scoreboard → sheet-bogeys-by-source → bogies table lifted byte-for-byte from `ai_output_md` → full user notes → past prints) through `briefingToHtml`; `lib/earnings/print-pdf.ts` (`renderHtmlToPdf` — `%%EOF`-gated poll for completion, since headless Chrome never fires `close` on this Mac; DI spawn seam; `printPdfViaLp`) turns it into a PDF and prints it duplex (`sides=two-sided-long-edge`) so notes overflow lands on the physical back of the same sheet. `printWorksheetNow`/`printArmedWorksheets` try this road first and fall back to the monospace sheet on ANY failure (no Chrome, no local preview, render/print error — never-silent; a PDF-road `lp` failure downgrades to monospace AND stamps on its success rather than retrying stampless — ruling recorded 2026-08-07). **Same-day live-user feedback, fixed in `56ad89f`**: (1) the sheet printed the outbound email's amber/cream envelope palette verbatim — `PRINT_CSS` now forces every inline background/text the envelope emits to white/black under `@media print` with `!important` (table borders untouched — that ruled grid is the fillable surface; header cells keep a light-gray tint so the header row still reads); (2) a second page printed with only the footer note (its inline spacer + the ancestor table's `break-inside:avoid` pushed the whole block to an orphan page) — fixed by targeting the footer's unique inline-style substrings with a shrunk spacer + `break-before/inside:avoid`; (3) the one-sheet ladder gained a third rung — `{ compact: true }` re-render (smaller font/spacing) before the notes-never-truncate "print anyway" floor, capped at 3 renders total, never loops. Live-verified: white background renders pure white; fit ladder measured full=3pp → drop-past-prints=3pp → compact=2pp on a real BRK.A preview.
  - **(b) Post-bogeys prose quality + provenance walkthrough — SHIPPED 2026-08-07 (`c026c4e`).** Root cause diagnosed first (catalog: `docs/superpowers/plans/2026-08-06-artifact-catalog.md`): `callClaude`'s `textBlocks.map(b => b.text).join("\n")` planted a bare mid-sentence newline at every `web_search` citation boundary — confirmed in 6/6 sampled sends — which the HTML renderer then split into disjoint one-line paragraphs. Fixed at the source (`joinClaudeTextBlocks`, joins with `""` not `"\n"`) rather than patching the renderer — a renderer-side fix was tried and rejected because it also merged genuine paragraph breaks (25→16 paragraphs on real data). A read-boundary repair (`lib/earnings/repair-citation-linebreaks.ts`) covers already-stored emails, applied ONLY at the two earnings display read sites (`/api/earnings/email-content` + `BogeysEditModal`) — never at compose, since compose is fixed at the root. New deterministic `renderSheetBogeysBlock` (`lib/digest/send-earnings-email.ts`, code-built from `earnings_bogeys`, zero AI) gives conflicting sheet-source bogeys (e.g. TMT Breakout vs FundaAI) their own per-source columns in both preview and recap instead of forcing a merge. Prompt rules: never re-list the system-rendered sheet-bogeys table, cite source labels in-cell when using sheet values, show disagreeing sources side-by-side rather than merged, and 1–3 flowing paragraphs per section (no filler openers, no orphan bold fragments).
  - **(c) User stock notes get cut off in printouts — SHIPPED 2026-08-07 (`c026c4e`).** Notes-are-sacred now holds on the primary print roads: the PDF road renders every family note in full (no cap, no chop); the monospace fallback's silent 4-notes×74-char chop is retired in favor of full-text word-wrapping, still bounded by the existing page cap. (The no-local-preview deterministic composer wasn't touched this round — see the deferred-minors item below.)
  - **(d) Post-print sequence: more automatic, less manual — OPEN, its own brainstorm.** As soon as a company reports, the numbers should be fetched and filled with no user action — possibly an agent-style fetcher polling at print time. Builds on what exists (T−90m Finnhub wire probe, retry-until-complete enrichment, push-at-print); first map today's remaining MANUAL touchpoints (actuals overrides via BogeysEditModal, blocked-recap unblocks, call notes, wrong-slot fixes) and design them away. Goal: the user focuses on decisions, not data entry. Deliberately excluded from this round's spec — next brainstorm.
- [ ] **Print-prose round deferred minors** (2026-08-07, from the final whole-branch review — batch when next touching `lib/earnings/print-pdf.ts` / `lib/earnings/print-sheet.ts`): `countPdfPages` is a token scan for `/Type /Page` objects, not a real PDF parser — a future Chrome using compressed object streams would silently undercount and downgrade every render to the monospace fallback (no alarm, just quietly worse fidelity — breadcrumb for the diagnostic ladder if the PDF road goes dark); the no-local-preview deterministic composer (`composeWorksheetForEvent`) still truncates unmarked content at its page cap (only the PDF + rich-fallback roads got the notes-are-sacred treatment this round); ~250ms/render Chrome spawn+cleanup latency (acceptable at current print volume); `verify-sector-tags.ts` and `verify-earnings-dates.ts` share the exact same `web_search` text-block `join("\n")` shape as the bug fixed in (b) above — masked there by the existing JSON control-char retry (`lib/ai/extract-json.ts`), so it hasn't manifested as a user-visible bug, but the root cause is structurally present in both. Small, low urgency.
- [x] **Wire-time follow-ups (a)–(f) — ALL SHIPPED 2026-08-05 evening** (subagent-driven batch, plan `docs/superpowers/plans/2026-08-05-wire-time-followups-batch.md`, commits `87f524f..6f53f2b`, per-task reviews + final whole-branch review READY, suites 4,362 Mac + 457 Worker green): (a) actuals road propagates Finnhub fetch errors (probe road verified: never stamps `wire_probe_empty_at` on error, already pinned); (b) exact-time verdicts family-aware + case-insensitive (writes uppercase-canonical, all readers family-aware — traced); (c) conflict-confirm routes through `resolveEarningsReleaseTime` (cascade input built fresh from the user's confirmation, no circularity with the row being corrected); (d) conflict popover has the "Reports at" editor (hoisted `ReleaseTimeEditor` shared with the passive popover); (e) sync upsert keeps an EARLIER earnings `release_time` (earnings-only CASE, macro COALESCE byte-identical + test-pinned; accepted trade-off: a wrong-early vendor slot can't be repaired by re-sync — self-heals ≤1 day via the daily verify pass, which writes directly); (f) `wire_probe_empty_at` is protective state in `deleteUnenrichedEventsForWeek`. **New informational from the final review:** Worker `workers/cron/src/enrich-actuals.ts::fetchFinnhubActual` still swallows `!res.ok`/missing-key into empty — pre-existing, absorbed by B8 retry-until-complete, but now diverges in spirit from the Mac road; candidate for a future Worker-parity touch.
- [x] **Earnings preview — source hierarchy work** — shipped 2026-07-17 (migration 068: user-editable ranked hierarchy + per-source prompt notes; rank-ordered two-pass fill with per-source diversity cap replaces the zero-hit tier gate; edition supersedence + cross-source dedup instruction; ManageSourcesModal editor; spec `docs/superpowers/specs/2026-07-17-earnings-source-hierarchy-design.md`). Deferred by design: hit-rate-based automatic ranking (schema accommodates later).

## Bugs / Quality

- [ ] **Performance numbers mismatch: per-account vs top-of-page** (user report 2026-08-10; ROOT-CAUSED same day, fix deferred to its own session) — on the Analysis tab `?view=performance`, the scope=all headline TWR sits BELOW every per-account row on 3Y/5Y/All (+73.8% vs +75.6/+105.3/+122.2 on 3Y) — mathematically impossible for a value-weighted blend, so the headline is wrong. Mechanism (verified by re-implementing both paths against the live DB): the headline re-derives monthly returns via Modified Dietz over SUMMED `monthly_snapshots.total_value` (`lib/compute/twr.ts` aggregate block), while per-account rows use each statement's STORED twr; acct-1's December year-end rows don't tie to the next January's `starting_value` (2022-12-31 total 362,408 vs 2023-01 starting 328,285; 2023-12-31 580,250 vs 2024-01 526,158), so the aggregate books each gap as a fake January loss (−9.8pp / −7.9pp months). Correcting just those two months moves All-time ~+80%→~+114%, back inside the component range. Fix plan (needs the December data question answered first — annual-summary semantics vs import defect, check the real Vanguard statements): (a) headline = value-weighted blend of per-account stored TWRs instead of re-derived Dietz on summed values; (b) return + render the aggregate `portfolioPartial` flag (computed at `twr.ts:479-541` but never returned — silently dropped months are invisible); (c) per-account rows disclose their own date windows (IBKR's +122% spans 31 months vs headline's 55, no caption says so); (d) `PerformanceView.tsx:80` uses `resolveScopeToSingleId` (CLAUDE.md scope violation, latent until a 4th account); (e) aggregate NULL-deposit months can't take the transactions fallback per-account can (`twr.ts:516-537`, live-fragile for the IBKR account since 2026-05). Own session; import-forensics on the two December statements first.
- [x] **Vanguard evening recap "significant moves" wildly wrong — ROOT-CAUSED + FIXED + REPAIRED 2026-08-10** (`d39cda9`; user report same day, example Friday 8/07 email: NET −8.8% on a day NET actually gained +5.6%, LQDT −6.2% on a flat day; UBER +6.4% was real). Root cause: `lib/tws/snapshot.ts` TICK_PRIORITY preferred LAST over CLOSE unconditionally, so the post-close background sync stored after-hours last-trades as daily closes (NET 8/06 stored 330.0 = the post-earnings AH spike vs real close 284.43) — quiet names hid it (AH≈close, the 5/29 "within 0.2%" measurement), earnings movers exploded it. Fix: `tickPriorityFor(nowEt)` — CLOSE-first outside US RTH, LAST-first inside; new `nowET()` in date-utils; injectable for tests. Repair: `scripts/repair-ah-closes.ts` (dry-run default, ohlcv_bars 1-day closes as authority, never guesses without a bar) applied live — **84 poisoned rows across Apr–Aug fixed** (worst PRIM −23.7%, INTC +20.1%, NET −17.4%, TER −8.3%), backup `data/backups/pre-ah-close-repair-2026-08-10.db`, re-scan idempotent-clean. **Follow-ups (next session or post-deploy):** (a) re-run `npx tsx scripts/refresh-vanguard-betas.ts` when the app is idle — 14 of ~200 names hit `database is locked` against the app's in-flight sync transaction; (b) valuations recompute self-heals on the app's next 30-min refresh cycle (recomputes from the repaired prices) — verify a post-repair "Valuations recomputed" line in `~/Library/Logs/Vanguard Dashboard/server.log`; (c) the packaged app was HTTP-unresponsive for stretches this afternoon while its background timers kept firing (documented event-loop starvation during sync) — worth watching, possibly its own investigation if it recurs outside sync windows. Residual: LQDT 8/06 (stored 44.84) had no daily bar to repair from — re-run the script after an ohlcv backfill covers it; foreign-listing (402340) session-clock nuance deliberately out of scope (US-RTH keying still improved it).
- [x] **Earnings recap → security attachment audit — DONE + one miss FIXED 2026-08-10** (`edf1e65`; user question same day). Audit verdict: linkage broadly healthy — FundaAI NET review → NET ✓, APP reviews → APP ✓, TMTB wraps + the MSFT Bull-vs-Bear → their tickers ✓ (non-held names like TEAM/LYFT deliberately unlinked, by-design universe restriction). ONE real miss: FundaAI "Review|APP & U 2Q26" (article 67770) extracted `["APP"]` only — the model dropped the single-letter ticker U despite it being in the subject; no code filter, pure model variance. Fix: deterministic `subjectSymbolBackstop` (subject-only, uppercase-token exact match against held+watchlist stocks, bypasses the Haiku verify layer which would drop a bare "U" as ambiguous) on BOTH linking sites (gmail process + reconcile-cloud-fetched; Worker never writes links, no parity change). Backfill script `scripts/backfill-article-symbol.ts` applied live: 67770 → U linked, mentioned_symbols now ["APP","U"].
- [x] **Earnings coverage-gap push notification listed only ETFs — ROOT-CAUSED + FIXED + REPAIRED 2026-08-10** (`14f0b34`; user report same day; the push fired Sat 8/09 16:47 ET from the briefing road's guard). The guard's filters were CORRECT — the data lied: securities rows for ARKK/HACK/IGV/KRE/SOXX/SPY/TLT/UCO/NCLD carried `security_type='Stock'` because IBKR labels every STK contract a stock (TWS positions map at `lib/tws/positions.ts` + ibkr-activity parser), defeating both the guard's stock-only candidate filter AND its 7/16 option-underlying ETF exclusion. Three-layer fix (each alone shown insufficient live): (1) enrich reads contract-details `stockType` and promotes NULL/'Stock'→'ETF' (ETF/ETN only, never downgrades); (2) `upsertSecurity` conflict clause treats incoming 'Stock' as weak evidence — never downgrades a fund-family type (the first live retype was reverted WITHIN MINUTES by the running app's TWS sync through the old COALESCE — caught and guarded same session); (3) `scripts/repair-etf-types.ts` applied live for all nine (backup taken, verified stuck post-guard). PSUS (closed-end fund, not an ETF — script hard-refuses it) added to `coverage_guard_ignored_symbols` instead. **Post-deploy check**: after the next electron:deploy, re-verify the nine are still 'ETF' after a TWS sync cycle — the running app executes the pre-guard binary until then. Remaining legit gaps (BANC, ZS, BRK/B) are real stocks, correctly flagged.
- [x] **Nightly QA fixer 2026-08-07 — LANDED 2026-08-10** as part of the session's full landing sweep (merges `0482b08` PR #38 superset + `80cc241` deep-fixes; all six 8/07 fixes reviewed against source, patch-equivalence verified via `git cherry` before branch deletion; the 8/09 and 8/10 fixer nights landed in the same sweep, merges `2e6a205`/`cd9efc4`/`cb55e5e`; suite 4,571 green post-landing). *(Original: branch `qa-deep-fixes-2026-08-07` (2 commits — `de2ae3e` stop truncating Security Detail transcript-refresh errors at 40 chars, `3300918` `formatShares` keeps fractional share counts at default digits) and branch `qa-fix-work-20260807` (4 commits — `27480c8` viewport-aware date-chip popover alignment (`qa:alerts-conflicts-mobile--date-popover-renders-offscreen-left`), `296ead1` tax-report USD totals exclude non-USD sales with disclosure (`qa:tax-lots-tax-report-card--krw-sale-summed-as-usd`, sibling of the 8/06 tax-lots fix, same root cause on a different surface), `0486379` cockpit withholds implausible Finnhub actuals from the figures line (`qa:today-cockpit--implausible-finnhub-actual-rendered-as-real-4x-beat`), `f5c5b74` earnings date-correction migrates the sent-email audit rows (`qa:today-earningshub-fix-date--correction-destroys-sent-email-audit`, likely the fixer executing the 8/06-decided plan)). Neither branch merged nor PR opened this session. Land via the standard pattern: review, merge (direct or via PR per the existing branch-naming split), verify all qa-* branches patch-equivalent before deleting.)*
- [ ] **Public-repo hygiene: real held tickers in tracked source files** (found 2026-08-07 during docs reconciliation, NOT this session's work): `lib/data/security-classifications.ts`, `tests/compute/hedging.test.ts`, and `docs/superpowers/{plans,specs}/2026-07-05-defense-hedging-tab*.md` are pre-existing tracked files that read as carrying real held-position tickers — the repo is public and the standing rule (`memory/feedback_no_sensitive_data_public.md`) is no real financial data in git-tracked files. Needs a decision: redact/anonymize vs confirm-and-accept (some may already be illustrative/synthetic rather than actual holdings — verify per file before touching anything). Worth a broader grep of the full tracked tree against the current held-symbol list while at it, not just these three files.
- [ ] **`electron:pack` DMG step fails with "No space left on device" (2026-08-07)** — `electron-builder --mac` builds and SIGNS `dist/mac-arm64/Vanguard Dashboard.app` fine, then dies in `dmgbuild` writing `/Volumes/Vanguard Dashboard/.DS_Store` (Errno 28) even with 32 GiB free on `/` — the staging volume is sized too small for the app, not a disk-space problem. **Workaround used + verified this session: `npm run electron:install` alone** (it copies the already-built, already-signed `.app` to `/Applications` and relaunches — no DMG involved), so local deploys are unaffected; only the distributable `.dmg` is missing. Fix candidates: bump the dmg `format`/size in `electron-builder.yml`, or drop DMG packaging entirely for local-only distribution (`--dir`). Small; only blocks producing a shareable DMG.
- [x] **`electron:install` relaunches the WRONG app — FIXED 2026-08-10** (`b21fbad`): both the `osascript ... to quit` and the `open` in the `electron:install` script now use the `/Applications/Vanguard Dashboard.app` full-path form, exactly as prescribed below. *(Original report, hit live 2026-08-09 during the send-race deploy: Two registered apps share the name: the freshly-installed `/Applications/Vanguard Dashboard.app` and the stay-open Desktop launcher `~/Desktop/Vanguard Dashboard.app`. LaunchServices picked the Desktop one, which runs `scripts/launch-dashboard.sh` → `npm run dev`, so the deploy ended with a **dev server on :3099 and the packaged app not running at all** — silently, since both answer :3099 and both serve the same source. Resolution order isn't stable, which is why prior deploys worked. Recovery used this session: quit the launcher by full path (`osascript -e 'tell application "/Users/Yitzi/Desktop/Vanguard Dashboard.app" to quit'`), then `open "/Applications/Vanguard Dashboard.app"` (path form, unambiguous). Fix is one word in the `electron:install` script — `open -a 'Vanguard Dashboard'` → `open "/Applications/Vanguard Dashboard.app"`; the sibling `osascript ... to quit` at the head of that script is ambiguous the same way and should also take the full path. Trivial, but it silently downgrades every deploy it hits.)*
- [ ] **Earnings running-marker has the same fire-and-forget shape as the 8/09 briefing bug** (filed 2026-08-09, NOT fixed — the send-race fix was deliberately scoped to briefing/digest/evening). `lib/calendar/email-sweep.ts:271` does `void setEarningsRunningMarker(cand.phase, cand.eventId)` against a 10-min Worker TTL (`workers/cron/src/earnings-markers.ts:26`) — structurally identical to the defect that lost the Sunday briefing. **Materially lower severity**: an individual earnings compose is 60–180s (well inside 10 min) and the sweep does no auto-refresh, so it lacks the multi-minute synchronous better-sqlite3 block that starved the briefing's marker write. Fix shape if it ever bites: the same `withRunningMarker` treatment, or simply await the set. Cheap to do opportunistically when next touching the sweep; do NOT widen it into the deliberately un-gated recap path.
- [x] **Nightly QA fixer 2026-08-06 — ALL LANDED same day** (session review + merge): auto-merged pre-session: tax-lots--krw-realized-loss-summed-as-usd-flips-year-total @ 09d7599 (USD headline totals exclude non-USD sales + disclosure; rows render native with currency label), analysis-income--by-account-total-fails-visible-arithmetic-hidden-fees @ 6eab283 (FEES column per the 2026-08-03 decision). **PR #33 reviewed + merged `32bfe42`** (aa4dafe intrinsic-violation guard on option today-moves — arbitrage-tell design, magnitude gates stay off-limits for options; b636443 Filtered tab honors search + source controls). **qa-deep-fixes-2026-08-06 reviewed + merged `3a0e45c` MINUS f525905** — that commit exactly re-applied the USD suggested-levels conversion the user rejected at the 8/05 landing (56777fd); reverted at landing `4e0fee6`, ledger entry → wontfix with the decision recorded, and the stale suggested-levels route comment that fed the fixer the wrong direction corrected `3e6a7a6` (fix shape if the $-glyph cosmetic bites again: currency LABEL on both lists, never USD conversion of one). Landed from the branch: calendar-rot test pin (suite green again), levels hit-rate distinct-fired-armed numerator, dismissed bucket render, Data Health discrepancy FX conversion, sender_email validation, compact OCC symbols in Armed tab, blank-note Save guard, CSV-guide comma fix (2 sync sites — the test fixture never carried the example). All qa-* local branches verified patch-equivalent + deleted; remote branch dies at next push. 36 findings dispositioned (23 auto / 5 needs-decision — **all 5 resolved by the user same day**, every recommendation accepted: migrate earnings_emails audit rows on date correction (+ skips, reconcile-sibling parity), plausibility-band filter + "outside scan range" flag + non-blocking arm warning, equity-resolution for extracted levels + user-run repair-option-attached-levels script, narratives never state numbers (deterministic substitution), sender_email warn-but-allow; DECIDED plans on the ledger, dispositions → auto for tonight's fixer).
- [x] **Nightly QA fixer 2026-08-05** — auto-merged: today-earningshub-bogeys-upload--502-raw-anthropic-error-blob-regression-1 @ 63fb764 (multi-symbol upload gets a 16k output budget; truncation + parse failures map to friendly typed errors). PR #31 (qa-auto-fixes-2026-08-05, review+merge manually): analysis-position-risk--vgt-407pct-vol-unadjusted-split @ 45da705 (split-signature vol guard + user-run scripts/repair-split-prices.ts — run with --apply after merging), security-detail-krw--suggested-levels-mix-native-level-with-usd-price @ 97e03ac, levels-performance--hit-rate-inconsistent-with-alerts-count-regression-1 @ d6963fd. 19 new findings dispositioned (16 auto / 3 needs-decision — see qa/findings/DECISIONS-PENDING.md, 5 total awaiting call). Stranded skipped per anti-strand guard: PR #30 (4 fixes) + qa-deep-fixes-2026-08-05 (4 fixes). **ALL LANDED 2026-08-05 evening** — see the Closed-this-session block (QA landing sweep): PR #30 + PR #31 + qa-deep-fixes merged with review fixes, VGT repair executed, all 5 pending decisions resolved.
- [x] **Nightly QA fixer 2026-07-31** — auto-merged: today-week-ahead--reaction-snapshots-never-render @ 43af51b (week view renders captured reaction summaries), analysis-defense--brk-b-holding-labeled-brk-a @ dd996ec (family rollups labeled with the held share class). PR #26 (qa-auto-fixes-2026-07-31, review+merge manually): data-health-summary--price-coverage-100pct-contradicts-own-panels @ 8780434, today-alerts--privacy-ai-suggestion-prose-leaks-prices @ a016fd3. 21 new findings dispositioned (16 auto / 5 needs-decision — see qa/findings/DECISIONS-PENDING.md, 11 total awaiting call incl. 3 HIGH). Stranded branches skipped per anti-strand guard: qa-fix-work-20260729 (4 fixes) + qa-deep-fixes-2026-07-29 (3) + qa-deep-fixes-2026-07-31 (2 fixes + the date-rot test repair the suite currently needs).
- [x] **Nightly QA fixer 2026-07-26 (supervised run #2, interactive)** — auto-merged: research-notes--empty-state-copy-blames-nonexistent-search @ 749a4cd (empty Stock Notes tab no longer blames a search; browser-verified). PR #24 (qa-auto-fixes-2026-07-26, review+merge manually): today-releases--implausible-actual-shown-as-fact-contradicts-earningshub @ 537ed9c, security-detail-transactions--krw-native-price-amount-rendered-as-usd @ a703773, security-detail--two-different-52-week-ranges-on-same-page @ d47ef7f. Verify-and-closed against existing main commits: analysis-notes-fab @ a5f49f4, analysis-macro-themes--view-sources @ 8c52dae (both browser-verified on :3096). Skill amended @ fd6d5e3 (worktree suite gate needs dummy ANTHROPIC_API_KEY — live-discovered). ARM precondition (1) satisfied pending user review of tonight's fixes.
- [x] **QA auto-fix pipeline SHIPPED (disarmed) 2026-07-26** — nightly fixer chained after the deep sweep: `/qa-fix-findings` skill (classify auto/needs-decision/needs-repro → TDD-fix in persistent sibling worktree, cap 4/night → LOW/prescribed+`auto_fixable` auto-merge to LOCAL main (never pushes; user pushes at session-end) → sanitized PR from origin/main for the rest → TODO-reconcile-then-rebuild → Pushover summary). Spec `docs/superpowers/specs/2026-07-26-qa-auto-fix-pipeline-design.md`; chain block in `qa/nightly-deep-qa.sh` gated on `deep-qa-config.json` `fixer.enabled=false`. Pre-steps landed same session: both stranded `qa-deep-fixes-*` branches merged (`f17b665`+`0ea7c11` — import NaN, LWC markers disposal, macro-theme dead click, trust-strip dates, NotesAmbient rail offset) and the 4th-sighting drawer z-index HIGH fixed (`e73aaae`, z-[55] per trust-strip precedent). First supervised dry-run DONE (42 open findings → 27 auto / 15 needs-decision / 0 needs-repro; Pushover live-validated; skill amended from run feedback).
- [x] **QA fixer ARMED 2026-07-27** — all 5 preconditions cleared: (1) user reviewed run-#2 fixes in-session (all four approved, no changes requested) and **PR #24 merged to main via merge commit `c6b5498`** (per-finding hashes `537ed9c`/`a703773`/`d47ef7f` kept reachable for the ledger's fix_commit references; suite 3861/3861 green post-merge); (2) ✅ lock PID-liveness; (3) ✅ sweep dedup match-and-hold `pr-open`/`merged-awaiting-deploy` — **proved live on the 7/27 sweep** (PR #24's 3 findings held, not re-minted); (4) ✅ clean-checkout preflight; (5) ✅ `fixer.enabled=true` flipped same session. The 3 PR-bound ledger entries transitioned to `merged-awaiting-deploy` (close for real once a post-rebuild sweep stops seeing the symptoms). First armed run: tonight 2026-07-28 ~02:45, cap 4/night, auto-merge LOW only. Also queued from review: `CallNoteModal.tsx:91` + `TranscriptCard.tsx:186` share the drawer bug's exact z-50 rail-tie (preemptive z-[55] candidates); 5 ledger-note inaccuracies logged in `qa/findings/fix-runs/2026-07-26.md` for cleanup. Two skill bugs live-discovered in run #2 and fixed same day: suite gate needs dummy ANTHROPIC_API_KEY (`fd6d5e3`), worktree cleanup must `--detach` (`a293b30`).

- [x] **Digest instruction leaks — 3 distinct classes — FIXED 2026-07-23** (`e64a88b..d0dd31d`, see the closed-session block at the bottom). Store-time desk-note validator + 5k min-transcript gate (the 7/23 CSX leak — blast radius was 3 rows: CSX/IMAX/INTC Q2, all repaired); per-element `key_themes` tag cleaning at storage + ALL render sites incl. ThemePills, new zero-import `lib/gmail/theme-sanitize.ts` (client-safe, re-exported from process.ts), Worker parity + parity pin, 3 rows repaired; second-person voice in ANALYSIS_SCHEMA descriptions + VOICE prompt rule + "the user"-free lint pin, 48 `portfolio_relevance` rows repaired. Bonus fixes: `stripModelPreamble` no longer decapitates a leading `**Guidance**` line; `cleanThemeElement` preserves trailing apostrophes. *(Original report: instruction leak in 7/23 digest CSX transcript entry — model asking for its inputs verbatim; 7/22 `<parameter name="key_themes">` fragment in Research Desk; 7/21-22 "the user's portfolio" phrasing in Portfolio-relevance blockquotes.)*
- [x] **Earnings: already-reported guard for previews + intel — FIXED 2026-07-23** (`6476241` + `66be650` + `d0dd31d`). Two-layer pre-send guard in the sweep (row `actual_value` check → live Finnhub probe; best-effort, fail-open) → permanent preview skip row + **`mac-sent` KV marker so the Worker fallback can't ship the same wrong-slot preview** (final-review catch); composer skips forceFresh intel when actuals exist; cockpit ensure list also requires actual-stage `pending` (post-print IV crush can't overwrite the recap's priced-in anchor even on a wrong slot); preview-prompt already-reported backstop; IMAX added to `SYMBOL_RELEASE_TIMES_ET` at **07:30 ET** (verified vs two BusinessWire mirrors — both Finnhub AND Nasdaq had mis-slotted it AMC). Residual accepted follow-ups: `scripts/backfill-symbol-release-times.ts` uses SQLite `date('now')` (UTC) not `todayET()` — fold into a future ET-anchoring sweep; cached-EDGAR upgrade tests' 33-char fixtures now skip the AI-summarize branch (bump one to LONG_TRANSCRIPT when touching that file). *(Original report: Finnhub AND Nasdaq both carried IMAX 7/23 as AMC 16:15 but it reported pre-market → preview fired 14:07 ET hours post-print, web search priced the actuals as street estimates, intel forceFresh at 15:04 ET showed post-print crushed IV as "Expected move".)*
- [x] **`securities.sector` Bloomberg-taxonomy corruption — FIXED + LIVE-REPAIRED 2026-07-28** (spec `docs/superpowers/specs/2026-07-28-sector-tag-verification-design.md`, 5-task SDD build merged `30b756f..f7070b1` + live-sweep fix `5b33c7e`, final review clean after a 6-finding fix wave). Migration 071 provenance stamps; 4 non-GICS Bloomberg buckets demoted from `normalizeSector`; web_search-verified sweep (`scripts/verify-sector-tags.ts`, feature key `sectorVerification` → $frontier) repaired **148 stocks + 350 option rows live** (UNH/OSCR/VRTX/ESTA/DHR→Healthcare, AMZN/MELI/REAL→Cons Disc, HOOD→Financials, UBER/LQDT/VRT→Industrials, LAND/KRC→Real Estate, IMAX→Comm Services; GOOG/META correctly confirmed unchanged); Data Health "Sector disagreements" panel is the standing drift alarm; final review also resurrected `macroEventToSectors` (briefing §6 sector clusters had been silently DEAD since June — Bloomberg strings matched zero rows) + fixed chat sector-filter examples + panel "(Financial)" blindness. Sector-ETF gap backlog cleared (75/75 resolved post-repair). Browser E2E 5/5 (allocation, Defense, UNH/AMZN pages, panel, console clean). **Residuals:** (a) ~40 null-sector non-held stragglers held by the panel — run `npx tsx scripts/verify-sector-tags.ts --apply <syms>` when API access returns; (b) macro-themes force-regen pending API access (Sunday briefing regenerates anyway); (c) chat sector-trust re-grade = user follow-up; (d) `lib/data/security-classifications.ts::SECTOR_MAP` appears dead/unimported — wire-normalized or delete some housekeeping pass. *(Original report: UNH/OSCR/VRTX/ESTA tagged Consumer Staples; AMZN/UBER/SHOP/HOOD/LQDT/REAL tagged Communication Services; blast radius Defense tab, allocations, reaction mapping, briefing clusters, chat.)* `normalizeSector` maps Bloomberg `Consumer, Non-cyclical` → Consumer Staples and `Communications` → Communication Services 1:1, but Bloomberg's buckets are NOT GICS: healthcare lands in Non-cyclical and internet names in Communications. Verified held-stock damage: **UNH, OSCR, VRTX, ESTA tagged Consumer Staples** (all healthcare — their own `fund_category` says Health Care); **AMZN, UBER, SHOP, HOOD, LQDT, REAL tagged Communication Services** (industry column = "Internet"); IMAX tagged Consumer Discretionary vs Comm-Services fund_category. Blast radius: Defense tab Tier-2 proxy attribution, allocation breakdowns, sector-ETF reaction mapping (`resolveSectorEtf`), briefing sector clusters, and chat (conversation 36 on 7/20 — the "unreliable chat" report — repeated these tags verbatim; re-grade chat trust after repair). Fix shape: sector re-classification sweep for held+watchlist stocks (Claude with industry+fund_category context, or prefer fund_category-derived GICS on disagreement), idempotent repair script, `normalizeSector` must stop treating those two Bloomberg buckets as directly mappable (route through industry-aware disambiguation), + a Data Health surface for sector↔fund_category disagreement so drift is visible. **Own session — touches the classification backbone** (`memory/project_analysis_classification_backbone.md`); brainstorm the disambiguation approach first.
- [x] **Electron server logs are not persisted — FIXED 2026-08-04**: `electron/server-log.ts` (electron-import-free, unit-tested) tees the Next server's stdout/stderr to `~/Library/Logs/Vanguard Dashboard/server.log` (5MB size rotation to `server.log.1`, one generation; timestamped `[server]`/`[server:err]`/`[electron]` tags; every failure path degrades to console-only — logging can never take the app down). Wired in `electron/main.ts::startServer`; lands in the DMG on the next `electron:pack`. *(Original: Finder-launched app discarded all server console output — the useRTH diagnosis had ZERO breadcrumbs.)*
- [ ] **Dead clicking — desktop Electron top-nav tabs intermittently ignore mouse clicks** (user report 2026-07-23; needs repro). Clicking Today/Accounts/Analysis/etc. does nothing — no highlight, no navigation. Investigation leads when it reproduces: TabDropdown's `position:fixed` menu layer or another invisible overlay eating clicks (NotesAmbient FAB region, chat-rail edge, `ScrollFade` wrapper over the tab `<nav>`), the `<Suspense>` plain-`<Link>` fallback swapping mid-click, Next router navigation silently pending, or Electron webContents stall after sleep. Next occurrence: note which tab, whether hover states still react, and whether DevTools console shows anything — then file with repro.

- [x] **Earnings date can be added but not corrected — FIXED 2026-07-26 evening (`9cade35`)** (user report same day; NET case — Finnhub carried 2026-07-30, real date Aug 6). Migration 070 `calendar_event_suppressions` — keyed on the SEMANTIC tuple (symbol, event_date, event_type), NOT source_key, so Finnhub AND Nasdaq are both blocked from re-inserting the same wrong date; `upsertCalendarEvents` (the single choke point every sync source flows through) skips suppressed non-manual events, table-existence tolerant for minimal test DBs; `DELETE /api/calendar/events` on a sync-owned EARNINGS row now suppresses-then-deletes via `deleteAndSuppressCalendarEvent` instead of 403 (symbol-less macro rows stay 403); EarningsHub renders the ✕ for sync rows too with "stays removed across syncs" confirm copy. Correcting a date = delete the wrong row + re-add via "+ Add ticker" (manual `insertCalendarEvent` deliberately ignores suppressions — explicit user action wins). Reusable repair CLI: `npx tsx scripts/correct-earnings-date.ts <SYM> <WRONG> <CORRECT> [bmo|amc]` — insert-first so `earnings_bogeys` migrate to the corrected event instead of dying in the delete CASCADE; refuses when the wrong row has captured actuals. **NET corrected live**: manual 2026-08-06 AMC row #1186, 3 newsletter bogeys migrated, suppression recorded, E2E-verified through the live route. +10 tests.
- [x] **Bogey screenshot ingestion — FIXED 2026-07-26 evening (`c6e6598`)** (user question same day). Answered: `read@myportfoliodesk.com` makes a research DOCUMENT, never bogeys — the bogeys path is the Today-view upload, which was PDF-only. Now `extractBogeysFromUpload(bytes, mediaType)` sends a Claude `image` block for PNG/JPEG/WebP/GIF (PDF keeps the `document` block; `extractBogeysFromPdf` stays as a back-compat wrapper); `resolveBogeysUploadMediaType` maps MIME-first with extension fallback ONLY on empty/octet-stream so a known-unsupported MIME (image/heic) can't sneak in via a ".jpg" name — iPhone *screenshots* are PNG and work directly; HEIC *photos* still need converting, and the 400 message says so. Route + `uploadStatementPdf` optional contentType (R2 archives keep the right type) + `BogeysUploadButton` ("+ Upload bogeys PDF/screenshot", accept extended) updated. +7 tests.
- [x] **Sunday 7/26 briefing cut off mid-sentence — FIXED 2026-07-26 evening (`50a4463`)** (root-caused same day). The 7/26 briefing was CLOUD-sent (local `calendar_briefings` newest = week-of 7/13; Mac slept through the Sunday window 7/19 AND 7/26) and `fallback-briefing.ts` generated at `maxOutputTokens: 8192` with only a `content` non-empty guard — a length-stop shipped mid-sentence. Fix: cap raised to 16384 + `finishReason === "length"` now appends an honest "*⚠️ This briefing was truncated at the model output limit…*" disclosure + `console.warn` (ship-anyway by design — a truncated Sunday briefing beats none). Sibling audit: `fallback-evening` already guards finishReason; digest/newsletter fallbacks use small per-article outputs — the briefing was the only gap. Worker DEPLOYED same session (version `34a1620e`) — live before next Sunday. +3 tests.
- [x] **Armed fixer run #1 (2026-07-28 02:45, autonomous) — WORKED AS DESIGNED + output landed same day**: classified 34 new findings (38 auto / 11 needs-decision → `DECISIONS-PENDING.md` / 0 needs-repro), fixed 4 (all HIGH → PR #25, correctly zero auto-merges under the LOW-only config), skipped 7 stranded on the sweep's own branch, skipped deploy. Same-day land (user-reviewed, MERGE-READY verdict): sweep branch merged `9f52e70` (7 fixes incl. HIGHs `f4d8a64` Accounts KRW FX + `109648c` consensus_value precedence — composes cleanly with PR #24's plausibility gate), PR #25 cherry-picked as `1c41ab2` (levels whitelist on display paths, 2 findings) + `128f5ce` (fixed-income keeps statement bonds), duplicate `160541d` skipped (superseded by `5544284`), PR #25 closed, branches deleted. Suite 3911/3911. **Fixer collision lesson: the fixer and an interactive session independently fixed the same cost-basis bug hours apart — check `DECISIONS-PENDING.md`/PR before hand-fixing ledger findings.** Reviewer follow-ups (non-blocking): `formatProfitFactor` lacks a unit test; `lib/digest/todays-reporters.ts:79` still reads bare `consensus_estimate` (email-surface parity with `effectiveConsensus`).
- [x] **Small follow-ups from the 7/28 overnight-land review — CLOSED 2026-07-28 evening session (`ca5a394`)**: `formatProfitFactor` moved to `lib/format.ts` (number-rendering single source) + unit tests incl. the 99.9 sentinel→∞; `todays-reporters.ts` now applies `effectiveConsensus` (TDD'd, email-surface parity); bonus: dead `SECTOR_MAP` export deleted from `lib/data/security-classifications.ts` (zero importers, non-GICS + stale tags — sector-session residual (d) resolved).
- [x] ✅ **Review + land the 2026-07-29 overnight branches — DONE 2026-08-03** (QA landing sweep): sweep branch fixes cherry-picked (`84f4c9a` equity-curve reindex, `b423e0f` CSV-guide header, `4e8a44a` allocation normalizer) and the fixer's 4 HIGHs landed (`7ca057d` position-risk gate, `9678f4d` charts last-price, `e2bafb7` alloc-% anchor; email-html superseded by PR #29's both-renderers `c1dff9f`). All 15 stale qa-* branches verified patch-equivalent vs main and deleted.
- [ ] **QA housekeeping: ledger burn-down — 73 open (6 high) as of 2026-08-03 pm re-baseline** (26 new + 47 known; high = FIFO audit [in motion, fixer-guarded] + VGT split [fixer scripts it tonight] + 4 knowns of which equity-curve reindex already has its fix on main `84f4c9a` awaiting post-rebuild sweep confirmation; remaining highs: classification asset-class synonym regression, bogeys save-actuals silent no-op, levels accept-arms-beyond-plausibility). Earlier history: was 66 open (8 HIGH) 7/29 am (was 44; the 7/29 overnight sweep minted new findings — but 4 of the 8 HIGHs already have verified fixes on `qa-fix-work-20260729` and 4 more findings are fixed on the unmerged sweep branch, see the review-and-land item above; the 2026-07-28 session closed 7: 3 HIGHs + suggest-all's cloud position_context bug + chart-disposal regression-5 verified-fixed + earnings-email archive + week-nav, and recorded 10 DECIDED plans so the fixer classifies them auto). Earlier history: (was 54) (overnight sweep: 37 new + 22 known re-seen = 59; the sweep's own auto-fix branch `qa-deep-fixes-2026-07-27` carried 5 fixes incl. 2 HIGHs — user-reviewed + merged `b81047e` same day, entries → `merged-awaiting-deploy`). Sweep verdicts: **ARM-gate-3 dedup-hold worked live** — PR #24's three `pr-open` fixes were match-and-held, not re-minted; 2 of 3 awaiting-evidence items CLOSED (trust-strip dates `b40bdc1`, import NaN `77b2118`); **chart disposal re-surfaced as `charts-mode-toggle--object-is-disposed-regression-5`** (5th regression, seen 7/27 post-rebuild — `4786597`/`0ea7c11` did not hold; true repeat offender, needs a root-cause session or the LightweightCharts patch bump filed 7/12). **All 3 HIGHs FIXED 2026-07-28** (`5544284` cost-basis COALESCE fallback → live 108/130; `d2a39fb` equity curve fullCoverageOnly → ends 119.9 vs TWR +18.12%; `6cca72a` position-risk whole-portfolio weight denominator → SPY 16.5%→4.67%, verified TRUE weight). Browser E2E on all three; investigation of the E2E's card-vs-drawer discrepancy proved the CARD correct and surfaced a new medium finding (drill-down lists un-aggregated per-account rows — `analysis-drilldown-risk--per-account-rows-read-as-whole-positions`, needs an aggregate-vs-label design decision). The remaining new findings have NO `DECIDED:` plans yet — the armed fixer classifies auto/needs-decision on tonight's first run. The xxxx-theme fix still carries its user-run cache-purge companion step (`scripts/repair-*` convention).
- [x] **Nightly smoke QA: portfolio-value extraction + run-death — FIXED 2026-07-20 pm.** Two-layer root cause: (a) the selector targeted `.text-4xl.font-semibold.font-mono`, which exists only in `PerformanceMetrics.tsx` — mounted NOWHERE since the 2026-04-29 IA collapse (`/dashboard` redirects to Today), so extraction was empty on every page; (b) `compare_with_tolerance`'s bare `float('')` exited python non-zero and `set -euo pipefail` killed the ENTIRE run at the first value check — the 8-tab smoke died nightly at line ~207. Fix: value now read from `GET /api/summary` (`totalValue` — the Electron-tray contract, redesign-proof), both compare helpers return `INVALID` on non-numeric input (a bad extraction FAILs one check, run continues), expected re-baselined from the April value. Clean run: 15 PASS / 7 FAIL / 2 SKIP, footer reached.
- [x] **Nightly smoke QA: 7 stale pre-redesign scenario checks — REWRITTEN 2026-07-20 evening, first-ever fully-green run (26 PASS / 0 FAIL / 0 SKIP).** All checks retargeted to the 6-tab IA with every selector live-verified against :3099 before landing: overview→Today (account count from the hero-strip "N accounts" text, data confidence via `button[title^="Data confidence"]` — the old data-health anchor only exists inside the popover now, portfolio-chart→"IBKR today" block since Today renders no chart by design), analysis split into `?view=performance` (drawdown+Sharpe, TWR relocated here with 5pp tolerance, XIRR) and `?view=diagnostics` (factor/scenario section headings — stable even mid-fetch), holdings/calendar navigate their redirect DESTINATIONS (`accounts?id=all#holdings`, `today?view=week-ahead`). Key gotcha caught live: `innerText` returns RENDERED text, so CSS-uppercased labels ("Week ahead", "Max drawdown") need lowercased matching — both would have shipped as new false FAILs otherwise. Also fixed the zero-FAIL summary printing "00" (`grep -c` prints 0 AND exits 1; `|| echo 0` doubled it). `qa/test-scenarios.md` rewritten to match; expected-values re-baselined (untracked, verified gitignored).
- [x] **Transcript cache: thin 8-K row permanently blocks the real transcript — FIXED 2026-07-19.** Root cause was one layer up from the TODO's guess: `fetchTranscript` already upgrades cached `edgar_8k` rows via AV on every cache hit, but the sweep orchestrator's own cache check `continue`d before ever reaching it. Fix in `lib/transcripts/same-day.ts`: cached-EDGAR rows stay candidates on a slower clock (10-day deadline from release / 24h pacing vs 36h/30min fresh — AV posts transcripts days later; ≤1 AV call/day/name vs the 25/day free tier), fresh candidates win the shared budget, failed upgrades (fromCache echo) never re-burn the AI desk note. Digest block gains best-source dedup so edgar + AV rows landing in one 24h window render once. Live repair: `scripts/upgrade-edgar-transcript.ts NFLX 2026 2` upgraded the poisoned row 4,091 → 36,126 chars with a real desk note (script kept — reusable). +9 tests.
- [x] **Digest `## Call transcripts` block: desk-note headings render un-demoted — FIXED 2026-07-19.** `demoteEmbeddedHeadings` in `lib/digest/call-transcripts.ts` at RENDER time (cached rows untouched): a leading title heading is dropped (duplicates the block's own `### TICKER — Qn call` header), other heading lines become `**bold**`; runs before truncation so the bold-marker balancing sees the final marker set. The upgraded NFLX desk note (leads with an H1) e2e's this in the 7/20 morning digest.
- [x] **Worker cloud-enrich reprocesses superseded sibling rows every tick — FIXED 2026-07-20 pm** (`superseded` skip in the candidate filter + `SnapshotCalendarEvent.superseded`, mirror of `ba1b39f`; pinned by a no-candidates test).
- [x] **Worker fallback synthesis lacks the held-ticker backstop — FIXED 2026-07-20 pm.** Scope finding during the fix: only `fallback-evening` has a synthesis path (`fallback-digest` composes per-source — no backstop needed there). `enforceHeldSections` Worker adaptation in `fallback-evening.ts` (buckets are `Record<symbol, RecentArticleMeta[]>`; issuerSiblings from fallback-earnings), wired as the last step of `synthesizeViaAI`; pure tests + a wired-through-`runFallbackEvening` test assert a buried held name reaches the sent email as a citation stub.
- [x] **Held name with essay-only coverage invisible + calendar-only empty-calorie sections — BOTH SHIPPED 2026-07-20 pm** (brainstormed same-day, spec `docs/superpowers/specs/2026-07-20-digest-thin-coverage-design.md`, 4-task subagent-driven build merged `b061f39..65dde7b`, final review READY 0C/0I). Calendar-listing-only held buckets (breadth-≥8 detection) partition out of synthesis into one `On this week's calendar:` roster line; unfiled essays render a `📄 Deep dives:` pointer line; Worker evening mirror (roster only) + `LISTING_BREADTH_MIN` parity pin. **E2E watch: still open after 7/21** — the 7/21 digest had only 3 commentary articles (<5) so it took the per-source fallback and synthesis (where the thin-coverage partition lives) never ran; correct-by-design, nothing exercised. Expect the roster line on the next heavy morning (~7 calendar stubs → one line); watch for family double-mention / dual-line cosmetics.
- [x] **Deploy-scoped `CLOUDFLARE_API_TOKEN` — DONE 2026-07-20 pm.** Account API token `crimson-block-4047` (Workers Scripts:Edit + Workers Tail:Read) in `.env.local`; verified end-to-end with a real deploy under the token (version `db728296`). Gotcha documented in the `.env.local` comment: wrangler reads the SHELL env, not `.env.local` — headless deploys thread it explicitly (`CLOUDFLARE_API_TOKEN=$(grep '^CLOUDFLARE_API_TOKEN=' .env.local | cut -d= -f2-) npx wrangler deploy`).
- [x] **Earnings newsletter fill — single-ranked-source SQL flood hardening — FIXED 2026-07-20 evening** (final-review Minor j, 2026-07-17). Went with the window-function option: `PER_SOURCE_FETCH_CAP = MAX_NEWSLETTER_ARTICLES × 3` (=18 — derived so a genuinely single-source symbol still yields 6 usable post-supersedence articles for pass-2) via `ROW_NUMBER() PARTITION BY source_id` before the rank-aware `LIMIT 30`. Because 18 < 30, a capped flooder leaves ≥12 pool slots for other sources AND `distinctSources` becomes truthful for free (1-source pool now implies only one source covers the family) — no separate `COUNT(DISTINCT)` query needed. Regression test watched-fail first (6× flooder, others evicted).

- [ ] **[FX] Watchlist current-price stays native for foreign names** (deferred by design, 2026-07-06 read-surface sweep). `getActiveWatchlist` renders `current_price` unconverted because the user-entered `target_price` is also native — converting only the live side breaks the upside-% and distance math. Zero foreign watchlist rows exist today. Revisit (convert BOTH for display, keep native for edit) if a foreign name is ever watchlisted.
- [ ] **[FX] Form 8949 tax report — foreign realized gains stay native-currency** (deferred by design). `/api/tax-report` / `lib/compute/tax-report.ts` / `getClosedTaxLotSales` intentionally USD-unconverted (historical/tax-accurate FX out of scope). Revisit only if a foreign position is ever sold and tax-accurate FX (per-lot acquisition-date + sale-date rates) is wanted. Medium.
- [x] **Drop legacy trade_roundtrip columns — ALREADY DONE, stale entry closed 2026-08-05** — migration `075_drop_legacy_roundtrip_columns.sql` shipped 2026-08-03 with the ledger rebuild; verified zero `entry_thesis`/`exit_assessment` references remain in `app/api/trade-review/route.ts` or `lib/mutations/trade-reviews.ts`. This entry predated the drop and was never reconciled.
- [ ] **Watch `period-attribution.ts` long-only behavior** (2026-05-10 reviewer flag) — `computePerPositionContributions` uses `quantity > 0`, silently excluding shorts from sector contribution. Re-evaluate when short positions exceed ~5% of NAV. *(Checkpoint 2026-07-07: 2 short rows ≈ 0.1% of gross MV — dormant.)*
- [ ] **Gmail filter for "forwarded via Cloudflare" mail** (2026-05-08) — test reply forwarded by Cloudflare Email Routing landed in All Mail, not Inbox. Defer until first real reply traffic; if placement persists, add a Gmail filter for the forwarded-via-Cloudflare pattern.
- [ ] **running-marker shared-key clear race** (found in the 7/12 triple-send investigation, mitigated not fixed) — `mac-running-{type}-{date}` is one KV key with un-tokened set/clear: run A's `finally` clear wipes run B's live claim, opening the Worker-fallback window. The new send mutex makes concurrent same-type runs impossible in ONE process, so this is only reachable cross-process (dev :3000 + Electron :3099 both cron-triggered — not a real deployment shape). If it ever bites: claim_token pattern from migration 063.
- [ ] **Morning-digest synthesis truncation on heavy windows** (observed 2026-07-15 during overnight-block E2E) — a 24-article window fell back to per-source with "Synthesis returned no usable content: output truncated by max tokens" despite the 8192-token cap raise. The fallback is the designed degradation, but if Mac-sent digests read per-source-ish on heavy days, revisit the cap or article batching in `lib/digest/synthesize.ts`. Low urgency; observe first.
- [x] **Earnings-intel accepted-deferred minors — BATCH SHIPPED 2026-07-21 (`935823e`, TDD throughout).** In-flight claim sets (events + AV families) close the TTL-stamps-after-compute race on overlapping cockpit polls (forceFresh bypasses the event claim by design); `latestSpot` rejects closes >4 calendar days old (long-weekend tolerant, mirrors findCrossedLevels) so a stale denominator can't fake a straddle; C/P `secdef/info` via `Promise.all`; `decorateCockpitIntel` family-dedupes history reads (GOOG+GOOGL = one query); migration 069 CHECK constraints on `implied_method`/`report_time` (table rebuild — SQLite can't ALTER ADD CHECK; applies to live DB on next app boot); boundary pins dte 21/22 + spread 0.5×mid + ATM-tie order; `formatEtTimestamp` EST/EDT/midnight-24 unit tests. Only the iv_approx 60% ceiling skipped (documented-deliberate in the original triage).
- [ ] **Plaid accepted-deferred minors** (final-review triage 2026-07-10 — batch when touching `lib/plaid/`): `plaidPost` unguarded `res.json()` on a non-JSON 5xx (raw SyntaxError instead of PlaidApiError); sync-route null message omits the not-configured cause; PlaidSection skip-reason branches unreachable while the in-app route forces; benign Link-cancel styled as an error; multi-tab `vgs:plaidLink` clobber edge; launchd script polish (.env.local guard / 2xx regex / tick-start log); plaid path doesn't run purge-expired-options/matured-bonds/classifyFactors (IBKR Web API path parity — new mid-month buys stay factor-unclassified until next TWS connect).
- [ ] **Today's data-quality chip is page-level, not per-row** (Scenario 1 #5). Spec-vs-code divergence: scenario expected per-row chips but `today/page.tsx` renders one `qualityChip` at the H1. Either update the SCENARIOS.md checklist OR ship per-row chips if the UX reads better. Defer unless user asks.
- [x] **HIG audit Low-severity tail — SHIPPED 2026-07-20 evening** (all three items): (a) app-wide gold-ink sweep via 4 parallel subagents with a rulebook — **152 swaps across ~75 files**; keeps documented per line (large-text, hover-states — `text-gold-ink hover:text-gold` idiom, icons/glyphs, opacity variants, and the critical dark-module exclusion: **LevelsPanel renders EMBEDDED inside MarketDataPanel** so its gold stays; SecurityChart's status banners kept as mixed-context; pill actives swapped since the active pill carries its own light `bg-panel`). (b) crosshair legend CONFIRMED broken and fixed: light-theme `--ink` #0a0a0a was invisible on the dark toolbar — new `.dark-module-chart .chart-legend` scope in globals.css re-maps `--ink/--ink-faint/--up/--down` to dark values (CSS vars resolve at use time, so every utility inside re-colors without specificity games; also fixed the equally-invisible symbol span); browser-verified computed rgb(229,229,229) in light theme. (c) footer "via TWS" label gets `pr-14` clearing the FAB's ~72px corner zone (fix in SecurityChart's shared footer so Security Detail benefits too). Verified: 3,699 tests green, 7-page browser sweep clean, swapped elements compute #8a6508.

- [x] **Anthropic API outage 2026-07-28 (~2h) — RESOLVED same day**: credit balance ran dry mid-sector-sweep (user topped up), then the org's monthly spend cap tripped (user raised it ~1pm ET). Post-restore checklist DONE: straggler verification re-run (**190 stocks now `gics_verified`**, 354 option rows synced; drift panel holds only 5 legitimately-unresolvable rows — KSTR/XRT mistyped ETFs, TWTR/OLO delisted, VCX refused — their real fix is a `security_type` cleanup), macro themes force-regenerated all 4 scopes (5 themes each — confirms AI Gateway path live). Passive watch: tonight's 7pm evening email + tomorrow's 8:45 digest confirm the cron paths naturally. Lesson for the silent-failure checklist: the usage-limit 400 ("You have reached your specified API usage limits") is a DISTINCT failure from credit exhaustion — both land as invalid_request_error 400s, checked at Console → Settings → Limits vs Plans & Billing.

## Live watches (passive — confirm on first natural occurrence, no code action)

- [x] **`## Today's reporters` block — FIRST VISUAL RENDER VERIFIED 2026-07-21** (7/21 morning digest, Mac-sent 8:47, single send): `AMC 16:15 · IBKR · held · $0.62 · $1.75B · ±4.5%` — all four watch columns (slot, position chip, consensus, cached implied move) rendered. The ±4.5% vs the 9:02 straddle recompute (±6.34%) is the designed "cached as of send time" behavior, not drift.
- [x] **First ≥3-recap same-slot day — EOD wrap debut — SUPERSEDED 2026-08-02.** The wrap DID debut (7/29 MSFT/META/HOOD at 20:12 ET, 7/30 AMZN/HUN/AAPL at 20:20 ET, BMO wrap 7/30 12:07) and the user's verdict was that 4-hour-late evening staples carry no value — the wrap is now RETIRED from the Mac sweep, replaced by the 7:45 ET morning debrief (see Closed this session 2026-08-02). New watch below.
- [ ] **First morning-debrief send (added 2026-08-02)** — next heavy AMC night (≥3 held same-slot reporters): individual recaps skip `wrap-pending` as before, then ONE "☕ Earnings Debrief — {date}" email lands 07:45–08:20 ET next morning with AI synthesis (no headline restatement) + per-name scoreboards + desk-note guidance excerpts + call notes + ET-timed roster of individually-recapped names; per-member recap chips + viewer show the debrief. Recovery if a morning is missed: `npx tsx scripts/send-morning-debrief.ts` (3-day self-healing lookback).
- [ ] **First read-through print push (added 2026-07-16; ticker corrected 2026-08-02)** — a **PRLB** print should push `→ XMTR (held): …` even though PRLB isn't held; verify title carries "— read-through". (Pair was seeded as dead ticker PRTO for its whole first season — fixed + PRLB 7/31 print backfilled `87fb461`, so XMTR's 8/04 preview carries the read-through block; the PUSH leg still awaits PRLB's next print.)
- [ ] **First auto-correction by the date-verification tier (added 2026-08-02)** — the 05:00 ET daily pass (hooked into the earnings sweep) should stamp/correct rows and Pushover a summary on corrections. The manual 8/02 `--apply` already corrected MELI 8/03→8/05 live; watch for the first fully-autonomous one + confirm corrected rows enrich (manual source_keys now ride the Finnhub road).
- [ ] **First TWS-sourced anchored reaction snapshot (added 2026-08-04)** — after the useRTH=0 + prior-close-anchor fix, the next earnings enrichment with TWS connected should produce `source:"tws"`, `pre_anchor:"prior_close"` (check `json_extract(reaction_snapshot,'$.source')`). Zero TWS reactions had EVER been captured for off-hours releases (the 7/19 "every reaction is yahoo" observation was this bug, not usage). Also confirm the first AMC capture anchors to the 16:00 bar.

- [x] **Straddle road — ROOT-CAUSED AND FIXED 2026-07-20 pm (RTH probe).** The RTH-timing hypothesis was wrong: during market hours a fresh session returned empty strikes for 9+ polls across BOTH JUL26 and AUG26 — polling alone never warms the cache. One `/iserver/secdef/search?symbol=X&secType=STK` call makes strikes populate on the FIRST poll (the 7/8 probe's "retries warm it" held only because that session's own search calls had primed the cache). Fix: warm-up search (best-effort, non-fatal) added to `resolveAtmContracts` before the month scan + probe script; `ResolveArgs` gains `symbol`. **Live-verified on production**: GOOG event 972 now has `implied_method='straddle'`, ±5.82% ($20.53 straddle mid / $352.62 spot, expiry 7/24) — the first straddle row ever. Residual watch: confirm this week's preview emails carry straddle-method implied moves (BMO pre-market + AMC post-close compute times should now work too, since the warm-up, not RTH, was the blocker). *(7/21 progress: IBKR event 974 recomputed as `straddle` ±6.34% at 9:02 ET — second-ever straddle row, first on a same-day reporter; remaining leg is eyeballing the ~14:15 ET IBKR preview email for the straddle figure.)*
- [x] **Cockpit intel line — VERIFIED ON A REAL REPORTING DAY 2026-07-21** (IBKR AMC): browser-verified `impl ±6.3% · hist ±5.1% (5/8)` on the AMC lane row at desktop 1440×900 AND mobile 390×844 (graceful 3-tier reflow, no overflow/truncation), countdown + stage chips + exposure all rendering, 0 console errors both widths.
- [ ] **First natural `ITEM_LOGIN_REQUIRED`** — confirms the reauth lifecycle live: Pushover once, Settings Reconnect → Link update mode → post-reauth auto-sync clears the banner.
- [x] **B8 Worker AMC reaction capture — VERIFIED LIVE 2026-07-19** (via NFLX 7/16 + Worker observability logs). NFLX's real AMC print (release 16:15 ET) carries a `yahoo`-sourced reaction snapshot captured by the Worker's evening ticks (every 15-min tick 17:30–20:00+ ET logged `primary: 530/1016` → `fallback: success, candidatesProcessed: 5`; Mac reconciled + DELETEd KV payloads each tick) and the recap was delivered — Yahoo 1-min bar latency at the evening tick is proven fine on a real AMC print. Caveat: the literal "Mac fully asleep" variant didn't occur (Mac was awake, TWS-less; reconcile path did the merge) — the capture road itself is what the watch existed for. Bonus observation: every reaction since 7/14 is `yahoo`, zero `tws` (TWS not connected all week) — expected given usage, not a bug.
- [x] **Duplicate-email fix soak (`ba1b39f`) — CLOSED 2026-07-19, all legs clean.** Sunday leg: exactly ONE briefing for 7/19, `sentBy: cloud` at 16:47 ET, `via: "sent"` (Worker KV marker). Mac was in DarkWake/Deep Idle on battery through the 16:30 window (pmset log), so launchd never ticked (weekly-email log has zero entries since 7/12 — sleep, not breakage; `bash -n` clean, job loaded) and the Worker fallback covered it at the 16:45 tick, single send. The launchd-retry leg didn't compete Sunday but was already verified on the 7/16+7/17 weekday legs. Six days post-fix, zero duplicates (history was 6/28 ×2, 6/30 ×2, 7/01 ×2, 7/12 ×3).
- [x] **R1b "Web API synced" label — CONFIRMED 2026-07-20** (incidental, during the smoke-QA fix verification): header chip read "TWS Disconnected · Web API synced 1h ago" in the live page snapshot — the label renders as designed.
- [ ] **TWS-derive FX base verification** ([FX-GOLIVE] residual) — on the next live TWS sync with a foreign position, check the log: a `[fx-rates] Skipped tws_derived write` line confirms TWS `Position.marketValue` is native too (then delete the TWS derive); a clean write near the ledger rate confirms USD-base.

## UX / page-level rework

- [ ] **Tab + subview tree rethink** — collaborative walk through the full tab + subview tree (Today / Accounts / Analysis / Research / Charts / Import, plus all subviews) to decide what belongs as a top-level tab vs a subview vs a settings-only surface. Brainstorm session before any code. (Part (a) — caret → `•••` — shipped 2026-05-01.)
- [ ] **Research Feeds filter-row UI rethink** — find a better UI for the source filter (the chip row became a `<select>` at some point; brainstorm wrap-to-lines / combobox). (Part (2) — mobile overflow + `.scrollbar-none` — shipped 2026-06-10.)

## Features / workstreams

- [ ] **C — In-app email schedule controls (Electron in-process scheduler)** (deferred from plan, full workstream). C1 AppSettings fields + migration, C2 SettingsModal "Email Schedule" section, C3 `electron/email-scheduler.ts` (two `setTimeout` chains aligned to ET wall-clock), C4 retire launchd plists after 7-day soak, C5 `/api/digest/status` returns `nextDailyAt`/`nextWeeklyAt`. ~1 day total. Quality-of-life, no urgency.
- [ ] **H — Per-source signal-extraction profiles** (deferred from plan, multi-session). Schema (`research_source_profiles` + `research_article_extracts`), 4-stage pipeline (format detect → section filter → structured extract → summary), 6 starter profiles (Vital Knowledge Catalyst Watch / Eliant / James Bulltard / Bloomberg Odd Lots / FundaAI / Helene Meisler), briefing prompt integration, discovery-loop UI. Multi-day; needs its own design doc. Plan section H1-H6 in `~/.claude/plans/i-want-to-plan-iterative-sedgewick.md`.
- [ ] **Slack integration** (2026-04-30) — scope TBD. Three directions: outbound (briefing/digest/alerts → channel), inbound (Slack threads as a research feed peer to Gmail), or both. Revisit when ready.
- [ ] **Background-refresh tail** (from the 2026-06-09 refresh-policy audit) — factor-analysis cache; portfolio risk-metrics roll-up. Both low-priority (compute-on-demand is fast enough today).
- [ ] **Worker fallback content parity with Mac** — mostly superseded (user notes + earnings bogeys mirrored since snapshot v5; B8 shipped cloud recap parity). Analyst recs + prior transcripts remain deliberately NOT mirrored per the divergence rationale (documented in CLAUDE.md's earnings-digest entry). Reopen only if a cloud-sent email reads materially thinner than the Mac's in practice.

## Roadmap / future

- [ ] **OpenAI voice API integration** — scaffolding started in Claude Co-Work. (a) output/read-aloud: briefing read aloud, per-company deep dives narrated, alert triage by voice; (b) input/voice bug reporting: in-app record button → Whisper/Realtime transcription → structured filing (destination TBD; likely capture a screenshot too). Scope: design doc + capture flow + first surface (probably Today "speak the briefing").
- [ ] **Privacy mode as default + Face ID / biometric unlock** — default privacy ON everywhere; unlocking real numbers requires Face ID (iOS WebAuthn) / Touch ID (Electron). Pieces: flip `PrivacyProvider` default + migrate localStorage; WebAuthn-gate the toggle on mobile; Electron biometric prompt; auto-lock policy (re-mask after N min idle / on focus). Cross-cuts voice output (reading real numbers aloud should respect the unlock state). Design doc first; biometric plumbing on iOS-Safari-over-Mesh + Electron is the risk surface.

## Deferred

- **E2E browser tests** (Theme J2, ~15 hr) — dedicated session.

---

## Closed this session (2026-07-20)

> All four found by grading the 7/20 morning digest against the composer rules (user-requested audit). Tests 3646 → 3673, `next build` clean, Worker deployed (version `33d074e5`), pushed `acac5d6..ff9b90b`.

- ✅ **Call-transcripts block → compact desk-note notice** (`865601c`) — the NFLX debut rendered a 600-char teaser cut mid-sentence + 1,140 chars of raw safe-harbor Q&A labeled "Guidance:" (the `extractGuidance` keyword-regex fossil). User decision: Guidance section + one-line Tone digest + deep link to Security Detail; raw `guidance` column never rendered; header carries the call date (SQL-coalesced from the out-of-window 8-K sibling — AV rows never have it).
- ✅ **inlineFormat URL corruption + access_token credential leak** (`74032c3`) — emphasis regexes ran over generated `href`s; underscores in JWT URLs (Stratechery `?access_token=`, beehiiv redirects) became `<em>` inside the attribute and the raw token leaked as visible text in the cc'd email. Fixed with NUL-placeholder protection in BOTH renderers (Mac + Worker mirror, both pinned); `cleanUrl` now strips `access_token` at extraction; `scripts/repair-source-url-tokens.ts` scrubbed all 68 stored rows.
- ✅ **Held-ticker section enforcement** (`de862f7`) — held CSX with two-article VK coverage was relegated to "## Also covered" despite the pinned prompt rule. `enforceHeldSections` post-processor (issuerSiblings-aware) appends a deterministic citation stub for any held bucket missing a `##` section. Mac-side only (see open item below).
- ✅ **BTC Overnight chip → rolling 24h** (`ff9b90b`) — chip said −0.1% vs VK's "dipped 75bp": daily-close pair measures only the partial UTC day for a 24/7 asset. New `fetchYahooRolling24hPct` (hourly bars); Worker mirror deliberately keeps the daily pair (its one-spark design sits at 49/50 of the subrequest cap; cloud block only runs Mac-asleep).

New open items from the same audit (added above): Worker synthesis held-ticker backstop, essay-only held-name visibility, calendar-only stub-section tension, deploy-scoped wrangler token.
- **Electron code signing** — `xattr -cr` workaround accepted for local-only distribution.
- **Finnhub surprise history** — free tier returns `[]`; revisit only if briefings feel thin. *(Intelligence-tier #10 needs per-symbol surprise history — the data-source decision likely resolves there.)*

## Known issues

- **WSH API error 10276** — "News feed is not allowed" (2026-04-07). May need TWS subscription time.
- **`next build` data collection** fails with an existing `data/vanguard.db` on disk. TypeScript compilation still succeeds. All 10 DB-loading pages are `force-dynamic`, so this only affects build-time collection, not runtime.
- **Chat levels/alerts/notes tools are security-scoped, not account-scoped** (U2 residual, known limitation, not a leak) — the scope hard-clamp covers the 9 account-bearing tools; levels/alerts/notes have no `account_name` param.
- **`$0` literal collides with skill-runner arg substitution** — noted in the import-monthly-statements skill file; if other skills ever render `$0`/`$1` literals, same trap. No action unless it bites again.

## Reminders (no code action)

- **August 2026 Vanguard import (added 2026-08-04)** — the July statement's Unsettled section (LAND/HUN sells, PAYC/PCTY 30-share covers, 91282CQQ7 T-note buy, 4 option closes; all settle 08/03) will appear in August's Completed transactions — import them then, per the skill's unsettled-activity rule. Also expect HUN's 0.543 orphaned fractional to liquidate (statement discloses the program). July gates record: `~/Desktop/Trading - Local/canonical/2026-07/GATES-202607.md`.
- **Hand ChatGPT the refreshed GitHub link** — README / screenshots / CHANGELOG were refreshed 2026-07-08 (`2430bf3`), so the factor-analysis collaboration can resume against a repo that matches post-redesign reality.

## Active design docs

- [2026-04-15-cloudflare-saas-rewrite.md](2026-04-15-cloudflare-saas-rewrite.md) — SaaS v3 scoping (D1, TWS bridge, multi-tenant, ~7 months)
- [2026-07-04-earnings-season-audit.md](2026-07-04-earnings-season-audit.md) — earnings-season audit; §4C #11-13 / §4E #17-18 is the open intelligence/volume scope

---

### Closed this session (2026-08-05 late evening — wire-time follow-ups batch + small-item cleanup)

> Second half of the evening session, user-picked off the "what else" menu. Subagent-driven build (6 tasks, 1 fix round), final whole-branch review READY; suites 4,362 Mac + 457 Worker green; commits `87f524f..25a9f6b` pushed; DMG rebuilt at session end.

- ✅ **Wire-time follow-ups (a)–(f)** — see the ticked entry above for the full breakdown (error propagation, family-aware verdicts, cascade on conflict-confirm, conflict-popover editor, earlier-wins upsert guard, protective probe stamp).
- ✅ **Negative-hpd "short" chip** — four surfaces via shared `<HoldingPeriodBadge>`; review fix round caught the snake_case Trade Grades surface + the unreachable header branch.
- ✅ **Stale legacy-columns TODO closed** (migration 075 had shipped 2026-08-03 unreconciled) + ✅ **`DEFAULT_VERIFICATION_LIMIT` 8→12** (user decision on cap-saturation data).
- ✅ **CLAUDE.md reconcile** (`25a9f6b`): wire-time batch conventions, split-signature guard + repair-script entry, scope-all TWR/XIRR coverage-guard + signed-transfer-flow + cost-basis-fallback conventions.

### Closed this session (2026-08-05 evening — QA landing sweep: PRs #30/#31 + sweep branch merged with review fixes, VGT repair executed, all 5 decisions resolved)

> Session focus chosen by the user from the open-items briefing. Every stranded fixer output landed in one pass; suite 4,345 green (3 env-only worktree failures pass with the dummy key), tsc clean, pushed `4bf3fad..56777fd`.

- ✅ **Multi-agent review of PR #30 + PR #31** (code-review skill, 5 reviewers + scoring per PR): three findings confirmed at 100 — (1) XIRR aggregate terminal value misaligned with the flow window at the coverage boundary (a statement-lag deposit read as a loss), (2) the split-signature guard silently dropped real option-premium doublings (10+ live pairs in the 2x band — INTC/BYRN/XLU calls) plus genuine ~50% crashes, (3) suggested levels rendered USD next to the same panel's native accepted list (KRW showed the same level 1,500x apart). Review comments posted on both PRs; sub-threshold notes (Dietz 0.5-weight approximation, slot-less AMC popover dead-end — 0 live rows can hit it, stale mirror comment on `getCrossAccountPositions`) recorded there.
- ✅ **Landed everything via a landing worktree** — merge commits keep every fixer hash reachable for the ledger (`8737da9` PR #30, `dedbc07` PR #31, `8abbdad` sweep branch; the `holdings.ts` overlap composed cleanly: `NULLIF(scaledCostBasisFallbackSQL, 0)`), then `56777fd` review fixes: XIRR terminal-date alignment (+ regression pin), option exemption on the split guard (+ pin at the exact 2.0 boundary), LevelsPanel suggested prices back to native (ATR stays USD — mirrors the KPI row), repair-script residual verify moved INSIDE the transaction (rollback instead of half-adjusted DB), split guard added to `refresh-vanguard-betas.ts` (sibling gap), `coercePercent` doc comment un-orphaned. GitHub auto-marked both PRs MERGED; all qa-* branches (local + remote) verified patch-equivalent and deleted, plus stale `worktree-deferred-decisions`/`worktree-nasdaq-enrich-road` remotes.
- ✅ **VGT 8:1 split repair executed** (see the closed `- [x]` entry above — backup, dry-run, apply, idempotency re-check, betas refresh).
- ✅ **All 5 pending QA decisions resolved by the user** — `DECIDED:` plans recorded on the ledger (dispositions flipped to `auto` for tonight's fixer): KRW tax-lots **both layers** (exclude+disclose now; broker-USD parser capture filed as its own TODO above — the user's "look through the IBKR statement" instinct verified: the July statement reports 402340.KS realized at −$2,684.28 USD), ETF-type heuristic + user-run repair, BUY_TO_OPEN sign repair + validate warning, Defense narrative regenerate control + BRK/B prompt fix, inactive sources suffixed in the Feeds dropdown.

### Closed this session (2026-07-26 evening — QA burn-down: supervised fixer run #2 + all 4 session-scale fixes + ARM gates 2-4)

> Ledger 42 → 32 open in one session. Supervised live `/qa-fix-findings` run #2 (non-dry-run) exercised every pipeline stage end-to-end; then the 4 decided session-scale fixes shipped interactively with TDD + agent-browser verification; then ARM gates 2-4 closed. Tests 3804 → 3828, tsc clean throughout. 11 commits on local main + PR #24.

- ✅ **Supervised fixer run #2 (live)** — 4/4 attempted findings fixed: `749a4cd` notes empty-state (LOW, auto-merged + deployed mid-session), PR #24 (`537ed9c` week-ahead implausible-actual guard, `a703773` KRW transaction FX join, `d47ef7f` 52wk-range freshness arbitration + as-of render). 2 verify-and-closed against existing main commits (`a5f49f4` FAB offset, `8c52dae` view-sources) with browser evidence; 3 more left for the sweep. **Two skill bugs live-discovered + fixed**: suite gate needs dummy `ANTHROPIC_API_KEY` in the secret-less worktree (`fd6d5e3` — 3 mocked AI tests need the var to exist; a red-suite misread would revert good fixes nightly) and Step-6 `checkout main` is impossible in a linked worktree → `--detach` (`a293b30`). Run log appended to `qa/findings/fix-runs/2026-07-26.md`.
- ✅ **Market regression flow-adjusted** (`5955c1f`, HIGH) — repro on live-DB copy confirmed 3 mechanisms: recorded flows (IBKR −$100k = fake −20.9% day → beta 0.169→0.606 post-fix), an UNRECORDED mid-July external flow (+$189k Plaid-anchor jump 7/17 — self-corrects when the July statement imports its flow row; CONFIRMED 2026-08-04: July import landed the real flow rows, +$200k 7/17 + $40k 7/02), stale-price attenuation (Vanguard `estimated` 83/85 days — self-heals under daily Plaid anchors). Helpers extracted to shared `lib/compute/flow-adjusted.ts` (risk.ts refactored onto it; new module avoids a factors↔risk import cycle). +3 tests.
- ✅ **Newsletter style-block leak** (`69c265b`, HIGH) — `NewsletterArticleFrame` sandboxed srcDoc iframe (no allow-scripts; theme tokens resolved concrete; auto-height); BOTH render sites converted (Feeds expand + Security Detail mentions — cross-cutting sibling). Browser-verified non-vacuously: hostile CSS alive INSIDE the frame, app chrome byte-identical. +4 tests.
- ✅ **Phantom open tax lots** (`dc33080`, MED) — computeTaxLots broker-close reconciliation: engine-owned `RECONCILE_CLOSE` synthetic transactions (user-approved design), deleted + regenerated per run so the real statement SELL supersedes; stock/ETF only; ledger-fresher guard; breakeven fallback when no price. +10 tests incl. self-heal.
- ✅ **Mobile conflicts surface** (`09452f9`, MED) — Conflicts tab in `/dashboard/alerts` (`?view=conflicts`) sharing the bell count's exact predicate; list mode on `/api/earnings/conflicts`; `EarningsDateChip` gains `onConfirmed` + `popoverAlign="right"`; popover z-50→z-[55]. Verification pass caught 2 popover defects pre-commit (mobile clipping + chat-rail z-tie); both fixed + re-verified. +3 tests.
- ✅ **Low-R² honesty caption** (`1e6cb90`, user-requested follow-up) — warn caption on Market Regression when R² < `LOW_R2_THRESHOLD` (10%, `lib/analysis/interpret.ts`); dataWindowNotice's honest-labeling sibling.
- ✅ **ARM gates 2-4** (`135da2d`) — lock PID-liveness (live holder never torn down, Pushover past 180m; dead = stale immediately; traps rmdir→rm -rf), sweep dedup match-and-hold for `pr-open`/`merged-awaiting-deploy` (load-bearing TONIGHT: PR #24's fixes would otherwise re-mint as new findings), clean-checkout delivery preflight. **Remaining to arm: user reviews run-#2 fixes (gate 1) → flip `fixer.enabled` (gate 5).**

### Closed this session (2026-07-26 — QA auto-fix pipeline + decision burn-down)

> Brainstorm → spec (`9918935`) → 6-task plan (`94910a6`) → subagent-driven build, every task review-gated; final whole-branch review on the top model: READY, 0 Critical. Tests 3804/3804 + tsc clean. 18 commits pushed; DMG rebuilt.

- ✅ **QA auto-fix pipeline shipped disarmed** (`c89dd11..dc3e552`) — `/qa-fix-findings` skill + nightly chain in `qa/nightly-deep-qa.sh` + `qa/lib/pushover.sh` + `fixer` config block. See the `- [x]` entry + ARM checklist under Bugs/Quality. Live incident during the build: two subagents independently tripped the `check-todo-reconciled.sh` hook on build keywords embedded as DATA (one evaded — adjudicated, content verified safe; one complied) → the skill's Hard rules 9/10 (keep build keywords out of ledger strings; hook denial = maintainer feedback, never evade) were live-validated before the pipeline ever ran.
- ✅ **Both stranded `qa-deep-fixes-*` branches merged** (`f17b665` + `0ea7c11`, branches deleted) — import-NaN message, LWC markers-plugin disposal, macro-theme dead click, trust-strip calendar-day dates, NotesAmbient rail offset. Clears the ledger's "MERGE IT" flag; findings close on next sweep vs the rebuilt DMG.
- ✅ **Drawer z-index HIGH (4th sighting) fixed for real** (`e73aaae`) — MacroThemeReceiptDrawer + DrillDownPanel to z-[55]/z-[54] per the trust-strip precedent; Playwright-verified above the rail, Close/Escape dismiss. Ledger carries the fix commit (anti-bounce).
- ✅ **First supervised dry-run + full decision burn-down** — 42 open findings classified (27 auto / 15 needs-decision / 0 needs-repro); user resolved ALL 15 same session (every recommendation accepted; `DECIDED:` plans on ledger entries; 4 session-scale, 1 with user-run purge companion). Skill amended from run feedback (`e6904d7`). 5 ledger-note inaccuracies logged in `qa/findings/fix-runs/2026-07-26.md`.

### Closed this session (2026-07-23 — email trust: instruction leaks + already-reported guard)

> User reported 6 issues; 4 root-caused same-day, 2 filed for later (sector-tag corruption = own brainstorm-first session; dead-clicking = needs repro). The email-trust pair executed as a subagent-driven 8-task plan (`docs/superpowers/plans/2026-07-23-email-trust-session.md`), commits `e64a88b..d0dd31d` (9), final whole-branch review on the top model: With-fixes → all findings fixed same session. Tests 3768→3803 Mac + 431→437 Worker, tsc clean both, Worker deployed.

- ✅ **Digest instruction leaks (3 classes)** — desk-note validator + min-transcript gate + 3-row repair; key_themes per-element tag cleaning (storage + every render site, `lib/gmail/theme-sanitize.ts` extraction, Worker parity + parity pin) + 3-row repair; second-person voice (schema + prompt + lint pin) + 48-row repair. Collateral finds fixed: `stripModelPreamble` decapitated leading `**Guidance**` lines; `cleanThemeElement` (plan's own regex) clipped trailing apostrophes.
- ✅ **Already-reported guard (IMAX case)** — sweep two-layer guard + permanent skip + mac-sent KV marker (final-review catch: without it the Worker fallback re-ships the wrong-slot preview), composer + cockpit intel gates, prompt backstop, IMAX 07:30 BMO override (wire-verified).
- Accepted follow-ups noted inline on the two closed items above (backfill `date('now')` UTC; cached-EDGAR test fixture bump).

### Closed this session (2026-07-21 late evening — QA investigation trio root-caused + fixed)

> User picked the 3 investigation items off the QA ledger. All root-caused via systematic debugging (3 parallel trace agents, evidence verified against code), fixed TDD-first in one commit `9f198b7`. Tests 3757→3768, tsc clean, browser E2E 3/3 PASS with 0 console errors. Ledger 10→7 open (all 3 marked fixed WITH fix commit + root-cause notes — the anti-bounce discipline).

- ✅ **What-if silent unknown-symbol drop** — root cause: `applyLegs` (lib/compute/exposure-delta.ts) `continue`d past unknown/unheld legs and the `ExposureDelta` payload had no field to report it (info destroyed server-side). Fix: `droppedLegs[{symbol, reason: unknown_symbol|not_held}]` + inline ⚠ warnings in WhatIfCalculator (Cash-Deploy pattern). Also surfaces the sibling sell-what-you-don't-hold skip.
- ✅ **"+ Note" orphaned journal notes (4th sighting)** — root cause found for real this time: TWO add-note links on Security Detail; the top "+ Note" emitted `?security=<id>` which the composer never reads (it prefills from `?type=`+`?symbol=` only; `security` only filters the list), while the working in-section link only renders when notes already exist — which is exactly why 3 prior closures were non-repro flips. Fix: top href now `view=notes&type=trade_thesis&symbol=<sym>&security=<id>`.
- ✅ **Performance frozen risk-metrics** — NOT a wiring bug: `startDate` threads correctly, but drawdown/Sharpe/curve read `daily_valuations` (floor 2026-03-27; risk full-coverage floor ~04-06) while TWR reads multi-year `monthly_snapshots`, so 3Y/All select identical rows. Fix (user-chosen honest labeling): `computeRiskMetrics` exposes `seriesStart/seriesEnd`; new pure `lib/compute/data-window.ts::dataWindowNotice()` (7-day grace, reuses `addDays`) captions the KPI strip + equity curve with the actual daily-data window. Self-heals as history accrues.

### Closed this session (2026-07-21 — digest-debut verification + earnings-intel minors + QA triage)

> User picked 3 items off the TODO; all landed. Tests 3699→3721 Mac + 419→426 Worker, tsc clean both sides, pushed `ce16155..8521a72`, Worker deployed `a7c08d49`. Three browser E2E passes, 0 console errors.

- ✅ **7/21 digest verification — 3 live watches closed** (no code): `## Today's reporters` first visual render (IBKR row, all 4 columns), cockpit intel line verified on a real reporting day (both widths), digest single-send clean. Thin-coverage roster watch stays open (3-commentary morning → per-source fallback, synthesis never ran — correct). Straddle road: IBKR recomputed `straddle` ±6.34% same-day; residual = preview-email eyeball.
- ✅ **Earnings-intel deferred-minors batch** (`935823e`) — 7 of 8 items + migration 069 CHECK constraints; only the iv_approx ceiling skipped (documented-deliberate). In-flight claim sets, spot staleness guard, Promise.all C/P, family-deduped history reads, boundary pins, formatEtTimestamp tests.
- ✅ **QA ledger triage 17→10 with 7 REAL fixes** (`8521a72`) — the repeat offenders were bouncing on non-repro closure flips with no fix commit ever written. Root causes found: model dumps its whole tagged response INSIDE the summary string field (`sanitizeModelSummary` at the storage boundary, Mac + Worker mirror, 9 rows repaired via `scripts/repair-xml-summaries.ts`); Tailwind v4 preflight margin reset kills UA `dialog{margin:auto}` centering (`m-auto` on shared ConfirmDialog fixes all consumers); toast container now clears MobileBottomNav (computed 88px verified); Trade Review AI prose PrivateText-wrapped on all 4 surfaces; Notes filtered empty state; SettingsModal Escape. Sync-Feeds-400 reclassified stale-vs-main (fix landed 7/12 `5340107`; sandbox tests the deployed standalone — closes on this session's electron:deploy).

### Closed this session (2026-07-20 evening — TODO sweep: 3 sized items cleared)

> User asked to "knock out" TODO items; all three sized-and-ready items shipped in one session. Tests 3698→3699, 4 feature commits `77a8f32..5806f21` + docs reconcile. Dev server left running on :3000.

- ✅ **Earnings newsletter single-source flood hardening** (`77a8f32`) — window-function per-source cap (18 = 6 slots × 3 editions/day, derived not picked) in `getNewsletterContext`'s candidate fetch; distinctSources became truthful for free (cap < LIMIT means a 1-source pool now implies genuinely-1-source coverage). TDD: watched the 6×-flooder monopolization fail first. Landed before the GOOGL/TSLA/INTC preview week.
- ✅ **Smoke-QA 7 stale checks rewritten** (`61946e0`) — first fully-green run ever (26P/0F/0S); see the detailed `- [x]` entry above. E2E: tonight's 2 AM run.
- ✅ **HIG low-severity tail, all three items** (`cd93b5a` + `5806f21` follow-up) — 152-swap gold-ink sweep (4 parallel subagents + rulebook), crosshair-legend invisibility CONFIRMED + fixed via the `.chart-legend` CSS-var remap, FAB footer clearance. Judgment saves: LevelsPanel is EMBEDDED in the dark module (gold kept); one swap was dead under an inline `style` color (removed in `5806f21`).

### Closed this session (2026-07-20 pm — earnings-week hardening)

> Session opened with a TODO accuracy audit (QA ledger regrown to 15, one truly-orphaned qa-fix branch found via content-level check) then ran the timed batch. Tests 3673→3681 Mac + 408→417 Worker, tsc clean both sides.

- ✅ **Straddle road root-caused + fixed via live RTH probe** — see the closed live-watch entry above. The headline: it was never RTH timing; production never issued the `/iserver/secdef/search` call that warms IBKR's chain cache, so strikes polled empty forever. GOOG 7/22 now carries the first-ever `straddle` intel row (±5.82%), in time for this week's GOOGL/TSLA/INTC previews.
- ✅ **Worker cloud-enrich superseded-sibling skip** + ✅ **Worker held-ticker synthesis backstop** (evening fallback only — morning cloud digest has no synthesis path by design; scope note in CLAUDE.md).
- ✅ **Orphaned 7/18 QA fix cherry-picked to main** (Discover-from-Gmail outcome scrollIntoView); all 7 `qa-deep-fixes-*` branches deleted after content-verification.
- ✅ **Deploy-scoped `CLOUDFLARE_API_TOKEN`** created + deploy-verified (`db728296`); headless wrangler deploys no longer need a browser.
- ✅ **Digest thin-coverage SHIPPED same-day** (brainstorm→spec `7518405`→plan `b061f39`→4-task SDD `2a943ea..65dde7b`→docs `82808bb`, final review READY 0C/0I, Worker deployed `da9028cb`) — calendar-listing-only held buckets → one roster line, unfiled essays → 📄 pointer line, Worker evening roster mirror + parity pin. E2E watch: 7/21 8:45 digest.
- ✅ **Smoke-QA value extraction + run-death fixed** (`b333c31`) — API-based extraction + INVALID compare guard; first footer-reaching clean run in months (15P/7F/2S); 7 stale pre-redesign scenario checks surfaced + filed. **R1b Web API synced label** confirmed live incidentally.

### Closed this session (2026-07-08 evening — TODO trim + earnings intelligence tier #9/#10)

- ✅ **TODO.md trimmed 885→81 lines** (`4c7032b`) — full Apr–Jul audit trail archived verbatim to `archive/TODO-closed-2026-07-08.md`; B13 + §4C/§4E promoted to explicit entries; 3 buried live-watches surfaced; 2 stale entries corrected (D-tier email filtering had shipped 2026-05-11; Theme K's ">1 option position" blocker obsolete at 60+ held options).
- ✅ **Earnings intelligence #9 implied move + #10 print history — SHIPPED** (spec `391f3ca`, plan `7feb04a`, feature `c27d346..f0a65cd` ff-merged, 15 commits, +2,589/−30 across 30 files). Full brainstorm→spec→10-task-plan→subagent-driven build in an isolated worktree: migration 065 (`earnings_intel` per-event + `earnings_report_history` per-symbol ≤12q), live-probe-pinned IBKR chain resolution (`scripts/probe-ibkr-option-chain.ts` — bid=84/ask=86, C-prefix only on 31, strikes cold-cache retry, info[0] 0DTE trap), pure `lib/earnings/implied-move.ts` (zero-import), AV+Yahoo history engine, `ensureIntelForEvents` orchestrator (30-min TTL, AV cap 5, never-throws), preview scoreboard rows + Past-prints block + prompt injection, recap inside/outside echo, cockpit `impl ±X% · hist ±Y%` decoration, snapshot v9 → Worker read-only rows with as-of labels. Tests 3243→3303 Mac + 332 Worker; live E2E: JPM/GS/BAC 7/14 cluster at ±4.0/5.7/4.1% (iv_approx — after-hours chain gap; RTH re-verify queued) + 12q history each, JPM EPS sanity-checked vs public record; post-merge browser E2E console-clean. **Final review caught a real cross-task bug (I1)**: cockpit TTL-expired polling would have recomputed implied moves post-print and poisoned the recap's "priced-in" echo — fixed `f0a65cd` (released events never re-ensured; preview-time intel frozen as the recap anchor). Spec: `docs/superpowers/specs/2026-07-08-earnings-intelligence-design.md`.

### Closed this session (2026-07-10/11 — Plaid live Vanguard holdings)

- ✅ **Plaid live Vanguard holdings sync — SHIPPED** (spec `52f5232`+refinements, plan `e40f377`, feature `6ef048e..28d4dde` ff-merged 21 commits, + post-merge live fixes `f546b07`/`feac721`). Mid-month Vanguard position/cash changes now reach the dashboard daily: `lib/plaid/` (fetch-based client, pure mappers, `refreshVanguardHoldingsFromPlaid` mirroring the IBKR Web API path), `lib/db/live-sources.ts` (`LIVE_SNAPSHOT_SOURCES=('tws','plaid')` single-source predicate swept over ~34 raw `!= 'tws'` sites + lint test), statement-wins guards extended (holdings `plaid:%`, snapshots `IN(...,'plaid')`, prices `WHEN 'plaid' THEN 3`), 5 API routes, `/dashboard/plaid-link` OAuth connect page + Settings PlaidSection + Accounts sync button, PLAID_* Electron threading, launchd `com.vanguard-skin.plaid-sync` (07:30 ET weekdays, loaded). Final review caught I1 cost-basis blanking (COALESCE fallback shipped) + I2 reauth loop; **sandbox E2E caught two live-only bugs**: Plaid rejects unregistered `redirect_uri` (now opt-in, `f546b07`) and the account auto-mapper mapped ALL 12 sandbox accounts onto Vanguard Taxable (investment-subtype allowlist, `feac721`). Tests 3339→3348. Spec: `docs/superpowers/specs/2026-07-10-plaid-vanguard-live-design.md`.

New open items from this session (filed above-the-line):
- ✅ **First-sync row-level verify DONE 2026-07-11** — the predicted ticker-drift hazard fired exactly as written (BRK.B dup + MTUM/HACK mistyped options → caught via `securitiesCreated`, fixed `e6b6dc9`, repaired, re-synced clean); user confirmed ICL sale + RRX/CROX buys are real mid-month trades and both account totals match Vanguard. The 07:30 tick is trusted; remaining live-watch is the first natural `ITEM_LOGIN_REQUIRED`.
- ✅ **First live Vanguard sync + two live-only mapper bugs fixed** (`e6b6dc9` + data repair): Plaid typed MTUM/HACK puts as 'etf' with unpadded OCC tickers (dup mistyped securities double-counting the position) and gave `BRK.B` for the statements' `BRK/B` (dup + phantom closure both accounts). Mapper now OCC-shape-detects tickers regardless of Plaid's type label; write layer resolves dotted→slash share-class forms. Repaired dups/phantoms in live DB, re-synced clean (143 holdings, 1 genuinely-new security: rolled HACK Aug put). Known-accepted: Plaid omits CUSIPs for treasuries/restricted names (17 unmatched — bonds stay statement-only; reconciler never touches them).

### Closed this session (2026-07-12 — deep-QA ledger cleared: 9 open findings → 1)

- ✅ **Four orphaned qa-deep-fixes branch fixes landed on main** (`5340107`) — root cause of the ledger's repeat "regressions": nightly auto-fix commits sat on unmerged `qa-deep-fixes-*` branches. Landed: classification `temperature` 502 (782d771), Sync Feeds silent-400 race (dd8696e), What-if mobile scroll (98e380f), transcript HTML entities + test (a022cfb). The five branches are now fully landed and can be deleted next housekeeping pass.
- ✅ **SPY "support @ $7,100" impossible-alert class killed** (`2963d17`) — 50% plausibility guard at all three layers (Mac `findCrossedLevels` via `LEVEL_PLAUSIBILITY_MAX_DISTANCE`, Worker `isLevelCrossed`, extraction-time `isImplausibleLevelPrice`), options exempt on Mac paths (premiums legitimately double); SPY levels 213/214 (raw SPX 7150/7100) marked rejected in prod DB. Read-only prod scan fires zero alerts.
- ✅ **Holdings quantity=0 tombstones filtered from single-account view** (`35514fe`) — ACWV/EEMV "0 shares · $0.00" rows on Roth; `!= 0` keeps shorts.
- ✅ **Four UI paper cuts** (`dda8272`) — import button gated on parse success ("Import 0 files" + explanation; no fix had ever existed on main), scenario rows render OCC options compactly via new `lib/format.ts::formatCompactOptionSymbol` + β hidden on factor-based presets (recipe beta 1.0 is a placeholder), macro Sources drawer closes on Escape, equity-curve "All" range gets explicit month-start ticks. Also gitignored `qa/tmp/`.
- Verification: tests 3366 Mac + 333 Worker (from 3352), tsc clean, 5/5 live browser E2E PASS (3 parallel agents). Ledger now Open: 1 (charts "Object is disposed" — LWC-internal, all app guards verified) · Fixed: 140 · Wontfix: 3.

New open items from this session (filed above-the-line): none code-blocking. Two judgment calls left with the user: GOOGL option level 143 (underlying-scale exit @388 on the option row, user-approved — reject/re-price?), and optional LightweightCharts patch bump for the disposal race.

### Closed this session (2026-07-12 — B13 pre-season fix)

- ✅ **[B13] Worker earnings fallback per-run candidate cap — SHIPPED** (audit P1). `MAX_CANDIDATES_PER_RUN = 5` in `workers/cron/src/fallback-earnings.ts` (~5 subrequests/send × 5 = ~25, headroom for snapshot read + IBKR refresh + KV probes + co-scheduled calendar-enrich under the shared 50-subrequest budget). Priority under the cap: previews first (their [105,120] window is one tick wide — deferral past it loses the email), closest release first; recaps last (4h window absorbs a 15-min deferral). Overflow reported as `skipped`/`deferred-cap` + console.warn, **markerless** so the next tick retries. TDD: 3 new tests (cap + markerless deferral, closest-release beats array order, recap defers before preview); Worker 336 + Mac 3369 all green.

### Closed this session (2026-07-12 — iPad parity tablet tier)

- ✅ **iPad tablet tier — SHIPPED** (spec `d909d00`, plan `ebd98c9`, feature branch `ipad-tablet-tier` 19+2 commits ff-merged). Full brainstorm→spec→8-task subagent-driven build: Phase-0 audit (4 parallel browser agents @ 834×1194 + 1194×834 — structural result CLEAN, 22 findings all tap-target/affordance class), T4 global input-zoom kill + text-size-adjust + dvh sweep, T1 chat-delete tap-trap (+ seam-collision fix round), T2 hit-extension idiom ~41 sites (asymmetric insets, per-neighbor gap math), per-surface pass (alerts ✕ 7×16→~31×40 effective, LevelsPanel, research chips, import Undo, cash-deploy), chrome nowrap fixes (incl. the cascade-layer discovery: unlayered `h1{text-wrap:balance}` beats ANY layered utility — heading nowraps need `!`), overlays/modals 85dvh + FAB clearance. **Root-cause bonus: `ScrollFade` was inert app-wide since `fb543a7`** (class toggled on inner scroller, `::after` on wrapper) — repaired + dark-module `--scroll-fade-color` var + band-widened 48px; Accounts holdings Value-column fade finally renders (the audit's one high-sev finding). Verification: E2E 13/13 flows (levels + bogeys round-trips DB-verified, chat streamed live), landscape 9/9, desktop 1440×900 8-route no-regression proof + scoping grep, suites 3369 Mac + 336 Worker, final whole-branch review (fable) READY 0C/0I/6M. Remaining: Phase 4 real-device pass (entry above). Session-end: CLAUDE.md conventions reconciled `82bb38a` (hit-extension idiom, band variants, heading-nowrap `!` gotcha, ScrollFade repair); B13 closed-block above shipped `9a280b2` + Worker deploy `c09a0b35` earlier this session.

### Closed this session (2026-07-12 late evening — apple-design HIG audit + remediation)

- ✅ **apple-design skill installed + first full HIG pass — High/Medium remediation SHIPPED** (`15095f4`, 21 files +117/−50). Installed `dickwu/apple-design-skill` (53 HIG reference docs) to `~/.claude/skills/apple-design`; ran the tablet-band (768–1279) accessibility + platform-conventions audit via 2 parallel measuring browser agents (portrait 834×1194 / landscape 1194×834 + 981/507 Split View widths). **Structural verdict: PASS everywhere** (zero horizontal overflow at any width, Split View handoff textbook, chat overlay + TabNav correct). Findings were coverage gaps, all fixed: (1) **SymbolLink hit-extension** — the T2 idiom had stopped at buttons/chips and missed the app's single most-tapped shared component (154 links/page at 19px on Accounts); ±6px vertical variant so dense-table rows can't steal each other's taps; (2) chart timeframe/duration/MA/Txns/S/R pills + Refresh (both surfaces; `inset-x-0` full-width variant for 2px-gap pill groups), all 4 ChatDrawer header buttons, `+ bog`, alerts triage/toolbar/filter buttons (~45px effective); (3) **contrast tier**: new `--gold-ink` (small-text gold, light #8a6508) + theme-aware `--warn` tokens (Chip warn was fixed `text-amber-300` = 1.5:1 on cream; same bug found + fixed in MorningBriefing/EventCard/TrustStripDrawer), light `--up` deepened #0d9456→#0a7d49 (3.9→4.8:1), dark-module dim floor raised #555/#666/#777→#8a8a8a incl. new `.dark-module-chart .chart-chrome` scoped rule (light-theme ink was leaking onto MarketDataPanel's chart footer/Refresh at 2.7:1 — sibling of the `--scroll-fade-color` pattern) + `SecurityChart` `C.text`; (4) `prefers-reduced-motion` global rule (app had zero support); (5) aria-labels on settings gear / watchlist / bell (title-only is invisible to iOS VoiceOver); (6) bell badge 9px→11px in 18px bubble, ⌘K/⌘J/⌘; hints `pointer-coarse:hidden`. Verification: 3369 Mac tests green, 8/8 browser E2E checks (idiom presence + computed colors + 1440 desktop no-regression proof — `::after` confirmed not painting under fine pointer). **E2E caught what vitest couldn't**: an illegal JSX comment placement in ChatDrawer took down every dashboard route while the suite stayed green (no test imports it); also hit the wedged-Turbopack-cache gotcha (rm -rf .next) before the final contrast check passed. Low-severity tail filed above. Skill verdict: worth keeping — the audit's value was coverage-pattern findings (per-component vs per-page) the original tier's own verification missed.

### Closed this session (2026-07-12 night — iPad Phase 4 real-device pass)

- ✅ **iPad parity Phase 4 real-device pass — COMPLETE, iPad parity effort CLOSED.** User ran the final-review device checklist live on the physical iPad (Cloudflare One enrolled, `http://100.96.0.1:3099`, both orientations): T4 input-zoom kill incl. chat drawer textarea ✓, true touch taps on hit-extended controls (incl. dense-table SymbolLink and 2px-gap chart pills) ✓, ⌘K/⌘J/⌘; hints hidden on touch ✓, Reduce Motion ✓, header SearchButton/Bell seam ✓. The feared `MultiChart.tsx` `h-[calc(100%-32px)]` time-axis clip did NOT materialize (labels fully visible in 2×2) — no layout change made. **One real gap found on-device and fixed same session**: compact/Watchlist chart panels had NO Txns/S/R/MA toggles (gated `!compact` in `SecurityChart.tsx`) with `showMarkers` hardcoded ON. Fix: both pill groups now render in compact mode (ScrollFade toolbar absorbs the width), `showMarkers` defaults `!compact` (grid panels start clean, Security Detail keeps markers ON), plus the latent init-effect bug the E2E re-verify caught — initial marker paint hardcoded `visible=true` (`SecurityChart.tsx:360`), harmless while the default was constant; fixed via `showMarkersRef` mirror (the `isPrivateRef` pattern — the init effect must not depend on the toggle or every Txns click rebuilds the chart). Verified: agent-browser E2E ×2 (found the desync, then confirmed clean fresh-load grid, per-panel toggle both directions, detail-page default preserved, 0 console errors), 3369 Mac tests green.

### Closed this session (2026-07-15 — email delivery deep-dive + overnight digest block)

- ✅ **10:47-late-digest investigation + Mac-asleep cloud reconciliation cluster SHIPPED** (`d7e647b`, Worker deployed ×3 final `b674c190`). Trigger: 7/15 digest arrived 10:47 instead of 8:45. Diagnosis in two acts: (1) inference pass (Resend log, KV fossils, status-page JSON APIs) fingered Anthropic's "Elevated errors" incident; (2) once the new `CLOUDFLARE_OBSERVABILITY_TOKEN` unlocked the Worker logs, the REAL cause surfaced — `TypeError: i.themes.join is not a function`: the model intermittently returns `key_themes` as a comma-joined STRING (`jsonSchema()` has NO runtime validation), string survives `.slice(0,5)`, renderer `.join()` crashed every tick 9:00–10:30 AFTER ~10 successful Claude calls, until a clean dice-roll at 10:45. Fixed at 4 sites w/ `normalizeThemes` + `mentioned_symbols` guard (`d510663`). Cluster fixes shipped alongside: Worker `/internal/marker` date param + `via: sent|attempting` discriminator; `reconcileRecentCloudSends` on-wake pointer advance (kills stale-window re-covering — tonight's evening recap was the last email to re-cover cloud-sent days); cloud-aware `/api/digest/status` + honest DigestCatchup banner (user's "always there and not accurate" item); `GET /internal/cloud-sent-earnings` + sweep backfill of `sent-by-cloud` audit rows (closes the 7/14 observed gap — was an open TODO above); snapshot staleness self-heal (`03731a4`, verified live healing the 2-day-stale snapshot). Ops: `pmset repeat wakeorpoweron MTWRF 08:40:00` registered + verified; observability token in `.env.local`. Tests 3379→3449 Mac + 337→364 Worker.
- ✅ **Overnight block in the morning digest — SHIPPED** (`36a0372`, spec `8461542`). Brainstorm→spec→TDD same evening: deterministic `## Overnight` scoreboard (KOSPI · Bitcoin · Nikkei · Hang Seng, user's order) + targeted VK-Dawn 1-2 sentence extract (new feature key `overnightCommentary` → `$cheap`), morning edition only, slotted after alerts before synthesis. Holiday guard (>3-day-old close → "closed"), per-symbol drop, numbers-only degradation, never blocks the digest. Worker numbers-only mirror via ONE spark request (budget 48→49/50) with timestamp-aware parse. Live E2E verified: real Yahoo moves + faithful Dawn extract (quoted figures verified verbatim in the source article). Spec: `docs/superpowers/specs/2026-07-15-overnight-digest-block-design.md`.

New open items from this session (filed above): R6 icon rebrand, R7 ATH hedge reminders, R8 Desearch MCP, R9 recap-viewer from headline numbers; synthesis-truncation observation; 7/16 mega-soak + key_themes soak live watches.

### Closed this session (2026-07-14 — duplicate outbound emails: root-cause + 4-layer fix)

- ✅ **Duplicate-send investigation + fix SHIPPED** (`ba1b39f`, Worker deployed `d4c2e07c`). Triggered by "recap emails in brother's spam" (verdict: Microsoft EOP junking on the recipient side — DMARC p=reject fully aligned, Resend `delivered`, fix is Safe Senders / tenant allow at woodlotcapital.com); investigation surfaced a duplicate-send epidemic instead: Sunday briefing ×3 (7/12, all three MAC-sent — TWS sections in email 3 disproved the cloud-fallback theory), briefing ×2 (6/28), digest ×2 (6/30 + 7/01), previews ×5-for-3-banks (7/14, Worker) + NKE ×2 (6/30, Mac). Two root causes: **(A)** launchd curl `--max-time 300` < grown pipeline (~9 min) → timeout ≠ cancellation → script retried while each abandoned handler kept running and sent; route had no in-process mutex and ignored `sentBy:"mac"`; attempt 3 even regenerated via `isBriefingStale` (its own sync re-created the week's events) burning a duplicate Opus run. **(B)** one print = two calendar rows (finnhub + nasdaq); Mac dedup lives in `reconcileEarningsDates` (superseded=1) + SQL filter, but the Worker never read the flag (present in the snapshot all along) and the Mac sweep had no family-level dedup for the pre-reconcile window. Fixes: scripts `--max-time 900` + retry ONLY on curl exit 7; `lib/cron/send-mutex.ts` (globalThis lock, 30-min stale takeover) acquired before the marker RTT in all three cron routes + `sentBy:"mac"` skip with `body.force` escape; Worker `findCandidates` skips `superseded`; Mac `findEmailCandidates` dedupes per issuer-family+event_date (finnhub preferred) for both phases. TDD throughout (+10 tests, each watched fail); 3379 Mac + 337 Worker green, next build clean. Soak watch filed in Live watches.

### Closed this session (2026-07-16 — earnings batch: 6 bugs + 5 features, 32 commits `1d6f348..35d953e`)

- ✅ **Finnhub foreign-listing echo guard + TSM repair** (`fc9bcb2`) — root cause verified via live API probe: querying TSM returns 2330.TW with TWD-scale figures ($1.27T "consensus" reached the preview email); mismatch ⇒ figures dropped (schedule kept) at sync + enrichment, Worker strict match pinned intentional; row 590 repaired from Nasdaq, real actuals ($4.31/$40.2B, SEC-verified) entered with recap muted per user. NFLX half of the QA finding was a false positive (post-split $0.80 correct).
- ✅ **Bogeys upload 502** landed from qa-deep-fixes-2026-07-16 (`1d6f348`); **Security Detail duplicate earnings** — getUpcomingEvents superseded filter (`09dd242`); **cockpit mutation sync** — earnings-data-changed event, instrumented-E2E verified, the 7/16 "Hub also stuck" claim did NOT reproduce on prod (`bcfc3b4`); **[R9]** headline figures open the recap viewer (`c76b55c`).
- ✅ **Guard debut triage** (`dae6f95` + data repair) — 21 gaps → 5 real self-resolving reporters; root cause was 17 mistyped/NULL security_type rows (SPY was 'Stock'; DRAM/KDEF verified as ETFs externally) + getHeldOptionUnderlyingSymbols now excludes fund-type underlyings (also trims ~10 pointless Finnhub scan calls).
- ✅ **#18 Today's-reporters digest block** (`ed4e895`) — deterministic morning table (slot/chip/consensus/cached implied move), Worker snapshot mirror, byte-parity renderer; slot detection phrase-only (ticker "BMO" = Bank of Montreal collision, caught in #17's review and fixed here too).
- ✅ **#13 read-through push at print** (`62b7744`) — gate widened to live read-through pairs at all 3 fire sites, → TARGET (status): hypothesis lines, snapshot v10 readThroughPairs. Spec: docs/superpowers/specs/2026-07-16-read-through-push-design.md.
- ✅ **#17 EOD earnings wrap — SHIPPED** (merge `f61a25e`, branch c9852f3..1b55643, subagent-driven, 5 tasks + final review). Per-(date,slot) ≥3 clusters suppress individual recaps (`wrap-pending`) and staple ONE email per slot; deadlines BMO 12:00 / AMC 20:00 ET (user-set), Worker gate → 20:59; claim-first choreography (refire drop, conflict abort, per-member compose degradation, outer-catch claim release); per-event audit rows + markers (no cluster marker — documented deviation). Reviews caught: GOOG/GOOGL threshold double-count, ticker-BMO slot collision, refire double-send race, premature reaction-less cloud wrap (isPayloadComplete gate), and the final review's yesterday-cluster suppressed-forever bug on the manual-actuals recovery path. Spec + plan in docs/superpowers/.
- ✅ **#11 newsletter bogey automation + #12 same-day transcripts — SHIPPED** (merge `35d953e`, branch 231cd6b..3ace61e, 6 tasks + final review READY 0C/0M). #11: migration 066 scanned-marker, extractBogeysFromNewArticles after levels in both sync routes, AMBIGUOUS_TICKER_WORDS stoplist (review killed the length-gate that silenced TSM/AMD prose), Sunday-briefing reminder line. #12: migration 067 pacing column, retry-fetch orchestrator last in the sweep (stamp-before-fetch, 36h deadline, ≤2/tick), transcriptSummary desk notes (preamble-stripped, full-replace echo), morning Call-transcripts digest block (markdown-span-safe truncation). Specs/plans in docs/superpowers/.
- Session mechanics: 3449→3617 Mac + 364→408 Worker tests, both tsc clean; Worker deployed (32972e60); QA ledger 12→8 open (TSM consensus, bogeys 502, cockpit-stale, security-detail dupes all fixed).

New open items filed above: 7/17 digest-blocks debut soak, EOD-wrap debut watch, first read-through push watch.

### Closed this session (2026-07-21 evening — TWS session-eviction root-cause + polite-yield fix, then probe-driven pivot to sessionless holdings)

- ✅ **"TWS keeps logging me out" ROOT-CAUSED + FIXED, then PIVOTED on scenario-b probe evidence** (branch `ibkr-session-yield`, a690fca..6522470, 11 commits, subagent-driven 6 tasks + reviews — MERGED to main ff + pushed, Worker deployed `5a7c6e8d`, Electron rebuilt, all 2026-07-21). Root cause: IBKR allows ONE brokerage session per username and every Web API touch POSTed `ssodh/init` with `compete:"true"`, deliberately evicting the TWS desktop login — fired by earnings-intel straddle passes (cockpit polling ~30-min cadence during earnings season), the disconnected-path refresh (kick → socket drop → 30-min re-kick feedback loop), and Worker earnings ticks that fetched live IBKR BEFORE marker checks. First fix (Tasks 1-5) shipped a blanket `compete:"false"` design. The mandatory scenario-b probe (TWS closed, required before merge) then DISPROVED that design: `compete:"false"` is refused in BOTH TWS-open AND TWS-closed runs (`auth/status` fails with `"Force compete capability must be used together with compete flag"` — this consumer key is provisioned with the force-compete capability and can never use `compete:"false"`) — a blanket compete:"false" design would have broken every away-from-desk Web API path. **Task 8 pivot (probe-driven):** holdings reads go SESSIONLESS — `/portfolio/accounts`, `/portfolio/{acct}/positions/{page}`, `/portfolio/{acct}/ledger` all return full data on a signed LST alone in every scenario (probe row 4), so Mac `fetchIbkrPortfolio` and Worker `fetchLiveIbkrPositions` no longer call `ssodh/init` at all — zero eviction risk, works with TWS open or closed. Only `/iserver`-needing consumers (earnings-intel Web API road, quote enrichment) still call `openSession`, which now ALWAYS uses `compete:"true"` (the only value this key accepts) gated behind a new `isTwsListeningLocally` check — a raw TCP probe of the local TWS API port, run BEFORE any IBKR network call; if TWS is listening locally, yield immediately (no LST mint, no IBKR call); only when TWS is confirmed absent does `compete:"true"` reach IBKR, with nothing to evict. `isSessionYield`'s post-init check is now a defensive backstop only. Worker's local `isSessionYield` mirror deleted (import-dead — the Worker holdings path is sessionless). `IbkrSessionYieldError` / `"ibkr-session-yield"` sentinel, degrade paths (intel falls to the next road, Worker's yield catch in `fallback-earnings.ts` is now defensive/unreachable, refresh releases the sync mutex), and the **TWS-first straddle road** (`lib/tws/atm-straddle.ts`) all carry over unchanged from Tasks 1-5 — only the yield DETECTION moved from the IBKR response body to the local port check. Single-machine TWS assumption documented. Docs updated: `scripts/probe-ibkr-compete.ts` header (full four-row verdict table), root CLAUDE.md + `lib/tws/CLAUDE.md` (compete bullets rewritten), `scripts/ibkr-refresh-holdings.ts` header. Tests 3757 Mac (root suite, includes Worker files) + 431 Worker (package-local) at head, both tsc clean.

**Live watches / follow-ups filed from this session:** first live successful `openSession` (compete:"true") call with TWS confirmed closed via the local port gate, verifying `/iserver` reads work away from the desk; no TWS logout during a full trading day post-deploy with TWS open (the gate should short-circuit every attempt before any IBKR call); first live "straddle via TWS" log line during an earnings pass with TWS open; Worker `wrangler tail` confirming zero `ssodh/init` calls from the Worker's live-IBKR fetch path ever, not just on marker-skipped ticks.

### Closed this session (2026-07-19 evening — pre-earnings-week bug batch + live-watch close-outs)

- ✅ **Thin-8-K transcript cache poisoning FIXED + NFLX repaired live** — real root cause one layer up from the TODO's guess: `fetchTranscript` already had the EDGAR→AV upgrade on cache hits; the sweep orchestrator's own cache check `continue`d before reaching it. Cached-EDGAR rows now stay candidates on a 10-day/24h upgrade clock (AV posts days later; ≤1 AV call/day/name vs the 25/day tier), fresh candidates win the shared budget, failed upgrades never re-burn the AI desk note. `scripts/upgrade-edgar-transcript.ts NFLX 2026 2` upgraded the live row 4,091→36,126 chars with a substantive desk note.
- ✅ **Digest transcripts-block heading demotion + best-source dedup** — `demoteEmbeddedHeadings` at render time (leading title dropped, H2s→bold, pre-truncation so bold balancing holds); edgar+AV rows in one 24h window render once. The upgraded NFLX note e2e's both in the 7/20 morning digest.
- ✅ **Duplicate-email soak CLOSED** (Sunday leg: single cloud briefing 16:47 ET, `via:"sent"`; Mac in DarkWake through the 16:30 window — launchd silence since 7/12 is sleep, not breakage) and **B8 CLOSED** (NFLX 7/16 AMC reaction captured via Worker Yahoo path, observability-log-confirmed). Straddle watch rewritten with the RTH-never-attempted structural finding; new minor filed (Worker cloud-enrich reprocesses superseded siblings).
- Verification: 3,646 Mac tests (354 files, +9), live NFLX repair verified in DB.

### Closed this session (2026-07-17 afternoon — insights-report follow-ups + live-watch verification sweep)

- ✅ **Usage-insights report actioned** (from `~/.claude/usage-data/report-2026-07-16-150834.html`): (1) global CLAUDE.md gains the `git commit -F <file>` convention (closes the inline-`-m` shell-quoting failure class); (2) **qa-deep-sweep per-zone checkpointing + same-day resume** — ledger merge now runs at each zone-agent return with `.sweep-progress.json` date-keyed progress, so an interrupted sweep loses at most the in-flight zone (report showed ~¼ of sweeps died mid-dispatch losing everything); (3) **Playwright profile-lock deadlock class killed** — global playwright MCP re-registered with `--isolated` (in-memory profiles; the 28-min browser-lock stall was Chromium's one-browser-per-profile-dir lock held by an orphan) + new `ab_reap_orphans` in `qa/lib/agent-browser-cleanup.sh` (kills `playwright-mcp`/`ms-playwright` processes with PPID 1 — provably orphaned; parentage not age, so live evening-session browsers never match) wired into deep-QA preflight + `ab_cleanup_init`; (4) researched + recorded WHY zone parallelism is structurally blocked (subagents share the parent's MCP server processes — per-subagent isolation impossible in one session; a future parallel design needs per-zone `claude -p` children + data-disjoint lanes) in the skill's Step-1 comment. Robustness-only scope user-approved; parallel rearchitecture deliberately deferred (nightly window makes sequential fine). Report suggestions NOT adopted, with reasons: per-edit typecheck hook (minutes of latency per edit; E2E already catches build-breakers), session-end/TDD CLAUDE.md additions (duplicate existing skill + workflow rules), "500 output-token limit lost sessions" friction (analyzer artifact — those sessions are marked unclear_from_transcript, i.e. unreadable by the report, not lost; 32/40 fully-achieved contradicts it).
- ✅ **Live-watch verification sweep** (logs + prod DB + received Gmail + Worker observability): **7/16 mega-soak CLOSED** — 8:45 Attempt-1 single send, second tick "mac already sent", `## Overnight` block rendered with live numbers + VK Dawn quote both 7/16 and 7/17, no re-covered windows. **key_themes soak CLOSED** — 0 error-level events in 48h of Worker logs (`themes.join` TypeError gone). **7/17 debut soak CLOSED with findings** — `## Call transcripts` debuted (NFLX desk note in the 7/17 digest; orchestrator fetched 7/17 01:13 via edgar_8k), newsletter bogey extraction live (first tick `bogeysScanned:10 bogeysStored:4`; 5 rows banked incl. MSFT consensus+whisper from TMT Breakout, NSC/INTC/GOOG/CSX from VK — next week's reporters pre-seeded), reporters block correctly absent (zero held reporters 7/17 — visual debut re-filed for week of 7/20). **Duplicate soak narrowed** to Sunday 7/19 briefing only (weekday digest + preview legs verified clean since 7/14; UNH `sent-by-cloud` audit backfill row confirmed working). **Theme K (Deferred) CLOSED** per its own stale-blocker text (60+ options held; Greeks/exposure/expirations E2E-verified via Analysis Deep Dive P1 + cockpit). Two new small bugs filed from the sweep: thin-8-K transcript rows permanently block the later real AV transcript (NFLX Q2 is live proof), and desk-note headings render un-demoted inside the digest transcripts block.

### Closed this session (2026-07-28 evening — decision burn-down + 3 HIGHs + archive + week-nav)

> Full-arc session: 9-decision QA burn-down (every recommendation accepted) → all 3 open HIGHs fixed with TDD + browser E2E → chart-disposal 5th "regression" resolved as verified-fixed (not re-fixed) → earnings email archive + week-ahead navigation shipped (both DECIDED that morning) → small follow-ups batch. Tests 3911 → 3940, tsc clean throughout, 8 commits pushed `b7d211b..ca5a394`.

- ✅ **QA decision burn-down** — 9 decisions recorded as `DECIDED:` ledger plans (IBKR-allocation full universe, single-engine Herfindahl, privacy-masked email viewer, earnings-email archive BOTH options, filtered-tab controls, week navigation, All-scope EmptySections, macro-theme %+relative badge, hide desktop FAB); equity-curve entry reconciled with its landed fix; the tax-lot recompute recommendation EXECUTED live (21 RECONCILE_CLOSE rows generated — dc33080 engine just needed its first run).
- ✅ **Security Detail $0 cost basis** (`b7d211b`, HIGH) — COALESCE-to-statement fallback (5544284 sibling) + null-honest TOTAL row; browser-verified vs open tax lots (113/136 Taxable + 17/17 Roth rows rescued).
- ✅ **Suggest-all honesty + the real bug it exposed** (`f1c5760`, HIGH) — `suggestOutcomeMessage` honest-failure banner; that banner immediately surfaced cloud-reconciled alerts' Worker-shaped `position_context` crashing the prompt builder (every cloud-fired alert had silently failed suggestions since mid-July) — `normalizePositionContext` shape-guard; live E2E "Generated 10 suggestions."
- ✅ **IBKR allocation full-universe switch** (`1c23211`, HIGH) — shared CTE (6 surfaces) + `getPortfolioExposureSummary` to per-(account,security) incl. shorts per the user decision overruling `keyBy:'account'`; CLAUDE.md convention updated; browser-verified 15 positions / net 112% gross 123% / truthful 15-of-15 coverage.
- ✅ **Chart-disposal regression-5 closed with evidence** — reliable repro run twice on current main (console + injected error trap + DOM): zero occurrences; `4786597` held (the 7/27 sighting predated the rebuilt DMG); LWC 5.2.0 has no disposal fixes so no bump; ledger carries fix_commit + evidence trail.
- ✅ **Earnings email archive** (`7a664c8` spec + `496cced`, DECIDED same morning) — `getSentEarningsEmails` + `GET /api/earnings/emails` + alerts **Emails tab** (89 rows, symbol filter, existing viewer, cloud-sent chip) + Security Detail per-family section + EarningsHub "All sent →"; three-surface browser E2E all PASS.
- ✅ **Week-ahead navigation** (`ba6426e`, DECIDED same morning) — `resolveWeekOfParam` (Monday-snapping, garbage-safe) + prev/next chevrons + "This week" pill + honest micro-label; restores the Calendar Living Record's only week-level browse path (8 weeks of enrichment reachable again). E2E surfaced a NEW display gap filed pre-classified: `today-week-ahead--reaction-snapshots-never-render` (reactions captured but never rendered in the week view — plan: reuse EnrichmentChips).
- ✅ **Small follow-ups batch** (`ca5a394`) — see the closed item above.

---

## Closed this session (2026-08-02)

> Earnings-pipeline hardening session: full A-to-Z walkthrough → user feedback review → three builds shipped via subagent-driven development on worktree branch `earnings-aug2-hardening` (25 commits, both plans opus-final-reviewed with all findings fixed), merged `0fcb678`, pushed. Tests 4,029 passing (+~100 this session); 4 pre-existing failures (3 cloud-reconcile + 1 chat-tools) belong to the un-landed 7/29 overnight branches.

- ✅ **PRLB read-through repair** (`87fb461`) — pair was seeded `PRTO→XMTR` (dead ticker) since 2026-05-02; fixed in DB + seed script (now fail-closed Finnhub-validates every symbol); PRLB 7/31 print backfilled (EPS 0.60 vs 0.55 · Rev 149.3M vs 147.1M · reaction PRLB −1.8%/SPY −0.38%) via new `scripts/backfill-reporter-print.ts`; `buildReadThroughEntries` verified returning the entry for XMTR's 8/04 preview.
- ✅ **Earnings date/slot verification tier** (plan `docs/superpowers/plans/2026-08-02-earnings-date-verification.md`; migration 072) — daily 05:00 ET Claude+web_search pass over held/watchlist/read-through candidates ≤7d out; confirmed mismatches auto-correct via `correctEarningsEventDate` (extracted to lib; adopt-vendor-row-over-mint on date moves; never deletes the corrected row; consensus carried; transactional; refuses on actuals); `[today, +37d]` verdict bound; T-2 re-open for unconfirmed rows; manual rows excluded from candidacy AND now enrich via the Finnhub road (`manual:SYM:DATE:earnings` source-key dispatch — also fixes every "+ Add ticker" row's recap path); sync upsert clears stamps when a vendor moves a date; Pushover on corrections; CLI `scripts/verify-earnings-dates.ts` (dry-run default). **Live 8/02 `--apply`: MELI 8/03→8/05 corrected (Monday phantom preview averted), VRTX/DOCN/SPCX/PCTY/XMTR/WIX verified, OCUL honestly unconfirmed.** Motivating incidents: RKT 7/30 Nasdaq-only phantom (single-source rows invisible to the Conflicts tab), LLY slot default, IMAX class.
- ✅ **Morning debrief replaces the EOD wrap** (plan `docs/superpowers/plans/2026-08-02-morning-debrief.md`) — user verdict: previews useful, evening recaps/wraps not (all 8 heavy-night recaps of 7/29–7/30 landed ~4h late). Individual fast recaps unchanged; wrap-suppression unchanged; `runWrapPass` retired from the sweep (`wrap-send.ts` kept for Worker parity + rollback); new `lib/earnings/debrief{,-send}.ts`: ONE "☕ Earnings Debrief" at 07:45–08:20 ET (day key `last_debrief_date`, debrief pass runs BEFORE the sweep's send loop), 3-day self-healing lookback, today-rows require `enriched_at` (no preempting richer individual recaps), wrap-style claims + full Mac↔cloud marker dance, span-safe desk-note excerpts (`truncateAtWordBoundary` + `demoteEmbeddedHeadings` exported/reused), date-bounded family call-note lookup (`getCallNoteNearDateForFamily`), AI synthesis (feature key `earningsDebrief`) forbidden from restating headlines; recovery CLI `scripts/send-morning-debrief.ts`.
- Ops note: repeating wake should move to `pmset repeat wakeorpoweron MTWRFS 07:40:00` (Saturday included for Friday AMC clusters; digest's 08:45 window falls back to the Worker path if the Mac re-sleeps).

---

## Closed this session (2026-08-02 evening — outbound-privacy parity batch)

> Deferred-backlog session: pmset wake re-registered live (`wakeorpoweron MTWRFS 07:40:00`, verified via `pmset -g sched` — Saturday debriefs + 07:45 window covered), then feedback #8 + all three Worker parity follow-ups shipped on branch `outbound-privacy-parity` (spec `docs/superpowers/specs/2026-08-02-outbound-privacy-parity-design.md`, implementation `edad759` + review-wave `bc773da` — the code reviewer caught two MORE briefing.ts count leaks, `formatCombinedPosition` + `formatOptionForPrompt`, incl. the fractional-share "65.50 sh" fingerprint; both converted to the direction-only idiom). Worker suite 438 passing; Mac suite green except the 4 pre-existing 7/29-branch failures.

- ✅ **#8 Direction-only outbound positions** (`edad759`) — share/contract counts AND return % removed from every outbound-email position rendering (user: "anyone can just do the math" — count × public price reconstructs exact $ exposure; supersedes the 2026-05-12 %-returns-OK direction). New shapes: `long AAPL (vanguard taxable)` / `short META (ibkr)` / `long AAPL $145 calls exp 2026-06-19 (ibkr)`; combined exposure is presence flags (`long shares + short options`); briefing `NET SHORT` marker number-free (Mac + Worker). Formatter interface slimmed (costBasis/latestPrice/multiplier gone); byte-parity mirror + pin updated; leak-audit tests now assert zero digits outside strike/expiry.
- ✅ **Worker parity (a)(b)(c)** — cloud wrap suppress-but-never-send; Worker `parseSourceKey` manual-earnings keys; debrief `no-recipient` skip before the day-key stamp. Details in the ticked backlog item above.

---

## Closed this session (2026-08-03 — QA landing day + feedback backlog completion + decision burn-down)

> Marathon session in three acts, all pushed through `754416a`. Tests 4,028 → **4,125 Mac / 446 Worker**, every suite green (the 4 date-rot failures that dogged main since 7/29 are gone). Every feature branch spec'd → TDD'd → reviewer-subagent'd → fix-waved → merged; three of four reviews caught material issues pre-merge.

- ✅ **QA landing sweep** — de-rot test fix cherry-picked (`6c3a309`→`5c863f9`, main green for the first time in days), PRs #26–#29 reviewed + merged (14 fixer fixes incl. the KRW trade-review FX conversion, implausible-actual recap withholding, shorts on Security Detail, PDF-import temperature fix), 15 stranded sweep/worktree fixes cherry-picked (equity-curve reindex, S/R disposed-series, greeks DTE label, week-ahead consensus retention, notes-refresh, compact-USD/negative-money renders, position-risk gate, charts last-price, alloc-% anchor, +6 more), ledger reconciled (19 → merged-awaiting-deploy, 4 open → fixed), all 15 stale qa-* branches verified patch-equivalent and deleted.
- ✅ **#7 fix-date chip** (`d7cbb5e`) — every EarningsDateChip status is tappable; passive statuses open a "Date is wrong?" popover → new `POST /api/earnings/correct-date` (404 no-row guard, 409 refusal verbatim) wrapping `correctEarningsEventDate`. Browser-E2E'd both paths on scratch rows (happy move + captured-actuals refusal); one wedged-dev-server false alarm mid-E2E (rm -rf .next + retry — agent's popover finding stood).
- ✅ **#5 sheet expected-move priority** (`c685c80`, Worker `d4a72fc8`) — migration 073 `earnings_bogeys.expected_move_pct`, all three inlets, zero-import resolver + byte-parity Worker mirror, sheet > straddle > iv_approx at composer/cockpit/Worker AND (reviewer catch) the morning digest's Today's-Reporters line both sides; `coercePercent` single-sourced in lib/format.
- ✅ **#3 reporter recap** (`54f201a`) — PRLB-class pure read-through reporters get a deterministic zero-AI recap at FIRST ACTUALS (~T+15–30 min) via a third `findEmailCandidates` road (reporter symbols join the status map — held-but-audited can't misread as reporter) + `lib/earnings/reporter-recap.ts` on the recap claim/marker rails; wrap-suppression exempts them. Review hardening: pre-print typo floor, shared `recordEarningsEmailAudit`, blockquote-safe hypotheses.
- ✅ **#6 printable worksheet** (`0597baf`) — migration 074 flags, 80-col/62-line pure composer, `lp` printing (20s timeout, EPIPE guard), sweep auto-print pass in [release−30m, +135m] once-only, ⎙ chip + modal Print-now. Review probes caught a 104-col header + unbounded-segment page overflow — clamped with adversarial tests. **Physically smoke-tested: XMTR 8/04 sheet printed on the HP MFP 4301.**
- ✅ **11-decision burn-down** (`754416a`) — all needs-decision findings decided; 10 → `auto` with verbatim plans for the nightly fixer; FIFO negative-holding-periods stays fixer-guarded pending the user-chosen data audit (open item above).

---

## Closed this session (2026-08-04 — XMTR earnings morning: nasdaq enrichment road + reaction accuracy + chips legibility)

> Live earnings-morning session driven by three user reports on the XMTR/WIX prints. Branch `worktree-nasdaq-enrich-road`, merged `55f67bb`, Worker deployed `7d0c8cff`. Tests 4,125 → **4,192** (15 new + the branch's), tsc clean. Operational recoveries done live: manual actuals + Finnhub-adjusted consensus written for XMTR (1241) / WIX (1242), both recap emails fired manually (wrap suppression had deferred them to tomorrow's debrief — see the new BMO decision item), and all three 8/04 BMO reaction snapshots re-captured with true numbers (XMTR +2.88% / WIX +7.37% / DOCN +3.66% vs SPY +0.76% / QQQ +1.93%).

- ✅ **`nasdaq:` source_keys ride the Finnhub actuals road** (`a9bc566`, Mac + Worker parity) — Nasdaq-only earnings rows (RKT 7/30 class; XMTR/WIX 8/04) fell through `parseSourceKey` to "unknown": the retry-until-complete runner attempted every tick but could NEVER fetch an actual, so the recap gate never opened and every such print ended in a blocked-recap Pushover. Same disease + cure as the 8/02 `manual:` parity fix. Diagnostic signature for the family: `enrichment_attempted_at` fresh + `actual_value` NULL + vendor has the data ⇒ suspect source_key dispatch.
- ✅ **Earnings reaction accuracy — prior-close anchor + TWS extended-hours bars** (`b8883e9`) — two compounding bugs from the XMTR recap's "way off" SPY/QQQ numbers: (1) `fetchTwsBars` passed useRTH=1, so TWS could never see premarket/after-hours bars — every BMO t_pre, AMC t_post, and 8:30-macro pre failed the 10-min tolerance and the whole TWS road silently lost to Yahoo (user's "99% sure TWS was up" pushback was correct — proven with an idle-TWS `upgradeReactionToTws` probe returning `tws_reaction_unavailable`); (2) release-window semantics measured XMTR from an 8:00 "pre" bar that was already +8% post-news (wire hit 7:05) — email said −4.8% when the print-day move was +2.9%. Earnings rows now anchor every t_pre to the last regular-session close (BMO: prior daily close; AMC: the 16:00 bar), flagged `pre_anchor:"prior_close"`, labeled "moves vs prior close" in the email scoreboard; macro rows keep release-window semantics exactly. Mac + shared yahoo module + Worker matcher mirror + all three callers.
- ✅ **EarningsHub chip cluster legibility** (`058ebee`) — user feedback: mashed, unclear armed state/button purposes. Full-word labels (`✓ preview` / `recap` / `gen recap`), worksheet chip SAYS its state (`⎙ arm` → `⎙ armed` gold → `⎙ printed` green), two wrap-group layout in a 96→160px Email column, `DESKTOP_GRID_COLUMNS` single-sourced. Browser-verified incl. `elementFromPoint` hit-tests after the first pass caught a shrink-0 root spilling the cluster over the Bogeys button (the 7/27 click-steal family — invisible on pending rows; hit-testing is now the verification bar for this cluster).
- Memory: `project_earnings_wire_time_tracking.md` (XMTR wire ~07:05 vs 08:00 slot; next-season release-time calibration idea + pre-release-probe constraint).

## Closed this session (2026-08-04 evening — deferred-decision burn-down + wire-time tracking build)

> Decision session: all four deferred items from the XMTR morning resolved in one pass (user picked: BMO exempt / worksheet stays opt-in / server logs now / wire-time design→build now). Branch `worktree-deferred-decisions`, 16 commits, merged fast-forward to `04647c9`, pushed `3c5eb8d`; Worker deployed `b133d58f`; DMG rebuilt + installed. Tests 4,192 → **4,263** (+455 worker), tsc clean at every gate. Subagent-driven build (6 tasks, every task reviewed; final whole-branch review caught 2 Importants the task reviews couldn't see — vendor-row slot guard fed null, probe error/empty conflation). Also cleaned up the stale merged `nasdaq-enrich-road` worktree whose leftover test copies produced 17 phantom failures in main-checkout suite runs (memory: `reference_vitest_sweeps_worktrees.md`).

- ✅ **BMO wrap-suppression exemption** (`d099fe9`, Mac + Worker) — only AMC clusters defer recaps to the morning debrief; a ≥3 BMO cluster (the 8/04 DOCN/XMTR/WIX case) gets individual same-morning recaps on both roads.
- ✅ **Electron server-log persistence** (`b085714`) — `electron/server-log.ts` tees the Next server's stdout/stderr to `~/Library/Logs/Vanguard Dashboard/server.log` (5MB rotation, timestamped tags, fails soft); closes the zero-breadcrumbs gap from the useRTH diagnosis.
- ✅ **Worksheet auto-arm — decided: keep opt-in** (no code; ⎙ chip stays the arming surface).
- ✅ **Wire-time tracking — full feature** (`fb337f8..236e703`, spec `docs/superpowers/specs/2026-08-04-earnings-wire-time-tracking-design.md`, plan `docs/superpowers/plans/2026-08-04-wire-time-tracking.md`) — migration 076; T−90m pre-release Finnhub probe (cap 6/tick, strict error-propagating probe so outages can't forge bounding evidence) captures early prints SAME tick + pulls `release_time` earlier-only; bounded-vs-unbounded observation honesty; resolution cascade user → web_verified → observed → legacy constant → defaults with vendor-slot guard; EarningsWhispers jump-start folded into the daily date-verification pass (self-extinguishing); `POST/GET /api/earnings/release-time` + "Reports at" override editor in the EarningsDateChip popover. Follow-ups parked in the open item above. WSH data feed evaluated and passed on cost (revisit next tax year).

## Closed this session (2026-08-05 — rich preview-based worksheet print)

> User feedback on the first live auto-printed worksheets (8/05 morning): the deterministic skeleton isn't the sheet they work from — their hand-worked AMZN printout IS the preview email (line-by-line bogies table hand-filled during the call). Full brainstorm → spec → plan → subagent-driven build (5 tasks, one Task-3 fix round for a real 3-page-cap hole the reviewer proved empirically, final whole-branch review "Ready to merge" + one fix wave). Branch `worktree-worksheet-rich-print`, 9 commits `6e27ead..f478bdf`, **draft PR #32 — awaiting user review + merge; DMG rebuild deferred to post-merge** (feature not on main yet). Tests 4,263 → **4,305**, build clean.

- ✅ **Rich worksheet composer** (`67b3abb..1d7c3f1`) — new pure/import-free `lib/earnings/worksheet-rich.ts`: markdown extraction from the stored preview (`earnings_emails.ai_output_md` bogies table + commentary span, Sources excluded), ruled 80-col monospace tables with pen-sized blank ACTUAL/Δ fill-in columns (— placeholders → blank ruled boxes; Metric 16 | Cons/Prior 41 | Actual 13 | Δ 6), scoreboard + past prints rebuilt live via the email's own exported renderers, ≤3 form-feed pages with hard cap enforced + footer-always-last (fix round: cap was commentary-only, verbose bogies tables overflowed to 4-5 pages).
- ✅ **Wait-for-preview auto-print gate** (`164b04c`, user decision) — armed events print only once a LOCAL completed preview exists (`getEmailAudit` + non-null `ai_output_md`; excludes `in_progress` claims + `sent-by-cloud`); skip-without-stamp retries each tick until release+30m. Cloud-sent/muted/skipped previews never auto-print (spec documents all cases); "Print now" always produces paper (rich → deterministic fallback, `composeWorksheetForEvent`).
- ✅ **Sweep ordering** (`945e4c9`) — auto-print pass moved AFTER the send loop so the tick that sends a preview prints it same-pass (invocationCallOrder-pinned).
- ✅ **Post-review hardening** (`f478bdf`) — over-long AI headings now word-wrap (an unwrapped >80-col line would desync form-feed pagination for the whole printout).

## Closed this session (2026-08-05 pm — APP/MELI preview race + Mac-aliveness marker)

> Live incident session, same afternoon the rich print merged: the user's APP preview arrived as the lean CLOUD email and the worksheet printed as the old deterministic sheet. Root-caused from `vanguard-calendar-enrich.log` + the (new, 8/04-shipped) persisted server.log: an AWAKE Mac lost a 2-minute race — its 13:59:36 tick missed the 16:15 crowd by 24s at the 135-min window edge, the LFMD compose (155s, incl. a 4096→8192 token-ladder retry) pushed launchd `StartInterval`'s job-completion re-anchor so the next tick slid to 14:17:11, and the Worker's fixed 14:15:00 grid tick cloud-sent lean APP+MELI at 14:15:50 → `sent-by-cloud` rows (NULL `ai_output_md`) → the rich worksheet's wait-gate correctly refused and manual print took the deterministic fallback. LFMD (Mac-sent) had auto-printed RICH at 14:02 — the new pipeline itself worked on day one.

- ✅ **Live recovery** — APP + MELI previews manually re-fired from the Mac (the claim code explicitly allows re-fire over a `sent-by-cloud` row, upsert-in-place); both rich emails re-sent, both worksheets verified composing rich (3 pages, bogies + past prints), APP printed via the API before the 16:15 call.
- ✅ **Mac-aliveness marker for previews** — the Worker earnings fallback was the ONE sibling without a `mac-recent-*` gate (level-scan 90-min, newsletter 60-min). Now: every completed sweep tick fire-and-forget POSTs `/internal/mac-recent-earnings-sweep` (25-min TTL — deliberately tight: the Worker preview window is 15 min wide, a stale-marker skip forfeits the cloud's only shot); Worker skips PREVIEW candidates while fresh (`mac-recently-swept`, markerless so retries survive marker expiry); recaps/actuals-capture deliberately un-gated. TDD both sides (Worker gate + quiet-tick/sending-tick Mac posts); CLAUDE.md sweep convention extended.

## Closed this session (2026-08-07 — QA landing sweep + earnings print/prose round)

> Two threads: (1) landed the stranded 8/06 QA fixer output (PR #33 + qa-deep-fixes-2026-08-06, minus one reverted commit), all 5 pending QA decisions resolved by the user; (2) shipped the headline earnings-workflow round from the 8/06 brainstorm — email-identical print sheet, root-caused prose flow bug, per-source bogeys table, notes-are-sacred — plus a same-day live-user feedback fix. Suite green throughout (4,441 +68 on the print/prose branch); DMG rebuild pending session-end.

- ✅ **QA landing: PR #33 + qa-deep-fixes-2026-08-06** — PR #33 (`32bfe42`: `aa4dafe` option today-move intrinsic-violation guard, `b636443` Filtered audit tab honors search box + source dropdown) and `qa-deep-fixes-2026-08-06` (`3a0e45c`, 8 fixes: calendar-rot test pin, levels hit-rate distinct-fired-armed numerator, dismissed-bucket render, Data Health FX conversion, sender_email validation, compact OCC symbols in the Armed tab, blank-note Save guard, CSV-guide comma) merged MINUS `f525905` — that commit re-applied the USD suggested-levels conversion the user rejected at the 8/05 landing; reverted (`4e0fee6`), ledger entry → wontfix, and the stale route comment that fed the fixer the wrong direction corrected (`3e6a7a6`).
- ✅ **All 5 pending QA decisions resolved by the user** — recorded as `DECIDED:` plans on the ledger, dispositions flipped to `auto`: earnings_emails audit-row migration on date correction; levels plausibility-band filter + "outside scan range" flag + non-blocking arm warning; equity-resolution for extracted levels + user-run repair script; narratives never state numbers (deterministic substitution); sender_email warn-but-allow.
- ✅ **Earnings print/prose round SHIPPED** (branch `earnings-print-prose-round`, squash-merged `c026c4e` — 8 SDD tasks, every task reviewed, final whole-branch review READY-WITH-FIXES then addressed; squashed deliberately because intermediate commits carried an un-redacted held/watchlist ticker roster in the artifact catalog and this repo is public): (a) email-identical one-sheet print — `lib/earnings/print-sheet.ts` + `lib/earnings/print-pdf.ts` (headless-Chrome PDF, `%%EOF`-gated completion poll, duplex `lp`), 3-rung one-sheet ladder (drop past prints → compact → print anyway), never-silent fallback to the existing monospace sheet; (b) root-caused the "prose doesn't flow" complaint to `callClaude`'s `join("\n")` over web_search citation-split text blocks, fixed at the source (`joinClaudeTextBlocks`) + a read-boundary repair for already-stored emails, plus a new deterministic per-source "Sheet bogeys" table; (c) notes-are-sacred (retired the silent 4×74-char note chop) on both the PDF and monospace-fallback roads.
- ✅ **Same-day live-user feedback fixed** (`56ad89f`): the first live rich auto-prints came out in the outbound email's amber/cream palette (fixed with a `!important` white/black `@media print` override) and left an orphan second page carrying only the footer (fixed the spacer/break-inside interaction); the one-sheet ladder's third `{ compact: true }` rung shipped in this same commit.
- New open items filed: item (d) post-print automation (its own brainstorm), tonight's unmerged QA fixer output (`qa-deep-fixes-2026-08-07` + `qa-fix-work-20260807`), public-repo hygiene on pre-existing tracked files carrying real tickers, and the print/prose round's own deferred minors (`countPdfPages` token-scan fragility, deterministic-composer truncation, ~250ms/render latency, the shared `join("\n")` shape still latent in `verify-sector-tags.ts`/`verify-earnings-dates.ts`).

---

## Closed this session (2026-08-09 — Mac-first send race)

> User asked why the Sunday briefing had not arrived; it had — twice. The Worker cloud fallback shipped a thin copy at 16:47 ET and the Mac shipped its full copy at 16:51, with byte-identical subject lines. Root-caused to TWO independent defects (either sufficient alone to lose the race), fixed both, mutation-verified both regression pins. Suite 4,457 Mac + 457 Worker green, tsc clean.

- ✅ **Mac-first send race FIXED** (branch `mac-first-send-race`, spec `docs/superpowers/specs/2026-08-09-mac-first-send-race-design.md` @ `7c7dd52`, implementation `1bc39e1`). **Defect 1**: the cron routes fired `void setRunningMarker(type)` and immediately entered the pipeline; synchronous better-sqlite3 work held the event loop for 215.6s during auto-refresh, so the un-awaited fetch never got a turn and its own 3s AbortController killed it — the marker was never written, and the failure surfaced ~7 min later as a misleading "worker unreachable" warning. The awaited `checkCloudMarker` one line earlier succeeded on the same tick, which is how the network was ruled out. **Defect 2**: `RUNNING_TTL_SECONDS` was 10 min — shorter than the gap from every Mac tick to the Worker's dispatch (digest 8:45→expiry 8:55 vs Worker 9:00; evening 19:00→19:10 vs 19:15; briefing 16:30→16:40 vs 16:45), so **digest and evening were never covered at all** and the briefing only when its launchd tick slipped past 16:35. Fast pipelines had masked both by winning on `mac-sent` instead (8/02 finished 1m54s early). **Fix**: `withRunningMarker` awaits the initial set and heartbeats every 2 min for the lifetime of the send (so no constant has to predict pipeline length again — the TTL now only has to outlive one starved gap), TTL raised 10→15 min, and `confirmMacSent` moved inside the wrapper and awaited, closing a pre-existing sub-second window where a fire-and-forget clear could land before the mac-sent write and leave neither marker set.
- ✅ **Scope decisions recorded**: briefing/digest/evening only (earnings recaps stay deliberately un-gated per 2026-08-05); heartbeat + 15-min grace over a single long TTL; no subject labeling and the Mac never suppresses its own send — suppressing on a cloud-sent marker would have meant receiving ONLY the degraded copy on 8/09, inverting the goal.
- ✅ **Both regression pins mutation-verified** (not just red-green on a missing symbol): removing the heartbeat fails only the renewal test (`expected 1 to be >= 7`); reverting the `await` to `void` fails only the ordering test. Each pin catches its own defect and nothing else.
- ✅ **Deployed**: Worker `npx wrangler deploy` (version `4eb0b0cf`), branch pushed, CLAUDE.md's Workers-Cron-hybrid entry corrected (it still documented the fire-and-forget 10-min behavior).
- New open item filed: the earnings sweep's running marker carries the same `void` + 10-min-TTL shape at lower severity (see Bugs / Quality).
- **Live verification pending**: Monday 8:45 ET digest is a free end-to-end check that sends nothing extra — if `mac-running-digest-<date>` is still in KV at 9:00 (where it would previously have expired at 8:55), the fix is confirmed on real traffic.

---
