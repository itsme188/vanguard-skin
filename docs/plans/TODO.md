# Vanguard Skin — TODO

> **In-repo shortlist.** Updated 2026-06-11 (**Nightly Deep QA system SHIPPED + first sweep's 11 findings ALL FIXED** — sandboxed "synthetic owner" exploratory sweep at 2:45 AM nightly; same-day finds incl. scope-blind TWR cards, what-if dropping the shorts book, two enrichment-data-loss bugs; 24 commits `2f33e06..cd5d986`, tests 2676→2703). Earlier 2026-06-10 PM (**IBKR risk-metric integrity SHIPPED** — fake 23% drawdown / negative Sharpe root-caused against IBKR PortfolioAnalyst PDFs: risk metrics now flow-adjusted (`2c036d1`), daily totals anchor to broker NetLiq (`8c85755`), same-day TWS ghost rows cleaned at sync (`dae09fb`); IBKR now −5.99% DD / Sharpe 1.45 / vol 19.3%, tests 2661→2676). Earlier 2026-06-10 (**digest structured-composer redesign SHIPPED** — 30-day per-source audit (~510 emails/28 sources) drove an edition-aware story-organized layout for morning+evening emails: Late arrivals → alerts → anomalies → Session narrative → per-name synthesis → Research Desk; `lib/digest/editions.ts` single-source + Worker byte-parity mirror; stale `last_digest_sent_at` cloud-skip fix (the 6/9 7pm regression); 20 commits `54fa693..b9f08d1`, tests 2418→2661 Mac / 224→236 Worker, Worker deployed `d05565e9`). 2026-06-07 (**3 review items SHIPPED** — chat real-time `query_market_snapshot` tool + live-market prompt hardening fixing the 6/5 hallucination `dbea3ce` (browser-E2E verified); evening-synthesis TIMEFRAME & THREAD COHERENCE rule for the 6/5 Goldman Sachs wording `57596a4` + Worker deploy `3b297a1d`; Sunday weekly briefing moved 3pm→4:30pm ET `d4a97ef` + Worker. Mac-side pieces go live on this session's DMG rebuild.). 2026-06-05 (**May statement imports + 4 import bugs fixed** — VB/Roth/IBKR all reconciled to 5/31. Fixed: option multiplier→100 default `94dbce9`, UTC timestamp display `94dbce9`, duplicate zero-amount gift/journal transfers `e17477a`, IBKR multi-block NAV + optional Account column `f8fd2d8`). 2026-06-04 (**IBKR connector Tiers 2 + 3 SHIPPED** — headless OAuth 1.0a data path live on Mac + Worker: `5568586`/`c36a95c`/`ec54aae`/`a354b9b`/`b1807f4`; cloud briefing/earnings now read the live IBKR book + KV-cached LST). 2026-06-02: closed-equity snapshot-diff reconcile via PR #23 (`58479c3`); IBKR Claude connector Tier 1 reconciliation. 2026-06-01: Significant Moves residual-zscore + 3% floor + trading-day-gap guard via PR #22 (merge `f96b0fe`, Worker `e5841e97`); Block 3 portfolio-share + multi-account fidelity (`a54c42f`/`2c60b70`).
>
> - v2 build log (Mar–Apr 2026): [archive/TODO-v2-complete-2026-03-30.md](archive/TODO-v2-complete-2026-03-30.md)
> - Master roadmap (off-repo, session-driven): `~/.claude/plans/last-session-session-summary-eventual-ripple.md`
> - Live topic index: the Claude memory at `MEMORY.md` (per-project auto-memory)

---

## Open roadmap (themed)

> Consolidates open items as of 2026-04-30 night. The chronological session log below is the audit trail — entries here cross-reference it where useful.

### Bugs / Quality

- **Bundled secrets in the shipped .app** (added 2026-06-11, found by deep-QA build) — `electron:pack` copies the full `.env.local` (27 keys incl. Resend/Pushover/Gmail/R2) into `Resources/standalone/`, and Next standalone auto-loads it. Any shared DMG ships every secret. Decide: stop bundling (Electron injects env from settings.json anyway — verify nothing else reads the bundled file) or accept for a never-shared personal app. ~1h incl. verifying a rebuilt DMG still boots. This bundling is also why `qa/sandbox.sh` pins keys — keep the pinning even after the fix (defense in depth).
- **`qa/expected-values.json` git-history scrub decision** (added 2026-06-11) — file untracked 2026-06-10 (`ef0883c`) but real portfolio totals remain in public-repo history since April. Owner call: git-filter-repo scrub (precedent: 2026-04-07 screenshot scrub; rewrites history, force-push) or accept. 
- **Watch the first unattended deep-QA sweeps** (added 2026-06-11) — 2:45 AM nightly. Expect: already-fixed findings flip to `fixed` after this session-end's DMG rebuild; tune the charter's NOT-a-finding list from false positives; flip `qa/deep-qa-config.json` to `"mode": "rotate"` after ~a week (~80% cost cut). If parallel zones are ever wanted again, solve per-agent browser isolation first.
- **Watch the next active IBKR trading day for valuation smoothness** (added 2026-06-10) — after the NetLiq-anchor + ghost-cleanup fixes (`8c85755`/`dae09fb`), an intraday-round-trip day should produce: `[syncPortfolio] Removed N same-day ghost holdings row(s)` in logs, a daily total equal to TWS's reported NetLiq, and no ±5%+ fake swings vs neighbors. If a spike recurs, compare `daily_valuations.total_value` vs `monthly_snapshots.total_value − cash_value` per day (diagnostic in `memory/project_valuation_integrity_invariants.md`).
- **Watch the first 3-4 structured digest/evening sends** (added 2026-06-10) — edition-collapsing quality (VK session told once, not 3×), no model self-talk, late-arrival block correctness, email length tolerable with 26+ name sections. Cosmetic known issue: the model sometimes emits the session heading as `#` instead of `##` (renders larger; tighten the prompt or post-process if it bothers). Mac side goes live on the 2026-06-10 DMG rebuild; cloud already live (`d05565e9`).

- ✅ **Analysis: scenario rate-shock model rebuild** — CLOSED 2026-06-09 evening. Options now inherit the underlying's factor exposure (COALESCE join, same rule as the heatmap) levered by delta elasticity Ω=Δ·S/V (|Ω|≤8, ±2.5 fallback, clamped ≥−100%); puts gain on down-shocks; `growth_vs_value.Value` −0.5→0 so value names stop spuriously GAINING on hikes; methodology strings updated. Live book: all 64 options now contribute (was $0). *(original diagnosis below)* (deferred from 2026-06-09 backbone session) — the "live now" rate-scenario number is unreliable: ~93% of the impact is a generic equity risk-off heuristic with round-number coefficients, only ~7% real bond/rate (duration) impact; **21% of the book (options) contributes exactly $0** (unclassified factors → zero) and value-tagged names spuriously *gain* in a hike. `security_betas` exists but is unused. Rebuild: bonds via `duration_years` (already populated), equities via `security_betas` (+ an empirical rate-beta table mirroring `security_regressions`), options via delta (Greeks engine already computes it). The backbone session's classification fixes already shrank the $0-impact blind spot — and the 2026-06-09 afternoon session (`2d80eed`) closed factor coverage to 154/154 incl. all 64 options, so the "unclassified → $0" leg is gone; what remains is the model itself (no delta leverage, no `security_betas`, legacy `scenarios.ts` reads `securities.style` not factors). Engine: `lib/compute/scenario-recipes.ts`. Spec it; ~1-2 sessions.
- ✅ **Analysis IA / navigation** — CLOSED 2026-06-09 evening. Canonical `?view=workspace|diagnostics|performance|trade-reviews` via single-source `lib/analysis/view-param.ts::resolveAnalysisView` (legacy `?mode=factors`, `?view=reviews` still resolve); `AnalysisViewToggle` 4-pill switcher on ALL sub-views (mobile reachability + dead-end fix); TabDropdown entries now the 4 canonical sub-views. *(original diagnosis below)* (deferred from 2026-06-09) — the Analysis tab is 4 sub-screens (Workspace / Diagnostics `?mode=` / Performance `?view=` / Trade Reviews `?view=`) reached 3 different ways: Performance & Trade Reviews are nav dead-ends (no cross-links), the mobile nav drawer never renders sub-views (3 of 4 unreachable on iPhone), URL params are inconsistent (`?mode=` vs `?view=`), and scope handling differs per screen (Workspace has none, Trade Reviews ignores `?scope`). Unify params + add a sub-screen switcher reachable on mobile. Medium.
- ✅ **Analysis interpretation layer** — CLOSED 2026-06-09 evening: `lib/analysis/interpret.ts` single source (16 pure functions + toneClass, 64 boundary tests) rendering one-line plain-English meaning under Sharpe/drawdown/vol/HHI/Beta/Alpha/R²/tracking-error/Greeks/duration + TWR-vs-XIRR spread; Greeks lines PrivateText-wrapped; Workspace reordered (tools before TrustStrip). Browser-verified desktop + iPhone. *(original note below)* (deferred from 2026-06-09) — every card shows correct numbers with zero plain-English meaning (HHI/Herfindahl, Sharpe, Beta/Alpha/R², options Greeks, bond Duration, factor names, "pp"/"Δ"/basis points). Add one-line "what this means / is this good-or-bad" per metric; the Workspace also leads with the TrustStrip data-quality metrics before the actionable tools (wrong hierarchy). Mostly copy. Medium, cross-cutting.
- ✅ **ETF look-through for allocation + scenario sector views** — CLOSED 2026-06-09 evening. `getAllocationByDimension('sector')` now explodes via the shared `explodeHoldingBySector` (dominant-sector position counts); `computeScenario` sectorMoves + `computeRecipeScenario` sectorOverrides both look through cached weights. *(original note below)* (fast-follow from 2026-06-09) — `lib/compute/explode-sector.ts::explodeHoldingBySector` is reusable but wired ONLY into cash-deploy. The Diagnostics allocation breakdown (`getAllocationByDimension` sector dimension) + scenario sector exposure should look through ETFs too so all sector surfaces agree. ~half session.
- ✅ **Cash-deploy: bonds show as "Unknown" sector** — CLOSED 2026-06-09 evening: sectorless bonds bucket as `Fixed Income` inside `explodeHoldingBySector` (covers cash-deploy + allocation + scenarios in one place). *(original note below)* (minor, found 2026-06-09) — held Treasuries have NULL `securities.sector` so they land in the cash-deploy "Unknown" bucket (~$48k / 3.4% of Vanguard Taxable) instead of "Fixed Income"/cash-like. Either tag bonds `Fixed Income` at the sector-write boundary or special-case bond `security_type` in `explodeHoldingBySector`/`loadCurrentHoldings`. ~30 min.
- ✅ **Garbage timestamp-symbol securities** — CLOSED 2026-06-09 evening, and it was the footprint of a WORSE bug: the pre-f8fd2d8 May import misread Date/Time as symbol (127 orphans), AND `assetCategory === "Options"` never matched real statements' "Equity and Index Options" so EVERY April+May option trade imported as a stock BUY/SELL under a duplicate raw symbol. Fixed parser (`.includes("Options")`), validation now covers the securities array + prices garbage + no-alphanumeric symbols, deleted 43 bad txns + 168 orphan securities + 2 more (`Settlement Date`/`UNKNOWN`), re-imported both statements (44 option trades correct, OCC symbols, verified vs statement), recomputed lots+valuations. `scripts/repair-ibkr-option-trades.ts`. *(original note below)* (data hygiene, found 2026-06-09) — 25 `securities` rows have a timestamp string ("2026-05-01, 14:28:49") as `symbol`, type Stock, no name — corrupt rows `isGarbageSymbol()` should have rejected at import. They permanently fail classification (Claude correctly balks). Identify the import path that created them + purge. ~1hr.
- ✅ **Option-sector classification has no refresh cadence** — CLOSED 2026-06-09 evening: auto-refresh Step 2.5 (full tier, free SQL pre-check) + folded into `POST /api/compute/classify`. Also NEW Step 2.6: `classifyFactors` runs on full refresh (incremental, zero Claude cost when classified). *(original note below)* (follow-up from 2026-06-09) — `scripts/classify-option-sectors.ts` is a one-off; new option positions on non-held underlyings will land blank-sector again. Fold `classifyOptionSectors` into an existing refresh (post-import hook or the classify route) or a periodic script. Low. *(Note 2026-06-09 afternoon: factor classification of new option underlyings IS now self-healing — any classify-button press / `run-classify.ts` run picks them up via the new underlying candidate query in `classifyFactors`. Sector is the remaining one-off.)*
- ✅ **`PATCH /api/research/sources` SQL column-name interpolation** — CLOSED 2026-06-09 evening: `UPDATABLE_SOURCE_COLUMNS` allowlist in `updateSource`; unknown keys dropped, never interpolated. *(original note below)* (hardening, found 2026-06-09 afternoon) — the route spreads `{ id, ...updates }` straight from the request body into `updateSource`, whose dynamic `SET` loop interpolates each key as a column name. TS types don't enforce at runtime; an unknown key just throws at prepare (no injection of *values* — those are parameterized), but an allowlist of updatable columns would make it correct-by-construction. ~15 min.
- ✅ **Silent internal data-loads** — CLOSED 2026-06-09 evening: SecurityChart shows a status-bar hint when the levels fetch fails (self-clears on poll); TradeReviewView detail panel renders the error + retry hint; ResearchFeedsView filter/search/sources-changed failures toast ("list still shows the previous selection"). *(original note below)* — three lower-priority cases intentionally left: `SecurityChart` level-overlay fetch skips silently on network error (chart renders without level lines, no hint), `TradeReviewView` expand/refresh leaves the detail panel blank on failed fetch, `ResearchFeedsView` filter-dropdown `refreshArticles` failures keep stale list under a new filter label. All self-heal on reload; fix if any recurs as a user-visible confusion. ~1hr batch.
- ✅ **Momentum Pulse #1 — freshness** (SHIPPED 2026-06-08 `27e1b6d`; Mac UI live on next DMG rebuild). New `lib/benchmark/yahoo-benchmarks.ts::fetchBenchmarkClosesFromYahoo` (TWS-free Yahoo top-off) wired into `runAutoRefresh` Step 5b on BOTH tiers; benchmark symbol list extracted to single-source `lib/benchmark/symbols.ts`. Verified against real Yahoo (pulled today's closes). +4 tests. Original diagnosis below:
  - *Diagnosis (kept for context):* **Momentum Pulse #1 — freshness: benchmark closes only advance on the connect-time full TWS refresh** (diagnosed 2026-06-08, verified against live DB). `MomentumPulse` (Today tile) reads SPY/MTUM/SPMO/USMV daily closes from `benchmark_prices`, which is written ONLY by Step 5 (`fetchBenchmarkPrices`) of `runAutoRefresh`, and that step runs ONLY when `level==='full'` (`auto-refresh.ts:202-223` — quick path is `Promise.resolve()`). The 30-min background refresh AND the manual ↻ button are both `quick`, so they skip benchmarks; a full refresh fires only on initial TWS connect AND requires TWS connected. Net: once connected, the tile freezes at the connect-time date for the rest of the session; weekend TWS-off means Friday's close is invisible until Monday's reconnect. **Proven 6/8:** held `prices` reached 6/8 (today, via quick), but `benchmark_prices` stopped at 6/5 — exactly the "stuck at June 4 across Fri/Sat/Sun" the user saw on the day momentum flipped (MTUM −5.95% vs SPY −2.58% on 6/5). **Fix:** decouple benchmark closes from TWS — add a TWS-free Yahoo top-off (`interval=1d` chart `timestamp[]`+`close[]`, sibling of `lib/queries/market-snapshot.ts::fetchYahooQuotes`) that runs on the QUICK tier (so background + manual advance them) regardless of TWS. Benchmarks are public/free market data; no reason to gate them on a brokerage session. Keep TWS fetch for the initial 2Y backfill.
- ✅ **Momentum Pulse #2 — usefulness + 2-col layout** (SHIPPED 2026-06-08 `27e1b6d`; Mac UI live on next DMG rebuild). `classifyStatus` rewritten as a 1d→5d→30d priority ladder + new `trigger` field; Friday now headlines "Sell-off in progress" (was "Momentum leading"). New takeaway subtitle ("MTUM −3.4% vs SPY today · 30d trend still +7.0%"); columns reordered 1d/5d/30d. Today header reworked to a 2-col row (releases left + momentum right), both halves always populated (releases falls back to next-upcoming; momentum renders every state incl. neutral/no-data). Browser-verified desktop+mobile. Original diagnosis below:
  - *Diagnosis (kept for context):* **Momentum Pulse #2 — usefulness: classification is 30d-dominated, so a sharp regime flip doesn't surface** (2026-06-08). `classifyStatus` (`lib/compute/momentum-spread.ts`) checks 30d first (`sell_off` if 30d<−3%, `leading` if 30d>1%), so even with fresh 6/5 data the headline reads "Momentum leading" (30d +7%) on the very day momentum cracked −3.4% in one session. User wants priority order **1d → 5d → 30d**: evaluate the shortest window first, let the first window that crosses its threshold drive the headline status, and surface long-window disagreement in the reason ("MTUM −3.4% vs SPY · 1d — 30d still +7%"). Reorder the tile's columns to 1d/5d/30d to match. Thresholds tunable (TDD).
- ✅ **Earnings date cross-check (Finnhub × Nasdaq) + human-in-the-loop conflict confirmation** (SHIPPED 2026-06-08 `50ed6b6`; Mac UI live on next DMG rebuild). Spec: `docs/superpowers/specs/2026-06-08-earnings-date-crosscheck-design.md`. New `lib/calendar/nasdaq.ts` (free Nasdaq calendar, US-listed) + `lib/calendar/reconcile-earnings-dates.ts` (clusters per issuer-family, resolves canonical date + trust status, marks `superseded`) wired into `syncCalendarForWeek`. Migration 057 (`date_status`/`date_conflict_with`/`superseded`). Resolution: agree→confirmed, past-with-actuals→auto-corrected, future-vs-future disagree→**conflict** (Nasdaq provisional, user confirms vs IBKR via `POST /api/earnings/confirm-date` → locked `user_confirmed`, never re-synced), one source→single. Readers (Hub/today/upcoming/week-ahead/email-cron) filter `superseded=0`. UI: `EarningsDateChip` (✓2src / 1src / 🔒 / ⚠confirm popover) on Hub rows + NotificationBell conflict count. **Verified on live data: RBRK ghost (Finnhub 6/8) superseded by real Nasdaq 6/4 → dropped off this week; synthetic conflict browser-verified end-to-end (chip + popover + bell).** +30 tests. Original diagnosis below:
  - *Diagnosis (kept for context):* **Earnings dates unreliable (Finnhub) — IBKR connector CANNOT replace it** (2026-06-08). Finnhub mis-dates / foreign-suffix-drifts (asks GFL, gets GFL.TO) / unreliable `hour` / actuals drift; worse, `getEarningsForWeekDeduped` dedup *prefers* Finnhub so better dates get overridden. **Confirmed dead end:** the IBKR Claude MCP connector exposes only account/price/trade/contract tools — NO earnings-calendar or consensus endpoint anywhere in the IBKR Web API (per `docs/plans/2026-06-01-ibkr-connector-assessment.md`). **Plan:** (a) reorder dedup so WSH (TWS, company-filing-sourced) + manual outrank Finnhub for held-name *dates*; (b) add a TWS-independent free source for dates (Nasdaq `api.nasdaq.com/api/calendar/earnings` and/or Yahoo `calendarEvents` per held symbol); (c) demote Finnhub to consensus-estimates-only. Design decision (source mix) still open.
- ✅ **IBKR connector — untapped capabilities (items 1/2/3)** — SHIPPED 2026-06-08 (`9757bc0` Worker leak-fix + `b0ff39c` enrichment; Mac UI + Greeks live on next DMG rebuild, Worker fix live on next deploy). Spec: `docs/superpowers/specs/2026-06-08-ibkr-market-data-snapshot-design.md`. **(1) Cost-basis privacy leak FIXED** — Worker `fallback-earnings.ts::renderPositions` was emitting `cost basis $18,300.00` / `total cost $X` into the shared earnings email (brother on cc). Ported `formatPositionPresence` → new `workers/cron/src/presence-position.ts` (byte-parity hand-copy, like `issuerSiblings`); positions now render `100 sh AAPL (IBKR, up ~12.0%)` — and live IBKR rows GAINED a return-% (threaded `mktPrice`→`latest_price` through `livePositionViewsForFamily`). **(2)+(3) Market-data snapshot enrichment** — items 2+3 are ONE IBKR Web API call (`/iserver/marketdata/snapshot`). New `lib/ibkr/market-data.ts` (`getMarketDataSnapshot` + pure `parseSnapshotRow`; **field codes VERIFIED via live probe**: `31`=last, `7283`=IV%, `7284`=HV%, `7293`/`7294`=52wk hi/lo). Migration 058 `security_quotes` (iv/hv/52wk/yield cache). `fetchAndStoreQuotes` (in `refresh.ts`, rides the existing OAuth session) captures held+watchlist (excl. option/bond/cash), caches IV/HV/52wk + writes price (`'tws'`) for the "free byproduct" scope (watchlist = new coverage). Consumers: Greeks uses `security_quotes.iv_underlying` as the fallback sigma when option price is missing (replaces blind 30%, `ivSource:"ibkr"`); new `<QuoteStats>` strip on Security Detail (52wk range + IV + HV, public data, unmasked). **Dividend yield DEFERRED** — not in the raw snapshot (probe-confirmed); column reserved nullable. **Live E2E: 84 securities, AAPL IV 0.243/HV 0.232/52wk 194.47–316.94 matched ground truth; browser-verified the strip.** +24 tests (Mac 2372, Worker 224). Tier-1 agent April/May transaction backfill already done.
- ✅ **IBKR dividend yield** — CLOSED 2026-06-09 evening. Probe-verified the IBKR session CANNOT provide it (fields 7286-7292 dead, fundamentals/summary ratios has no yield + 503s; `scripts/probe-ibkr-dividend-yield.ts`). Filled instead from Finnhub free `/stock/metric` `currentDividendYieldTTM` (live-verified KO 3.205%): capped rotating batch in `fetchAndStoreQuotes` (`lib/quotes/finnhub-dividend-yield.ts`), keep-last-known COALESCE upsert, `Div yield (TTM)` cell in QuoteStats. *(original note below)* (2026-06-08, ~30 min) — the raw `/iserver/marketdata/snapshot` does NOT expose dividend yield (probe-confirmed across 3 warm-up polls for AAPL); the `security_quotes.dividend_yield` column is reserved nullable. The IBKR connector sources it from a separate fundamentals endpoint (`/iserver/secdef/info` or `/fundamentals`). When wanted: verify that endpoint's shape with a live probe, fetch yield in `fetchAndStoreQuotes`, surface it in `<QuoteStats>` (a `Stat label="Div yield"` cell is already trivial to add). Low priority — IV + 52wk range are the headline value.
- ✅ **Chat can't reach current market data — hallucinated on 2026-06-05** — diagnosed + fixed 2026-06-07 (`dbea3ce`; browser-E2E verified; Mac changes live on this session's DMG rebuild). **Root cause (verified):** the chat exposed 26 tools, ZERO web-search, and no market-overview tool — only per-symbol daily closes via `query_price_history`; FRED is prior-day-lagged. And the system-prompt hallucination guardrails covered only "is this position held," with nothing forbidding market-commentary confabulation. Plus the injected current date was UTC (`route.ts:85`), a day ahead after ~8pm ET. **Fix (both layers, TDD):** (1) NEW `query_market_snapshot` chat tool — `lib/queries/market-snapshot.ts`, **local-first** (latest-vs-prior close for SPY/QQQ/DIA + held names via the anomaly engine's SPY-anchored consecutive trading-day pair, reusing exported `resolveTradingDayPair`), **Yahoo live fallback** (`fetchYahooQuotes`) only when the local book is stale (>4 cal-days) or has no clean pair; every result carries `source`/`asOf`/`stale`/`note`. Registered in `lib/chat/tools.ts` CHAT_TOOLS + `executeTool` + label. (2) Prompt hardening — shared `LIVE_MARKET_RULE` injected into both `getSharedCore` AND the macro prompt: "you CANNOT observe live/intraday market; you MUST call `query_market_snapshot`; never state moves more current than `asOf`; if `source:'none'`/stale say data is unavailable, don't invent." (3) `route.ts:85` UTC→`todayET()`. +9 tests (snapshot query 4, tool dispatch 2, prompt pins 2, coherence pin shared). Full suite green (Mac 2328, Worker 217), tsc clean. **Pending:** browser E2E (ask the chat "what's the market doing today" and confirm it calls the tool + reports as-of) — not yet done.
- ✅ **6/5 Friday evening recap — confusing Goldman Sachs wording** — diagnosed + fixed 2026-06-07 (`57596a4`; Worker DEPLOYED `3b297a1d`; Mac `synthesize.ts` live on this session's DMG rebuild). **Root cause (verified): NOT a bug and NOT a hallucination — a synthesis-coherence failure.** The 6/5 evening email was sent by the **cloud Worker fallback** (Mac log: `already_sent_by_cloud`), whose synthesis prompt is thinner than the Mac's. Every fact traced to a real source or real price: GS rose into Thu ($1065→$1092, financials rotation, art 21509), fell ~5% Fri ($1092→$1038, broad selloff, art 22337), Solomon AI-jobs/SpaceX **verbatim** from art 21439. "Up Thursday / down Friday" = two days, not a contradiction — but `bucketByCompany` pooled the mixed-Thu + bearish-Fri + macro-Solomon articles into one GS paragraph and the prompt (one paragraph per name, no temporal/thread discipline) let Sonnet fuse them into a self-contradictory run-on; "sell-off hit brokers/banks" was an unsourced model generalization. GS didn't trip the anomaly engine (z≈1.16<2), so the framing was pure synthesis. **Fix (TDD):** added a `TIMEFRAME & THREAD COHERENCE` rule to BOTH synthesis prompts — `lib/digest/synthesize.ts` SYSTEM_PROMPT and `workers/cron/src/fallback-evening.ts buildSynthesisPrompt` (now exported + pinned) — instructing the model to attribute each move to its trading day, keep structural catalyst threads separate from same-day tactical moves, and not assert an unsourced sector driver. +2 pin tests; suites green.
- ✅ **Move Sunday weekly briefing 3pm → 4:30pm ET** — done 2026-06-07 (Mac `d4a97ef`, Worker `c9f2f033`→redeployed `3b297a1d`). Eliant Capital (user said "Alliance Capital"; src id=18, a preferred weekend source) had published its weekly *after* 3pm ET the last two Sundays, so the 3pm send missed it. Moved both schedulers to 16:30 ET in lockstep: (a) `scripts/send-weekly-briefing.sh` et-gate `in_et_window "7"/"1" 16 30` (the installed `~/Library/LaunchAgents/com.vanguard-skin.weekly-email.plist` points straight at the repo script with `StartInterval=300` + no time logic, so the change went live on the next 5-min tick — no reload/DMG needed). (b) Worker `parseJobFromClock` in `workers/cron/src/index.ts` gained a **new `EXPECTED_MINUTE_BRIEFING` gate** (default 30) — the old gate was hour-only, harmless at 15:00 but would have fired the cloud fallback at 16:00 (~30min early) the same way the 8:00-vs-8:45 digest bug did; `wrangler.toml` set `EXPECTED_HOUR_BRIEFING="16"` + `EXPECTED_MINUTE_BRIEFING="30"`. **Deployed** (version `c9f2f033`, bindings confirm 16/30). Without the deploy the un-deployed Worker would have cloud-sent at 15:00 and dedup-suppressed the Mac's correct 16:30 run — so the deploy was required, not optional. Worker tests updated to the 16:30 contract + 2 new minute-gate regressions (216/216 pass). et-gate boundary re-exercised (fires 16:30-16:39 Sun/Mon, skips 15:00). Mac-side change committed `d4a97ef`.
- ✅ **`query_market_snapshot` DIA gap** — CLOSED 2026-06-09 evening: `closeOn` falls back to `benchmark_prices` (findCrossedLevels precedent); DIA serves on the fresh-local path (live-verified). *(original note below)* (new 2026-06-07) — the new chat tool prefers the local book's last *close* (user chose local-first over intraday); it only hits Yahoo when local is >4 cal-days stale (`MARKET_SNAPSHOT_STALE_DAYS` in `lib/queries/market-snapshot.ts`). Two low-priority follow-ups: (1) **DIA isn't in the local `prices` table**, so on the common fresh-local path it's silently skipped (SPY/QQQ + held VTI show; the Yahoo fallback does fetch DIA) — add DIA to the benchmark/price sync if local coverage matters. (2) If *intraday* priority during market hours is ever wanted, flip the local-first ordering / tighten the stale threshold.
- ✅ **Option multiplier NULL→1 on canonical-CSV imports (1/100th valuation)** — closed 2026-06-05 (`94dbce9`). New options imported via canonical CSV (no multiplier column) landed at `multiplier=NULL` → `COALESCE(...,1)` → valued at 1/100th, leaking ~$79k of VB option value into inferred cash. `upsertSecurity` now defaults option multiplier to 100 when missing/≤1 (TWS enrichment already set it; this covers writers that don't). `scripts/fix-option-multipliers.ts` repaired 123 existing securities + recomputed valuations. VB 5/31 holdings/cash split now ties to the statement. +4 tests.
- ✅ **Import History showed next-day date (UTC rendered as local)** — closed 2026-06-05 (`94dbce9`). `created_at` is SQLite `datetime('now')` (UTC, no tz marker); `ImportHistory` parsed it via `new Date()` which treats it as local time, rolling evening imports to the next calendar day. New `lib/format.ts::parseStoredTimestamp` parses as UTC; `ImportHistory` uses it. +3 deterministic tests. (Same UTC-storage root as the email-date rule, now in a UI helper — grep other `new Date(<stored ts>)` sites if it recurs.)
- ✅ **Duplicate zero-amount gift/journal transfers silently dropped** — closed 2026-06-05 (`e17477a`). Gifting/journaling shares produces zero-amount `TRANSFER_IN`/`TRANSFER_OUT` rows; two same-day same-symbol journals share an identical key + amount (cents=0 for both) so the `:cents` disambiguator collided and the 2nd was dropped by `INSERT OR IGNORE`. Parser now appends a stable `:#N` ordinal to the 2nd+ identical source_key (first keeps the bare key → re-import idempotent). Co-Work prompt + `docs/canonical-csv-guide.md` gained a gifted/journaled-shares rule. +2 tests. (Backfilled the one XMTR row that was dropped from May's VB import.)
- ✅ **IBKR activity parser: multi-block NAV + optional Account column** — closed 2026-06-05 (`f8fd2d8`). May IBKR Activity Statement exposed two bugs April didn't: (1) securities-lending/multi-currency statements append a SECOND zeroed `Change in NAV` block (+ `0%` TWR) that overwrote the real ending value → snapshot silently dropped by the `endingValue !== 0` guard; fixed by capturing only the first block + first TWR. (2) IBKR omits the `Account` column on single-account Trades sections, shifting every column by one → symbol read a timestamp → all 131 trades rejected by validation; fixed by resolving Trades columns BY NAME from the header (legacy-index fallback). +2 tests. IBKR imported + reconciled to 5/31 ($484,424.41, TWR 7.71%). **Note: `commitImport` does NOT recompute daily_valuations itself** (the API route does, post-commit) — script-based imports must run `computeDailyValuations(db)` explicitly afterward.
- ✅ **Significant Moves anomaly detector over-fires on flat days** — closed 2026-06-01 (PR #22, merge `f96b0fe`; feature commit `44cfc41`, Worker port `0d94e6d`, tie-break `199a427`). Old gate `|move| > max(2×|beta×SPY|, 1%)` collapsed to the 1% floor on flat-index days, flagging ordinary noise. Replaced with a hybrid two-gate: a name flags only when `|move| ≥ 3%` AND `z ≥ 2σ`, where `z = |move − beta×SPY| / residual_std` (the stock's own idiosyncratic daily vol from the 60-day regression, newly persisted via migration 056). Graceful degradation to the 3% floor when `residual_std` absent. Mac engine (`lib/digest/anomalies.ts`) + Worker fallback (`workers/cron/src/fallback-evening.ts::evaluateAnomalies`) kept in lockstep (parity-tested); R2 snapshot carries `residualStd`. Email shows all flags (cap removed); chip reads `Nσ`. Browser-verified on a flat tape (SPY +0.2%) — flagged exactly 1 name (LAND −3.3%, 2.5σ) vs the old "everything >1%". +52 tests. Design/plan: `docs/superpowers/specs|plans/2026-06-01-significant-moves-residual-zscore*`. (PR #22, merge `f96b0fe`; feature commit `44cfc41`, Worker port `0d94e6d`, tie-break `199a427`). Old gate `|move| > max(2×|beta×SPY|, 1%)` collapsed to the 1% floor on flat-index days, flagging ordinary noise. Replaced with a hybrid two-gate: a name flags only when `|move| ≥ 3%` AND `z ≥ 2σ`, where `z = |move − beta×SPY| / residual_std` (the stock's own idiosyncratic daily vol from the 60-day regression, newly persisted via migration 056). Graceful degradation to the 3% floor when `residual_std` absent. Mac engine (`lib/digest/anomalies.ts`) + Worker fallback (`workers/cron/src/fallback-evening.ts::evaluateAnomalies`) kept in lockstep (parity-tested); R2 snapshot carries `residualStd`. Email shows all flags (cap removed); chip reads `Nσ`. Browser-verified on a flat tape (SPY +0.2%) — flagged exactly 1 name (LAND −3.3%, 2.5σ) vs the old "everything >1%". +52 tests. Design/plan: `docs/superpowers/specs|plans/2026-06-01-significant-moves-residual-zscore*`.
- ✅ **NFLX β=−14.31 → trading-day-gap corruption in return series** — closed 2026-06-01 (`d174e89`, in PR #22). Root cause: the `prices` table has a ~9-month hole between the last Vanguard `canonical` statement anchor (2025-06-30) and the start of `tws` daily capture (2026-03-27); `computeBeta` (`refresh-vanguard-betas.ts`, `LIMIT 70` rows) and `computePositionRisk` (`risk.ts`, 365-day window) paired adjacent rows without a trading-day check, so one pair spanned the gap and injected a spurious single-period return (plain appreciation for most names; a stock split for NFLX → −91% → β=−14.31). Fix: `calendarDaysBetween()` in canonical `date-utils` + a 7-day gap guard in both sites. Live re-backfill: NFLX −14.31→0.15, GOOG 3.26→1.55, **0 remaining |β|>3**; NFLX `residual_std` 14.35%→1.85% (un-mutes it in the new anomaly detector). `security-regression.ts` (252-day window) + `daily_valuations`-based portfolio stats were unaffected (windows don't reach the anchor). +6 tests. See `memory/project_prices_gap_guard.md`.
- ✅ **Trade-vs-settlement date mismatch** — closed 2026-05-11 night (`57152d5`). Investigation found the original framing assumed `settlement_date` would be the signal, but reality: IBKR statements don't populate it (NULL on all rows); Vanguard uses it as T+1 (chip would show <24hr per trade). Real user pain is structurally different — Vanguard holdings are statement-period-stale until next monthly import. Pivoted to `<SnapshotAge>` primitive surfacing "Vanguard · Apr 30 · 11d ago" on Today header + per-account view; tone escalates at 8d (mid-cycle) and 22d (statement-cycle missed). 8 unit tests on `computeSnapshotAgeMeta`; browser-verified at 390×844 + 1280×800.
- ✅ **Earnings transcripts panel — visual + data-correctness fix** — closed 2026-05-05 night (`cd9f45b`). Three sub-fixes: (a) **Data-correctness bug** in `lib/transcripts/fetch.ts` — the EDGAR fallback was taking `filings[0]` (most recent 8-K) regardless of whether it matched the requested year+quarter, silently caching wrong-quarter content under right-quarter labels. New `deriveFilingReportingQuarter()` maps a filing date to its calendar reporting quarter (Apr-Jun → Q1, Jul-Sep → Q2, Oct-Dec → Q3, Jan-Mar → Q4-prior); fallback now picks matching filing or returns null. Tickers on non-calendar fiscal years under-match (safe failure). Sibling fix: `getTranscriptForChat` no longer returns the just-deleted in-memory legacy body when its post-invalidation re-fetch fails. (b) **Panel polish** in `app/dashboard/security/[id]/page.tsx` — section now always renders (was silent-hiding when empty), shows an intentional empty-state body explaining the source-priority chain when no transcripts cached, native `<details>` show-more disclosure after first 8 rows (was hard-clipped at 4). (c) **New `↻ refresh` client island** (`TranscriptsRefreshButton.tsx`) wired into `TerminalSection`'s action prop — POSTs `/api/transcripts {ticker}` and `router.refresh()` on success. (d) Audit script `scripts/audit-transcripts-quarter-mismatch.ts` (print-only by default, `--delete-flagged` for cleanup) — live DB ran 0 mismatches, both existing edgar_8k rows correct. +6 tests (3 deriveFilingReportingQuarter + 3 EDGAR quarter-match behaviors). Visual smoke verified via agent-browser at :3000 against APP (empty state) + TER (populated). Source-replacement question (API Ninjas premium-gated, Motley Fool brittle, EDGAR press-release-only) parked as a separate scoping item below.
- ✅ **Eli email landed in junk — deliverability hardening shipped** — closed 2026-05-08 (`df836df`, `4bddf00`, `84d0bc1`, plus DNS publish). Three-pronged fix: (1) **DMARC TXT record** published at `_dmarc.myportfoliodesk.com` with `v=DMARC1; p=none; rua=mailto:dmarc-reports@myportfoliodesk.com; pct=100; aspf=r; adkim=r;` — the most likely root cause; Gmail's 2024+ Sender Guidelines penalize domains without DMARC. Verified propagation across Cloudflare 1.1.1.1, Google 8.8.8.8, local resolver, authoritative `johnathan.ns.cloudflare.com`. (2) **Outbound headers** added in `lib/email.ts` + `workers/cron/src/resend.ts` (Mac SMTP + Worker REST mirrors): `List-Unsubscribe` (mailto), `List-Unsubscribe-Post: List-Unsubscribe=One-Click`, `Reply-To` (defaults to `replies@<domain>`, env override available), `Message-ID` (UUID@domain). (3) **Cloudflare Email Routing rules** for `replies@` + `dmarc-reports@` → `isaac@wolfsonfamily.com` so reply-to traffic and DMARC reports both flow somewhere readable. Reply test forwarded successfully (lands in Gmail All Mail — Inbox placement TBD on first real reply; may need Gmail filter for "forwarded via Cloudflare"). Audit baseline: `docs/email-deliverability-audit-2026-05-08.md`. **Next step (not blocking)**: after 1-2 weeks of clean DMARC reports landing in `dmarc-reports@`, escalate policy from `p=none` → `p=quarantine`.
- ✅ **EarningsHub privacy leak** — shipped 2026-05-01 quick-wins sweep (`92f76d6`). NumCell + MobileCard wrap Cons.EPS / Act.EPS / Cons.Rev / Act.Rev in `<PrivateText>`. Symbol / status chip / slot / delta stay visible (per public-vs-portfolio split).
- ✅ **Chat message renderer privacy leak** — shipped 2026-05-01 quick-wins sweep (`92f76d6`). New `PrivateMarkdown` component wraps assistant messages (block-safe — bare `PrivateText` would put `<p>`/`<table>` inside a `<span>`); `PrivateText` wraps user messages (plain text inside `<div>`). Verified on dev server: full message bodies mask to `•••` under privacy mode, no hydration warnings.
- ✅ **IBKR April 2026 transactions gap** — closed 2026-06-02. The April statement already arrived + imported (178 txns, keyed `ibkr:trade:`/`ibkr:div:`). Verified COMPLETE + accurate by reconciling against the live IBKR Claude connector (broker source-of-truth): all 72 April stock trade symbol×side aggregates match exactly (0 connector-only / 0 DB-only), and cost basis on every overlapping position matches to the penny. The 256-connector-fills vs 178-DB-txns difference was statement fill-aggregation, not missing data. (Importing the connector trades would have DOUBLE-COUNTED — different source_key scheme.)
- ✅ **IBKR holdings stale (TWS not synced since 4/30)** — fixed 2026-06-02 by a full `/api/tws/auto-refresh` once TWS was open; currently-held IBKR positions now fresh as of 2026-06-02 (20 positions = 16 stocks + 4 options), matching the broker's live book. (Surfaced originally by the connector reconciliation.)
- ✅ **Closed equity positions linger as phantom holdings** — fixed 2026-06-02 (surfaced when the TWS sync above didn't clear them). TWS sync writes only *currently-held* positions; a fully sold/covered stock/ETF disappears from the snapshot and its last row was never zeroed (no purge existed for closed equities, only options/bonds). New **`lib/mutations/closed-equity.ts::reconcileClosedEquityHoldings`** — a **snapshot-diff** reconciliation (the robust signal, since equities have no expiry/maturity and the local trade ledger is often incomplete): any stock/ETF absent from an account's latest *full* holdings snapshot gets a `quantity = 0` row at the snapshot date. Non-destructive (preserves cost-basis/history), self-healing (a later real sync wins), idempotent. **Shrink guard** (skip if latest snapshot < 50% of prior) prevents a partial/failed TWS sync or under-extracted Vanguard PDF from wiping the book. Wired into TWS auto-refresh **step 1.8** + the `commitImport` post-commit hook (runs on the next IBKR/Vanguard statement import too). +8 unit tests. Live reconcile cleared 44 phantoms (35 IBKR + 9 Vanguard/Roth, all verified genuinely closed); IBKR now matches the broker's 16-stock live book exactly.
- ✅ **Notes — reset `formSymbol` on type-switch** — closed 2026-05-12 (`7dfa733`). Third reset point added to the type-`<select>`'s onChange: clears `formSymbol` when next type is journal. Belt-and-suspenders-and-belt with the existing handleCreate gate and the post-save reset from `1e070a2`. 11 LOC. Not behaviorally observable until a future refactor drops a defense layer.
- **Existing CRCL-tagged journal entries pre-fix** (2026-05-11) — the `1e070a2` fix prevents new tag-leaks but doesn't clean up rows that were already silently tagged. User can delete them from the Notes UI; no auto-migration since the count is presumably small and the user knows which ones are wrong. Track only if the cleanup becomes annoying.
- ✅ **Today view: IBKR holdings prices go stale, no manual refresh** — closed 2026-05-02 (`f7cd593`, drift catch 2026-05-03). `IbkrRefreshButton.tsx` mounted on Today's IBKR section header — POSTs `/api/tws/auto-refresh {level:"quick"}`, polls sync-status, calls `router.refresh()`. "Xd ago" age label visible alongside.
- ✅ **Earnings recap data gap — `reaction_snapshot` NULL on shipped recaps** — closed 2026-05-03. TWS upgrade path returned `reaction_present:false` (TWS bars empty for those windows), so fell back to Yahoo. Surfaced a real Worker bug: `workers/cron/src/yahoo.ts` was missing `&includePrePost=true`, which means *every* BMO/AMC release in the cloud-fallback path was capturing zero post-window bars. New `scripts/backfill-reaction-from-yahoo.ts` (Mac-side) imports the same Worker helpers + writes locally. LIN id 278 → SPY +0.55% / QQQ +1.12% / TLT +0.12% / LIN +3.21%. HUN id 274 → SPY +0.23% / QQQ +0.19% / TLT +0.03% / HUN +4.38%. Both `source: "yahoo"` so a future TWS upgrade can overwrite. ✅ **Worker `wrangler deploy` shipped 2026-05-03** (later session, version `2dba8e8c`) — cloud-fallback path now correctly captures pre/post-market bars for BMO/AMC releases.
- ✅ **Stale TLT 4/24 $86.50 call still in `holdings`** — closed 2026-05-03. Found 5 stale rows total (TLT, SPY 4/10, 3× HOOD 4/24). New `lib/mutations/expired-options.ts::purgeExpiredOptionHoldings(db, graceDays=1)` + 5 unit tests. Wired as Step 1.5 of `lib/tws/auto-refresh.ts` so every TWS connect cleans up automatically. One-shot `scripts/purge-expired-options-once.ts` ran the initial cleanup; live DB now shows 0 expired option holdings.
- ✅ **Phase 5 thorough sweep — 17 user-visible bugs closed + TWS writer fixes + IBKR name backfill** (2026-05-03 evening) — Four rounds of fixes covering every finding from the section-by-section iPhone sweep, plus root-cause prevention so the same drift doesn't recur. **Round 1 (`b5106b2`):** sector tilt empty-string label, AnalysisView Roth scope + disjoint scope semantics, IBKR/Position-Risk duplicate-symbol guard, formatExpiry YYYYMMDD fallback, INTC + HOOD + ALAB DB row normalization (also fixed Option Expirations card by side-effect), negative-EPS format `-$0.56`, "1 contract" wording for option trades. **Round 2 (`37d0d08`):** Detected Strategies + Option Expirations now scope-filtered, Recent Transactions Type chip on mobile, caret sweep (▼ → ••• on review-card / per-trade card / Build Custom Scenario), risk-free rate label reads from FRED cache (3.68% vs hardcoded 4.5%), 4-digit year on Drawdown dates, CUSIP: prefix cleanup (6 rows + parser fallback). **Round 3 (`4287f40`):** vol/Sharpe sublabel "{n}/30 days · need {30-n} more" instead of bare em-dash, Factor Heatmap mobile-short labels (Rates/Growth/Cycle/Int'l/Onshore/Tariff/AI/Crypto/Reg), Top 10 chart YAxis width + scaling height, Position-Level Risk drops secondary name on mobile, Trade Reviews month dropdown short format ("Mar 2026 · 98 trades"). **Round 4 (`65513ba` + `7d0c789`):** TWS-enrichment normalization at-source (positions.ts now writes ISO YYYY-MM-DD + capitalized security_type) + at-boundary defense (upsertSecurity catches malformed values from any future writer), enrichSecurities() extended to backfill `name` from contractDetails.longName, new `scripts/backfill-security-names.ts` ran live against TWS and populated 42 IBKR/Vanguard names ("CLOUDFLARE INC - CLASS A", "AMAZON.COM INC", etc.). All 1461 tests pass. Browser-verified end-to-end at 390×844 in light + dark mode after each round.
- ✅ **Four silent-data-loss bugs in canonical-csv import — closed 2026-05-04 evening** (`33f3c63`). Surfaced during April 2026 statement upload. (1) Co-Work emitted `# filename.csv` comment lines as the first line of each CSV — defeated header detection. Parser + detector now strip leading `#` and blank lines. (2) Validator's `isValidQuantity` required `>= 0`, silently dropping 16 VB rows + 4 Roth rows (sells, closes, withdrawals, fees, redemption) when Co-Work used Vanguard PDF's signed-quantity convention. Parser now auto-normalizes negatives to abs() with warning. (3) Source-key formula `canonical:txn:{acct}:{sym}:{date}:{type}` collided on same-day same-symbol same-type fills — a 100-share RSP SELL on 4/13 was silently lost when a 400-share RSP SELL on 4/13 hit first. Formula now appends `:{cents}` (Math.round(amount*100)). Migration 046 backfills existing source_keys to match. (4) IBKR snapshot upsert used INSERT OR IGNORE, so the rich ibkr-activity statement row was preempted by an earlier sparse TWS-source row (`total_value=$448,941` instead of the statement's $449,764 with starting_value/twr/deposits). Engine + TWS positions.ts both updated to use ON CONFLICT DO UPDATE WHERE source IN ('tws','manual') so statements always win. Plus Co-Work prompt teaches positive-quantity convention, CASH placeholder, account-name aliases, and no comment lines. 1466/1466 tests including 5 new regressions. All three account totals reconcile to statements (VB $1,301,484, Roth $104,764, IBKR $449,764).
- ✅ **Trade Review option-origin context (RSP narrative fix)** — closed 2026-05-05 (`0bc20af`). The RSP April review narrated the user's trade as a plain "buy 200 RSP, buy 200 more, sell 400 on 4/13" stock-only sequence — missing the actual story (5 long $190 calls bought 3/30, expired ITM on 4/10, 500 shares assigned at strike, 400+100 sold 4/13 for ~$3k net profit on the option play). The trade table the AI saw only had stock buys/sells; the option exercise that forced the share movement was invisible. Per-trade `marketContext` now includes an `OPTION-DRIVEN ACTIVITY on <symbol>` block when there are EXERCISED / ASSIGNED / EXPIRED transactions on the underlying within `[earliestEntry-30d, exit+7d]`. Each event surfaces direction (long/short, call/put), contracts, strike, expiration, opening premium per contract, opening date, opening type (BUY_TO_OPEN / SELL_TO_OPEN). System-prompt CRITICAL RULE added with the RSP example so the AI knows to narrate the option play, not the isolated stock buy/sell. Window is wide because LEAPs can sit open for months — the exercise driving the trade may predate the user-visible sell by weeks. Reuses canonical option txn types already produced by Vanguard PDF parser; no schema changes. +3 regression tests (RSP scenario, no-op when no events, formatted-output check). 1471 → 1474.
- ✅ **Trade Review cross-model fallback + debug dumps** — closed 2026-05-05 (`da48f89`). Opus 4.7's emphatic-prompt retry from `f80c5f6` ALSO failed on Vanguard April — model returned empty `review_markdown` twice in a row on that specific input. This is genuinely model-specific, not truncation. Added a third attempt that falls back to the *other* model in the FEATURE_MODELS pair (`tradeReviewMain` ↔ `tradeReviewMainLarge` = Opus ↔ Sonnet 4.6). Different models have different structured-output quirks; when one consistently refuses on a specific input, the alternative usually succeeds. Side-effects: persists actual `modelUsedSpec` into `trade_reviews.model` so the meta footer shows whether Opus or Sonnet produced the review; writes prompt + AI-response previews to `data/trade-review-debug/{ISO}-{accountId}-{periodStart}-{phase}.json` on every empty-markdown path so we can diagnose what's making one model balk (data/ is gitignored). Final error message updated to "across 3 attempts" with concrete next steps if all 3 fail (split period / remove Q&A answers / check for unusual characters in symbol/notes).
- ✅ **Trade Review retry-on-empty-markdown** — closed 2026-05-05 (`f80c5f6`). Vanguard April hit the freshly-shipped pre-save guard with only 12 trades and a 12k token budget — Opus 4.7 in structured-output mode returned an empty `review_markdown` despite plenty of budget. Not strictly truncation; model-side decision. Replaced the "OUTPUT ORDER" prompt addition (was likely making Opus underdeliver) with "REVIEW_MARKDOWN IS THE PRIMARY DELIVERABLE" that frames the field as the required artifact and `trade_grades` as secondary. Added retry-on-empty: if first `generateObject` call returns `review_markdown` <100 chars, retry once with emphatic system-prompt addendum saying "your previous attempt returned EMPTY review_markdown … you MUST produce a complete review (1500+ chars)". Final guard kept for the case where retry also fails — distinguishes "model misbehaving on this specific input" from one-off truncation. 1471/1471 tests still pass.
- ✅ **Trade Review hardening — three independent bugs + column unscramble** — closed 2026-05-04 night (`b7d4d1c`). All surfaced during user's first April 2026 trade-review pass. **(1) INTC period-high stuck at $70.33** (Roth April): `getStockPriceContext` preferred `ohlcv_bars` whenever ≥5 rows existed, never falling through to `prices`. INTC's bars stalled at 4/23 while the daily-snapshot prices extended through 4/29 — so the AI saw period high $70.33 on 4/17 even though the 4/29 close was $94.60 and the trade exited at $87.76. Fix: unified per-date `{high, low}` map merging both tables (bars contribute true high/low; prices fill gaps as close/close), plus always folding entry/exit prices into the range — they're real market-traded prices for the security on those dates. **(2) `NOT NULL constraint failed: trade_reviews.review_markdown`** (Vanguard April): `generateObject` was hitting the output-token cap mid-tool-call on big-month outputs (~35 trades × ~300 tokens each + 2-5k markdown + 1.5k patterns/strengths = >12k into an 11k budget). AI SDK v6 doesn't strictly enforce `required` post-hoc, so the partial object came back with `review_markdown` empty/missing → SQLite NOT NULL surfaced as the symptom. Fix: max_tokens cap 32k→64k, per-trade scaling 200→400 (baseline 8k→12k); pre-save guard throws a clear "AI returned incomplete response" error before save; prompt instructs the model to write `review_markdown` first as additional defense. **(3) Column-naming scramble** (proactive review found this): the writer was mapping AI's `assessment` / `what_worked` / `what_didnt` into columns whose names didn't match — `entry_thesis` stored "assessment", `exit_assessment` stored "what_worked", `what_went_well` stored "what_didnt", `what_went_wrong` was always NULL. The Trade Review tab read/write was internally consistent so labels looked right there, but the Security Detail page read raw column names and rendered "Entry / Exit" labels with mismatched content. Migration 047 adds `assessment` column and moves all 155 existing rows via single UPDATE (RHS uses OLD values for atomic 3-way swap). Writer + readers updated; legacy columns kept for back-compat. Security Detail labels now read **Assessment / Worked / Didn't** with semantically correct content. **(4) UI bugs in TradeReviewView**: API errors read JSON body instead of `res.statusText`; `doGenerate` uses local flags instead of stale closure for questions/complete state; Tailwind dynamic opacity (`${pnlColor}/70`) replaced with literal `text-up/70` / `text-down/70` so JIT picks them up; FIFO tooltip on hold-days chip; expanded card keyed on `saleTransactionId` instead of array index. 1471/1471 tests (was 1461; +10 from 2 new INTC regressions plus ride-along). Live DB migrated; backup at `data/vanguard.db.pre-047-backup-20260504-193035`.
- ✅ **IBKR holdings upsert: statement wins over TWS intra-day** — closed 2026-05-04 evening (`f75176c`). The IBKR-activity parser already extracted holdings (lib/import/parsers/ibkr-activity.ts:380-441) — the bug was actually in the engine's holdings UPSERT, same pattern as the snapshot bug fixed earlier in `33f3c63`. Engine used INSERT OR IGNORE so 18 of 19 IBKR statement holdings were silently dropped because TWS auto-refresh had written intra-day 4/30 positions before the statement import. Engine now uses ON CONFLICT DO UPDATE WHERE holdings.source_key LIKE 'tws-%' (statement source_keys preserved, TWS rows overwritten). Live DB fixed in place: 18 stale TWS-source rows replaced with statement values, 19 closing prices populated. IBKR April now shows $358,640 holdings / $91,124 cash / $449,764 total = matches statement exactly. 3 new regression tests (engine.test.ts 21→24, overall 1466→1469).
- ✅ **Canonical-CSV parser hardening — comma-bearing numerics rejected** — closed 2026-05-04 (`ff6059f`). Pre-flighting an April-statement import surfaced that `parseFloat("1,234.56")` returns `1` (silent truncation), not `NaN` — meaning any comma-grouped amount Co-Work emitted would silently corrupt the imported value. New `parseStrictNumber()` helper in `lib/import/parsers/canonical-csv.ts` returns NaN for any comma-bearing string so existing isNaN guards + validate.ts skip-with-warning take over instead. All 17 numeric parses in the canonical-csv parser now route through it. `app/dashboard/components/CanonicalCsvGuide.tsx` also got an explicit "no comma thousands separators" rule in both per-format constraints and the all-formats general-rules block (the prompt body the user copies into Co-Work). 2 new regression tests (transactions: NaN amount → caught by validate.ts; holdings: NaN quantity → row skipped by parser). 187/187 import tests pass.
- ✅ **Trade Review month dropdown overstates trade count** — closed 2026-05-11 (`8af5317` + `729137a` follow-up). `getAvailableReviewPeriods` now wraps the COUNT in a per-sale coverage CTE joining `tax_lot_sales.quantity_sold` against `transactions.quantity` with the same `MIN_LOT_COVERAGE = 0.9` threshold the runtime filter uses (exported from the same file to prevent drift). `ReviewPeriod` gained `reviewableCount`; TradeReviewView's dropdown renders "April · 9 of 12 reviewable" when there's a gap, falls back to "12 trades" on full coverage. Sibling `detectNewTradeReviewPeriods` got the same SQL pattern in the follow-up commit (type consistency + same UX framing from the import-hook path). +2 regression tests covering partial coverage + null actual_qty defensive default.
- **Drop legacy trade_roundtrip columns** (new 2026-05-04 night, deferred from `b7d4d1c`) — Migration 047 added `assessment` and moved data into semantically correct columns, but kept `entry_thesis` and `exit_assessment` for backward-compat reads on rows written before migration. After ~3 months of runtime confirming no readers regress (2026-08+), schedule a `chore` migration to DROP these two columns and remove the legacy fallback branches in `app/api/trade-review/route.ts` (lines ~84-93) and `app/dashboard/security/[id]/page.tsx` (the `resolveAssessment` etc helpers). The detect-by-pragma branch in `lib/mutations/trade-reviews.ts` can also drop. ~20 min.
- ✅ **Investigate Vanguard Taxable 114-position count on 2026-03-27** — audited 2026-05-11. Snapshots are 111 (Mar 27) → 123 (Apr 30); the 12-position delta decomposes to 20 legit new options + 5 sells/short-closes + ~4 stale-row leftovers + ~7 ghost positions. **Not an engine bug.** The import engine's UPSERT on `(account_id, security_id, as_of_date)` is correct; supersession isn't relevant because as_of_dates don't overlap between statements. **Real root cause: Co-Work output uses inconsistent symbol conventions across statements** — `BRK B` (space) in March's holdings CSV vs `BRK/B` (slash) in April's, and US Treasury bonds stored by NAME (`U S TREASURY BOND 3 2/15/48`) in March but by CUSIP (`912810SA7`) in April. Each variant creates a distinct `securities.id` row, so the same instrument double-counts across snapshot dates. Three confirmed pairs in the live DB: BRK B (1892) ↔ BRK/B (1996), 1927 ↔ 2079 (3% '48), 1851 ↔ 2080 (4.75% '55). Wrote `scripts/merge-duplicate-securities.ts` (dry-run by default, `--apply` to commit) that repoints every FK reference from old→new and deletes the orphan row. **Open follow-up**: harden the Co-Work prompt in `app/dashboard/components/CanonicalCsvGuide.tsx` to require stable symbol convention across statements (prefer CUSIP for bonds, slash form for dual-class equities). Without the prompt fix new duplicates accrue with every monthly statement.
- ✅ **Harden Co-Work prompt: stable symbol convention across statements** — closed 2026-05-11 (`a881abb`). Added Symbol Conventions rules to all three Co-Work prompt locations per the "three places must stay in sync" rule: `transactions` constraints array in CanonicalCsvGuide.tsx, `holdings` constraints array, the General-rules block in `buildAllFormatsPrompt`, plus `docs/canonical-csv-guide.md` General Rules section. Bonds (incl. US Treasuries) → 9-character CUSIP; dual-class equities → slash form (`BRK/B`, `LBRD/A`, `LSXM/K`). Each future monthly statement now anchored to one symbol per instrument; the dedup script (`scripts/merge-duplicate-securities.ts`) remains as the recovery path if drift ever recurs. All 193 import tests still pass.
- **Verify new email design renders cleanly in real Outlook + Gmail clients** (added 2026-05-03 with `b2201cc`) — the design was tested in Safari only. After the next DMG rebuild, the first weekly briefing in the new theme ships to the user's brother (Outlook) and any Gmail recipients. Spot-check both: confirm font fallback to Segoe UI (Outlook) / SF Pro (Apple Mail) / Roboto (Gmail Android) reads cleanly, no broken styles, scoreboard tables still print white-on-cream as intended, no Outlook-specific layout regressions (e.g., 680px wrapper rendering at full width, inline `border-radius` rounded corners may degrade to square — that's expected and acceptable). Action: ask brother + check own Gmail web after first send. If anything broken, fix before locking in.
- **Watch Llama 4 Scout on `alertSuggestion` for 48h** (added 2026-05-08, commit `d252985`) — swapped `workers-ai/@cf/meta/llama-3.3-70b-instruct-fp8-fast` → `workers-ai/@cf/meta/llama-4-scout-17b-16e-instruct`. Cloudflare AI Gateway dashboard filtered by `feature=alertSuggestion` shows cost / latency / quality. Roll back to Llama 3.3 (one-line revert in `lib/ai/models.ts:60`) if quality drops, latency >5s, or any empty responses. Check Saturday + Sunday next week.
- **DMARC escalation → `p=reject`** (`p=quarantine` APPLIED + verified live 2026-06-01; next step is `p=reject` after ~1-2 weeks clean — re-decode reports first). Original investigation (2026-05-29): — investigated the aggregate reports (IMAP-fetched + gunzipped the actual XML, since the Gmail MCP can't decode attachments). **Findings:** legit mail (Amazon SES `54.240.9.x`/`10.x`, which Resend sends through) is **100% DKIM+SPF aligned**; but docomo.ne.jp + heteml.jp reports show an **active, growing spoofing campaign** (3→7→11 forged `From: myportfoliodesk.com`/day across 5/26–5/29, all auth-fail, botnet IPs). With `p=none` those forgeries are delivered. **Action (user must apply at Cloudflare DNS — I have no DNS access):** set `_dmarc.myportfoliodesk.com` TXT to `v=DMARC1; p=quarantine; rua=mailto:dmarc-reports@myportfoliodesk.com; pct=100; aspf=r; adkim=r` — zero risk to legit mail. Then `p=reject` after ~1-2 weeks clean. As of 2026-05-29 NOT yet applied. Re-decode runbook + detail: `memory/project_dmarc_spoofing.md`.
- **Observe first Mon-Thu evening cycle on Mac primary** (added 2026-05-08, follow-up to evening-email ship) — after this session's DMG rebuild, the next Mon-Thu 19:00 ET launchd cron should hit the rebuilt `/api/cron/evening` route on port 3099 (Electron app's bundled server). Tail `~/Library/Logs/vanguard-evening-email.log` after the firing to confirm primary path succeeds. If primary fails, Worker cloud fallback (`vanguard-skin-cron.isaac-3d1.workers.dev`) takes over automatically — but log diagnosis is owed to confirm the launchd → port-3099 → DMG-rebuilt-server chain works end-to-end on real schedule.
- ✅ **Worker fallback synthesis validation parity** — closed 2026-05-11 (`48fd4a0`). Ported Mac's 4 strict checks (truncation guard via `finishReason === "length"`, inline `stripModelPreamble` mirror, leading-`#` header check, ≥200 char min) into `workers/cron/src/fallback-evening.ts::synthesizeViaAI`. On any failure returns null → caller falls back to per-source. +3 tests in `workers/cron/test/fallback-evening.test.ts` covering each rejection path; default mock updated to ≥200 chars + leading `#` to satisfy the new validators.
- **Gmail filter for "forwarded via Cloudflare" mail** (added 2026-05-08, surfaced from Step 2 manual smoke) — test reply forwarded by Cloudflare Email Routing landed in Gmail All Mail, not Inbox. Real reply traffic from human senders may behave differently (less spammy-looking), but if All Mail placement persists, set up a Gmail filter "from `noreply@notify.cloudflare.com` OR forwarded-via-cloudflare → Skip Inbox: NO; mark important". Defer until first real reply traffic — don't preemptively over-tune filters on synthetic test data.
- ✅ **Extract `buildLatestHoldingsCTE` helper** — closed 2026-05-11 (`48fd4a0`). Discovery during research: `latestHoldingsPredicate` in `lib/queries/latest-holdings.ts` was ALREADY the canonical shared helper (8+ Analysis Deep Dive consumers used it). Real work was migrating 4 still-inline copies: `lib/queries/analysis.ts:68-77` `LATEST_HOLDINGS_CTE` (kept the wrapping const but body now uses the helper), `lib/queries/security-detail.ts:186` `getHoldingsBySecurity`, `lib/compute/scenarios.ts:93`, `lib/compute/cost-basis-reconciliation.ts:90`. `lib/compute/daily-valuation.ts:77` kept inline with a load-bearing comment — its prepared-statement-in-hot-loop pattern needs positional `?` for `as_of_date <= ?`, incompatible with the helper's literal substitution. CLAUDE.md updated.
- ✅ **PerformanceView at 553 lines — extract `<PeriodAttributionSection>`** — closed 2026-05-11 (`48fd4a0`). New `app/dashboard/components/PeriodAttributionSection.tsx` accepts `{ attribution, benchmarkSymbol }` and renders the 4 attribution sub-blocks (contributors / detractors / sector contribution / beta-vs-alpha). PerformanceView is now 411 lines.
- ✅ **Worktree-aware `DATABASE_PATH` env support** — closed 2026-05-11 (`48fd4a0`). Turned out to already be implemented in `lib/db.ts:9` (`process.env.DATABASE_PATH || …`) with the same use-case comment I'd have written. Only `.env.local.example` documentation was missing — added both `DATABASE_PATH=` and `VANGUARD_DB_DIR=` entries with worktree audit-script use case.
- ✅ **Drawer `PerformanceContent` — surface divergence numbers** — closed 2026-05-11 (`48fd4a0`). `AnalysisTrustState` gains `perAccountReconciliation: PerAccountReconciliation[]` with `{accountId, accountName, latestStmtMonth, divergenceBp, withinTolerance}` per scoped account. `TrustStripDrawer.PerformanceContent` renders a styled `<ul>` with one row per account: colored dot (green within tolerance / red out / gray unresolvable) + stmt month + `+Xbp` or `-Xbp` delta. Mac-side `getAnalysisTrustState` loop in `lib/queries/analysis-trust-state.ts:114-167` collects all per-account results instead of breaking on first failure; `performanceReconciledThru` rollup unchanged in semantics. +1 regression test.
- **Watch `period-attribution.ts` long-only behavior** (new 2026-05-10, P1 reviewer flag) — `computePerPositionContributions` uses `hs.quantity > 0` (not `!= 0`) — silently excludes short positions from sector contribution analysis. The comment justifies this as "long-only by design" but as the user accumulates more short option / short stock positions, the attribution gap could mislead. Re-evaluate when short positions exceed ~5% of NAV.
- ✅ **Vanguard PDF imports leave expired options in `holdings`** — closed 2026-05-12 (`21851e2`). Same commit added a post-commit purge step in `commitImport` that runs BOTH `purgeExpiredOptionHoldings(db)` AND `purgeMaturedBondHoldings(db)` for holdings-snapshot sources only (`vanguard-pdf`, `vanguard-export`, `vanguard-holdings`, `ibkr-holdings`, `ibkr-activity`). Transaction-only sources skip the sweep. Each purge runs in its own try/catch so a failed sweep cannot mask a successful import. +3 regression tests in `tests/import/engine.test.ts` covering Vanguard PDF expired-option purge, ibkr-activity matured-bond purge, and monthly-values no-op.
- ✅ **Outbound emails — strip exact position dollar amounts** — closed 2026-05-12 (`3d8c73f`). New `lib/digest/presence-only-position.ts` exports `formatPositionPresence` + `formatCombinedExposurePresence` — ownership-disclosure strings with no cost basis / market value / notional dollar exposure. Relative-% returns kept for long positions (P&L direction is fine; what stays hidden is underlying dollar size); shorts omit % because cost-basis sign convention varies across import paths. Strike + expiry on options stay visible — public market data. Earnings email `formatPositionLine` and `formatPositionSummary` rewritten to use the helpers; `PreviewContext` gains `longShares/shortShares/longContracts/shortContracts` (signed totals retired). `formatCombinedPosition` in briefing also stripped its `~$X mkt val` tail. Briefing prompt §7 rewritten: qualitative or %-of-holdings sizing only; explicitly bans multiplying share count × close to derive notional dollars. Decision on "you're up 3% on this": keep — relative-return reveals no $ exposure. +443 lines of regression tests across `presence-only-position.test.ts` + `earnings-prompt-no-dollar-leak.test.ts` + extensions to `briefing-combined-positions.test.ts`.
- ✅ **Sunday briefing using stale (Thursday/Friday) close prices despite TWS connected** — closed 2026-05-12 (`e4ad876`). `sendBriefingEmail` now runs `runAutoRefresh(db, "quick")` after the positions sync — snapshot prices + valuations recompute (~2-3 min). Best-effort: failures log but do not block the briefing. Adds a one-row freshness probe that logs `[send-briefing] Latest price as_of_date: YYYY-MM-DD (Nd old)` to launchd logs so future stale-briefing investigations start with a breadcrumb instead of from scratch. Yahoo fallback path NOT implemented — primary need was the missing refresh call; Worker cron fallback already handles "Mac offline at send time" via its own cloud path. +232 lines of regression tests.
- ✅ **5/12 morning briefing incorrectly framed APP as "only mentioned indirectly by TMT Breakout"** — closed 2026-05-12 (`221edc1`). Diagnosis: the synthesis system prompt let Sonnet confabulate coverage-depth labels from bucket summaries that don't actually convey "lead topic vs. one of many tickers." New system prompt rule block in `lib/digest/synthesize.ts`: (a) **COVERAGE-CHARACTERIZATION RULES** explicitly forbid "only mentioned indirectly / in passing / tangentially / without focus / briefly noted / appeared only as a footnote / not the focus of any source" + require narrating WHAT the source said (from the supplied summary) instead of HOW prominently; (b) **HELD-TICKER PRIORITIZATION** requires every held ticker with ANY coverage in today's buckets to get its own `##` section — held names cannot be relegated to "## Also covered" even on single-article coverage. +2 regression tests pin the rule text so future contributors cannot silently drop the guardrail.

### Earnings emails

- ✅ **Earnings preview/recap "B-grade audit" close-out** — shipped 2026-05-05 (`921d552`). Three parallel audits (DB state, composer code, UI surfaces) found 13 distinct gaps; fixed in one commit. **Composer correctness:** `callClaude()` was hardcoded `model: SONNET_MODEL` despite computing `featureKey` for AI Gateway tagging — model swaps in `FEATURE_MODELS` had zero effect; now routes through `resolveFeatureModel(featureKey)`. `renderHeadlineTable` consensus precedence was asymmetric (cons read `consensus_estimate`; actual fallback read `consensus_value`) — Δ math was anchored on different baselines; both columns now read `consensus_value ?? consensus_estimate`. `getCrossAccountPositions` MAX(as_of_date) per (account, security) silently dropped cost_basis whenever a TWS row existed on a later date than the most recent statement (TWS rows have `cost_basis=NULL`); the AI saw "cost basis ?" and hallucinated "around your cost basis" for PRIM (user up 115%) — fixed via `COALESCE(h.cost_basis, latest non-null statement row)`. `findEmailCandidates` recap gate now requires `actual_value IS NOT NULL` + `sendEarningsEmail` defensive guard (409 if manual route bypasses cron) — recaps used to fire on `enriched_at` alone and the AI filled actuals from consensus or `web_search`. **Output discipline:** new `SYSTEM_PROMPT` in callClaude tells the model "first character must be `#`, no preamble, no narration, no closing commentary" + new `stripModelPreamble()` post-processor as a failsafe — eliminates the "Good, now I have enough to construct the brief" leak. Em-dash continuations after a blank line now merge into the previous `<p>` in `briefingToHtml::convertMarkdown` — fragmented sentences read whole. **Site-wide guard:** `isPlausibleEarnings` extended from read-throughs only to scoreboard — flagged actuals (e.g. >1.7× EPS deviation) blank out and a `⚠ Reported actuals were flagged as implausible` italic sub-line surfaces beneath the table. **Layout:** chat right-rail collapsible at xl+ via `vgs:chatRail` localStorage + anti-FOUC `data-chat-rail` attribute on `<html>` + `--chat-rail-width` CSS variable; chevron at top-right of rail collapses, header `ChatToggleButton` becomes visible (CSS attribute selector) to re-expand; Cmd+J expands when collapsed. EarningsHub auto-yields to mobile cards when chat rail is open and viewport <1536px (the 750px-content column-collapse case at 1280px) — `earnings-hub-desktop`/`earnings-hub-mobile` classes + `[data-chat-rail="open"]` media query. **Data hygiene:** `fetchFinnhubEarningsForSymbols` now uses the queried symbol as canonical (was trusting Finnhub's response symbol, which returns "GFL.TO" for held US-listed GFL → never surfaced as held); raw Finnhub symbol preserved in `raw_json.finnhub_symbol`. Live DB cleaned: 2 `GFL.TO` calendar events migrated to `GFL`. 13 of 15 NULL `reaction_snapshot` recaps backfilled via `scripts/backfill-reaction-from-yahoo.ts` (ALAB + PRIM 5/5 AMC failed — Yahoo bars not yet available at backfill time, retry next session). **Polish:** Privacy toggle label state-consistent (`aria-label="Privacy mode"` + `aria-pressed`) instead of action-on-state ("Hide amounts"). BogeysEditModal renders explicit "None yet" empty state when no manual bogeys exist; revenue placeholder no longer overflows mid-number ("$3.85B or 3.85B" instead of "$3.85B / 3.85B / 3,850,000,000"). Pre-release events with no consensus render `Consensus not yet published` italic across a 4-col span instead of a row of bare em-dashes (XMTR case). **Cleanup:** deleted `app/dashboard/components/CalendarView.tsx` (dead code, only self-references — `/dashboard/calendar` redirects to `/today?view=week-ahead` which uses a different component). Tests 1474/1474; build clean.
- **Earnings audit follow-ups** — (1) ✅ ALAB + PRIM 5/5 AMC `reaction_snapshot` backfill — closed 2026-05-05 night, Yahoo bars now populated, both captured (-8.10%, -26.41%, both `source: yahoo`). (2) **Open:** watch the next 4-5 days of preview/recap emails to confirm the system-prompt + `stripModelPreamble` actually eliminate the self-talk leakage in production (only verified via tests + system-prompt logic). (3) ✅ Fresh calendar sync to populate PRTO + RDDT — closed 2026-05-05 night, sync ran 64 symbols (60 held + 4 reporters via the merge in `lib/calendar/sync.ts:167-173`). PRTO has no Finnhub data at all (free tier); RDDT/HUN reported 4/30 outside the [5/4, 5/10] window. Real-world data state, not a wiring bug. Read-throughs verified end-to-end via synthetic `buildReadThroughEntries`+`renderReadThroughsBlock` test against APP/META/GOOGL/LIN/XMTR — APP correctly composes META + GOOGL bullets with cons/actual/beat/reaction. (4) ✅ `isPlausibleEarnings` extension to `EarningsHub.tsx` — closed 2026-05-05 night (`8d3380c`). New `actualsAreImplausible` helper; desktop ⚠ glyph + tooltip in delta cell; mobile italic warning replaces Act line. 5 unit tests.

- ✅ **Earnings transcripts source replacement** — CLOSED 2026-06-09 evening. Research verdict: Alpha Vantage `EARNINGS_CALL_TRANSCRIPT` is FREE (25 req/day; doc: `docs/plans/2026-06-09-transcripts-source-scoping.md`). Wired as chain step 2 (cache → API Ninjas → AV → EDGAR), Motley Fool scraper retired from the chain, `ALPHA_VANTAGE_API_KEY` threaded through the 4 Electron touchpoints + SettingsModal + dev-mode whitelist. USER ACTION: sign up for a free key + spot-check post-call latency. *(original note below)* (new 2026-05-05 night) — API Ninjas premium-gated (silent fail without paid key), Motley Fool scraper brittle to fool.com HTML changes, EDGAR 8-K fallback now quarter-match-guarded but limited to a press-release excerpt (not a full call transcript). Today's session fixed the EDGAR data-correctness bug but the underlying source-quality story remains. Scope a session to evaluate alternatives: paid API Ninjas Business tier ($99/mo), Polygon transcripts add-on, IEX Cloud, AlphaVantage. Decision needs cost/coverage trade-off review.
- ✅ **Per-ticker BMO/AMC time overrides** — closed 2026-05-03. New `SYMBOL_RELEASE_TIMES_ET` constant in `lib/calendar/release-times.ts` (AAPL=16:30, AMZN=16:01, GOOGL/GOOG=16:01, META=16:05, MSFT=16:05). `earningsHourToReleaseTime(hour, symbol?)` and `resolveReleaseTime({ symbol })` consult the map first, then fall through to BMO/AMC defaults. `lib/mutations/calendar.ts::deriveReleaseTime(eventTime, symbol)` extended for the manual-insert path too. Worker mirror NOT needed: `workers/cron/src/fallback-earnings.ts` only reads `release_time` from snapshot, never writes — Mac-side fix propagates via the nightly R2 snapshot. 9 new tests. 5 future cohort rows backfilled in live DB via `scripts/backfill-symbol-release-times.ts` (idempotent).
- ✅ **Per-event preview/recap one-off skip** — closed 2026-05-03. Migration 045 + new `earnings_email_skips` table (mirrors the `earnings_emails` audit-row dedup pattern). New `lib/mutations/earnings-skips.ts` + `lib/queries/earnings-skips.ts`. `findEmailCandidates` extended with parallel `LEFT JOIN earnings_email_skips … es.id IS NULL` for both preview + recap. New `app/api/earnings/skip/route.ts` (POST/GET/DELETE, in-app pattern, no cron auth). `EarningsRowChips.tsx` rewritten — pending chips reveal a red-tinted "skip" button on hover; skipped chips render with strikethrough + un-skip on click. 9 new tests (6 mutation + 3 findEmailCandidates filter). API smoke verified end-to-end. Visual hover-reveal verification deferred to next session (Playwright MCP install).
- ✅ **Morning digest synthesis — adaptive synthesis-vs-per-source layout** — closed 2026-05-08 (`c97d60b`, `6dc1fab`, `f5fe618`). New `lib/digest/synthesize.ts` runs Sonnet 4.6 cross-source synthesis via Cloudflare AI Gateway (new feature key `dailyDigestSynthesis`); `generateDigestSinceAdaptive` in `lib/digest/daily-digest.ts` selects synthesis ≥5 articles (with per-source tail below for traceability) or per-source <5 articles. Strict-mode validation throws `SynthesisEmptyError` on length-truncation / preamble-leak / no-markdown-headers / <200 char output → adaptive caller catches both `SynthesisEmptyError` AND generic Error (network/rate-limit) and falls back to per-source. Observability via `synthesis_fallbacks_last_30d` settings ring buffer (30-entry telemetry). Cost: ~$0.05/day for morning digest when ≥5 articles.
- ✅ **Digest sort toggle — by publication vs by company** — closed 2026-05-08 via the synthesis ship above. Synthesis IS the by-company layout (`bucketByCompany` groups articles by ticker / company / topic before the Sonnet pass). Email auto-picks: ≥5 articles → synthesis (by-company), <5 → per-source. In-app `<DigestEmailViewer>` toggle preserves both layouts on demand (existing pattern). The "Settings preference" delivery option from the original entry was rejected in favor of the adaptive auto-pick.

### Page-level rework

- ✅ **Analysis Deep Dive — P1 Trust Layer** — shipped 2026-05-10 (`b4483a4`, `7157753`, `8e163f5`, `3937daf`, `64b73e9`, `ca0a6ce`, merge fast-forwarded into `main` then pushed `e0f3bad..ca0a6ce`). 6 commits, +29 tests (1766 → 1795), worktree-driven subagent-flow execution. Four trust gaps closed: (1) **Options Greeks card** was dropping 52 of 55 option positions due to global-max `as_of_date` subquery — fixed with per-(account, security) CTE + `quantity != 0` (short positions surface) + diagnostic surface showing why specific positions can't compute Greeks (no underlying price / expired / IV solver / no option price). (2) **Options Expirations card** decoupled from Greeks compute via new `lib/compute/options-expirations.ts` + `/api/compute/options-expirations` — renders even when underlying prices are missing. (3) **Trust Strip** at top of Analysis page with 5 clickable cells (factor coverage % / last classify / performance reconciled-thru / stale prices / bond duration coverage); drawer with details + fix actions (classify-missing → POST `/api/compute/classify-factors`, refresh prices → POST `/api/tws/auto-refresh` quick). Privacy-mask wraps portfolio-derived counts. (4) **TWR reconciliation** via `lib/compute/twr-reconcile.ts` + `scripts/audit-twr-vs-statements.ts` pre-merge gate — compares computed per-month TWR to `monthly_snapshots.twr`; gate PASSED at 99.2% within 5bp (single outlier is the documented Dec-2022 annual-summary case). Critical insight discovered mid-implementation: `computeTwr(db, { endDate })` returns CUMULATIVE TWR but `monthly_snapshots.twr` is PER-MONTH — fix is `startDate === endDate === periodEnd`. (5) **Performance page upgrade** — was bare-bones TWR/MWR percent returns; now adds reconciliation strip, 4-metric headline (TWR / MWR / Max Drawdown / Sharpe), equity curve with benchmark overlay (`PerformanceCurveChart` named export in `EquityCurveChart.tsx`), and period attribution section (top contributors / detractors / sector contribution / beta-vs-alpha via local OLS regression in new `lib/compute/period-attribution.ts`). Spec: `docs/superpowers/specs/2026-05-10-analysis-deep-dive-design.md` (574 lines, all 13 dials in scope). Plan: `docs/superpowers/plans/2026-05-10-analysis-deep-dive-p1-trust-plan.md` (1727 lines, 5 vertical slices). P2/P3/P4 plans get written when each phase begins.
- ✅ **Analysis Deep Dive — P2 Construction Mode** — shipped 2026-05-10 via PR #20 (rebase-merge `8a98aa4..96ae7f0` on main, 6 commits incl polish). Slices A-E: AnalysisView refactor into MetricCard / ClassificationCard / FactorModeCard, What-if engine (`lib/compute/exposure-delta.ts` + `/api/analysis/what-if` + `WhatIfCalculator.tsx`), Cash-Deploy solver (`lib/compute/cash-deploy.ts` + `/api/analysis/cash-deploy` + `CashDeployCard.tsx`) seeded with VTI/QQQ/SPY/DIA sector compositions, 8 factor-anchored scenario recipes (`lib/compute/scenario-recipes.ts` w/ `FACTOR_SHOCK_SENSITIVITIES`) replacing 9 arbitrary presets, Workspace becomes default landing (legacy reachable via `?mode=classification|factors`). Polish commit `96ae7f0` extended `WhatIfCalculator.tsx::DeltaTable` to render sectioned rows for Top-3 concentrations + sector weights + factor tilts (engine returned the data; UI under-rendered) with 0.5pp materiality filter, and suppressed misleading "Sector Unknown 100% (cap 30%)" cap-flag in `computeFlags`. Migrations 049 (`construction_caps`) + 050 (`benchmark_compositions`) — note: spec said 049=benchmark; actual swap means **P3/P4 must renumber to 051-053**. +40 net tests (1798 → 1838). 3 pre-existing `send-evening` failures unchanged.
- ✅ **Analysis Deep Dive — P3 Narrative Layer** — shipped 2026-05-10/11 on `claude/analysis-deep-dive-phase-3` (22 commits + 1 doc-reconcile, rebase merge target). 4 slices, all live: **Slice A** factor narratives end-to-end (Sonnet 4.6 via AI Gateway, cached in migration 052 `analysis_narratives`, pre-generated by Sunday briefing pipeline, on-demand fallback via `/api/analysis/narrative`, rendered as `<NarrativeBlock>` above FactorAnalysis / RiskMetrics / PositionRisk / FactorHeatmap). **Slice B** per-security factor lens on Security Detail (`<FactorProfileSection>` showing qualitative chips + quantitative β/vol/R²/corr from new `computeSecurityRegression` engine cached in migration 051 `security_regressions`, plus deferred portfolio-share contribution placeholder; cache backfill script + Sunday auto-refresh hook). **Slice C** generic `<DrillDownPanel>` (slide-in 480px desktop / full-screen mobile, sortable holdings table, ESC + backdrop dismiss) wired to 4 trigger surfaces: classification pie/legend in AnalysisView, factor heatmap chip click via FactorModeCard, sector tilt bucket in FactorAnalysis, "View top 10 by risk →" button in PositionRisk. **Slice D** week-over-week deltas via `latestHoldingsPredicate({asOfDate})` (the prior `cutoff: true` mode collapsed and migrated), `computeFactorAnalysis` + `computeRiskMetrics` gain optional `asOfDate?: string`, API routes return `{data, weekAgo, delta}`, tiny `<WeekOverWeekBadge>` renders ↑/↓ magnitude / 7d next to each numeric metric. Net +75 tests (1838 → 1913). Migrations 051 + 052; new feature key `analysisFactorNarrative`. Side benefits: 3 more inline-CTE instances retired via the asOfDate refactor (per-(account, security) keyBy is now the universal pattern). PositionRisk per-row deltas deferred for a follow-up since the panel ships a "top-N" gesture instead. Block 3 of FactorProfileSection (portfolio-share contribution) deferred — needs scope-aware factor totals + delta math; placeholder + TODO in-code.
- ✅ **Analysis Deep Dive — P4 Macro Overlay + polish (closes deep dive)** — shipped 2026-05-11 via merge `e774b5e` on `main` (subagent-driven 17-task flow on `claude/analysis-deep-dive-phase-4`; 23 commits including doc-polish + 2 test-brittleness fixes; pushed origin `555bb92..8271475`). Five slices, all live: **Slice A** macro-themes pipeline (migration 053 `analysis_macro_themes`, cache helpers, feature key `analysisMacroThemes` → Sonnet 4.6 via AI Gateway, Zod-validated 3-5 theme array, `buildMacroSignalBlob` over 7d research + enriched events + alerts, `generateMacroThemes` cache-first composer with under-threshold short-circuit + code-fence trim + dollar-leak post-filter, `/api/analysis/macro-themes` GET cache-first + POST force-regen rate-limited 1/day/scope, Sunday-briefing pre-gen hook `Promise.allSettled` over 4 scopes). **Slice B** `<MacroOverlayCard>` + `<MacroThemeReceiptDrawer>` replace P2 placeholder in Workspace; SymbolLink requires `securityId` so alert symbols render as plain text, and `articleId` URL filter is absent in Research route so articles link to `/dashboard/research?view=feeds` with title in tooltip (both documented inline). **Slice C** `renderMacroThemesMarkdown` pure renderer + cache read in `lib/calendar/briefing.ts` (NOT `send-briefing.ts` — implementer corrected the plan's file assumption; one cache, four consumers all using `mondayOf(weekOf)` for the cache key). **Slice D** cross-cuts: `applyThemeAwareBoost` in Cash-Deploy (1.15× for defensive sectors during risk-off net themes, aggressive sectors during risk-on; multiplicative on `|gapPp|`; wired into `suggestAllocation` sort via new `gapClosureScore` field), `matchScenariosToThemes` decorates `ScenarioRecipe` with `liveNowReason` matching `primaryFactor → factor_label`, UI renders amber "live now" pill in `ScenarioModeling.tsx`. **Slice E** 3 P3 follow-ups closed (60s GET cache-miss rate-limit on `/api/analysis/narrative`, `<PositionRisk>` per-row W-o-W badges via extracted `computeWeekOverWeekDelta` pure helper, bond `maturity_date` regex via `upsertSecurity` integration reusing existing `lib/bonds.ts::extractMaturityDate`). Two test-brittleness fixes landed on `main` post-merge: `f39aebb` mocks `generateMacroThemes` in macro-themes POST rate-limit test, `8271475` `vi.mock`s `ai` package's `generateText` in narrative `forceRegen` test — both were timing out / passing-by-accident when `ANTHROPIC_API_KEY` was loaded. New AI cost ~$0.85/month. Net +45 tests against the post-merge `.env.local`-loaded baseline (1912 → 1959 pass on `main`; 3 remaining `send-evening` failures unchanged, separate cleanup track). Migration sequence is now 048 (security_betas) → 049 (construction_caps) → 050 (benchmark_compositions) → 051 (security_regressions) → 052 (analysis_narratives) → 053 (analysis_macro_themes). Three deferred items accepted at plan time: (a) `computeFactorAnalysis` lacks `tilts[]` field so `top_contributors` degrades to `[]` and `exposure_bucket` reads "low" uniformly (defensive `(as any)` cast + eslint-disable comment in `macro-themes.ts`), (b) `SymbolLink` requires securityId, (c) `articleId` URL filter absent. Plan: `docs/superpowers/plans/2026-05-11-analysis-deep-dive-p4-macro-plan.md` (2,455 lines, 99KB). **Full Analysis Deep Dive (P1+P2+P3+P4) complete and on main.**
- ✅ **Notes area rework — expanded context** — CLOSED 2026-06-09 evening (no migration): `trade_thesis` re-presented app-wide as **Stock Notes** (position notes, thesis updates, watch-rationale jots); journal placeholder explicitly market/trading psychology; Security Detail gains `+ Add note` prefilled via `?type=trade_thesis&symbol=` (browser-verified). NotesAmbient routing question still parked. Original: journal should be reserved for **market & trading psychology** (how I feel about the market, how I'm trading), with a separate destination for **non-earnings stock-specific thoughts** (position notes, thesis updates, "why I'm watching this" jots). Earnings stays earnings. Likely a new note type (`stock_thought`?) or per-security notes panel. Open question parked from Phase 4: should NotesAmbient's "Save to Notes" route the user to a full notes page for the just-saved entry?

### New features

- ✅ **Evening email + cross-source synthesis + deliverability + recipients UI** — closed 2026-05-08 (24-commit branch `feature/evening-email`, merge `84da8f4`). Mon-Thu 7pm ET / Fri 5:30pm ET evening email that wraps the post-morning-digest newsletter flow plus a 2× beta Vanguard anomaly flag for unusual moves (no $ / shares / portfolio % language for privacy — brother is on cc). 6-phase ship: (1) `security_betas` cache (migration 048) + nightly refresh + `lib/digest/anomalies.ts` (1% absolute floor, top-5 cap by ratio, `directionFlipped` semantic). (2) Cross-source synthesis composer + adaptive layout (see "Morning digest synthesis" close above). (3) Deliverability hardening (see "Eli email landed in junk" close). (4) Mac primary path: `lib/digest/send-evening.ts` + `app/api/cron/evening/route.ts` + launchd plist (Mon-Thu 19:00, Fri 17:30). (5) Worker cloud fallback: R2 schemaVersion 3 with cached betas + Vanguard symbols + per-email recipients, `workers/cron/src/fallback-evening.ts` mirrors Mac composer using snapshot data + Yahoo last-2-closes. (6) Per-email recipients UI: `lib/queries/email-recipients.ts` + `EmailRecipientsSection` in SettingsModal + `/api/settings/email-recipients` GET/PATCH (3 keys: briefing/digest/evening). Tests 1520 → 1766 (+246 new, all passing). Worker cron consolidated to a single `*/15` trigger (`9ad4573`) because the 9-trigger registration hit Cloudflare's 5-cron free-plan ceiling — JS-side `parseJobFromClock` dispatches by ET wall-clock. Spec: `docs/superpowers/specs/2026-05-08-evening-email-and-synthesis-design.md`. Plan: `docs/superpowers/plans/2026-05-08-evening-email-and-synthesis-plan.md`. Manual steps complete: DMARC publish, CF Email Routing for replies@ + dmarc-reports@, launchd load, wrangler deploy, smoke tests against both Worker fallback (`htmlLength: 19222`) and Mac primary (`success: true, sentTo: isaac@wolfsonfamily.com`).
- ✅ **Stock-to-stock read-throughs — preview composer integration** — closed 2026-05-03 (`9086167`). Both prereqs now satisfied: (a) `reaction_snapshot` enrichment gap closed earlier this session (`55975af` + Worker deploy), (b) Worker `wrangler deploy` ran today shipping the `&includePrePost=true` fix to cloud-fallback. Built `getReadThroughReporterSymbols(db)` and `getReadThroughsForTargets(db, targets)` in `lib/queries/read-through-pairs.ts`. `lib/calendar/sync.ts` now merges read-through reporters with held stocks before the Finnhub sweep so non-held names (PRTO, RDDT) reach `calendar_events` automatically. Composer's `renderPreviewPrompt` injects an `## Read-throughs from this earnings season` block between newsletters and analyst sections (per design doc §4): one bullet per matched reporter with consensus, actual + beat math (EPS as $ delta, Rev as %), reaction snapshot (stock + SPY + QQQ T+2h), and the pair's hypothesis. Sorted weight-desc, deduped per reporter, 14-day lookback. End-to-end verified for APP 2026-05-06 preview: META + GOOGL surface as cluster reporters with their actual prints + reactions. Defensive `isPlausibleEarnings` guard skips reporters whose Finnhub actual is implausibly far from consensus (e.g. GOOGL bogus 5.11 EPS vs 2.70 consensus). +13 tests. Design doc: `docs/plans/2026-05-02-stock-to-stock-read-throughs.md`. Recap-side rendering deferred to next session (see open below).
- ✅ **Read-throughs — recap-side rendering** — closed 2026-05-11 (`4b5acf3`). Sibling slot mirrors the preview-side block from `9086167`: `renderRecapPrompt` now injects `## Read-throughs from this earnings season` between newsletters and analyst sections. Anchors the print in cluster context the reader already saw at preview time ("the META/GOOGL prints we framed for you earlier reported $X · reaction Y%"). Block returns "" when `readThroughs` empty so existing recap output for symbols with no cluster mappings is unchanged. +5 prompt-integration regressions including positional sanity (block sits before "## Your task" output spec); test fixtures use the exported `EarningsPreviewContext` + `EarningsRecapContext` types as guardrails against silent context drift.
- ✅ **Finnhub data quality — `isPlausibleEarnings` extended to scoreboard** — closed 2026-05-05 (`921d552`). `renderHeadlineTable` now applies the same >1.7×/<0.5× EPS sanity guard that read-throughs uses; flagged actuals blank out and a `⚠ Reported actuals were flagged as implausible vs consensus` italic sub-line appears beneath the scoreboard. EarningsHub display (table) still shows raw values — extending the guard there is a sibling follow-up if it surfaces. Recap composer reads `event.actual_value` directly; same blanking flows through.
- ✅ **IBKR connector Tier 2/3 — OAuth 1.0a headless data path** — closed 2026-06-03/04. First-party OAuth 1.0a (account is Pro, retail-headless) activated after the consumer key propagated overnight + the access-token-secret was **regenerated post-activation** to bind server-side. Shipped across: **Tier 2** Mac Web API read (`5568586` — `lib/ibkr/{config,web-api,map-positions,refresh}.ts`; writes mirror TWS `source='tws'` = zero downstream changes) + **auto-fallback** (`c36a95c` — `/api/tws/auto-refresh` uses Web API when TWS down); **Tier 3 cloud OAuth** WebCrypto port (`ec54aae`, Worker `1ffc7748` — `workers/cron/src/ibkr-oauth.ts`: BigInt DH + `subtle.sign` RSA-SHA256/HMAC, prepend constant, sig key shipped as PKCS8 DER); **Tier 3 delivery** (`a354b9b`, Worker `b501f900` — `workers/cron/src/ibkr-positions.ts` mapper port wired into `fallback-earnings` [live IBKR **replaces** stale snapshot IBKR rows for the family — no double-count; Vanguard/Roth kept] + `fallback-briefing` [holdings context unions current IBKR symbols]; evening skipped — no slot + budget-tight); **LST session cache** (`b1807f4`, Worker `e4329e75` — KV-cached 24h LST in `ibkr-lst`, drops-cache+re-mints-once on 401/403, so we don't re-run the rate-limited DH+RSA handshake per run). All best-effort → snapshot on failure. Live-verified end-to-end on both Mac (acct U13643679) AND Worker (16-22 positions, OCC option parse, `cachePresent:true`, IBKR expiration epoch-millis). Tests: Mac 2305 + Worker 214. **Optional polish left (not blocking):** tickle keep-alive for very-long-lived sessions; surface live Worker IBKR read in a richer cloud format. Assessment: `docs/plans/2026-06-01-ibkr-connector-assessment.md` §5b. Topic: `memory/project_ibkr_connector.md`.
- **Richer cloud IBKR rendering + presence-only port (privacy-aware)** (scoped 2026-06-04; pick up when ready) — the IBKR connector Tier-2/3 polish, reframed by a privacy finding. **Lead with the bug, not the feature:** the Worker's `fallback-earnings.ts::renderPositions` currently hand-rolls raw dollar formatting and emits **exact cost basis** (`cost basis $18,300.00 (~$122.00/sh)`) straight into the AI prompt → echoed verbatim into the *shared* earnings email (brother on cc). The Mac composers deliberately avoid this by routing through `lib/digest/presence-only-position.ts::formatPositionPresence` (relative % OK, absolute $ never). So the cloud earnings fallback likely already leaks absolute cost basis — independent of any feature work. **The clean move (fixes leak + ships the feature in one change):** (1) port `formatPositionPresence` to the Worker (new `workers/cron/src/presence-only-position.ts`, parity-tested against the Mac copy), (2) route the cloud IBKR/Vanguard position block through it, (3) since the helper already accepts `latestPrice` to compute the %, thread the live `mktPrice` — *already fetched in `LiveIbkrPosition` but dropped at the `PositionView` projection in `ibkr-positions.ts::livePositionViewsForFamily`* — to render **live P&L % per held name** ("150 sh AMZN, ibkr, up ~17% into the print"). **Privacy verdict per option:** live P&L % = SAFE (longs only; shorts omit %); current stock price/strike/expiry = SAFE (public market data); absolute $ market value / net liq / cash / `get_account_summary` = FORBIDDEN in shared email. **Remaining menu after the core change:** (A) live P&L % in earnings email [the natural win, included above]; (B) briefing gets full presence-only position detail not just symbols (`fallback-briefing.ts` currently only unions IBKR *symbols*); (C) IBKR section in the evening email — deliberately skipped at delivery, subrequest-budget-tight (needs budget accounting before adding the OAuth handshake + positions read). Data note: IBKR `/portfolio/{acct}/positions` also returns `mktValue` (Mac maps it, Worker doesn't port it) + typically `unrealizedPnl` (neither captures) — both are absolute-$, so privacy-blocked for the email anyway. Treat as a real feature: brainstorm + TDD, not a raw edit. Topic: `memory/project_ibkr_connector.md`; supersedes the "richer cloud format" half of the optional-polish note in the closed Tier-2/3 item above. **Tickle keep-alive** (the other half of that note) is separately optional and lower value at our weekly/hourly read cadence — skip unless we start reading IBKR every tick.
- **Slack integration** (2026-04-30) — scope TBD. Three directions: outbound (briefing/digest/alerts → channel), inbound (Slack threads as a research feed peer to Gmail), or both. Captured only this session; revisit when ready.
- ✅ **Background / server-side refresh policy** — CLOSED 2026-06-09 evening: full audit at `docs/plans/2026-06-09-refresh-policy-audit.md` (job ownership matrix + the 4 candidates decided). Real gap fixed: factor classification was manual-only → now auto-refresh **Step 2.6**. Plus the Mac-first tick offset fix (Worker dispatch one tick after the Mac; cloud had been winning every digest/evening since 6/3 — needs `wrangler deploy`). Previously 🟡: — partially addressed 2026-05-11 night. The **highest-impact gap (Pushover alerts silently stop firing when Mac is asleep)** is now closed end-to-end via Tier 4a (`0889492` Worker + `492ee7a` Mac): Worker scans static `security_levels` from the v4 R2 snapshot every 15 min during market hours, fires Pushover directly, writes `cloud-fired-level-{id}` KV markers; Mac auto-refresh reconciles markers into `level_alerts` on wake + posts `mac-recent-scan` so Worker skips overlap. **Still open:** newsletter ingestion fallback (Mac launchd-only — no Worker mirror; newsletters pile up unread when laptop closed >1 day); factor-analysis cache; portfolio risk metrics roll-up; benchmark refresh on weekends; source performance attribution roll-up.

### Existing carries (still open, pre-2026-04-30)

- **Level source performance attribution (Theme D)** — wait until ~30 alerts have fired.
- ✅ **Caret affordance dislike** — shipped 2026-05-01 quick-wins sweep (`790d317`). `TabDropdown.tsx` swaps `▾` for `•••` overflow icon; rotation animation replaced by gold open-state color. Per `feedback_no_carets.md` ("standard '•••' overflow icon"). All caret affordances now retired across the app.
- ✅ **In-app config UI for `FEATURE_MODELS`** — CLOSED 2026-06-09 evening: settings key `feature_model_overrides` consulted FIRST by `resolveFeatureModel` (injection seam `lib/ai/override-source.ts`, 30s TTL + write invalidation), `GET/PATCH /api/settings/ai-models`, `AiModelsSection` in SettingsModal (Electron-only, honest feedback). Bonus fix: `vanguard-pdf.ts` module-level model const would have frozen past any override — now per-call. Worker keeps its own constants (accepted gap). Original: let user swap models without editing `lib/ai/models.ts`. Multi-session (settings table override layer + resolver change + Settings UI). User noted "applies to other areas as well" — audit env-var/code-only configs at start.
- ✅ **`bg-panel` opaque-rendering investigation** — shipped 2026-05-01 quick-wins sweep (`790d317`). Audited every `bg-panel` usage in `app/dashboard/components/`; identified 4 latent siblings of the `4de767f` MobileNavDrawer bug (DataConfidenceIndicator, TwsStatus, NotificationBell hover, TabDropdown menu) — all descendants of header's `backdrop-blur-xl` stacking context. Patched each with defensive inline `style={{ backgroundColor: "var(--panel)" }}`. iPhone B4 verified pass (no header popovers visible on portrait, no regression on what is visible). Underlying root cause (Tailwind v4 token resolution under iOS Safari `backdrop-filter` parents) still unconfirmed — fix is defense-in-depth, not a definitive root-cause patch.
- ✅ **Wire `scripts/refresh-risk-free-rate.ts` into launchd** — closed 2026-05-03. Inlined into `scripts/send-daily-digest.sh` (zero new launchd entry; piggy-backs on weekday 8:45 ET cron). Fire-and-forget on FRED outage so digest delivery never blocks. Script also got a `dotenv` loader so it works under `npx tsx` without manual env sourcing. Live cache seeded: risk-free rate now 3.680% (vs the 0.045 hardcoded fallback — meaningful correction for Sharpe + Black-Scholes).
- ✅ **Run `scripts/backfill-bond-durations.ts` against live DB** — closed 2026-05-03. Script ran cleanly but found 0 candidates: all 16 held bonds have `maturity_date IS NULL` because the bond name field text ("DUE MM/DD/YY") was never parsed into the structured column. Sibling gap filed below; not a script issue.
- ✅ **Verify Phase 5 visual rendering on iPhone (via Playwright MCP at 390×844)** — closed 2026-05-03 evening. All 8 deferred items swept against `:3000`. Passes: A1 Today (MomentumPulse renders, no overflow), A2 Performance (pills + cards stack), A3 Trade Reviews redirect (`research?view=reviews` → `analysis?view=trade-reviews` works), A5 IncomeYieldSection (renders cleanly on `/dashboard/analysis` — walkthrough's "/dashboard/accounts" reference was stale), B1 EarningsHub mask (`Cons ••• · •••` confirmed on MobileCard), B2 Chat overlay (`fixed z-50 bg-canvas inset-0` true full-screen, `PrivateMarkdown` masks per `92f76d6`), B5 dark mode (Bloomberg-pro soft-black `rgb(10,10,10)`, no washed-out elements on Today or Analysis Performance). Three new findings filed below (sector legend bug, trade-review card caret, dropdown text truncation). One UX note on chat: when private, message bodies collapse to bare `•••` markers and bubble shape becomes very small — that's the documented design tradeoff per the walkthrough (token-level masking was rejected as too complex), not a regression.
- **`~/.npm/_cacache` root ownership fix** — needs interactive `sudo chown -R Yitzi:staff ~/.npm`.
- ✅ **Polish — Security Detail Transactions sort URL params** — verified-non-bug 2026-05-03. `app/dashboard/components/TransactionsSection.tsx:6-7,58,160-206` already uses `<SortableHeader>` + `useSortParam` ("secTxns" scope). Phase-4 fix in `b60d71e`-ish was complete; this entry was a stale duplicate.
- ✅ **Backfill bond `maturity_date` from name field text** — closed 2026-05-03 (`bc5ed10`). Extended `extractMaturityDate()` in `lib/bonds.ts` from 1 format to 4: parenthesized "(due MM/DD/YY)", bare "DUE MM/DD/YY" (Vanguard PDF), "MTD YYYY-MM-DD" (IBKR / canonical CSV), and a "TREASURY ... MM/DD/YY MM/DD/YY" two-date fallback for CUSIP-style names without DUE/MTD. Same parser is the import-time hook in `lib/import/parsers/vanguard-pdf.ts:315` so future imports auto-populate. New idempotent `scripts/backfill-bond-maturity.ts` ran 16/16 bonds successfully. Downstream: `scripts/backfill-bond-durations.ts` then populated 5 held bonds with per-bond duration_years (5.56y to 24.68y), replacing the 5-year flat fallback in scenario rate-shocks; the 24.68y long bond's rate sensitivity was previously understated ~5×. +9 fixture tests including a false-positive guard for non-treasury names with two date-like substrings.
- ✅ **Mobile dropdown menu has clear/transparent background** — closed 2026-05-03. Audited every overlay/popover surface that could surface on mobile. Strongest match: `CommandPalette.tsx` — `SearchButton` is in the header without `md:hidden`, so iPhone users open it via the header search icon; the palette wrapper used `bg-canvas` Tailwind class with no inline fallback while sitting inside a `backdrop-blur-sm` parent (exact same pattern as the four header popovers patched in `790d317`). Patched with defensive `style={{ backgroundColor: "var(--canvas)" }}`. Also defensively patched `AddLevelPopover.tsx` (same `bg-panel`-in-stacking-context pattern, reachable via security-detail chart on mobile). Visual iPhone verification deferred to next session (Playwright MCP install).
- ✅ **Browser-automation tooling for agent-browser** — closed 2026-05-03. Diagnosed: agent-browser definition exists but had no browser tools, only inheriting whatever MCP the parent session had loaded; no Playwright/Chrome/Puppeteer MCP was configured anywhere. Fixed: added `@playwright/mcp@latest` to global `~/.claude.json::mcpServers.playwright`, fenced the agent-browser frontmatter with the 19 `mcp__playwright__browser_*` tools, added a "Required Tooling" preamble that fails loudly instead of silently falling back to WebFetch. Backup at `~/.claude.json.bak.20260503-163931`. Restart Claude Code to activate; first navigation triggers ~150 MB Chromium download.

### Reminders (no code action)

- **Continue factor-analysis collaboration with ChatGPT** — give it the freshly refreshed GitHub link after this session's README + screenshot + CHANGELOG sweep so its context matches the post-redesign reality.

### Closed this session (2026-06-10 evening → 06-11 — Nightly Deep QA system + first sweep + all 11 findings fixed)

Built the exploratory "synthetic owner" QA system (spec `docs/superpowers/specs/2026-06-10-nightly-deep-qa-design.md`, plan + subagent-driven exec), ran the first full sweep same day, and fixed every finding it produced. 24 commits `2f33e06..cd5d986`; tests 2676 → **2703**; all pushed; DMG rebuild via this session-end.

- ✅ **Deep-QA system live**: `qa/sandbox.sh` (VACUUM INTO DB copy + deployed standalone on :3097, every bundled-dotenv key pinned empty), `/qa-deep-sweep` skill (7 zones, sequential agents, deduped `qa/findings/ledger.json` + FINDINGS.md, Pushover on new only, auto-fix branch for objective breakage), `com.vanguard-skin.nightly-deep-qa.plist` 2:45 AM local + run mutex. Ops lessons baked in: `launchctl load` fires a missed StartCalendarInterval immediately (rogue run collided 38 min, killed by PID → mutex); parallel zone agents corrupt each other on the shared Playwright browser (→ `maxConcurrentAgents: 1`).
- ✅ **Public-repo leak remediated** (`ef0883c`): `qa/expected-values.json` (real portfolio total, tracked since April) untracked + gitignored. History scrub = open decision below.
- ✅ **All 11 sweep findings fixed** (each verified vs live DB; ledger annotated with fix commits): scope-blind TWR/MWR headline cards — period return now leads, annualized demoted (`0848567`); gen-button wiped manual actuals (`309fc2a`); alpha ≡ −beta — portfolio-return input was structurally zero, now flow-adjusted + gap-guarded (`c4c7233`); what-if +$57,771 phantom = After book dropped the 4 shorts (`88980c2`); Finnhub refresh destroyed macro enrichment + cascaded earnings-email audit rows — `deleteUnenrichedEventsForWeek` (`16f3b92`); LWC disposed-object on symbol switch (`1fe41c0`/merge `aeb30d4`); mobile Feeds overflow = unbreakable ticker runs (`2367bf7`); blank-chart honest empty state (`cb09346`); manual-event ✕ delete (`fd94957`); levels unmasked as public market data (`1586f4e`); signed negative zero normalized in shared formatters (`b848a43`).

### Closed this session (2026-06-10 — IBKR risk-metric integrity: flow-adjusted risk + NetLiq-anchored valuations)

User-reported bug (not a TODO item): Analysis Performance showed IBKR max drawdown 23% + negative Sharpe; IBKR PortfolioAnalyst PDFs (TWR +16.41%, Sharpe ~1.75, worst day −2.60%) proved both wrong. Two root causes found and fixed; 3 commits, tests 2661 → 2676, browser-E2E verified (−5.99% / 1.45 on the live page). New memory: `memory/project_valuation_integrity_invariants.md`.

- ✅ **Risk metrics flow-adjusted** (`2c036d1`) — `computeRiskMetrics` computes drawdown/vol/Sharpe on a growth index `r_t = (V_t − F_t)/V_{t−1}` from `is_external_flow=1` rows (same as TWR), never the raw value series. The 4/14 $100k IBKR withdrawal had read as a −18.9% "day" → fake 23% drawdown + negative Sharpe. Drawdown dollar fields still report raw account values at peak/trough dates.
- ✅ **Daily totals anchor to broker NetLiq** (`8c85755`) — `computeDailyValuations` Phase 2 reverted to inferred cash (`snapshot_total − holdings_value`) so `total_value ≡ monthly_snapshots.total_value` on anchor dates; the 2026-04-16 `ddb63f9` "prefer TWS-reported cash_value" had paired real cash with ghost-inflated holdings → fake ±13% days (4/23 −$36.6k / 4/24 +$57.6k vs TWS's own smooth NetLiq). `cash_value` is now fallback-only. Live recompute: every corrupt day ties to TWS NetLiq to the dollar; IBKR vol 74%→19.3%.
- ✅ **Same-day TWS ghost-row cleanup** (`dae09fb`) — new `lib/mutations/same-day-tws-holdings.ts::removeStaleSameDayTwsHoldings` wired into `syncPortfolio`: deletes tws-sourced rows for the sync date absent from the just-committed position set (intraday round-trips / covered shorts lingered because the upsert never deletes + zero-qty positions are skipped). 50% shrink guard (mirrors `reconcileClosedEquityHoldings`); statement rows never touched. `PositionSyncResult.staleRowsRemoved` added.

### Closed this session (2026-06-09 night — digest redesign: edition-aware structured composer)

- ✅ **Why the 6/9 7pm evening email re-covered yesterday** — root-caused + fixed (`9c4f068` + buffer `b9f08d1`). `last_digest_sent_at` is only written by Mac-side sends; the cloud Worker won every send 6/4-evening→6/9-morning, so the Mac's pointer froze at June 4 and tonight's first Mac-won email opened a 5-day window (trimmed to ~June 8 by the 30-article cap). Fix: both cron routes' "cloud already sent" skip branches now call `advanceDigestMarkerAfterCloudSend` (forward-only, Worker marker's `sentAt` − 5-min overlap buffer; legacy markers → now−30min). Worker `getMarkerStatus` returns `sentAt` (the KV value was always an ISO timestamp).
- ✅ **Digest redesign shipped** (spec `docs/superpowers/specs/2026-06-09-digest-redesign-design.md`, plan `docs/superpowers/plans/2026-06-09-digest-redesign.md`; 20 commits `54fa693..b9f08d1`). Driven by a 30-day audit: VK = 138 emails/30d in 7 fixed editions, TMTB = 52 in 2 wraps, META covered by 20 sources — overlap is structural. New layout (both emails, "complete record, better organized" per user): `## ⏰ Late arrivals` (≤60 min after previous send, rescues TMTB's 8:48 Morning Wrap vs the 8:45 send) → alerts → anomalies (evening) → `## Overnight & Setup`/`## The Session` + one `##` section per covered name (edition supersedence: VK Dawn→Mid-Day→Recap told ONCE) → `## Also covered` → `## Research Desk` (essays rendered in code, never merged; deep-dives cross-filed into name sections via deterministic `insertCrossFilePointers`, issuer-family aware). `lib/digest/editions.ts` = single source for source kinds + edition regexes; Worker mirror byte-parity below header (parity test). Article cap 30→40 with cap-saturation guard re-fetching the late tranche. `maxOutputTokens` 4096→8192 (live preview proved 4096 truncates the new contract on a 28-article day → silent per-source degradation). Worker fallbacks mirror the structure (`fallback-digest` deterministic Commentary/Research-Desk split; `fallback-evening` synthesis gains the section contract + EDITION COLLAPSING; TIMEFRAME block synced to the Mac's stronger HARD wording). Preview API + `<DigestEmailViewer>` gained a "Structured" default view, browser-verified against live data. Tests 2418→2661 Mac / 224→236 Worker; Worker deployed `d05565e9`.

### Closed this session (2026-06-09 afternoon — classification follow-through + honest-buttons sweep + small items)

User reported the morning session's fixes "weren't in the app" — investigation proved the DMG/DB/push were all fine; the real gaps were (a) the factor classifier never covered options on non-held underlyings, (b) the Trust Strip coverage metric ignored the option→underlying inheritance the heatmap applies, (c) classify buttons reported bare zeros. 4 commits (`2d80eed`/`21bc5e8`/`9b2d52f`/`b89b9cf`); tests 2407 → 2418; `tsc --noEmit` clean; all changes browser-E2E verified on :3000.

- ✅ **Factor classifier covers option underlyings + shorts** (`2d80eed`) — `classifyFactors` gathers distinct underlyings of currently-held unexpired options (`latestHoldingsPredicate`), creating minimal securities rows (`source_key 'underlying:SYM'`, 12 created) when absent; main candidate query `quantity > 0` → `!= 0` (PINS/FEZ shorts were unclassifiable). Result gains `candidates` + `underlyingsCreated`. **Live DB now 154/154 factor coverage (64/64 options).**
- ✅ **Trust Strip factor coverage inheritance-aware** (`2d80eed`) — `getAnalysisTrustState` applies the same option→underlying COALESCE rule as `getFactorHeatmap`/`getFactorCoverage`; the strip had counted ~51 row-less options as permanently missing, contradicting the heatmap below it.
- ✅ **Honest no-op/failure feedback on 15 components** (`21bc5e8`) — 3-agent audit of all 39 mutating components for "click → silence"; pattern: check `res.ok`+`data.success`, explain no-ops ("Nothing to classify — …"), never close drawer/dialog before the outcome renders, revert optimistic state with a reason. Touched: FactorModeCard, ClassificationCard, TrustStripDrawer (×2 buttons + array-stringify bug), alerts Suggest-all, DataConfidenceIndicator Fix, DigestCatchup, ManageSourcesModal (×3), ScenarioModeling custom compute, TranscriptCard, WatchlistButton, EarningsDateChip, EarningsRowChips, ChatInterface delete, ResearchFeedsView unfilter, ResearchDocumentsView delete+tags.
- ✅ **`allow_off_topic` admin UI + cron-evening tsc fix + TODO diagnosis formatting** (`b89b9cf`) — see ticked entries in their original sections below.

### Closed this session (2026-06-09 — Analysis classification data backbone)

User asked to "work on the analysis tab to make it more usable." A 4-agent usability audit + the user's own pain points surfaced 5 issues, 4 of them rooted in the **same disease: fragmented classification data** feeding otherwise-correct math. Full brainstorm → spec (`docs/superpowers/specs/2026-06-08-analysis-classification-backbone-design.md`) → plan (`docs/superpowers/plans/2026-06-08-analysis-classification-backbone.md`) → subagent-driven execution with per-track spec+quality review. **30 commits on `main` (`2ebbf45..b3f139d`); 41 files, +1149/−65; tests 2372 → 2407 (all green); `next build` clean.** Headline: **Vanguard Taxable Technology 8.1% → 23.2%** (browser-E2E verified), "Unknown" sector bucket 19.6% → 3.4%. All data scripts run against the live DB.

- ✅ **GICS sector normalization (single source)** (`c9b32cd`/`d51aee0`/`b40874f`/`81b6a03`) — `lib/securities/normalize-sector.ts::normalizeSector` (Bloomberg + casing variants → canonical GICS-11) applied at all 3 sector-write sites (`tws/contracts.ts`, `compute/classify-factors.ts`, `import/engine.ts`), raw preserved into empty `industry`; `scripts/backfill-sectors.ts` rewrote 44 rows. Root cause of the cash-deploy "Tech < 20%" artifact (portfolio was Bloomberg-tagged, benchmark GICS — `Financial` vs `Financials`).
- ✅ **Option → underlying linkage (issuer-family aware)** (`83a2438`/`2609f7b`/`b946fd9`/`c5cee54`) — `lib/securities/resolve-underlying.ts::resolveHeldUnderlying` (OCC parse + `issuerSiblings`, GOOGL→held GOOG); backfilled `underlying_symbol` on 122 options; **fixed the canonical-CSV parser** (`resolve-description` directly-provided-OCC path omitted `underlyingSymbol` — user-approved protected-pipeline edit). Option factor coverage 23 → 34.
- ✅ **Claude classification fallback + honest reporting** (`5598464`/`496c16b`/`dd2d835`/`bdf6da9`/`ceafd61`/`cf4d305`) — new `securityClassification` feature key; `classifyUnresolvedWithClaude` heals the 220-key static map's unresolved tail (211 names); honest toasts (sector classify surfaces unresolved count; factor classify returns 502 + error toast on all-batch failure via `isFactorClassifySuccess`); new `lib/ai/extract-json.ts::extractJsonArray` strips LLM prose preamble before `JSON.parse` (factor classifier went 0→17 on a "I need to…" preamble batch).
- ✅ **ETF sector-weight look-through** (migration 059 `etf_sector_weights` `f70de6b`; `40119fc`/`cd392db`/`799eac8`/`64be957`) — Claude+web_search fetches per-ETF GICS weights (Finnhub ETF endpoints premium-gated — verified), validated (sum≈100, GICS-mapped, last-good fallback); `lib/compute/explode-sector.ts::explodeHoldingBySector` distributes an ETF's MV across sectors, wired into cash-deploy. 20 held ETFs populated.
- ✅ **Option-underlying sector classification (Unknown-gap closer)** (`80a3293`/`b59fa79`/`41615aa`) — `lib/securities/classify-option-sectors.ts::classifyOptionSectors` Claude-classifies the GICS sector of held blank-sector options' underlying tickers (incl. options on non-held stocks), strict GICS-11 guard. 44 options sectored → Technology 17.8% → 23.2%, Unknown → 3.4%.
- ✅ **Top-10 chart contrast** (`85fd18b`) — `ClassificationCard.tsx` Top-10 chart swapped hardcoded dark hex (`#E5E7EB`/`#94A3B8`/`#0F1219`) for theme CSS vars; readable on Amber + dark (browser-verified).
- ✅ **Misbehaving `Edit|Write` hook fixed** (`bab8744`) — the prompt-based `.claude/settings.json` PreToolUse hook denied ALL legitimate edits (its ALLOW verdict was treated as a block); replaced with deterministic `.claude/hooks/block-db-edits.sh` (rejects only `*.db`/`*.sqlite`).
- ✅ **Flaky `analysis-narrative` test fixed** (`b3f139d`) — mocked `getCachedNarrative` so the cache-miss test no longer reads the production DB's narrative cache (the `feedback_ai_test_mocking.md` class); strengthened the cache-hit-bypasses-rate-limit assertion.

### Closed this session (2026-05-11 night — pre-travel resilience sweep)

User is traveling soon and wanted (1) any open bugs that could surface or mislead while away from the desk, and (2) features that improve the on-the-road experience. Tier 1 verification surfaced one real bug (PATH issue silently failing risk-free-rate refresh for ~10 weekdays); Tier 4a closed the biggest travel-resilience gap (Pushover alerts silently stopped firing when Mac asleep — no Worker fallback existed). 6 commits on `main`; tests **2005 → 2053** (+48 Mac net) + **107 → 121** (+14 Worker). Worker deployed live (version `f191125c`) with PUSHOVER_APP_TOKEN + PUSHOVER_USER_KEY secrets stored. Mac-side Pushover tokens written to Electron `settings.json` via the Settings UI; new Next.js subprocess (PID 5688, started 17:56:27) confirmed via timestamp comparison to include the new env vars. Pushover end-to-end verified via direct curl test (status 1, request id `55f5daed-...`).

- ✅ **Tier 1 verification: PATH export for launchd npx invocations** (`f604d5d`). Daily-digest cron silently failed risk-free-rate refresh for ~10 weekdays via `npx: command not found` in launchd's non-interactive shell. Mirrored `run-snapshot.sh` pattern in `send-daily-digest.sh` + hardened latent same-shaped path in `enrich-calendar-events.sh`. Smoke-tested under `env -i` sanitized shell: rate updated 3.680% → 3.690%.
- ✅ **Tier 2 PWR sector reclassification** (`10243b8`). See entry above in "Bugs / Quality".
- ✅ **Tier 3 Vanguard snapshot-age label** (`57152d5`). See Trade-vs-settlement entry above.
- ✅ **Tier 4a Worker cloud-side price-level scan + Pushover fan-out** (`0889492` + `492ee7a`). See Background/server-side refresh policy entry above. Mac-side Pushover tokens also configured in Electron Settings UI (writes to `~/Library/Application Support/Vanguard Dashboard/settings.json::pushoverAppToken|pushoverUserKey`). Electron app relaunched at 17:56:27 to pick up env. Worker deployed `f191125c` with `npm run deploy` + `npx wrangler secret put` for both Pushover secrets.
- ✅ **Tier 5 Email Filtering D Slice 1** (`ffad3f5`). See Email Filtering D entry above. D3+D4+D5 deferred to a future session.

### Closed this session (2026-05-11 evening — small-wins sweep #2 + briefing follow-ups close-out)

After verifying that several "open" entries had already shipped (A8 data half in `6c7a766`, PWR band-aided in HARD RULE #4), and that read-throughs recap-side was confirmed-open per its own follow-up note, batched 7 confirmed-open items into one session. Tree clean, **2005/2005 tests pass**.

- ✅ **Co-Work prompt symbol convention** (`a881abb`). Bonds → CUSIP, dual-class → slash form, in all three "must stay in sync" prompt locations + docs. Closes the Vanguard Taxable audit follow-up.
- ✅ **Trade-review month dropdown coverage filter** (`8af5317` + `729137a`). SQL CTE computes per-sale coverage; dropdown shows "9 of 12 reviewable" when there's a gap. `MIN_LOT_COVERAGE` exported as a shared constant so SQL threshold and JS filter can't drift. +2 tests.
- ✅ **A7 — long-only → cross-account net in briefing main holdings** (`d238aef`). `quantity > 0` → `!= 0` + `SUM(quantity) AS net_qty` aggregation; extracted formatter to exported `formatHoldingsList()`; net-short positions get a `NET SHORT n` marker. +5 tests.
- ✅ **A8 — Opus directive for thesis verbatim** (`6fb0845`). Strengthened the §5 levels watch-list bullet from "Ground each in the source author's thesis where cited" to a HARD RULE that forbids inferring direction from price action alone. Data half (`${thesis}` interpolation) had already shipped in `6c7a766`.
- ✅ **A5 — auto-regen on self-admission hit** (`cea7bb7`). New `lib/calendar/briefing-self-admission.ts` is the single source of truth for the regex catalog (shared with the verify script); new `generateBriefingTextWithRegen` helper wraps the model-call pair with 1-shot retry; addendum quotes the matched phrases explicitly. +23 tests.
- ✅ **Read-throughs — recap-side rendering** (`4b5acf3`). Sibling slot mirrors the preview-side block from `9086167`. Block returns "" when entries empty (no behavior change for symbols without cluster mappings). +5 prompt-integration regressions.
- ✅ **Notes — journal entries inherit residual ticker from prior Earnings/Trade-Thesis save** (`1e070a2`, user-reported mid-session). Found and fixed `app/dashboard/components/NotesView.tsx`: `formSymbol` is one shared `useState` slot across all note types but the dropdown only renders for Earnings/Trade-Thesis. React state persisted across type switches AND across saves (post-save reset cleared content/tags/sentiment but missed symbol). Fixed at two layers: (a) `handleCreate` build-time gate only attaches `body.symbol` when the dropdown was actually rendered for this type, and (b) `setFormSymbol("")` added to the post-save reset block as defense-in-depth. Reproduce: pick Earnings + CRCL → save → switch to Journal → save → server tagged the journal entry with CRCL.

Session-wide test delta: 1970 → 2005 (+35).

### Closed this session (2026-05-11)

- ✅ **P3 Narrative Layer** — see closeout in "Page-level rework" above (`cf84d9b` merge, 23 commits across 4 slices).
- ✅ **Nightly QA snapshot refresh** (`b9f8ea7`) — auto-cron produced a stale `qa/qa-report.txt`; committed verbatim.
- ✅ **CLAUDE.md latestHoldingsPredicate note** (`1b03f60`) — P2 "extract this helper" follow-up promoted to shipped state; added the asOfDate option + the 8+ consumer surfaces.
- ✅ **Small-wins sweep — 7 post-P4 follow-ups** (`48fd4a0`, 16 files, +439/-194) — fix send-evening test mocks, macro-themes prompt nit, document DATABASE_PATH, extract `<PeriodAttributionSection>`, Trust drawer per-account divergence, Worker fallback synthesis validation parity, migrate 4 inline CTE sites to `latestHoldingsPredicate`. Tests 1959 → 1966.
- ✅ **Real per-factor macro exposure (no more uniform "LOW")** (`62e67c3`, 5 files, +251/-17) — new `computeMacroFactorTilts` aggregates portfolio weighted exposure across 9 FACTOR_COLUMNS; macro-themes consumer drops `(as any)` cast. All 4 scopes' caches regenerated post-fix. Tests 1966 → 1970.
- ✅ **Vanguard Taxable 114-position audit close-out** (`a043f5f`) — proved engine is correct; root cause is Co-Work symbol-convention drift across statements (`BRK B` ↔ `BRK/B`, treasury names ↔ CUSIPs). `scripts/merge-duplicate-securities.ts` written + applied against live DB (3 pairs merged: 1892 → 1996, 1927 → 2079, 1851 → 2080; FK integrity clean post-merge; backup saved at `data/vanguard.db.pre-dedup-20260511-145235.bak`). New follow-up filed: harden Co-Work prompt for stable symbol convention.
- ✅ **P4 Macro Overlay + Polish** — see closeout in "Page-level rework" above (merge `e774b5e`, 23-commit branch). Closes the Analysis Deep Dive.
- ✅ **Brittle-AI test pattern in narrative `forceRegen` test** (`8271475`) — `tests/compute/analysis-narratives.test.ts > forceRegen=true should bypass cache` was a hidden pre-existing failure on main: it asserted `.rejects.toThrow()` but resolved successfully once `.env.local` was loaded (real Sonnet call). Fixed via top-of-file `vi.mock("ai", ...)` that replaces `generateText` with a deterministic rejection. Same root cause as P4's POST rate-limit fix in `f39aebb`. The 3 remaining `send-evening` failures are a different test-harness issue, not AI brittleness.

### P3 follow-ups (status after 2026-05-11 P4 ship)

- ✅ **GET /api/analysis/narrative cache-miss rate-limit** — closed 2026-05-11 (`5341de7`, P4 Slice E). New separate `lastCacheMissAt` map + `CACHE_MISS_WINDOW_MS = 60s` window; route-level cache lookup bypasses the limiter on hit (cache reads never count against budget). `__resetRateLimitForTests` clears both maps for test isolation.
- ✅ **PositionRisk per-row W-o-W badges** — closed 2026-05-11 (`749ac77`, P4 Slice E). Extracted `computeWeekOverWeekDelta` as a pure exported helper from `PositionRisk.tsx` (3 unit tests); component reads `data?.weekAgo?.positions`, computes per-row delta from `riskContribution`, renders `<WeekOverWeekBadge kind="neutral" asPercent digits={1}>` with "n/a" fallback when no week-ago row.
- ✅ **FactorProfileSection block 3 — portfolio-share contribution** — closed 2026-06-01 (`2c60b70`). New `computeSecurityFactorShare` (`lib/compute/factors.ts`) built on `getFactorHeatmap` + shared `factorExposureTotal` helper (ties out with `computeMacroFactorTilts`); server-computed prop in `security/[id]/page.tsx`; rendered with `<Pct>` privacy masking. Browser-verified on AMZN (8 active-factor rows; numbers mask under privacy mode, labels/chips stay visible).
- ✅ **`buildContextForSurface` multi-account fidelity** — closed 2026-06-01 (`a54c42f`). Extended `computeFactorAnalysis`/`computeRiskMetrics`/`computePositionRisk` with `accountIds?: number[]` (via `normalizeAccountIds` shim + new `getDailyValuationsForAccounts` that sums the series across accounts); `buildContextForSurface` now passes the full resolved set (the "NOTE" preamble removed); fixed the identical `firstAccountId` collapse in `macro-themes.ts`. Back-compat deep-equal guards + multi-account + captured-prompt tests. Explicit non-goal: `/api/compute/*` routes NOT migrated (already correct for the current 1-account-per-named-scope structure).

### Open follow-ups discovered this session (2026-05-11 P4 ship)

- ✅ **3 pre-existing `send-evening.test.ts` failures** — closed 2026-05-11 (`48fd4a0`). Root cause: 3 tests seeded an article with hardcoded `received_at = "2026-05-08 14:00:00"` and never called `setLastDigestSentAt`, so `lib/digest/send-evening.ts:75-80`'s fallback (now-24h, ≈2026-05-10 today) silently filtered the article out → no email sent → mock assertions failed. Applied the passing-test pattern (`setLastDigestSentAt(db, twoHoursAgo) + seed article with fresh `new Date().toISOString()`) to lines 175 / 190 / 206. Same hardcoded-date anti-pattern documented in `memory/feedback_ai_test_mocking.md` for AI mocks; lesson generalized.
- ✅ **`computeFactorAnalysis` lacks `tilts[]` field — visible UX consequence** — closed 2026-05-11 (`62e67c3`). New exported `computeMacroFactorTilts(db, options)` in `lib/compute/factors.ts` aggregates `getFactorHeatmap` rows through `exposureMultiplier(value)` (No=0 / Low=0.25 / Moderate=0.5 / High=0.75 / Very High=1.0; categorical Growth/Value/Yes/International=1.0; null/Unknown=0). Returns `FactorMacroTilt[]` per FACTOR_COLUMNS factor with `exposurePct` (0-100, weighted) + `topContributors` (top 5 by weighted exposure, capped at 3 by consumer). `computeFactorAnalysis` now includes `tilts` in its return. `macro-themes.ts` drops the `(factorResult as any)?.tilts?.find(...)` cast + 3 eslint-disable lines — replaced with type-safe access. +4 tests; live DB sanity: `growth_vs_value` 45.6pp very-high, `crypto_adjacent` 4.2pp low. All 4 scopes' macro themes regenerated post-fix; Workspace card now shows real exposure variation by scope.
- ✅ **Sonnet quality nit on macro themes — sometimes splits one theme into two** — closed 2026-05-11 (`48fd4a0`). Appended `"Prefer fewer broader themes over many narrow ones — split only when the underlying drivers are independent."` to `SYSTEM_PROMPT` in `lib/compute/macro-themes.ts`. Will surface effect on next regen / next Sunday pre-gen.
- **DMG rebuild needed for live app to see P4 UI** — `npm run electron:deploy` triggered as part of this session's `/session-end` ritual. Until that ships, the live Mac app + iPhone via mesh continue to run pre-P4 code (no macro overlay, no "live now" pill, no W-o-W badges).

### Open follow-ups discovered this session (2026-05-11 night — pre-travel sweep)

- ✅ **Functional verification of Mac-side Pushover** — closed 2026-05-14 (mid-travel session). Inserted guaranteed-crossing AAPL `support` level at $299 (current $298.63), `POST /api/alerts/detect` returned `{scanned:1, fired:1, deduped:0}` in 47ms, `level_alerts` row written, `sendLevelAlertPush` fire-and-forget called. User confirmed phone buzz. Cleanup: both test rows deleted. Caveat surfaced: notification's "Open in dashboard" link pointed to `localhost:3099` — fixed in same session (see PUSHOVER_LINK_BASE entry in close-out below).
- ✅ **Functional verification of Worker-side Pushover** — closed 2026-05-14 (mid-travel session). First trigger correctly skipped (mac-recent-scan marker present from auto-refresh ~30 min earlier). After temporarily clearing the marker: `/internal/trigger?type=level-scan&dryRun=true` returned `{scanned:9, fired:0, deduped:0, skipped:0}` in 3.6s — full path exercised (R2 snapshot load + Yahoo price fetch per symbol + comparison + would-fire detection). Marker restored. No false-fire risk; live snapshot has 9 static levels (no MA-based on Worker per design).
- ✅ **Pushover token bootstrap from `.env.local`** — closed 2026-05-14 (`4e9bd88`). Two issues fixed: (1) `.env.local` had `PUSHOVER_USER_TOKEN=…` (typo) instead of `PUSHOVER_USER_KEY=…` — the code reads the latter, so on a fresh install bootstrap would have silently skipped the user key. Renamed in `.env.local`. (2) Net-new `PUSHOVER_LINK_BASE` env var added to four touchpoints (AppSettings interface, bootstrapFromEnvLocal, getSanitizedSettings, main.ts env plumbing) + set to `http://100.96.0.1:3099` in `.env.local` + `settings.json` + Worker secret. Fix is fully effective only after the upcoming DMG rebuild (current packaged Electron from 5/12 doesn't have the new env-threading line); Worker side is live now.
- ✅ **Newsletter ingestion fallback when Mac is asleep** — closed 2026-05-11 late-night (`dc6e17f` Worker + `5a029d8` Mac). See entry in "Closed this session (2026-05-11 late-night)" below.
- ✅ **D3 + D4 + D5 of Email Filtering D** — closed 2026-05-11 late-night (`22ebcd9`). See entry below.
- **Mac-side Pushover via Settings UI not persisted to git** — the tokens are now in `~/Library/Application Support/Vanguard Dashboard/settings.json` which is OUTSIDE the git repo. If the Mac is wiped + the project re-cloned, the tokens would need to be re-entered manually OR sourced from `.env.local` (currently missing). Acceptable since both are user-machine-local secrets.

### Closed this session (2026-05-11 late-night — Email Filtering D + newsletter cloud fallback)

User picked "2 and 3 this session" from the deferred queue. Two work units shipped end-to-end, 3 feature commits + tests/build/wrangler clean. Test counts: Mac 2053 → 2092 (+39 net), Worker 121 → 130 (+9 net). Migration 055 applied to live DB pre-commit (schema_migrations id 56).

- ✅ **Email Filtering D — D3 + D4 + D5 close-out** (`22ebcd9`, 16 files, +920/-8). D3 added `is_portfolio_relevant: boolean` to `ANALYSIS_SCHEMA` in `lib/gmail/process.ts` so Claude votes yes/no on every article; post-extraction branch flips `is_relevant=0` + `excluded_category='off_topic'` when false. Per-source `allow_off_topic=1` opt-out via migration 055 (no UI yet — direct UPDATE in DB for now, since no source currently needs it). Prompt biased to under-filter per user preference. D4 added `relevantOnly?: boolean` option to `getRecentArticles` (default off — Feeds tab still sees everything) + unconditional `is_relevant=1` filters on `getFullTextForSources` / `getArticlesForSecurity` / `getRecentArticleSummaries` + two earnings-email inline queries. Opt-in callers: `generateDigestSince`, `generateDigestSinceAdaptive`, `generateDigestByCompanySince`, briefing `allRecent`, trade-review period articles. D5 added desktop-only Filtered tab in Research → Feeds — toggles between "All articles" and "Filtered (n)" pill, grouped list by `excluded_category`, Unfilter button per row, optimistic UI with refetch on failure. New `getFilteredArticles` + `getFilteredArticleCount` queries; `unfilterArticle` mutation; `POST /api/research/articles/[id]/unfilter` route; `GET /api/research/articles?filtered=1` mode. Mobile audit path deferred per user agreement — tracked in `memory/project_email_filtering_d5_mobile_deferral.md` + MEMORY.md Deferred Decisions. +17 tests: `tests/gmail/process-portfolio-relevance.test.ts` (D3 gate, allow_off_topic, defensive default), `tests/queries/research-relevant-only.test.ts` (D4 opt-in semantics), `tests/mutations/research-articles-unfilter.test.ts` (D5 mutation + query).

- ✅ **Newsletter cloud fallback — Worker side** (`dc6e17f`, 3 files, +682/-0). New `workers/cron/src/newsletter-fetch.ts` mirrors Tier 4a's `level-scan.ts` shape (DI seam for snapshot/Gmail/Claude). Gates: `shouldRunNewsletterFetch` is top-of-hour only within ET 06:00-20:59; `mac-recent-newsletter-sync` KV marker (60min TTL) skips when Mac is alive; per-message dedup checks both snapshot's `recentArticlesMeta` AND existing `cloud-fetched-newsletter-*` KV keys (avoid re-Claude'ing across consecutive Worker ticks). Reuses existing `WORKER_GMAIL_*` OAuth refresh-token + `fallbackNewsletterProcessing` AI Gateway feature key. Hourly caps: 10 articles total / 3 per source. Writes per-article payload as `cloud-fetched-newsletter-<gmail_message_id>` (72h TTL) with full body, html, AI fields + Worker's `is_portfolio_relevant` vote. Three new `/internal/*` endpoints (GET, DELETE, POST mac-recent-newsletter-sync), all X-Cron-Secret gated. Scheduler dispatch from `parseJobFromClock`'s sibling block in `scheduled()`. `/internal/trigger?type=newsletter-fetch` for smoke testing. +9 tests covering gating, dedup, KV writes, source-isolation under listMessages errors. `npx wrangler deploy --dry-run` clean.

- ✅ **Newsletter cloud fallback — Mac side** (`5a029d8`, 5 files, +604/-0). New `lib/research/reconcile-cloud-fetched.ts::reconcileCloudFetchedNewsletters` drains KV entries via `INSERT OR IGNORE` on `gmail_message_id` (UNIQUE) — silent dedup if Mac's own fetch picked up the same message during the asleep-wake gap. D3 gate is applied AT reconcile time (not in Worker), so per-source `allow_off_topic` is honored — Worker stores Claude's raw vote, Mac decides what to do with it. Symbol→security linking via existing `findSecurity` query. KV DELETE on each success; failed deletes are idempotent on retry. New `POST /api/research/reconcile-cloud-fetched` (cron-auth gated). Wired as Phase 0 of `/api/research/sync` (UI SSE flow, surfaces `reconcile-cloud` SSE event with reconciled count) AND as the first DB step of `/api/cron/research-sync` (launchd path, returns `cloudReconciled` + `cloudSkipped` in JSON response). After each Mac `fetchNewArticles`, `postMacRecentNewsletterSyncMarker` fires-and-forgets to silence the Worker's next tick. +9 tests covering D3-gate application, allow_off_topic escape hatch, gmail_message_id dedup, source-missing path, KV delete-on-success, empty-payload case. Closes the "Mac asleep → newsletters pile up unprocessed" travel-resilience gap.

### Closed this session (2026-05-12 — bond duration + D5 mobile + newsletter cloud smoke test + diagnostic logging)

- ✅ **Bond duration backfill** — ran `scripts/backfill-bond-durations.ts` against live DB; 2 held bonds got per-bond duration (912810SA7 → 18.50y, 912810UK2 → 24.66y). The 24.66y long bond's rate sensitivity was previously understated ~5× by the 5-year flat fallback in scenario rate-shocks. Sibling carry: 1 matured T-bill (912797TH0, matured 2026-04-14) still in holdings — separate from bond duration scope but flagged below.
- ✅ **D5 mobile audit surface** (`bbbba4b`) — one-line gate flip `hidden md:flex` → `flex` at `app/dashboard/components/ResearchFeedsView.tsx:410`. Pill row appears on mobile when `(filteredCount > 0 || viewMode === "filtered")`. No new components. Deferral memory `project_email_filtering_d5_mobile_deferral.md` removed.
- ✅ **Worker deploy + newsletter cloud fallback end-to-end verified** — yesterday's deploy `e95d104b` shipped newsletter-fetch code live; today's smoke test exercised the full pipeline. **Root cause found and fixed:** Gmail API was disabled in Cloud project `716508959265` (the project tied to `WORKER_GMAIL_CLIENT_ID`), so every `gmail.googleapis.com/users/me/messages` call was returning 403 PERMISSION_DENIED. OAuth refresh succeeded but every listMessages errored. User enabled Gmail API in the Cloud console; re-test then fetched 8 newsletters end-to-end (Worker → Gmail → Claude via AI Gateway → KV write → Mac reconcile → KV cleanup). The `skipped_already_in_db: 8` reconcile result is the **expected** outcome when Mac and Worker race — Mac's normal Gmail path wrote first, gmail_message_id UNIQUE constraint detected duplicates, Worker payloads got DELETEd from KV after dedup detection. System works correctly under concurrency.
- ✅ **Newsletter-fetch diagnostic logging** (`0b91ab4`, deployed as Worker version `71da807c`) — 3 new console.log statements in `runNewsletterFetch` (starting / per-source listed-deduped-candidates / done) + `scheduled()` dispatcher always logs result kind (was suppressed for `no_articles` / `skipped`, which hid every quiet firing). Future "did Worker fire?" investigations now take 30 seconds via `wrangler tail` instead of hours. 130/130 Worker tests pass.

### Closed this session (2026-05-14 — mid-travel: cloud-fallback hardening + Pushover link fix)

User reported missing 5/13 morning digest + missing 5/13 evening recap from a plane. Investigation surfaced a real cloud-fallback gap (Worker cron alive but neither path wrote a `cloud-sent` marker for either window). Plus a Pushover deep-link bug that only surfaced once the user tried tapping a notification from outside the Mac. 2 commits on `main` + Worker deployed live as `bf45f5c6`. Tests: Worker 130 → 142 (+12), Mac 2138 pass (3 pre-existing `ibkr-activity` date failures unchanged).

- ✅ **Cloud fallback survives full-day Mac outage** (`ffce179`, 11 files, +409/-64). Four sub-fixes in one logical change. (1) Enabled Worker observability (`[observability]` block in `wrangler.toml` → 7d log retention) so silent failures stop being undebuggable. (2) Reordered `scheduled()` — `runJob` is awaited FIRST before earnings-fallback / newsletter-fetch / level-scan / calendar-enrich dispatch via `ctx.waitUntil`. Email is the highest-stakes work; running it alone dedicates the invocation's wall-clock + subrequest budget to landing the marker write before competitors start. (3) New `cloud-attempting-{type}-{date}` KV marker (10-min TTL) — Worker writes it BEFORE the heavy Gmail+Claude+Resend work, clears after. Prevents the next 15-min tick from re-entering an in-flight fallback and shipping a duplicate; `checkCloudMarker` on Mac treats `cloud-attempting` as `sentBy=cloud` (skip-to-Mac signal). (4) New `runCatchUp()` sweep at the end of every cron tick — re-tries digest/evening/briefing inside post-dispatch ET windows when no `mac-sent` or `cloud-sent` marker exists. Bounded so "morning digest" never ships at 4pm: digest 9:00-12:00 ET Mon-Fri, evening 20:00-23:00 ET Mon-Thu / 18:00-22:00 ET Fri, briefing 17:00-22:00 ET Sun. Mac side: new `confirmMacSent(type)` helper in `lib/cron/running-marker.ts`, called from all three `/api/cron/{briefing,digest,evening}` routes after a successful send via the new `POST /internal/mac-sent` endpoint (X-Cron-Secret gated). Without this, catch-up sweep would fire a duplicate every weekday Mac is awake-and-launchd-succeeds (because `mac-sent` is only written when Worker's primary call to Mac succeeds — rare on Mesh CGNAT). Investigation: KV had 24 `enrich-fail-2026-05-13-*` markers (cron alive) but zero `mac-sent` or `cloud-sent` for either window. Mac launchd logs confirmed (`vanguard-daily-digest.log`): 5/13 8:45 ET digest all 3 attempts failed (curl timeout — Electron not running, laptop closed). Evening 5/13 19:49 ET: `Send failed: connect ENETUNREACH 54.157.71.137:465` — airplane WiFi blocked Resend SMTP outbound. Worker fallback should have caught both; my fix hardens the path. Dry-run smoke against deployed Worker confirmed digest fallback returns `{kind:"success", processedCount:13, sentBy:"cloud"}` in 274s (primary fast-fails CF 530/1016 because MESH_HOSTNAME isn't reachable from CF edge; fallback completes).

- ✅ **Pushover deep link points to Mesh IP, not localhost** (`4e9bd88`, 2 files, +11/-0). Test push from the AAPL verification above arrived correctly but the "Open in dashboard" link defaulted to `http://localhost:3099` — which from a phone resolves to the phone itself, producing "Safari can't connect." Fix: new `PUSHOVER_LINK_BASE` env var threaded through the standard 4 Electron touchpoints (AppSettings interface, bootstrapFromEnvLocal, getSanitizedSettings, main.ts env plumbing). Set to `http://100.96.0.1:3099` (Mesh IP, same as MESH_HOSTNAME) in `.env.local` + `settings.json` + Worker secret. Worker-fired Pushover gets the fix now (secret immediately effective); Mac-fired Pushover needs the DMG rebuild from this session-end to pick up the new env-threading code (verified by `asar extract-file` against the current packaged `main.js` — shipped binary from 5/12 19:08 lacks the line).

### Closed this session (2026-05-21 — mid-travel: Mac launchd ET-gate fix)

User noticed mid-trip that the cron-driven emails (when they did land) were arriving on Israel's local schedule, not ET. Root cause: macOS launchd has no per-job timezone override — `StartCalendarInterval` always fires at system local time. In Israel (IDT, UTC+3), `Hour:8 Minute:45` meant 8:45 IDT = 1:45 AM ET; evening cron at `Hour:19` fired at 12:00 PM ET; nightly QA at `Hour:2` fired at 7 PM prior-day ET. Worker side was already ET-correct via `Intl.DateTimeFormat("America/New_York")` in `workers/cron/src/dst.ts`, but Mac launchd was the silent half. Mac primary path "winning the race" against Worker meant the wrong-time email shipped first and Worker's correctly-timed fallback was skipped via marker dedup.

Pattern: switch the 5 time-of-day plists to `StartInterval=300` (every 5 min) + a shared `scripts/lib/et-gate.sh` helper sourced by each wrapper. Helper caches ET clock once via `TZ=America/New_York date` and exposes `in_et_window <dow_set> <hour> <min> [window=10]`. Bash octal trap on 08/09 minutes handled via `10#` prefix. Marker dedup in `/api/cron/*` absorbs the 1-2 ticks that land per 10-min window. Pre-existing `calendar-enrich.plist` already used this pattern (every 15 min, no time-gate); `research-sync.plist` had a handrolled gate that read **local** time despite a comment claiming otherwise — both fixed in the same commit.

- ✅ **All Mac cron jobs gate on ET wall-clock** (`c425b87`, 9 files, +108/-33). New `scripts/lib/et-gate.sh` (helper, +37 LOC). 5 plists converted: `daily-digest`, `weekly-email`, `evening-email`, `nightly-qa`, `state-snapshot`. 5 shell scripts gated: `send-{daily-digest,weekly-briefing,evening-email}.sh`, `run-snapshot.sh`, `qa/nightly-qa-cron.sh`. `run-research-sync.sh` replaced its handrolled local-time gate with the helper. `send-weekly-briefing.sh::WEEK_OF` and `nightly-qa-cron.sh::TODAY` also fixed to use ET dates so filenames/payloads align with the cron cycle. CLAUDE.md documents the pattern + warns against re-introducing `StartCalendarInterval`. Verified: 5 out-of-window cases exit in 8-16ms; 26 boundary assertions on `in_et_window` all pass; in-window positive case (research-sync) ran end-to-end successfully at 11:07 ET Thu (fetched 1 article, processed 11, marker dedup logic exercised). All 5 plists `plutil -lint` clean; `launchctl unload && load` succeeded; `launchctl list` shows all 5 with exit code 0.
- ✅ **Refresh nightly-qa report timestamp** (`63341aa`, 1 file, +2/-2). Pre-flight timestamp rolled 5/14 → 5/21 from a 2:11 AM ET nightly-QA run that landed during the session. Trivial.
- ✅ **Push `f9af693` + `22c2edb` to GitHub** (closed via this session's push of `63341aa`). Per MEMORY.md, the previous session's commits were local-only because git push failed under spotty Israel WiFi. Today's `git push origin main` succeeded (`96a674f..63341aa`), catching up all 4 commits on `main`. GitHub source-of-truth now current with Worker production state.

### Closed this session (2026-05-20 — mid-travel: cloud digest unsilencing)

User reported "none of the cloudflare automation worked" 6 days into the Israel trip — no daily digests since 5/14, no Sunday briefing 5/17, no evening recaps. Investigation walked a 4-layer evidence trail (KV markers / R2 snapshot age / Worker logs / manual `/internal/trigger` dryRun) and surfaced THREE cascading root causes that each masked the next: (1) Anthropic API credit balance was $0 — every `processArticle` call returned 402; (2) once credits were topped up, the digest hit Cloudflare Workers' free-tier 50-subrequest cap (15 articles × 2 calls + 5 messages/source × 28 sources = 70 subrequests); (3) once subrequest cap was lifted, Resend REST rejected the comma-joined multi-recipient `to` field as malformed (Mac's nodemailer accepted it natively, but Worker's hand-rolled Resend client wraps `to: [opts.to]` without splitting). 1 commit on `main` (`f9af693`) + Worker deployed live as `2e339fb7`. Tests: Worker 142 → 144 (+2 regression tests pinning the comma-split behavior). End-to-end confirmed: 5/20 digest delivered via Resend (id `ff107914-...`, 10 articles, `cloud-sent-digest-2026-05-20` marker in KV, user confirmed receipt).

- ✅ **Cloud digest survives multi-recipient + subrequest cap + silent failures** (`f9af693`, 3 files, +78/-4). Three concrete fixes in one commit because each only surfaced once the prior was lifted. **(1) `fallback-digest.ts` subrequest cap** — `MAX_ARTICLES_PER_RUN` 15 → 10, new `MAX_MESSAGES_PER_SOURCE = 1` constant (was hardcoded 5). 28 sources × 1 list + 10 articles × 2 = 48 subrequests, safely under 50 cap, with headroom for recipient resolution + Resend send. **(2) `fallback-digest.ts` observability** — new `listErrors` / `articleErrors` / `lastError` counters track upstream failures separately from "nothing new to process." When `composeDigestMarkdown` returns null AND any errors occurred, return `kind: "error"` with descriptive message instead of `kind: "no_articles"`. The OLD silent-swallow path is what hid the credit exhaustion for 6 days — every Claude call threw a 402 caught in `try/catch`, `newProcessed.length === 0`, returns `no_articles`, identical to a quiet news day in marker state + Worker logs. **(3) `resend.ts` comma-split** — `opts.to` now splits on commas before wrapping in array. Mac's nodemailer handles `"a@x.com, b@y.com"` natively; Resend REST 422s with "Invalid `to` field" on the same input. BRIEFING_EMAIL_TO is multi-recipient by default. Fix applies to all three Worker fallbacks (digest + briefing + evening) since they funnel through this one `sendEmail`. +2 regression tests pin the comma-split + whitespace-trim behavior. Saved new memory entry `feedback_cloud_silent_failure_checklist.md` — 7-step diagnostic ladder so next time something like this happens, the first check is "Anthropic billing," not "let me read 800 lines of Worker code."

### Open follow-ups (carried + new)

- **App icon redesign — DEFERRED to a dedicated design session** (2026-05-29) — user wants a new icon (rebrand → "Portfolio Desk"); direction chosen = ledger/desk motif. Three rendered candidates explored this session (ledger sheet, open ledger book, PD monogram) — none finalized, user punted to a focused design session. No assets committed. Build path when resumed: design SVG → `sharp` rasterize 1024 master → `sips` iconset + `iconutil` → `build/icon.icns`, hand-build `app/favicon.ico`, monochrome tray template PNGs (`public/tray-iconTemplate.png` @1x 18px + @2x 36px) → `npm run electron:deploy`.
- ✅ **Apply DMARC `p=quarantine`** — DONE 2026-06-01 (user applied at Cloudflare; verified live on 1.1.1.1 / 8.8.8.8 / authoritative). Next: `p=reject` after ~1-2 weeks of clean reports.
- **Verify the next scheduled emails arrive on the right day + time** (2026-05-29) — after this DMG rebuild + the Worker deploy (`72b31a9e`), confirm: (a) the morning digest arrives ~8:45 ET not 8:00, (b) the in-email date matches the ET day (no longer a day off), (c) the next market holiday (Independence observed Fri Jul 3 2026) correctly suppresses the daily digest + shifts nothing wrongly. Cheap glance at the inbox on those days.
- ✅ **Push `f9af693` to GitHub** — closed 2026-05-21 via this session's `git push origin main` (range `96a674f..63341aa`). GitHub now in sync.
- ✅ **Audit sibling fallbacks for the same silent-swallow pattern** — closed 2026-05-31 (`2313621`). See "Closed this session (2026-05-31)" below. Found two gap types, not one: `fallback-briefing`/`fallback-evening` had UNWRAPPED external calls (raw throws), while `runEarningsFallback`/`runCloudFallback` had caller-side masking (`index.ts` logged earnings only on `swept>0`, marked calendar-enrich "success" despite `failures>0`). Worker deployed `193a9e50`.
- **Power on Mac for ~10 min to refresh R2 snapshot** (new 2026-05-20) — `state/vanguard-state-*.json.gz` snapshots stopped at 5/14 because the 2am `com.vanguard-skin.state-snapshot.plist` launchd hasn't fired since Mac closed for the trip. Briefing + evening fallbacks still send but with stale portfolio context (5/14 holdings, 5/14 beta cache, "state snapshot 2026-05-14" footer). Daily digest is less affected (fetches Gmail live). Once Mac boots and the 2am cron fires once, snapshot refreshes for the next ~7 days of fallback usage.
- ~30 stale `enrich-fail-*` KV markers from the credit-exhaustion window — auto-expire on 72h TTL, no action needed. Calendar enrichment for events 5/19-5/20 won't recover (post-release reaction window passed); future events will enrich normally now that credits are funded.

- **Verify Mac-fired Pushover link after DMG rebuild** (new 2026-05-14) — re-fire AAPL test push (or wait for next real cross) once today's `npm run electron:deploy` ships. Notification should open `http://100.96.0.1:3099/dashboard/security/1721` cleanly on phone via WARP. ~30 sec when ready.
- **Quick-tunnel vs named-tunnel decision now more relevant** (deferred decision, surfaced 2026-05-14) — today's airplane WiFi outage proved the Mesh-only path can fail when WARP itself is blocked (corporate captive portal, in-flight WiFi). A named Cloudflare Tunnel (public HTTPS URL) would survive both. See `memory/project_tunnel_decision.md`; revisit when Pushover-link-dead-on-travel-WiFi events accumulate, or when SaaS v3 work needs a stable public endpoint anyway.

- **Cost monitoring for Worker newsletter-fetch** — partially closed (bounded ceiling documented: hourly cap × 15 ticks/day × Sonnet pricing ≈ <$5/mo realistic, <$207/mo absolute worst case). Programmatic queries blocked by missing `CLOUDFLARE_API_TOKEN` in `.env.local` (would need `AI Gateway:Read` scope). Manual dashboard check at `https://dash.cloudflare.com/{account}/ai/ai-gateway/{gateway}` filtered by `feature=fallbackNewsletterProcessing`. 24-48h after enabling Gmail API: expect ~0 calls/day when Mac is alive, bounded by hourly cap when asleep.
- ✅ **`allow_off_topic` source admin UI** — DONE 2026-06-09 (`b89b9cf`): "off-topic OK" chip-button per source row in `ManageSourcesModal` (optimistic + revert-and-explain), TS types added (`ResearchSource.allow_off_topic`, `updateSource` param), round-trip regression test; browser-verified on 37 sources incl. reload persistence. *(orig:)* migration 055 column shipped but no UI surface to flip it — only path was raw `sqlite3 UPDATE`.
- ✅ **Matured T-bill 912797TH0 still in holdings** — DONE (verified 2026-06-01): `purgeMaturedBondHoldings` shipped (`lib/mutations/matured-bonds.ts`, `21851e2`) + wired into auto-refresh step 1.7 and `commitImport`; 912797TH0 no longer in live holdings. *(orig:)* sibling pattern to the expired options purge from session 2026-05-03 (`970d8a4`). Matured 2026-04-14, still appears in holdings query. Bonds need a similar "purge matured bonds with grace period" sweep — `lib/mutations/expired-options.ts::purgeExpiredOptionHoldings` is the template. ~30 min. Surfaces in scenario rate-shocks + bond duration analytics until cleared.
- **Gmail API enablement gotcha for future Cloud-project changes** — if `WORKER_GMAIL_CLIENT_ID` ever gets rotated into a new Cloud project, the new project also needs Gmail API enabled at `https://console.developers.google.com/apis/api/gmail.googleapis.com/overview?project=PROJECT_ID`. Diagnostic logging now surfaces this in seconds — error message includes the activation URL with the right project ID. Fallback-digest path uses the same credentials, so it was silently 403'ing too (just never exercised since Mac always sent the digest itself).
- ✅ **`tests/api/cron-evening.test.ts:143` tsc type error** — DONE 2026-06-09 (`b89b9cf`): `checkCloudMarker` mock annotated `Promise<{sentBy, date} | null>`; `tsc --noEmit` clean repo-wide for the first time since 2026-05-31. *(orig:)* `vi.fn(async () => null)` inferred `Promise<null>`; latent because vitest is esbuild-only.
- **Official-bar daily-close capture (anomaly accuracy increment)** (deferred 2026-05-31) — the `4702780` Significant Moves fix kept `DELAYED_FROZEN` in `snapshot.ts` because the evidence showed the frozen close was accurate (the bug was the UTC date, not the frozen value). If byte-perfect daily closes are ever wanted, source the close used by anomalies/valuations from official daily bars (`fetchHistoricalPrices` is already authoritative, dated by the real bar) rather than the live snapshot — wire a post-close held-securities bar fetch. Not justified by current evidence; revisit only if frozen-staleness recurs on an actual trading day.
- **Restart the long-running `npm run dev` on :3099 if still up** (2026-05-31) — during the Significant Moves session the dev server kept the OLD `snapshot.ts` in memory (HMR didn't reload the TWS-client module graph) and wrote premature `2026-06-01` rows when TWS reconnected. This session's `electron:deploy` rebuilds the installed app with the fix; but if a separate dev server is also serving :3099, restart it (`rm -rf .next && npm run dev`) so it stops stamping forward-dated frozen rows on off-hours auto-refresh. (Self-corrects Monday during market hours regardless.)

### Closed this session (2026-05-29 — travel/holiday date fixes + Significant Moves + DMARC investigation)

- ✅ **ET-anchor all date/time + holiday-aware scheduling + Today Significant Moves** (`c7c0dfa`, 31 files). Travel + a holiday weekend surfaced a date/time cluster. (A) `todayET()` + ET-based `getCurrentMonday()` in `lib/calendar/date-utils.ts`; routed Today page / Week-Ahead / Earnings add-form / UpcomingEvents / calendar week-range route through ET — fixes "Earnings this week" showing last week + day-off displays. (B) Every outbound-email date string now renders `timeZone:"America/New_York"` (Mac send-digest/send-evening/daily-digest/group-by-company/briefing-html + Worker fallback-digest/evening/html.ts); Worker briefing weekOf → `briefingWeekOf()` (no week-jump on Monday) — fixes "every email's date a day off". (C) Worker digest fires 8:45 not 8:00 (`EXPECTED_MINUTE_DIGEST=45`) — fixes ~hour-early arrivals when the cloud fallback sends while Mac asleep. (D) NEW `lib/calendar/market-holidays.ts` (+ parity-tested Worker mirror), verified 2026-2027 NYSE closures; daily+evening skip on closures, Sunday briefing shifts to Monday on holiday weekends; wired into Mac cron routes + Worker runJob/catch-up + launchd briefing wrapper. (E) NEW "Significant Moves in Vanguard Holdings" card on Today tab reusing `lib/digest/anomalies.ts` (AnomalyFlag gains securityId). Also fixed a pre-existing date-relative time-bomb in `macro-themes.test.ts`. Mac 2173 / Worker 151 tests pass. **Worker deployed (version `72b31a9e`)**; Mac side ships with this DMG rebuild.
- ✅ **DMARC investigation — domain actively spoofed** (no code; recommendation only). Decoded the real aggregate-report XML: legit SES/Resend mail 100% aligned, but docomo/heteml reports show a growing spoof campaign — escalate `p=none`→`p=quarantine` (see urgent open item above + `memory/project_dmarc_spoofing.md`).
- ✅ **Housekeeping** (`3465b5f`) — committed the pending nightly-QA report refresh (`qa/qa-report.txt`) that was uncommitted at session start, to leave the tree clean.

### Closed this session (2026-06-01 — backlog reconciliation + 2 genuinely-open quality items)

User asked for the open backlog, chose the "action + quality smaller-fixes" track. An audit (Explore agents) found **5 of 7 "quality fixes" were already shipped** (`48fd4a0`/`62e67c3`/`4b5acf3`/`bc5ed10`) — stale entries that never got ticked. Built the 2 genuinely-open ones. 2 commits pushed `a4bca5b..2c60b70`. Tests 2188 → 2211 (+23). `tsc` clean except 1 pre-existing unrelated error (`cron-evening.test.ts:143`).

- ✅ **FactorProfileSection Block 3 — portfolio-share contribution** (`2c60b70`). New `computeSecurityFactorShare` in `lib/compute/factors.ts`, built on `getFactorHeatmap` + a shared `factorExposureTotal` helper (single source of truth for the per-factor bucket total — `computeMacroFactorTilts` now uses it too, and a regression test pins that they tie out). For each active (multiplier>0) factor it returns the security's share of the whole-portfolio exposure + first-order delta. Server-computed prop in `security/[id]/page.tsx` (fast pure read, no client fetch); rendered in `FactorProfileSection.tsx` with `<Pct>` masking on the portfolio-derived %/pp while factor labels + classification chips stay visible. Browser-verified on AMZN (8 rows; privacy toggle masks numbers to ••• with labels intact; no console errors; blocks 1/2 unregressed).
- ✅ **`buildContextForSurface` multi-account fidelity + sibling fix** (`a54c42f`). The narrative context builder (and the identical pattern in `macro-themes.ts:256`) passed only `accountIds[0]` of a multi-account scope to the single-id compute fns — silently dropping every other account. Extended `computeFactorAnalysis`/`computeRiskMetrics`/`computePositionRisk` with `accountIds?: number[]` (new `normalizeAccountIds` shim; precedence: array wins, lone id wraps, neither = whole portfolio), threaded `IN (...)` filters through tilts/concentration/position-risk, and added `getDailyValuationsForAccounts` (sums the valuation series across the set — the correct way, since vol/Sharpe/Herfindahl can't be averaged per-account). `buildContextForSurface` now passes the full resolved set (cosmetic "multiple accounts" preamble removed). Back-compat deep-equal guards prove `{accountIds:[1]}` === `{accountId:1}` so all single-id callers (API routes, PerformanceView, chat) are untouched. Multi-account tests + a captured-prompt narrative test (NVDA, held only in account 2, now appears). **Non-goal:** `/api/compute/*` routes NOT migrated off `resolveScopeToSingleId` — already correct for the current 1-account-per-named-scope structure ("all" → null → all-accounts path).
- ✅ **Backlog reconciliation** — verified-already-done items marked closed in this file + the memory index: matured T-bill purge (`21851e2`), EarningsHub privacy leak (`92f76d6`), risk-free-rate cron wiring (`4381bca`), bond-durations live run, Security-Detail txn sort URL params, Research-docs FTS title indexing, chat-delete ConfirmDialog. Memory index TODO section rewritten open-only (removed ~25 struck closed entries per its "archived in git log" policy + fixed 2 run-on lines).
- ✅ **DMARC `p=quarantine` applied + verified** 2026-06-01 (`b43d5d5`) — live on all resolvers; next step `p=reject` ~2026-06-15 on the Mac (remote `/schedule` can't decode reports). **Open (kept):** `cron-evening.test.ts:143` tsc error (pre-existing, ~5 min), `allow_off_topic` source admin UI, Slack, FEATURE_MODELS config UI, Notes rework, transcripts source replacement, + the post-DMG/monitoring carries.
- ✅ **Session-end housekeeping** — CLAUDE.md multi-account compute convention doc (`f13d669`), nightly-qa refresh (`4c1ad62`).

### Closed this session (2026-05-31 — Worker fallback observability + Significant Moves price-accuracy fix)

- ✅ **Surface upstream failures in sibling cloud fallbacks** (`2313621`, 9 files, +479/-23). Ported `fallback-digest.ts`'s error-counter observability (`f9af693`) to the four sibling Worker fallback paths so the next upstream failure (credits, rate-limit, Resend 422, FRED/Yahoo breakage) surfaces as a loud `kind:"error"` / `console.error` instead of a silent no-op. The audit found TWO gap types: **(A)** `fallback-briefing` had UNWRAPPED external calls (`loadLatestSnapshot`/`generateText`/`sendEmail` threw raw → only caught by `runJob`'s outer try as an undifferentiated stack) — now each wrapped with a stage-named `kind:"error"`; `fallback-evening` same on snapshot + sendEmail (synthesis/anomaly catches left as intentional degradation). **(B)** Caller-side masking — `runEarningsFallback` + `runCloudFallback` already counted failures but `index.ts` only logged earnings when `swept>0` and marked calendar-enrich "success" even with `failures>0`; added `lastError` to both result types + caller `console.error` on total/partial failure. +11 tests (extended briefing/evening, NEW `fallback-earnings.test.ts` + `calendar-enrich.test.ts` — neither existed). 162/162 Worker tests pass; `tsc` clean. **Worker deployed (version `193a9e50`).** Gotcha hit + fixed: `vi.clearAllMocks()` keeps implementations, so a `mockRejectedValue` leaked across tests — re-establish defaults in `beforeEach`.
- ✅ **Significant Moves / evening anomaly block showed inflated moves (5/29 PM email)** (`4702780`, 4 files, +226/-52). Root cause **verified against real market history** (stockanalysis/Yahoo), not assumptions — the user explicitly corrected an early wrong assumption (I'd treated a weekend-stamped row as ground truth). Reality: the **5/29 closes were correct**; the **5/28 prior-close was corrupt** — a late-ET TWS snapshot stamped a frozen prior-session price under the wrong day via `new Date().toISOString()` (UTC), so `computeAnomalies` diffed a stale-too-low 5/28 against a correct 5/29 and **overstated real moves** (HOOD shown +25.8% vs real +11.1%; the whole basket inflated while SPY was calm — the tell). Two stacked defects fixed: **(1) `lib/digest/anomalies.ts`** now resolves ONE consecutive `(latest, prior)` trading-day pair from SPY (filtering non-trading-day phantom rows via `lib/calendar/market-holidays.ts`) and compares every held name on those exact two dates — phantom weekend rows, stale funds, and gaps become clean OMISSIONS, and a latent cross-sectional bug (each name used its own `MAX(date)`) is fixed. **(2) `lib/tws/snapshot.ts`** ET-anchors the price date (`todayET()`) + skips writes on market-closed days (`isMarketClosed`) so the writer can't stamp a frozen tick under a wrong/weekend date; date injectable (`asOfDate`) for deterministic tests. +4 tests; 2188/2188 pass. **Live data repaired**: deleted 161 phantom `2026-05-31` rows + 161 premature `2026-06-01` rows, refetched official daily bars via `/api/tws/prices` (5/28 now matches real to the cent), recomputed valuations. **Browser-verified** the live Today card now reads HOOD +11.2% / MSFT +5.4% / NET +6.0% (real), stale funds omitted. The premature `6/01` rows re-demonstrated the bug live — the running server still had OLD `snapshot.ts` (wrote them when TWS reconnected); this DMG rebuild deploys the fix.

---

## Shipped since v2 (2026-03-30 → 2026-04-22)

Headline features not reflected in the v2 archive:

- **Security Levels + Alerts + MA-based levels** (migrations 029-031, `94379f1`, `62f98bc`)
- **Newsletter level extraction + review gate** (migration 032)
- **Mobile Today view** — phone-first single-column summary (`ad79afd`)
- **Privacy toggle** — `<Money>`/`<Pct>`/`<Shares>` masking (`68a2409`)
- **Pushover mobile push** for level alerts (`90c6589`)
- **Weekly briefing overhaul** — Opus 4.7 + Finnhub + weekend deep-reads
- **Data Integrity Overhaul** — IBKR cashBalance (migration 028), two-tier account cards
- **Trade review FIFO fixes** — expired options, short-sale P&L (migration 023)
- **AI Gateway + model-agnostic routing** — FEATURE_MODELS policy plane (`8c82c1e`)
- **Cloudflare Workers AI downroute** — Llama 3.3 + Kimi K2.6 (`264bc73`)
- **R2 PDF archival** — 39 Vanguard PDFs backfilled (`264bc73`, migration 033)
- **Chat history delete** (2026-04-22) — sidebar trash icon + `deleteConversation` mutation + `DELETE /api/chat/conversations/[id]`. Completes Theme I.
- **EDGAR 10-K / 10-Q section extraction** (2026-04-22, migration 034) — new chat tool `query_filing_section` summarizes Item 1A Risk Factors and MD&A via Sonnet, cached per `(symbol, accession, section)`. Feature key `filingSectionExtraction` in FEATURE_MODELS.
- **Research PDF knowledge base** (2026-04-22, migration 035) — upload analyst reports / research notes as PDFs; Claude extracts metadata + full body; SQLite FTS5 powers lexical search; chat tool `query_research_documents`; Documents tab in Research view. `researchDocumentExtraction` → Sonnet via native `document` content block.
- **Terminal redesign Phase 2 — MarketDataPanel KPI row** (2026-04-24) — 5-cell quote-strip between chart and LevelsPanel: Open / Day Range / 52w Range / Volume / ATR(14). Hidden on securities with no daily bars. New `<KpiCell>` primitive in `TerminalSection.tsx`; new queries `getLatestDailyBar` + `get52WeekRange` + `getKpisForSecurity`. Shipped alongside a `bare` option on `<Money>` to fix a `$$` double-dollar bug in the hero and new cells.
- **Stability burndown** (2026-04-24) — +43 tests. Fixed flaky transcript test (mocked EDGAR/Motley Fool/API Ninjas fetchers). Added 7 auto-refresh pipeline integration tests. Added 19 Cloudflare Worker cron tests (dst gates, KV dedup, primary/fallback paths, `/internal/*` handlers). Added 7 API route contract tests (sync-status, levels/extract, research/sync SSE).
- **Levels & alerts polish — Sessions 4+** (2026-04-20 → 2026-04-22, discovered during reconciliation pass) — items from master roadmap Theme A/L that shipped without being reflected in this file: A2 chat tools `query_levels` + `query_alerts` (`lib/chat/tools.ts:726,755`), L1 alerts inbox grouping into "Triggered today" / "Older pending" (`app/dashboard/alerts/page.tsx:277`), L4 `AlertsBell` hover preview, L5 alert row price-source chip (SMA/EMA), L8 `RecentAlertsPanel` on Security Detail.
- **Q1 2026 Vanguard Taxable income gap — closed** (2026-04-23) — all 11 Vanguard brokerage PDFs imported via Claude Co-Work. 167 dividends ($8,941) + 19 interest ($3,219) landed. VMFXX sweep-sign bug (201 rows) SQL-repaired in place; canonical CSV prompt patched (`176a064`) across all 3 synced sources so future imports preserve signed amounts.

---

## Next up (unblocked)

- ✅ **useCallback render-loop audit** — complete 2026-04-21, 0 candidates found. Pattern documented in `memory/feedback_usecallback_pattern.md`.
- ✅ **Next.js 16 async-params migration** — no-op as of 2026-04-22.
- ✅ **`trade-roundtrips.test.ts` TS errors** — fixed 2026-04-21 (`458ad59`).
- ✅ **Sortable column headers** — shipped 2026-04-23 (`882e620`). Shared `<SortableHeader>` + `<SortPicker>` + `useSortParam` hook. Applied across 7 tables (Holdings, 2× Transactions, 2× Tax Lots, Alerts, LevelsPanel).
- ✅ **Research mention extraction quality** — shipped 2026-04-23 (`64bdc39`). Two-layer gate (word-boundary + Haiku) at extraction time; 250 articles re-verified via `scripts/backfill-research-mentions.ts`. New FeatureKey `researchMentionVerification` → Haiku 4.5.
- ✅ **IBKR options metadata backfill** — shipped 2026-04-23 (`8707181`). 393 of 394 options now have structured `underlying_symbol` / `option_type` / `strike_price` / `expiration_date`; CALL/PUT chips now render on Security Detail.
- ✅ **Settings UI fallback for localhost** — shipped 2026-04-22 (`8ce0099`).
- ✅ **Q1 2026 Vanguard Taxable income gap** — closed 2026-04-23. See "Shipped since v2" above.

---

## Open items

Closed this session (2026-04-24 evening — Calendar Living Record Tier-3 sprint):

- ✅ **Phantom alert after full inbox clear** — closed 2026-04-24 afternoon (`c363e6d`). CustomEvent `alerts-updated` dispatch from inbox → AlertsBell listener. Sibling fix for ReviewBell via `reviews-updated`.
- ✅ **Global / in-page search** — closed 2026-04-24 afternoon (`e3085d6`). Cmd+K palette expanded to 7 source types (added research_article, research_document via FTS5, level, alert); AllHoldingsTable gained text filter.
- ✅ **Calendar / macro event post-release enrichment** — closed 2026-04-24 evening (migration 041, commits `58eef66` through `e353a79`). Full pipeline: scheduler + FRED/Finnhub actuals + TWS reaction capture + UI chips + briefing context block. Every-15-min Workers cron trigger covers off-Mac windows (primary-only for now). Plan: `docs/plans/2026-04-24-calendar-living-record.md`.
- ✅ **L9 — Calendar × Levels cross-reference** — verified shipped in earlier session (already in `CalendarView.tsx:276-281` via `getActiveLevelCountsForSecurityIds`).

Theme D capstone:

- ✅ **Theme D — Level source performance attribution v1** — shipped 2026-04-24 evening (`f906284`). New query `getSourcePerformance` + `/dashboard/levels/performance` page (empty-state tolerant) + chat tool `query_release_reactions` + Data Health "Unmapped sector ETFs" admin section.

Still open:

User-reported (2026-04-24):

_(All three user-reported items closed this session.)_

From master roadmap — reconciled 2026-04-24 evening after drift audit:

- ✅ **A3 — Daily-digest "Yesterday's triggered alerts"** — already shipped (`formatTriggeredAlertsSection` in `lib/digest/daily-digest.ts:22`); two bugs in it were also fixed this session (`6cb2cac`).
- ✅ **A5 — `expires_at` field in LevelsPanel form** — already shipped (`LevelsPanel.tsx:484 expiresAt state + :816 Field "Expires (auto-deactivate)"`).
- ✅ **L2 — LevelsPanel mobile quick-add form** — already shipped (`LevelsPanel.tsx:488 showAdvanced state, :778 "More options ↓" disclosure`).
- ✅ **L3 — Provenance filter in LevelsPanel** — already shipped (`LevelsPanel.tsx:861 authorFilter set + :898 filter application`).
- ✅ **L6 — Chart crosshair legend mobile density** — already shipped (`SecurityChart.tsx:736 hidden md:inline-flex for O/H/L + :757 mobile-only delta-from-open`).
- ✅ **H3 — Vanguard PDF direct import** — already shipped (`vanguard-pdf.ts:735 extractTransactionsFromPdf + :770 Promise.allSettled parallel holdings + transactions extraction`).
- ✅ **A13 — Cross-form validation audit** — completed 2026-04-25. Full walkthrough of 11 submit forms (LevelsPanel, SendDigestPanel, ManageSourcesModal, NotesView, TradeReviewView, ReconciliationTable, CorporateActionsSection, AnalysisView, ChatInterface, AddLevelPopover). Every `disabled={loading || !field}` guard is either correctly mode-aware (`disabled={loading || (priceSource === "static" && !price)}`) or a legitimate required-field gate with no mode-switching. No bugs found.

UX — tab navigation (flagged 2026-04-24):

- ✅ **Tab dropdowns for subviews** — shipped 2026-04-25 (`e6559d6`, follow-up `418bf21`). Research tab now shows a caret-dropdown with Notes / Trade Reviews / Feeds / Documents; Analysis shows Classification / Factor Exposure. Desktop-only — mobile keeps the in-page pill toggle. New `TabDropdown.tsx`; `nav-tabs.ts` extended with `subviews` + `matchParam`; `ResearchViewToggle` + `AnalysisView` mode-toggle gated to `md:hidden`. Keyboard: ArrowDown opens, Arrow keys/Home/End cycle, Enter selects, Escape/outside-click closes. **Non-obvious CSS fix:** the parent `<nav>` has `overflow-x-auto` which clips `position: absolute` descendants (overflow on one axis forces the other to clip too). Fix: menu uses `position: fixed` with `top`/`left` from `getBoundingClientRect()` so it escapes all ancestor clipping, plus scroll+resize listeners to keep the menu glued to the tab. Calendar has no subview toggle — the dropdown slots in if one is ever added. **Follow-up build fix (`418bf21`):** `TabDropdown` calls `useSearchParams()`, which blocks static pre-rendering for any dashboard page eligible for it (e.g., `/dashboard/levels/review`, `/alerts`, `/notes`). Wrapping the render in `<Suspense>` with a plain-`<Link>` fallback in `TabNav.tsx` isolates the CSR bailout to the dropdown itself.

New from Phase 9 (calendar-enrich Worker):

- ✅ **Phase 9b — Calendar-enrich cloud-fallback parity** — shipped 2026-04-25, code-only (flag-gated). Worker now reads the nightly R2 state snapshot (extended to cover trailing-24h events), filters in-window candidates, fetches actuals from FRED + Finnhub and reaction bars from **Yahoo Finance** (1-min chart endpoint, no auth, 10+ day retention), and writes per-event `cloud-enriched-{id}` payloads to KV (7d TTL). Mac's new `/api/calendar/reconcile-cloud-enrich` route reads those payloads on every wake (piggy-backed on every `/api/calendar/enrich` call), upserts into calendar_events with TWS-always-wins precedence on reaction_snapshot, and DELETEs the KV key. New `POST /api/calendar/enrich { upgradeReactionToTws: true }` body flag lets the UI re-capture TWS bars over a cloud-enriched row. Claude `nonfred` path is deferred on cloud (Worker writes `deferred: true` payload; Mac's next wake runs Claude itself). Gated by `CLOUD_ENRICH_ENABLED=true`; ship is safe at flag=false. **User actions before flip (3 secrets, $0):** `cd workers/cron && npx wrangler secret put FRED_API_KEY / FINNHUB_API_KEY / CLOUD_ENRICH_ENABLED` then `npx wrangler deploy`. **Provider choice:** Polygon Starter ($29/mo) rejected because its 15-min data delay creates a timing hole — events tick out of the 2h candidate window before Polygon's bars catch up. Polygon Advanced ($199/mo) is real-time but overpriced for our 10-20 candidate/day volume. Finnhub `/stock/candle` is paid-only since 2024 (verified 2026-04-25: 40-char free-tier key returns "You don't have access to this resource."). Yahoo's unofficial `query1.finance.yahoo.com/v8/finance/chart/{symbol}?interval=1m&period1&period2` endpoint is the free + real-time winner; verified returns 113+ 1-min bars 10 days back for SPY. Risk: unofficial, Yahoo could break; mitigation: graceful null fallback. New files: `workers/cron/src/yahoo.ts`, `workers/cron/src/enrich-actuals.ts`, `workers/cron/src/reaction-matcher.ts` (mirror of `lib/calendar/reaction-snapshot.ts` helpers), `app/api/calendar/reconcile-cloud-enrich/route.ts`. Test deltas: +7 cloud-enrich Worker tests, +7 Mac reconcile tests = +14, 1227 → 1241.

Other open (from MEMORY, not previously in this file):

- [x] **IBKR April 2026 transactions gap** — ✅ closed 2026-06-02 (see Bugs/Quality entry); April statement imported + reconciled against the connector. May statement also imported 2026-06-05 (`f8fd2d8`), so IBKR is current through 5/31.

Closed this session (follow-up):

- ✅ **UMich Consumer Sentiment calendar gap** — closed 2026-04-25 (`01bf248`). User flagged today's 04-24 Final release wasn't captured. Two bugs: hardcoded schedule only had prelim dates (2nd Friday, no 4th Friday finals), AND non-FRED events were inserted with null `event_time` → `resolveReleaseTime` had nothing to resolve → `release_time` stayed null → enrichment runner filtered them out (Consumer Confidence row id 67 was silently broken the same way). Fix: `NonFredEvent.releaseTime` required field, UMich split into prelim/final arrays with distinct shortNames, push site emits `event_time: src.releaseTime`. 2026 UMich dates cross-checked against `data.sca.isr.umich.edu/schedule.php`. Ran sync for weekOf 2026-04-20 → 2026-07-27 to upsert new rows. Today's 04-24 Final retroactively enriched via `/api/calendar/enrich {eventId:97}` (actual="49.8" via Claude+web_search; reaction absent because TWS is disconnected right now — acceptable for this one-off).

New from Phase 4 E2E scenario sweep (2026-04-25):

- [x] **Security Detail Transactions table sort doesn't write URL params** ✅ DONE (verified 2026-06-01): `TransactionsSection.tsx` uses `<SortableHeader>` + `useSortParam("secTxns", …)` — sort persists to URL. (Scenario 2 #9). Clicking the DATE header (which shows a ↓ arrow) does not update `location.search` — other tables with the `useSortParam` pattern (Holdings, LevelsPanel) persist sort state to the URL. Likely the Security Detail Transactions section doesn't wrap its header in `<SortableHeader>` or isn't scope-keyed. 10-min fix.
- [x] **Research Documents search may miss tag matches** ✅ DONE (verified 2026-06-01): `research_documents_fts` (migration 035) indexes title/author/source/summary/mentioned_symbols/raw_text — not body-only. (Scenario 6 #4). FTS5 search is body-only; a document tagged `ai risk / agentic ai` doesn't surface for query "ai". Design choice or gap? If the FTS index should include tags/title, extend the `INSERT INTO research_documents_fts` trigger. ~30 min.
- [x] **Chat delete uses `window.confirm` instead of toast-confirm** ✅ DONE (verified 2026-06-01): `ChatInterface.tsx` now uses in-app `<ConfirmDialog>` (deletePending state), not `window.confirm`. (Scenario 8 #7). `ChatInterface.tsx:418` — swap for the `ToastProvider` confirm pattern used elsewhere in the app. Testable + consistent. ~15 min.
- [ ] **Today's data-quality chip is page-level, not per-row** (Scenario 1 #5). Spec-vs-code divergence: scenario expected per-row chips but `today/page.tsx` renders one `qualityChip` at the H1. Either update the SCENARIOS.md checklist OR ship per-row chips if the UX reads better. Defer unless user asks.

User-reported (2026-04-25 evening — all closed in this session):

- ✅ **Research document upload — "Metadata response was not valid JSON"** (JaguarAnalytics "Hidden Angles" PDF). Closed 2026-04-25. Root cause: `lib/research-documents/extract.ts:160` had `maxOutputTokens: 4000` ("metadata response is tiny") but the prompt allowed multi-paragraph `summary` with no length cap. Long-form research digests overflowed mid-string in `summary`. Fix: bumped to 8000 + added explicit "max 1500 chars" cap in `METADATA_PROMPT` summary section.
- ✅ **First BMO earnings (NSC) didn't enrich post-release** — closed 2026-04-25. Two pieces: (a) earnings window widened from 2h to 12h for `source='finnhub' OR event_type='earnings'` in `lib/calendar/enrichment-runner.ts` (markets don't open till 09:30 ET — pre-fix a 08:00 BMO earnings was useless to enrich); (b) "one-event-per-pass" theory disproved — runner correctly processes ALL in-window candidates per pass. NSC (id 102) wasn't picked at 08:13 because Finnhub sync didn't insert it until 12:00 ET (DB created_at confirmed). NSC retroactively enriched today via `/api/calendar/enrich {eventId:102}` — actual EPS 2.65, Rev $2.998B (reaction absent because TWS disconnected, acceptable). Regression test added.
- ✅ **Nightly QA cron now actually running** — closed 2026-04-25. Wrote `~/Library/LaunchAgents/com.vanguard-skin.nightly-qa.plist` (StartCalendarInterval Hour=2 Minute=0, daily) + `launchctl load`. Wrote the missing `memory/project_nightly_qa.md` topic file documenting what runs, what it tests, files, manual-run command, and add-new-check workflow.
- ✅ **Daily digest auto-send "skipped: No processed articles in selected range" (3-day silent failure)** — closed 2026-04-25. Real bug discovered after user's clarification about manual catch-up at 7 PM yesterday. Race condition: `sendDigestEmail` read `getLastDigestSentAt(db)` AFTER 60-90s of fetch+process awaits. A concurrent manual trigger (e.g., 8:46 phone-tap while 8:45 cron mid-process) updated `last_digest_sent_at` to "now"; cron then read poisoned timestamp → zero matches → skip. Fix: capture `sinceSnapshot` BEFORE any await in `lib/digest/send-digest.ts`. Regression test in `tests/digest/send-digest-race.test.ts` uses `vi.hoisted` to inject the timestamp update during the await.
- ✅ **DigestCatchup banner: "wasn't sent at 9 AM" + shows pre-trigger** — closed 2026-04-25. `app/dashboard/components/DigestCatchup.tsx` now uses `DIGEST_TIME_LABEL = "8:45 AM"` constant + only shows banner if `now >= 8:45 AM local` AND `lastSent < scheduled-trigger-today`. Pre-8:45 weekdays the banner is hidden ("expected, not late"). CLAUDE.md updated from "9 AM" to "8:45 AM".

Phase 4 E2E sweep cleanup (also closed this session):

- ✅ **E2E #1 — Security Detail Transactions sort URL params** — closed 2026-04-25. Real bug in `lib/hooks/useSortParam.ts`: `setSort` compared against URL params (always null on first click of default-sorted column) instead of the displayed sort, so first click on the DATE column wrote redundant `?...Sort=trade_date&Dir=desc` with no visual change ("feels broken"). Fix: compare against `sort.field`/`sort.dir` (which include defaults). Tri-state cycle now visibly engages from click 1.
- ✅ **E2E #2 — Research Documents FTS5 tag matches** — closed 2026-04-25 as verified-non-bug. Migration 036 already extends FTS5 to include `tags` column + triggers wire it correctly. Live DB confirms FTS5 query "ai" returns the doc tagged "agentic ai" / "ai risk". Added explicit regression test in `tests/queries/research-documents.test.ts::"FTS5 query 'ai' surfaces a doc tagged 'agentic ai' / 'ai risk'"`.
- ✅ **E2E #3 — Chat delete + Research doc delete swap window.confirm for ConfirmDialog** — closed 2026-04-25. `ChatInterface.tsx` and `ResearchDocumentsView.tsx` both swapped to the project's `ConfirmDialog` component (the same pattern `NotesView` uses). `deletePending` / `confirmingDelete` state holds the target; `<ConfirmDialog variant="danger">` renders the modal.
- ✅ **E2E #4 — Today data-quality chip placement** — closed 2026-04-25 as spec correction. Updated `tests/e2e/SCENARIOS.md` Scenario 1 #5 to reflect the page-level qualityChip currently shipped (per-row chips would add row-clutter for marginal info — user opted close).

Closed this session (2026-04-25 night — data-integrity burndown from 9-tab audit, `b60d71e`):

- ✅ **Import engine price-source mapping** — derived prices from holdings were hardcoded `source: "canonical"` (priority 4) regardless of parser. Vanguard-PDF-derived prices now inherit `vanguard-pdf` (priority 3) so a stale manual price (also priority 4) can no longer survive a fresh PDF import. New helper `holdingDerivedPriceSource()` in `lib/import/engine.ts`. Regression test added in `tests/import/engine.test.ts`.
- ✅ **Analysis scope leak in `getFactorCoverage.bySource`** — `lib/queries/analysis.ts:579-586` was querying all `security_factors` globally, breaking the Factor Exposure bar chart under single-account scope. Fixed via CTE-joined `scoped_security_ids` that covers both direct holdings AND option underlyings.
- ✅ **Missing `scope` param on 3 API routes** — `/api/compute/xirr`, `/api/compute/reconciliation`, `/api/compute/options-strategies` now accept `scope` via `resolveScopeToSingleId`. Peer routes (risk, factors) already had it. `options-strategies` also got a bonus fix: per-account `MAX(as_of_date)` subquery (global MAX could pick a different account's stale date).
- ✅ **Gmail date-header silent fallback** — `lib/gmail/fetch.ts:194` silently fell back to `new Date()` (now) on unparseable `Date:` headers, poisoning `received_at` with future timestamps and breaking digest filtering. Now drops the message with a `console.warn`.

**Audit meta:** 9 parallel Explore agents spawned (one per dashboard tab) surfaced 11 candidate findings. 6 were real bugs (shipped above); 7 were false positives verified-and-skipped (bond par on holdings page, AllHoldingsTable allocation %, getCashAnchors TWS filter, Money null rendering, reaction-snapshot DST year-rotation, calendar enrich POST force-dynamic, send-digest timestamp ordering). Grep-verify before acting saved ~4 bad fixes. 1245 → 1246 tests.

User-reported (2026-04-25 late night — open):

- ✅ **Mobile dropdown menu has clear/transparent background** — closed 2026-05-03 (see themed Open Roadmap section). Patched CommandPalette + AddLevelPopover with defensive inline backgroundColor, mirroring the `790d317` recipe.
- [ ] **Desktop tab-subview dropdown visibility + tab/subview rethink** — the caret-dropdowns shipped in `e6559d6` work but are low-contrast / easy to miss. Two-part: (a) visual pass on `TabDropdown` (stronger caret affordance, maybe hover underline, better menu chrome), and (b) a collaborative walk through the full tab + subview tree (Today / Overview / Accounts / Holdings / Analysis / Charts / Calendar / Research / Import, plus all subviews) to decide what actually belongs as a top-level tab vs a subview vs a settings-only surface. Brainstorm session before any code.
- [ ] **Research Feeds filter-row UI rethink** — part (1) of the original two-part item: find a better UI for the source filter (the chip row became a `<select>` at some point; brainstorm wrap-to-lines / combobox). Part (2) SHIPPED 2026-06-10 (`2367bf7`): the deep-QA sweep found the actual mobile overflow was unbreakable ticker runs in AI summaries (fixed with `break-words`), and the referenced-but-never-defined `.scrollbar-none` utility now exists and hides the chip-row scrollbar.

Closed this session (2026-04-27 — email session: weekly briefing accuracy + digest race + Codex critical-bug-pass + calendar sync wiring):

- ✅ **Codex critical-bug-pass — calendar auth + derived-price provenance + release_time write-path** (`ce39140`, 17 files, +950/-264). Three coupled fixes from the 2026-04-26 Codex handoff: (a) `/api/calendar/enrich` and `/api/calendar/reconcile-cloud-enrich` now require `X-Cron-Secret` on every call — earlier "trusted local UI without secret" path was removed because the reconcile fetch built its target URL from the inbound `Host` header, leaking the secret to a caller-controlled origin. Reconcile logic extracted into `lib/calendar/cloud-reconcile.ts` (single source of truth) and called in-process — no internal HTTP hop. (b) `lib/valuation.ts::unitPriceFromMarketValue` centralizes `marketValue / quantity` with bond ÷100 + option × multiplier handling; `lib/import/engine.ts` no longer overwrites a higher-priority price already written earlier in the same import batch. Bonds and options now derive prices correctly. (c) `lib/mutations/calendar.ts` persists `release_time` on upsert (was relying on a separate backfill script); `lib/calendar/release-times.ts` normalizes earnings hours so finnhub-imported rows land enrichment-eligible immediately. CLAUDE.md updated to reflect the new auth model. +22 tests across 6 test files.
- ✅ **Briefing A1+A2+A3 — pass current prices, issuer-family, deterministic macro exposure** (`c4a9305`, 9 files, +1,426/-11). Closes the three regressions from 4/26's Sunday briefing. **A1**: `buildCurrentPrices(db, args)` returns a Map of held + option-underlying + earnings symbols; `formatCurrentPricesBlock` renders a Current Prices section + inline `last $X.XX (date)` per holding; prompt directive forbids inferring or fabricating prices. Live regen confirmed TER appears at $403.00 against the $180 strike — no more "well below $180" hallucination. **A2**: New `lib/securities/issuer-family.ts` maps GOOG/GOOGL, BRK A/B variants, FOX/FOXA, NWS/NWSA, UA/UAA, LBRDA/LBRDK, LSXMA/LSXMK, HEI/HEI.A; `buildCombinedPositionsForEvents` rolls up sibling-class positions per earnings event so the prompt sees one combined Alphabet roster (65 sh GOOG common + 1 GOOGL LEAP). Live regen confirmed "Long GOOG outright + GOOGL Jan-27 $220 calls" shipped correctly. **A3**: `macroEventToSectors(event)` maps event_type → sector whitelist; `buildMacroExposures(db, events)` queries holdings by sector and feeds the result to the prompt. +29 tests across briefing-current-prices, issuer-family, briefing-combined-positions, briefing-macro-exposures, plus 3 verify scripts under `scripts/verify-a*-*.ts`.
- ✅ **A3 directive strengthened — verbatim rule** (`f02517c`, 2 files, +25/-1). E1 Level 2 verification showed Opus still dropping XMTR + PRIM + CLH + GFL from the ISM Manufacturing exposure paragraph despite all four being in the deterministic Holdings exposure field. The original directive ("Use this list verbatim") wasn't authoritative enough — Opus pattern-matched to its training-prior idea of "industrial" and added PWR (DB-classified as Consumer) plus a "VIS basket" ETF. Strengthened to imperative HARD RULE language that frames the exposure list as data, forbids ETF additions / prose hand-waves / substitutions, and explicitly calls out the PWR-as-Consumer case. Regression test asserts the strengthened directive language stays in `briefing.ts`. **Verification deferred to next Sunday's automated 3pm cron** rather than burning another $0.65 in Opus tokens.
- ✅ **A6 — run calendar sync before briefing generation so PCE/GDP land** (`a15beb5`, 4 files, +431/-218). Sunday 4/26's briefing missed PCE + Q1 GDP advance (both released Thursday 4/30) because `sendBriefingEmail` ran `syncPortfolio` (TWS positions) but not calendar sync. The original plan called this "A6: Add PCE + GDP to FRED macro-events sync" — investigation showed that was a false reading: release IDs 53 (GDP) and 54 (Personal Income & Outlays / PCE) were already in `TRACKED_RELEASES`, and the plan's "release_id 21" actually maps to H.6 Money Stock. The real fix is structural: extracted the three-phase calendar sync (WSH → Claude macro → Finnhub) into `lib/calendar/sync.ts::syncCalendarForWeek(db, weekOf, opts)` as a reusable, non-SSE library function. Route file becomes a thin SSE bridge. `sendBriefingEmail` calls the library before generation. Errors in any phase are logged and never block — partial calendar data is still better than no briefing at all. +8 new tests in `tests/calendar/sync.test.ts`.
- ✅ **Digest race fix — close the 8:45→8:57 duplicate-send window** (`a98453b`, 12 files, +450/-22). Three interacting bugs caused today's daily digest to skip its 8:45 cron, let the user manual-send from the app, and then deliver a thinned-out duplicate from the Worker fallback at 8:57. **B1**: `sendDigestEmail` gains `skipMarkerUpdate`; `DigestCatchup` posts `{mode:"since_last", skipMarkerUpdate:true}` so a manual catch-up cannot poison cron's `last_digest_sent_at` while cron is in flight. **B2**: Worker primary timeout 120s → 300s in `wrangler.toml`. **B3**: New `mac-running` KV marker (10-min TTL) — Mac POSTs to Worker's new `/internal/running-marker` endpoint at the entry of `/api/cron/{briefing,digest}` with action=set, clears in finally. Worker re-reads markers right before firing fallback and skips when (a) mac marker is now present (slow but successful primary), (b) cloud marker is now present (race), or (c) macRunning is set. Fire-and-forget on Mac side; never blocks email delivery on Worker RTT. **B4**: `tests/digest/send-digest-race.test.ts` + `tests/workers/cron.test.ts` extended with post-cron-manual-trigger and slow-Mac-during-timeout scenarios. **Worker deployed live** — verified the 401-on-bad-secret response confirms the route is wired. Tomorrow's 8:45 AM digest is the production verification.
- ✅ **Codex CLI project configs landed** (`fedf783`, 11 files, +482/0). The `.agents/skills/` (data-auditor, db-query, run-tests, session-end, ship, test-writer) + `.codex/hooks/` (block-db-edits, **block-sqlite3-writes** — closes the .db loophole that surfaced when Codex routed around Edit/Write by using sqlite3 via Bash, check-todo-reconciled, post-edit-lint, stop-vitest) were created in the working tree by the 4/26 migration session but never committed. Now in git so any clone of the repo gets the same Codex behavior.

Operational follow-ups completed in-session (no commit):

- ✅ **Worker deployed** (`vanguard-skin-cron.isaac-3d1.workers.dev`) — `npm run deploy` from `workers/cron`. `PRIMARY_TIMEOUT_MS=300000` confirmed in deploy output; `/internal/running-marker` returns 401 on bad secret (route exists), `/health` returns 200.
- ✅ **release_time backfill** — 14 rows in 30-day window had null `release_time`. `VGS_DB_PATH=$(pwd)/data/vanguard.db npx tsx scripts/backfill-calendar-release-times.ts` resolved all 14, post-check shows 0 remaining.
- ✅ **Calendar sync run for week_of=2026-04-27** — 8 macro events landed including `fred:53:2026-04-30` (Q1 GDP Advance Estimate) and `fred:54:2026-04-30` (March Personal Income & Outlays / PCE).
- ✅ **E1 Level 2 — full briefing regen for week_of=2026-04-27** — 19,461 chars, 23 events. A1 ✅ (no fabricated TER price), A2 ✅ (GOOG + GOOGL LEAP combined), A6 ✅ (PCE + Q1 GDP both surface), A5 ✅ (zero self-admission hits), A3 ⚠️ partial (4 names dropped, PWR + VIS basket added). A3 directive strengthened in `f02517c`; next Sunday is the live re-test.

New from this session (open):

- ✅ **A3 — first-tier deterministic enforcement (cluster line injection)** — closed 2026-05-03 (`6c7a766`). Pre-renders the `**Holdings exposed:** SYM1, SYM2, ...` cluster line per macro event in `formatEventForPrompt` as a "REQUIRED §6 cluster (paste this exact line)" field. Verbatim guarantee no longer relies on Opus's restraint. §6 directive rewritten to 5 numbered HARD RULES (verbatim-paste, no-lumping, cluster-after-prose, trust-the-data, skip-if-no-mapping). Live verified 5/04 regen: 8/8 PASS on the verifier across all macro events with deterministic exposure (Housing Starts, ISM Services 33-name, JOLTS, ADP, Claims, NFP, UMich Sentiment). Full-paragraph TS rendering (the "level 2 escalation" in the original A3 entry) remains optional — only needed if cluster injection ever fails on a future week.
- ✅ **A5 — auto-regen on self-admission hit** — closed 2026-05-11 (`cea7bb7`). New `lib/calendar/briefing-self-admission.ts` is the single source of truth for the regex catalog (`buildSelfAdmissionRegex`, `findSelfAdmissions`, `buildSelfAdmissionAddendum`); the verify script and the auto-regen now share patterns. New `generateBriefingTextWithRegen` helper wraps the model-call pair (cap = 1 retry; verify-briefing-content.ts scan stays as failsafe). On self-admission hit, retry prompt quotes the matched phrases explicitly. +17 unit tests for the helper (positive matches, no false positives on benign "data"/"verify", dedup, infinite-loop guard) + 6 integration tests for the regen flow using `vi.mock("ai", ...)` per the AI-mock memory pattern.
- ✅ **A7 — long-only holdings filter → cross-account net** — closed 2026-05-11 (`d238aef`). Main holdings query at `briefing.ts:65` flipped `quantity > 0` → `!= 0` with `SUM(h.quantity) AS net_qty` aggregation so cross-account direction is visible. Extracted formatter to exported pure `formatHoldingsList()` helper. Net-short positions render explicit `NET SHORT n (cross-account net)` marker so Opus does not narrate them as "you hold X"; long-only positions keep the compact original line. Sibling sector-cluster queries in `querySymbolsBySectors` intentionally STAY at `> 0` (positive sector exposure framing) with an inline comment so future "consistency" refactors don't flip them. +5 regression tests.
- ✅ **A8 — HOOD-style level wording — pass thesis verbatim** — closed across two commits. Data half shipped 2026-05-03 in `6c7a766` (the `${thesis}` interpolation at `briefing.ts:314,325` was already inlining the stored thesis text in both triggered and nearby level rows; `verify-briefing-content.ts` already had an "A8 thesis paraphrase" scan). Directive half closed 2026-05-11 in `6fb0845` — strengthened the OUTPUT TEMPLATE bullet for §5 watch list from a soft "Ground each in the source author's thesis where cited" to a HARD RULE that quotes the stored thesis as the authoritative read on direction, forbids inferring "breakdown vs pullback" from price action alone, and explicitly handles the "no stored thesis" case.
- [ ] **C — In-app email schedule controls (Electron in-process scheduler)** (deferred from plan, full workstream). C1 AppSettings fields + migration, C2 SettingsModal "Email Schedule" section, C3 `electron/email-scheduler.ts` (two `setTimeout` chains aligned to ET wall-clock), C4 retire launchd plists after 7-day soak, C5 `/api/digest/status` returns `nextDailyAt`/`nextWeeklyAt`. ~1 day total. Quality-of-life, no urgency.
- 🟡 **D — Email filtering (admin-mail short-circuit + relevance flag)** — D1+D2 shipped 2026-05-11 night (`ffad3f5`). Migration 054 added `is_relevant` / `excluded_category` / `excluded_reason` to `research_articles` (existing 642 rows grandfathered as relevant); `lib/gmail/short-circuit.ts` is a pure helper checking subject lines against 4 categories (receipt, welcome, gift, admin); `lib/gmail/fetch.ts` short-circuits matches with `is_relevant=0` before AI processing; `lib/gmail/process.ts` filters them out of unprocessed queue. 24 new tests calibrated against 14 real noise rows in DB. **Still open:** D3 `is_portfolio_relevant` at-Claude judgment in `ANALYSIS_SCHEMA`; D4 digest queries respect `is_relevant`; D5 audit UI in Research → Feeds → "Filtered" tab. ~4-6 hr remaining.
- [ ] **H — Per-source signal-extraction profiles** (deferred from plan, multi-session). Schema (`research_source_profiles` + `research_article_extracts`), 4-stage pipeline (format detect → section filter → structured extract → summary), 6 starter profiles (Vital Knowledge Catalyst Watch / Eliant / James Bulltard / Bloomberg Odd Lots / FundaAI / Helene Meisler), briefing prompt integration, discovery-loop UI. Multi-day; needs its own design doc. Plan section H1-H6 in `~/.claude/plans/i-want-to-plan-iterative-sedgewick.md`.
- ✅ **PWR sector reclassification** — closed 2026-05-11 night (`10243b8`). Spot-checked 10 industrials (PWR, XMTR, ETN, CLH, GFL, NSC, CSX, URI, EME, PRIM); only PWR was miscat. Path (b) one-row UPDATE on live DB (`SET sector='Industrial' WHERE id=1855`) — confirmed PWR is already in `security_factors`, so `classifyFactors` won't reset it (candidate query filters `WHERE sf.security_id IS NULL`). HARD RULE #4 in `briefing.ts:390` weakened from PWR-specific carve-out to generic principle. Briefing tests 215/215 still pass.

Closed this session (2026-04-27 evening — earnings digest MVP):

- ✅ **Earnings digest Tier 1 — preview + recap email composer** (`8e24802`, 6 files). Pre-earnings (2h before) + post-earnings (2h after) briefing emails for held names. New `lib/digest/send-earnings-email.ts` builds context from cross-account positions (incl. options via `underlying_symbol` traversal — caught the Vanguard TER LEAP that Tier-1 v1 missed), preferred-source newsletter feeds (Vital Knowledge, TMT Breakout, Eliant, Purple Drink, Meisler — last 7 days, fall back to any source last 30 days), analyst recommendations trend + press releases + prior-quarter transcript best-effort. Anthropic `web_search_20250305` fills gaps for missing consensus / sell-side commentary. Two new feature keys (`earningsPreview` + `earningsRecap`) → Sonnet 4.6 on Anthropic. New `POST /api/earnings/email { eventId, phase, to? }` for manual fire. Two scripts: `scripts/preflight-earnings-data.ts` (analyst + PR sync) and `scripts/fire-earnings-emails.ts <preview|recap> <date> [bmo|amc] [--dry-run]` (auto-finds held-stock earnings for a given date+slot, calls the API). 5 production emails fired (GLW, KO, TER, plus 2 GLW iterations) — user verified TER reads as "good proof of concept/first pass, links work" and flagged the LEAP miss which was fixed in-session. Plan: `~/.claude/plans/okay-let-s-see-if-joyful-feather.md`.
- ✅ **TER 2026-04-28 calendar row inserted manually** (id=259). Finnhub had only the Q2 row (2026-07-27); user-confirmed Q1 release tomorrow AMC is Teradyne. Inserted with `source='manual'`, `release_time='16:15'` so the new email composer treats it identically to Finnhub-sourced rows.
- ✅ **GLW + TER pre-flight syncs** ran at session start: 8 monthly analyst-recommendation snapshots (4 each — 21 SB+B / 5 H consistent for GLW, 15 SB+B / 7 H / 1 S for TER), 250 press releases (157 GLW + 93 TER over last 30 days). Price targets remain empty for both per Finnhub free-tier silent return; web_search fallback is the mitigation.
- ✅ **Option-position traversal bug** (`8e24802`, fix mid-session). `getCrossAccountPositions` joined on `s.symbol IN family` only; option holdings (OCC-format symbol like `TER   280121C00180000`) never matched. Extended WHERE to `(UPPER(s.symbol) IN family OR UPPER(COALESCE(s.underlying_symbol, '')) IN family)` and added option-formatting branch in the renderer that surfaces strike/expiry/multiplier and explicit notional (1 contract × 100 = 100 shares notional). Combined-exposure summary now lists shares + contracts separately with a directive to reason about delta-weighted asymmetry rather than summing raw counts. Re-fired TER preview confirmed both legs (Vanguard Taxable LEAP + IBKR stock) appear.

Closed this session (2026-04-28 — earnings digest Phase 1 + 2 + 3 + workflow):

- ✅ **`/session-end` is the authorization** (`583db88`, 4 files). Missed today's 06:00 ET earnings-preview window because a pending commit-confirmation went unseen. New rule in `~/.claude/CLAUDE.md` + all three project session-end checklists: invoking `/session-end` IS the green light for commit + push + rebuild; do not pause for confirmation. Destructive ops (force-push, branch delete, drop data) still require per-action confirmation.
- ✅ **Phase 1 — notes pull + event-symbol reaction + light-table scoreboard** (`07a1073`, 10 files, +478/-34). Three additions on top of last night's Tier 1 MVP. (a) New `getNotesForFamily(db, family, daysBack)` in `lib/queries/notes.ts` (joins via `securities.symbol` OR `underlying_symbol` so a note attached to a TER LEAP option surfaces under TER's earnings event). Composer threads `userNotes` through `PreviewContext` + `RecapContext` and renders `## Your prior notes on {symbol}` block FIRST in both prompts so the AI frames the briefing as conversation with the user's prior thesis. (b) `ReactionSnapshot` gains optional `symbol?: BenchmarkReaction & { symbol: string }`; `captureReactionFromTws` + `captureReactionFromYahoo` accept `opts.eventSymbol` and capture event-symbol bars. Recap can now say "GLW closed +4.2% vs SPY +0.1%". Mirrored in `workers/cron/src/{reaction-matcher,yahoo,calendar-enrich}.ts`. (c) `briefingToHtml` parses GitHub-flavored markdown tables and emits email-safe `<table>` with **light palette** (white bg, dark text, warm-gold-tint header, medium-gray borders) regardless of the surrounding dark email — user explicitly flagged a dark-bg scoreboard is unprintable. Empty/em-dash cells get 14px vertical padding for handwriting. New deterministic `renderHeadlineTable()` in the composer builds a 6-row scoreboard (EPS, Revenue, Guidance, stock @ T+2h, SPY @ T+2h, QQQ @ T+2h) from `consensus_estimate` + `actual_value` + `reaction_snapshot` so preview + recap share identical shape. Both prompts restructured to lead with AI-generated `## Line-by-line bogies` markdown table, then prose. +6 tests; 178/178 calendar+digest passing. Verified live: GLW recap fired three times during iteration (v1 prose, v2 dark scoreboard, v3 light scoreboard) — v3 locked.
- ✅ **Phase 2 — Earnings Hub on `/dashboard/today` + manual-add CRUD + audit table** (`481681d`, 12 files, +1,145). Visible-from-the-app surface for the earnings digest. Migration 042 adds `earnings_emails` audit table (UNIQUE(event_id, phase) + ON DELETE CASCADE + 2 indexes); `sendEarningsEmail` writes audit row on success (ON CONFLICT updates in place). New `getSymbolStatus(db, symbols[])` query in `lib/queries/briefing-symbols.ts` returns held/watchlist/neither in one round-trip. New `getEarningsForWeekDeduped(db, weekOf)` in `lib/queries/calendar.ts` uses ROW_NUMBER() to prefer Finnhub when manual+finnhub rows exist for same (symbol, date, type). Extended `app/api/calendar/events/route.ts` with POST/PATCH/DELETE — only `source='manual'` rows are user-editable (sync-owned rows return 403). Three new mutations in `lib/mutations/calendar.ts` (`insertCalendarEvent` / `updateCalendarEvent` / `deleteCalendarEvent`). New `mondayOf()` helper in `lib/calendar/date-utils.ts`. New `EarningsHub.tsx` (server) + `EarningsHubAddForm.tsx` + `EarningsHubRefreshButton.tsx` (client) plug in between TodayReleases and Alerts. Day-grouped list with status chips (held/watchlist/no-position + preview-sent/recap-sent/pending). +12 tests in `tests/queries/earnings-hub.test.ts`; 190/190 passing.
- ✅ **Phase 3 — Cron automation, self-gating sweep, live verified** (`38ac2d3`, 7 files, +555/-21). Three cron-authenticated routes (`/api/cron/earnings-{preview,recap,sweep}`) all using new `lib/cron/wrappers.ts::withCronAuth` for the X-Cron-Secret pattern. Sweep route does work in-process: `findEmailCandidates(db)` returns preview candidates (release_time in [now+105m, now+135m]) + recap candidates (enriched_at within 4h) for held|watchlist symbols with no audit row, dispatches each to the composer. Composer's UNIQUE-protected audit-row insert is dedup floor — next 15-min tick sees the row and skips. `scripts/enrich-calendar-events.sh` restructured to run BOTH enrich AND sweep per launchd tick (was exit-on-first-success, now sequential). New `scripts/sweep-earnings-emails.ts` for tsx fallback. **Live verification at 14:06 ET 2026-04-28**: sweep returned `{swept:3, sent:3, failed:0}` for HOOD + V + TER previews inside the 16:15 AMC window. First time the system fired earnings emails without manual intervention. Audit rows confirmed for event_id 249/258/259 with `phase=preview` and `sent_at` within minutes of fire.
- ✅ **Phase 5 + weekly Finnhub auto-refresh** (`e49303c`, 7 files, +425/-10). Self-contained settings UI for the earnings emails. `lib/queries/earnings-settings.ts` stores `earnings_emails_enabled` + `earnings_emails_muted_symbols` in the existing key-value `settings` table — changes apply to the next 15-min cron sweep without an app restart (no env-var threading). New `app/api/settings/earnings/route.ts` (GET + PATCH); new `EarningsEmailsSection` client component slotted into SettingsModal with master toggle + chip list of muted symbols. `findEmailCandidates` reads settings at top, returns [] when disabled, filters muted symbols at candidate level. +9 settings tests passing. Weekly Finnhub refresh: `lib/digest/send-briefing.ts` now syncs current week + next week so Finnhub-newly-published earnings dates surface on the Earnings Hub without manual click.
- ✅ **Session-end docs reconciliation** (`99f79eb`). MEMORY.md gains a Recent Work entry covering today's nine commits + meta-findings; CLAUDE.md adds an Earnings Digest architecture bullet + four new API route entries (`/api/cron/earnings-{preview,recap,sweep}`, `/api/calendar/events` POST/PATCH/DELETE, `/api/settings/earnings`).
- ✅ **Phase 4 — Worker cloud fallback + Mac/Worker marker handshake** (`880d84f`, 10 files, +1,126/-27). Lean cloud-fallback shipped + Worker deployed to `vanguard-skin-cron.isaac-3d1.workers.dev`. New `workers/cron/src/fallback-earnings.ts` produces a compact email (scoreboard + cross-account positions + actuals + reaction) when Mac primary is unreachable, with a footer disclosing limited context — newsletter bogies, analyst data, prior transcripts, and user notes are intentionally NOT mirrored to avoid divergence as we iterate on Mac content. Snapshot bumped to `schemaVersion: 2` (adds holdings, securities, accounts, earnings_emails, earnings settings). Worker `html.ts` ported today's markdown-table parser + light-palette table rendering for visual parity with Mac primary. Marker plumbing keyed on (phase, eventId): `mac-sent-earnings-*`, `cloud-sent-earnings-*`, `mac-running-earnings-*`. Three new Worker internal endpoints (`/internal/earnings-marker`, `/internal/earnings-running-marker`, `/internal/earnings-sent-marker`) all X-Cron-Secret-gated. Mac cron routes (`/api/cron/earnings-{preview,recap}/route.ts`) updated with the marker dance: pre-check cloud-sent → set running on entry → write mac-sent on success → clear running in finally. Wider gate `shouldRunEarningsFallback` (Mon-Fri 05:00-20:00 ET) covers BMO previews + AMC recap window which the calendar-enrich 09:30-18:00 gate would miss. **Worker deployed live**: `https://vanguard-skin-cron.isaac-3d1.workers.dev` — the new earnings endpoints respond on the new schedule (`*/15 * * * *` cron trigger). Smoke test: `/internal/earnings-marker` returns `{sentBy: null}` for event 247 (no marker yet — expected since Phase 4 just shipped). Bad-secret returns 401. **Caveat**: cloud fallback is gated on snapshot v2 — last night's R2 snapshot is still v1 so the fallback short-circuits until tonight's 2am snapshot writes v2. Marker writes from the Mac side are immediately active.

Closed this session (2026-04-28 evening — earnings polish + Today/Hub redesign):

- ✅ **Track A — `formatLargeUSD` + AI prompt directive** (`d891a62`). New utilities `formatLargeUSD` / `formatLargeNumber` / `parseLargeUSD` in `lib/format.ts` (boundaries: ≥$1B → `$N.NNB` 2dp, ≥$1M → `$N.NM` 1dp, ≥$1k → comma-grouped, sub-$1k → 2dp EPS-scale). Email scoreboard's inline `formatRevenue` swapped to the shared util. Both `earningsPreview` + `earningsRecap` system prompts now carry a strict number-format directive forbidding raw-digit revenues like `4,345,870,107`. +13 boundary tests in `tests/lib/format.test.ts`.
- ✅ **Track B — SymbolLink consistency** (`d891a62`). `EarningsHub` ticker is now a real `<SymbolLink>` (was a plain span); `TodayReleases` prefixes earnings rows with the clickable symbol. Other components (`UpcomingEventsCard`, `MorningBriefing`, `CalendarView`) already had the conditional pattern.
- ✅ **Track C — In-app email viewer** (`d891a62`). `earnings_emails.ai_output_md` already stored markdown; `briefingToHtml` was already pure → no migration. New `EarningsEmailViewer.tsx` modal iframe-renders the full sent email at fidelity (light scoreboard rebuilt deterministically + AI prose verbatim). New `GET /api/earnings/email-content?eventId=&phase=` returns `{title, sentAt, sentTo, fullHtml}`. New `lib/queries/earnings-emails.ts` exposes `getEmailAudit` + `getSentPhasesForEvents`. `renderHeadlineTable` + `formatDateLong` exported from the composer for re-use. `EarningsHub` row chips converted to clickable `EarningsRowChips.tsx` button-style; `CalendarView` expanded panel exposes "View preview email" / "View recap email" links via the new query passed from the page.
- ✅ **Track D — Bogeys upload + manual entry + actuals override + composer integration** (`d891a62`, the meaty one). Migration 043 `earnings_bogeys` (event_id, source pdf_upload/manual/newsletter, source_label, eps/rev consensus + whisper, segments JSON, guidance_notes, R2 key, research_document_id, research_article_id) with UNIQUE on (event_id, source, source_label). New AI feature key `earningsBogeysExtraction` → Sonnet 4.6 with native PDF document content block. Multi-symbol fan-out: `lib/earnings/extract-bogeys.ts` extracts per-symbol from one PDF, fans out via `issuerSiblings()` to matching `calendar_events` for the target week. R2 archival via `buildStatementKey({sourceType:"earnings_bogeys_pdf"})` (graceful no-op without R2 env vars). Three new routes: `POST /api/earnings/bogeys/upload` (multipart), `POST /api/earnings/bogeys` (manual entry CRUD), `POST /api/earnings/actuals` (manual override of reported EPS/Revenue when enrichment misses — writes back to `calendar_events.actual_value` in Finnhub-shape so all downstream readers Just Work). Composer integration: `getBogeysForEvent` + new `renderBogeysBlock` sits directly after user notes in both prompts; recap explicitly anchors beat/miss on whisper when present. UI: `+ Upload bogeys PDF` button on Earnings Hub footer, per-row `+ bog` / `✎ bog` chip opens `BogeysEditModal.tsx` with three sections (existing list + delete, **actuals override**, manual entry). All currency inputs accept `$4.34B` / `4340M` / raw via `parseLargeUSD`. +13 tests across `parseExtractionResponse` tolerance + `upsertBogey` idempotency.
- ✅ **Track E + redesign — Bloomberg-style Earnings Hub data desk + dual-class fixes + finnhub-figure formatter** (`d891a62`). Today page no longer caps at `max-w-2xl` (672px) — uses dashboard's `max-w-[1600px]` (verified at 1392px on a 1440 viewport). Alerts + Levels grid collapses to single col when nearby-levels card is empty (was leaving a 684px dead column). EarningsHub rewritten as Bloomberg-style data desk: 10-col explicit grid on desktop (`When · Pos · Ticker · Cons EPS · Act EPS · Cons Rev · Act Rev · Δ · Bogeys · Email`), gold day-of-week separators with mono uppercase tracking, hex-spec `#10b981` / `#ef4444` for delta (no Tailwind theme drift). Mobile: stacked 4-row card per event, no horizontal overflow. New shared `lib/format/finnhub-figure.ts` (`parseFinnhubFigure` + `formatFinnhubFigure[Compact]`) — applied EVERYWHERE Finnhub strings reach the user: Hub rows, `TodayReleases` est line + `EnrichmentRowSummary` + `EnrichmentDetail`, email scoreboard. No more raw-digit revenue leaks anywhere. Dual-class fixed: `getSymbolStatus` walks `issuerSiblings()` so GOOGL events show HELD when user holds GOOG; new `getSecurityIdForSymbolWithSiblings` + post-process in `getEarningsForWeekDeduped` resolves null `security_id` via family for SymbolLink. Font sweep on Today: 12-13px row body → 14px to match Security Detail's TerminalTD baseline. +8 tests in `tests/lib/finnhub-figure.test.ts`. Verified visually via two `agent-browser` runs at 1440×900 + 375×812: table renders all 10 columns with compact numbers, day headers gold, GOOGL HELD chip, mobile cards stack cleanly, no console errors, alerts spans full width.

New from this session (open):

- [ ] **Earnings preview — source hierarchy work** (next phase, scope TBD per user 2026-04-27). Currently the composer treats all preferred sources equally and falls back to a wider net only when zero preferred articles match. Future: per-source weighting by historical hit-rate, per-source content-extraction profiles (TMT Breakout's "bogies" sections vs. VK's free-form prose vs. Eliant's options-heavy framing), and a deduplication step when multiple sources cover the same name. Multi-session — needs design doc.
- [ ] **Worker fallback content parity with Mac** (deferred — sequel to Phase 4). Once Mac email content stabilizes after iteration, mirror the rich context (newsletter bogies, analyst recs, prior transcripts, user notes) into the Worker fallback. Today's lean fallback is intentional: trying to mirror now would create a divergence problem the moment we touch the Mac prompt.

Closed this session (2026-04-28 night — design preview mockups for holistic redesign):

- ✅ **Holistic app redesign — mockup phase** (`a1b5067`, 58 files, 6,504 LOC). Six aesthetic directions built under `app/design-preview/*` for browseable comparison. **Direction chosen for light mode:** Sage & Linen + IBM Plex (warm cream `#f7f4ed`, deep moss brand, sage gain / burned sienna loss, Plex Sans body + Plex Mono numbers). **Direction chosen for dark mode:** Bloomberg-pro (soft-black `#0a0a0a`, amber brand `#ffb84d`, Plex Mono everywhere, semantic green/red). All five reference views (Today, Overview, Security Detail, Holdings, Calendar) live in both chosen directions at `/design-preview/refined` and `/design-preview/dark`. **Rejected:** Light Paper, Pure Terminal as default (kept as basis for Bloomberg dark), Modern Brokerage v1, Source Serif headers. Mobile-responsive baked in (≤390px clean) — three CSS lessons captured in topic file: `min-width: 0` on grid children prevents track inflation, `overflow-x: clip` on body+main is the safety net, Sparkline uses viewBox + 100% width never hardcoded SVG width attribute. Body text contrast bumped to 8.7:1 on cream (was 4.5:1) after user feedback. Smallest font tier raised 20% (10→12px floor) after user feedback. **Topic file overhauled:** `memory/project_holistic_redesign.md` now captures chosen palettes + tokens + primitives + mobile lessons + rejected-direction notes + 5-7 phase rollout plan.

New from 2026-04-28 night (open — gating future work):

- [ ] **UX audit before visual rollout** (~60-90 min, supervised). Run `~/.claude/plans/ux-audit.md` in a fresh session. Structured walkthrough of 22 surfaces (9 tabs + chat drawer + 4 email surfaces + 5 sub-pages + cross-cutting flows). Output: prioritized wishlist at `~/.claude/plans/ux-wishlist.md` with severity tags (STRUCTURAL / FLOW / POLISH / CUT / MISSING). Recommendation in wishlist drives whether visual rollout proceeds as-planned (0 STRUCTURAL items) or stalls for UX restructure (1+ STRUCTURAL items). **Must run before visual rollout.**
- ✅ **Visual rollout — Phase 1 (Foundation)** — shipped 2026-04-29 evening (`92ceaf4` on branch `redesign-rollout`, NOT yet merged to main). 7 files changed (+321 / −177): dual-theme token system in `app/globals.css` (`:root` Sage & Linen light + `[data-theme="dark"]` Bloomberg-pro), font swap to IBM Plex Sans + Mono with Instrument Serif kept transitionally, anti-FOUC inline script in `app/layout.tsx`, new `ThemeToggle.tsx` (sun/moon icon, localStorage `vgs:theme`), header rename "Vanguard Skin" → "Portfolio Desk" in `app/dashboard/layout.tsx` + `MobileNavDrawer.tsx`, `DataConfidenceIndicator` chrome shrink to dot + %, `EarningsHub.tsx` theme-adaptive refactor (replaced ~40 hardcoded dark hex with token classes). Tests at baseline (1376 pass / 4 pre-existing fail in `tests/security-levels.test.ts`); next build clean. Two iterations on user feedback during review: EarningsHub theme-adapt + ink-faint contrast bump 3.7:1 → 5.7:1 → 7.0:1. Worktree at `~/code/vanguard-skin-redesign-worktree/` on branch `redesign-rollout`. Plan: `~/.claude/plans/redesign-rollout-handoff.md` (8 phases total).
- ✅ **Visual rollout — Phase 2 (Chrome lock-in)** — shipped 2026-04-29 (`1e049e6` on branch `redesign-rollout`). See "Closed this session (2026-04-29 — visual rollout Phase 2)" above.
- ✅ **Visual rollout — Phase 3 (Today re-skin + Week-Ahead sub-view + Hero absorption)** — shipped 2026-04-30 (`becd42f` on branch `redesign-rollout`, 22 commits). Phase 3 base landed in the first ~30 min as `0f1b8c0`; remaining 21 commits were a multi-hour design exploration that **pivoted the light-mode palette from Sage & Linen → Amber (Bloomberg-light)** and locked specific layout/shadow/density decisions. Locked design lives at `/design-preview/today-options/amber-roomy-final`: strip-header (no Hero card), medium density (p-4 sm:p-5), 3-layer moss-tinted shadow (ring 20% + close drop 40% + ambient 30%), big section gaps (space-y-8). Production state at end of Phase 3: still on Sage & Linen tokens — locked design isolated to `/design-preview/*` routes. Memory file: `memory/project_holistic_redesign.md` (REVISED 2026-04-30 section). Production port = Phase 3.5 below.
- ✅ **Visual rollout — Phase 3.5 (Production port of locked Today design)** — shipped 2026-04-30 evening (`537f915` on branch `redesign-rollout`, 5 files +127 / −112). Token swap in `app/globals.css :root` (canvas/panel/raised/muted/edge → Amber values, gold #3a4a3f → #b8860b, up/down → emerald/ruby, shadow-card → 3-layer moss-tinted), borderless light cards via new rule scoped to `:root:not([data-theme="dark"])` so dark mode keeps its hairlines (its shadow has no ring layer). `app/dashboard/today/page.tsx` Hero card → slim horizontal strip with `border-b border-edge` and smaller font clamp. Card paddings tightened to medium tier (`p-4 sm:p-5`), section spacing bumped (`space-y-6 → space-y-8`). `WeekAheadView.tsx`, `TodayReleases.tsx`, `NearbyLevelsCard.tsx` mirrored to medium density. **Verified**: 1376 vitest passing (4 pre-existing B1 failures, baseline), `next build` clean, browser agent visual diff confirmed production matches reference, Accounts + Research auto-adapted via global token swap, dark mode preserved card borders, mobile reflowed without horizontal scroll, privacy mode masks portfolio strip + holdings correctly. Pre-existing follow-up surfaced (NOT a Phase 3.5 regression): EarningsHub "Cons. Rev / Act. Rev / Act EPS" columns leak dollar amounts under privacy mode — see new open item below.
- ✅ **Visual rollout — all 9 phases SHIPPED** — closed 2026-04-30 night-end. Phases 0-3.5 + 4-7 landed across the day (`redesign-rollout`); Phase 8 closed at `6909e4c`; merged to `main` as `a800884` during `/session-end`. Holistic redesign rollout complete.

Operational follow-ups completed in-session (no commit):

- ✅ **TER 2026-04-28 calendar row inserted manually** (id=259, last night). Finnhub had only Q2 (2026-07-27); user-confirmed Q1 release tomorrow AMC. Inserted with `source='manual'`, `release_time='16:15'`.
- ✅ **GLW + TER pre-flight syncs** (last night): 8 monthly analyst-recommendation snapshots + 250 press releases over last 30 days. Price targets remain empty per Finnhub free-tier silent return.
- ✅ **Live AMC preview fire via cron** (14:06 ET today): HOOD + V + TER previews delivered to production recipient list via the new `/api/cron/earnings-sweep` endpoint. Audit rows landed; next 15-min tick will skip them cleanly. **Recap window for these three opens at ~18:15 ET when the post-release enrichment runner populates `actual_value`.**

Closed this session (2026-04-29 — IA session + Portfolio Desk rebrand + Resend email swap):

- ✅ **IA session — decisions locked** (no commit; output at `~/.claude/plans/ia-decisions.md`). 30-min run-through of the 10 decision threads from `ia-session-plan.md` plus the 4 audit-gap pre-session questions. Final shape: 9 desktop tabs → **6** (Today | Accounts | Analysis | Research | Charts | Import); cut Overview (→ Today), Holdings (→ Cmd+K ticker-jump + Accounts table), Calendar (→ Today sub-view); Performance + Trade Reviews → Analysis sub-views; Notes ambient + Research home + mobile slot; Alerts unified single auto-promoted stream; chat = persistent right rail (desktop) + overlay (mobile); Cmd+K repurposed to global ticker-jump; mobile bottom nav = Today / Research / Chat / Notes / Analysis; cross-cutting rules locked (no horizontal scroll, progressive disclosure, reader-app long-form, dot+% data confidence, **rename "Vanguard Skin" → "Portfolio Desk"**). Visual rollout phases sketched at the bottom of the decisions file. Memory entries created for both the IA decisions and the Portfolio Desk branding (domain `myportfoliodesk.com` purchased same day). **Visual rollout is gated on `ia-decisions.md` existing — gate is now open.** One functional bug surfaced for Phase 7 prep: newsletter level extraction is broken (Alerts auto-promoted stream would render empty for the wrong reason without the fix).
- ✅ **Resend email swap — outbound migrated, inbound preserved** (`dc9577d`, 15 files, +197/-142). Outbound (Sunday briefing, daily digest, earnings preview/recap) now sends from `{briefing,digest,earnings}@myportfoliodesk.com` via Resend SMTP (Mac) and Resend REST (Worker fallback). Inbound stays on Gmail intentionally (Vital Knowledge IMAP on Mac, OAuth REST for newsletter listing in `workers/cron/src/gmail.ts` — kept read-only after stripping `sendMessage` + `buildMime` + dead base64 encoders). Cleaner call sites: 3 send-* files lose redundant env-var checks; new shared sender modules accept `fromLocalPart` so adding a new outbound surface is a one-arg change. Settings UI fields added through `electron/settings-store.ts` + `electron/main.ts` + `lib/settings/file-store.ts` + `app/dashboard/components/SettingsModal.tsx` (with inbound/outbound labels). Worker `Env` swapped `FROM_EMAIL` → `RESEND_API_KEY` + `RESEND_FROM_DOMAIN`; live `wrangler secret put` + `wrangler deploy` shipped Worker version `cb266b46-68be-4c48-b381-62103d4d61a2`. **Live verified end-to-end**: smoke test from `noreply@myportfoliodesk.com` reached both `isaac@wolfsonfamily.com` + `esafier@woodlotcapital.com` with `dkim=pass header.i=@myportfoliodesk.com` + `spf=pass smtp.mailfrom=...@send.myportfoliodesk.com` + DMARC implicitly aligned. Cloudflare Email Routing catch-all (`*@myportfoliodesk.com` → `isaac@wolfsonfamily.com`) configured for inbound replies. **Tomorrow 8:45 AM ET digest is the first cron-driven test**; verification routine `trig_01P1u1jtRdvvwyDSseowDbEU` scheduled via `/schedule` to fire 9:00 AM ET tomorrow and report via Gmail MCP. Memory file: `memory/project_resend_email_swap.md`.

Operational follow-ups completed in-session (no commit):

- ✅ **`myportfoliodesk.com` domain purchased on Cloudflare** (12:43 ET) — DNS already on Cloudflare; "Auto configure" Resend → CF DNS path verified domain in <2 min.
- ✅ **Worker secrets pushed live** — `RESEND_API_KEY`, `RESEND_FROM_DOMAIN` set; `FROM_EMAIL` deleted; `WORKER_GMAIL_*` preserved for inbound.
- ✅ **Cloudflare Email Routing enabled on `myportfoliodesk.com`** — apex MX/SPF/DKIM Locked; catch-all rule active to `isaac@wolfsonfamily.com`; redundant `briefings@` custom rule deleted (catch-all covered it).
- ✅ **Verification routine scheduled** — one-shot remote agent `trig_01P1u1jtRdvvwyDSseowDbEU`, fires 2026-04-30T13:00:00Z (9:00 AM EDT), uses Gmail MCP to confirm digest landed with correct From-address + auth headers + INBOX label. Result transcript at https://claude.ai/code/routines/trig_01P1u1jtRdvvwyDSseowDbEU.

Closed this session (2026-04-29 evening — visual rollout Phase 0 + Phase 1):

- ✅ **Rollout handoff plan written** — `~/.claude/plans/redesign-rollout-handoff.md`. 8 phases (0-8) with per-phase goal, surfaces touched, dependencies, pre-flight, acceptance checklist, rollback unit, supervised-vs-autonomous flag, risks. Includes pre-rollout blockers table (B1 broken newsletter level extraction → Phase 7; B2 Analysis data-quality session → Phase 5; B3 worktree setup → Phase 0; B4 optional earnings-mute), enforcement checklist (5 cross-cutting rules from `ia-decisions.md` re-verified at every phase), full-rollout risk register. Memory file `project_holistic_redesign.md` updated to reflect gate cleared.
- ✅ **Phase 0 — worktree setup** (this session, no commit). `git worktree add ~/code/vanguard-skin-redesign-worktree -b redesign-rollout` with isolated `node_modules` (712 packages), `data/vanguard.db` + `-shm` + `-wal` copied for consistent SQLite snapshot, dev server boots on `:3098` without colliding with main's `:3099`. **Discovery:** 4 pre-existing test failures in `tests/security-levels.test.ts` on the `findCrossedLevels` + `review_status='auto_approved'` whitelist path — verified to reproduce in main with no code changes. These are the same code path as B1 (broken newsletter level extraction) and have been documented as B1's reproducer in the handoff doc. **Workaround applied:** `~/.npm/_cacache/content-v2/sha512/61` is owned by `root` (some past `sudo npm install`), so the worktree install used `--cache /tmp/npm-cache-redesign-rollout` to bypass it.
- ✅ **Phase 1 — Foundation** — see "New from 2026-04-28 night" line above; full details there.

Closed this session (2026-04-29 — visual rollout Phase 2):

- ✅ **Visual rollout — Phase 2 (Chrome lock-in)** — shipped 2026-04-29 (`1e049e6` on branch `redesign-rollout`, NOT yet merged to main). 12 files changed (+231/-427 — net-negative because three redirect pages dropped 60-150 LOC of DB queries each for a 6-line `redirect()` call). **6-tab desktop nav:** `nav-tabs.ts` reduced 9 → 6 (Today / Accounts / Analysis / Research / Charts / Import); cut Overview, Holdings, Calendar; Trade Reviews subview dropped from Research (relocates to Analysis in Phase 5). **Mobile bottom nav:** Calendar slot replaced with Notes (`/dashboard/research?view=notes`); active-state now uses `useSearchParams` so Research highlights when view≠notes and Notes highlights when view=notes — both can route to the same path without highlighting together. Wrapped in `<Suspense>` for the searchParams CSR-bailout. **Chat split into three layout modes** via new `lib/hooks/useIsLargeDesktop.ts` (`(min-width: 1280px)` matchMedia): mobile (<768px) keeps fullscreen overlay; desktop (768-1279px) keeps slide-in drawer + Cmd+J toggle; xl (≥1280px) is always-visible 480px right rail. Layout reserves `xl:pr-[480px]` so content doesn't underlap. ChatToggleButton hidden at xl. Cmd+J at xl dispatches new `focus-chat-input` event (ChatInterface focuses textarea) instead of toggling. **CommandPalette repurposed as global ticker-jump:** `/api/search` gained `?type=security` short-circuit (returns just securities, skips notes/articles/etc); palette UI is symbol-first with placeholder "Jump to ticker (AAPL, NVDA, ...)" + ticker rendered in gold mono on left. **Old routes redirect** (files retained through Phase 8 cleanup so iPhone home-screen shortcuts don't 404): `/dashboard` → `/dashboard/today`, `/dashboard/holdings` → `/dashboard/today`, `/dashboard/calendar` → `/dashboard/today?view=week-ahead`. Acceptance: tests 1376/1380 (4 pre-existing security-levels failures, B1 reproducer — no Phase 2 regression); next build clean (redirect routes now Static ○ since they no longer hit DB; force-dynamic baseline preserved on the 7 surviving DB pages); 9-item agent-browser checklist all PASS at 1440x900, 1024x768, 390x844 (no horizontal scroll, rail always-visible at xl, Cmd+K returns AAPL in gold mono, all three redirects land on Today). DMG NOT rebuilt per handoff policy (rollout work stays in worktree; DMG ships from main only after Phase 8 merges).

New from this session (open):

- [ ] **`~/.npm/_cacache/content-v2/sha512/61` is root-owned** — needs `sudo chown -R Yitzi:staff ~/.npm` (user-approved 2026-04-29 but Claude can't sudo from non-interactive session). Run as `! sudo chown -R Yitzi:staff ~/.npm` from a Claude prompt OR run manually in any terminal. Until fixed, future `npm install` in worktree must pass `--cache /tmp/npm-cache-redesign-rollout` (or any non-default cache path).
- ✅ **B1 — newsletter level extraction + 4 test failures** — closed 2026-04-30 night-late (`c819c55` on `main`, cherry-picked to `redesign-rollout` as `fa61b55`). Two halves: (a) `seedPrice` test default date `"2026-04-20"` had aged out 10 days past the 4-day stale-price guard — 4 tests failing on `findCrossedLevels` were calendar-bitrot, fixed by switching the default to `new Date().toISOString().slice(0, 10)`. (b) `newsletterLevelExtraction` was on Kimi K2.6 (Workers AI) which produced 0 levels in 30 days; direct curl probe confirmed Kimi's `reasoning_content` consumed the full 2048 `max_tokens` budget before any visible JSON (`finish_reason: length`, `content: null`). Reverted to `anthropic/${SONNET_MODEL}`; smoke on 10 articles produced first real `pending_review` row (TER entry @ $280, Irrational Analysis). 1380/1380 on main, 1387/1387 on rollout. Phase 7 unblocked.
- ✅ **Phase 8 brand-prose sweep** — closed 2026-04-30 night-end (`6909e4c` on `redesign-rollout`, merged via `a800884` on `main`). All 3 references swapped: `lib/compute/tax-report.ts:274` (TXF identifier — preserved the `A` TXF control prefix), `app/dashboard/components/CanonicalCsvGuide.tsx:121,144` (both prompt builders), and `docs/canonical-csv-guide.md`. `tests/import/canonical-csv.test.ts` had no brand string — three-way sync was effectively two-way here.
- ✅ **Phase 8 font-serif sweep** — closed 2026-04-30 night-end (`6909e4c`). All 8 production `font-serif` references replaced with `font-medium` weight bumps (WelcomeOverlay, DataHealthView, ChatInterface, alerts, today, levels/performance, WeekAheadView ×2). `Instrument_Serif` import + variable + className removed from `app/layout.tsx`; `--font-serif` token removed from `app/globals.css`. Acceptance gate met: `grep -rn "font-serif" app/dashboard lib` returns zero. WelcomeOverlay's "Vanguard Dashboard" h1 text intentionally preserved — that's the Electron app display name, separate from the Phase 1 chrome rename.

Closed this session (2026-04-30 — visual rollout Phase 3 + Phase 3.5):

- ✅ **Visual rollout — Phase 3 (Today re-skin + design exploration + light-mode pivot)** — shipped 2026-04-30 across 22 commits ending `becd42f` on branch `redesign-rollout`. Phase 3 base (`0f1b8c0`) landed in ~30 min: Today re-skin + Week-Ahead sub-view at `?view=week-ahead` + Hero portfolio card absorption from old Overview tab. Remaining 21 commits were a multi-hour design exploration that **pivoted the light-mode palette from Sage & Linen → Amber (Bloomberg-light)** and locked specific layout/shadow/density decisions. Direction iterated through Sage & Linen tints → uniform Quiet/Zones/Newsprint/Editorial → Forest/Sienna/Amber → Amber border experiments → frame thickness → border-vs-glow → final direction. Locked at `/design-preview/today-options/amber-roomy-final`: strip-header (no Hero card), medium density, 3-layer moss-tinted shadow (ring 20% + close drop 40% + ambient 30%), big section gaps (space-y-8). Detail in `memory/project_holistic_redesign.md` (REVISED 2026-04-30 section). **Iteration cost note:** "vibe-driven" feedback (too vanilla, more color, more grounded) was hard to translate to specific CSS without comparison previews. Lesson: for substantial design exploration, build the `/design-preview/<feature>-options` comparison page IN PARALLEL with the production work, not after.
- ✅ **Visual rollout — Phase 3.5 (Production port of locked Today design)** — shipped 2026-04-30 evening (`537f915`, 5 files +127/-112). Token swap in `app/globals.css :root`: canvas/panel/raised/muted/edge → Amber values, gold #3a4a3f → vivid amber #b8860b (Bloomberg-light brand), up/down → emerald/ruby for white-card contrast, blue → deep navy #1a3b8a, ink hierarchy 21:1 / 14:1 / 7:1, shadow-card → 3-layer moss-tinted (kept moss tint after Sage→Amber pivot because moss reads as a believable cast shadow on warm canvas; pure-black would feel harsh). Borderless light cards via new rule scoped to `:root:not([data-theme="dark"]) .rounded-xl.bg-panel|.bg-panel-sage|.bg-panel-warm|.card-elev { border: 0 }` — dark mode keeps its hairlines (its shadow has no ring layer + #0d0d0d-on-#0a0a0a brightness delta is too subtle). `app/dashboard/today/page.tsx` Hero card → slim horizontal strip (border-b border-edge, smaller font clamp 22-28px). Card paddings tightened to medium tier (`p-4 sm:p-5`), section spacing bumped (`space-y-6 → space-y-8`), Holdings list `-mx-4 / px-4 / py-2`. `WeekAheadView.tsx` mirrored to medium density (DayCard p-4 sm:p-5, day grid gap-4, event tile p-3, day header mb-3, event list space-y-2). `TodayReleases.tsx` + `NearbyLevelsCard.tsx` padding tweaks. **Verified**: `npx vitest run` 1376 passing (4 pre-existing B1 failures, baseline); `npx next build` clean; browser-agent visual diff confirmed production `/dashboard/today` matches reference `/design-preview/today-options/amber-roomy-final`; Accounts + Research auto-adapted via global token swap with no breakage; dark mode preserved card borders; mobile reflowed without horizontal scroll (scrollWidth=541 ≤ innerWidth=546 at 546px); privacy mode masks portfolio strip + IBKR holdings + change pills correctly. Handoff doc updated to mark Phase 3.5 DONE; Phase 4 (Research) is now the next slot. DMG NOT rebuilt per handoff policy (rollout work stays in worktree; DMG ships from main only after Phase 8 merges).

New from this session (open):

- [x] **EarningsHub privacy-mode leak** ✅ DONE (closed 2026-05-01 `92f76d6`; verified 2026-06-01): Cons/Act EPS+Rev cells wrapped in `<PrivateText>` (desktop + mobile card). (surfaced 2026-04-30 by browser-agent visual check during Phase 3.5). When `vgs:privacyMode=on`, the EarningsHub "Cons. Rev / Act. Rev / Act EPS" columns still show real dollar amounts (e.g. `$0.85 → $269.7M`, `$0.70 → $4.348`). Other EarningsHub elements + portfolio strip + IBKR holdings DO mask correctly. Pre-existing — NOT introduced by Phase 3.5; the table just hadn't been routed through `<Money>` / `usePrivateFormatter` when EarningsHub was first built. Fix: wrap the cons/actual cells in `<Money>` (or `<PrivateText>` for the Finnhub-shape strings). ~20 min standalone bug fix; consider folding into Phase 4 if it touches the same component.

Closed this session (2026-04-30 evening — visual rollout Phase 4):

- ✅ **Visual rollout — Phase 4 (Research restructure + ambient Notes + reader-app Feeds + email reader treatment)** — shipped 2026-04-30 evening (`3a4d014` on branch `redesign-rollout`, 7 files +268/-66). **ResearchViewToggle** drops the Trade Reviews entry; legacy `?view=reviews` still resolves so bookmarks keep working until Phase 5 moves Trade Reviews to Analysis. **NotesAmbient** (new component): floating pen-icon FAB on every dashboard page + Cmd+; keyboard shortcut, drafts persist to `localStorage:vgs:notes-ambient` on debounced keystroke, "Save to Notes" POSTs a `journal` note via `/api/notes`, gold dot affordance when an unsaved draft exists, `xl:right-[500px]` offset keeps the FAB + open panel clear of the persistent chat rail at ≥1280px. **ResearchFeedsView** source filter unified to a single native `<select>` on every viewport (per user feedback the desktop pill cluster was "a jumble"); article summaries + portfolio_relevance bumped to 17px / 1.7 line-height; `.prose-reader` / `.prose-newsletter` to 18px / 1.7. **briefingToHtml** body 14px → 16px and 1.65 → 1.7 line-height; paragraph / list-item / blockquote vertical rhythm relaxed for reader-app feel. Verified: 1376 vitest passing (4 pre-existing B1 failures, baseline); `next build` clean. DMG NOT rebuilt per rollout policy. Visual sign-off via iPhone over LAN at `192.168.1.154:3098` — desktop browser verification was blocked because the worktree has no Playwright install.

New from this session (open):

- [ ] **Notes area rework** (Phase 4 follow-up, 2026-04-30) — current note types `journal / earnings / trade_thesis` leave no natural home for a "stock-related but not earnings" thought. Rework so a per-security non-earnings note has a clear destination. Open question parked: should NotesAmbient's "Save to Notes" route the user to a full notes page for the just-saved entry? (User: "I'm not totally sure on that. You could leave it for now.") Revisit during the rework — likely interleaves with Phase 5 (Analysis) since Trade Reviews relocates then.
- ✅ **Caret affordance dislike** (2026-04-30) — closed 2026-05-01 quick-wins sweep (`790d317`). `TabDropdown.tsx` swaps `▾` for `•••` overflow icon; gold open-state color replaces rotation animation. All caret affordances retired.
- [x] **Wire `scripts/refresh-risk-free-rate.ts` into launchd OR daily-digest cron** ✅ DONE (closed 2026-05-03 `4381bca`; verified 2026-06-01): inlined into `scripts/send-daily-digest.sh`. (Phase 5 D2 follow-up, 2026-04-30 late night) — script committed in Phase 5 fetches DGS3MO from FRED and writes to settings cache, but doesn't auto-run. Two options: (a) new launchd plist `~/Library/LaunchAgents/com.vanguard-skin.risk-free-rate.plist` with daily 9am ET trigger (simplest), OR (b) inline `refreshRiskFreeRateFromFred(db)` call into the existing daily-digest path which already runs FRED-adjacent code (zero new launchd surface). Until wired, the rate stays at fallback 0.045 → Sharpe ratios drift slightly. ~10 min for either option.
- [x] **Run `scripts/backfill-bond-durations.ts` against live DB** ✅ DONE (ran 2026-05-12; verified 2026-06-01): all 5 held bonds now have `duration_years`. (Phase 5 D4 follow-up, 2026-04-30 late night) — script committed but not yet executed against the production DB. `npx tsx scripts/backfill-bond-durations.ts --dry-run` first to preview, then without flag to apply. One-time op; affects scenario rate-shock numbers for held bonds with null `duration_years`.
- [ ] **Verify Phase 5 visual rendering on iPhone** (2026-04-30 late night) — Phase 5 work done in worktree but not visually tested via iPhone-over-Mesh on `:3098` because session ended at commit time. Check: Momentum Pulse tile renders correctly when status is non-neutral (likely currently neutral so won't show on Today — that's expected behavior); Performance sub-view layout at 390px; Income/Yield section at the bottom of Analysis; benchmark picker dropdown on Factor Analysis card; sector beta tooltip expand/collapse on Scenarios card; EmptySection rendering on Options sub-cards (no options held in any account). Worktree dev server `:3098` is the verification target.
- ✅ **EarningsHub privacy leak still open** (carried from Phase 3.5) — closed 2026-05-01 quick-wins sweep (`92f76d6`). NumCell + MobileCard wrap pre-formatted Finnhub-shape strings in `<PrivateText>`.

Closed this session (2026-04-30 late night — visual rollout Phase 5):

- ✅ **Visual rollout — Phase 5 (Analysis re-skin + Performance + Trade Reviews + Momentum Pulse + B2 fixes)** — shipped 2026-04-30 late night (`3550b62` on branch `redesign-rollout`, 26 files +1460/-82). **Streams A-C (additive sub-views):** (A) Momentum Pulse — added MTUM/SPMO/USMV to `BENCHMARK_SYMBOLS`, new `lib/compute/momentum-spread.ts` with `computeMomentumPulse` returning `{leading | neutral | weakening | sell_off | insufficient_data}`, +7 tests, new `MomentumPulse.tsx` Today tile that renders only on non-neutral status. All public market data → `formatPercent()` not `<Pct>` (per `feedback_privacy_market_data.md`). (B) Trade Reviews relocated to `/dashboard/analysis?view=trade-reviews`; `/dashboard/research?view=reviews` now redirects; legacy data-loading branch removed from research page. (C) Performance sub-view at `/dashboard/analysis?view=performance` — TWR + XIRR + period selector (YTD/1Y/3Y/5Y/All) + scope picker + per-account breakdown when >1 account. **Stream D (B2 data-quality):** (D1) `daily-valuation.ts:117` cashBalance=0 audit claim was wrong — Phase 2 already infers cash from monthly snapshot anchors; added clarifying comment so future readers don't trip on it. (D2) FRED DGS3MO risk-free-rate plumbing replaces hardcoded 0.045 in `risk.ts` + `options-greeks.ts`; new `lib/queries/risk-free-rate.ts` with lazy 48h cache in settings table, defensive `try/catch` for in-memory test DBs without settings; new `scripts/refresh-risk-free-rate.ts` ready for launchd. (D3) Per-scope benchmark default + dropdown picker on FactorAnalysis card — Vanguard→VTI, IBKR→QQQ, Roth→SPY, ALL→SPY; factors API already accepted `benchmark` param. (D4) `scripts/backfill-bond-durations.ts` (years_to_maturity × 0.85) replaces the 5y flat fallback for held bonds; ScenarioModeling card gained an expandable details section listing all sector/style/size beta multipliers. (D5) New `IncomeYieldSection` at the bottom of Analysis — TTM dividends + interest + fees + net income + top 10 payers + per-account breakdown; new `lib/queries/income.ts`. (D6) `EmptySection` primitive replaces silent `return null` in OptionsGreeksCard / OptionsStrategies / ExpirationCalendar / FixedIncomeCard with titled empty card + "Why is this empty?" reason. **Re-skin:** all 10 Analysis card components swapped to Phase 3.5 Amber chrome (`bg-panel rounded-xl p-4 sm:p-5 card-elev`); borders auto-drop in light mode via `globals.css`. **Verified:** 1383/1387 tests passing (4 pre-existing B1 failures, baseline; +7 from new momentum tests, no regressions); `next build` clean; type-check clean. **B2 closed** — Phase 5 unblocked + shipped. DMG NOT rebuilt per rollout policy.

Closed this session (2026-04-30 night — visual rollout Phase 6):

- ✅ **Visual rollout — Phase 6 (Accounts re-skin + Holdings table absorption)** — shipped 2026-04-30 night (`82fa8fd` on branch `redesign-rollout`, 6 files +185/-79). New `lib/queries/holdings.ts::getAllHoldings(db)` hoists the cross-account holdings SQL out of the old (now-redirect) `/dashboard/holdings/page.tsx` into the canonical `lib/queries/` location; `AllHoldingsTable` re-exports its row type from there to eliminate the duplicate definition. New `app/dashboard/components/AccountSelector.tsx` (client) renders a pill row at the top of `/dashboard/accounts` with an "All Accounts" entry plus per-account pills; `?id=all` triggers the cross-account view (renders `<AllHoldingsTable>` inside `<section id="holdings">` so `#holdings` deep-links land correctly), per-account remains the default. `AccountDetail` stripped of its internal pill row (now provided by the page so both branches share the same selector). **De-caret pass:** `ReconciliationSection`'s rotating chevron `<polyline>` swapped for literal "Show"/"Hide" text on the right side of the section header — `aria-expanded` retained for a11y. **Holdings redirect retargeted:** `/dashboard/holdings` now → `/dashboard/accounts?id=all#holdings` (was `/dashboard/today` from Phase 2; matches Phase 6 spec). Verified: 1376 vitest passing (4 pre-existing B1 failures, baseline); `next build` clean; agent-browser visual at desktop (1280px) + mobile (390px) confirmed all 6 acceptance criteria PASS — pill row renders + active-state correct, `?id=all` shows 146-position table with working sort + filter, `/dashboard/holdings` redirect lands at `accounts?id=all#holdings`, ReconciliationSection has zero `<svg>` inside the toggle button, mobile `documentElement.scrollWidth === clientWidth === 390` (no page-level horizontal overflow; table scroll lives inside `overflow-x-auto` wrapper), `<SymbolLink>` navigates to Security Detail. Pre-existing dev-mode `Performance.measure` console error on `HoldingsRedirect` was present before Phase 6 (only the redirect target string changed) — not a regression. DMG NOT rebuilt per rollout policy.

Closed this session (2026-04-30 night-late — B1 fix on main + visual rollout Phase 7 + iPhone polish):

- ✅ **B1 — newsletter level extraction + 4 test failures** — closed 2026-04-30 night-late (`c819c55` on `main`, cherry-picked to `redesign-rollout` as `fa61b55`). See ticked entry in "New from this session (open)" block above for full detail. 1380/1380 on main, 1387/1387 on rollout.
- ✅ **Visual rollout — Phase 7 (Alerts auto-promoted stream + Levels review merger)** — shipped 2026-04-30 night-late (`9ad23f8` on branch `redesign-rollout`, 6 files +762/-568). New `<NotificationBell>` replaces the prior split `<AlertsBell>` + `<ReviewBell>` — single bell that polls both `/api/alerts?countOnly=true` and `/api/levels/review?countOnly=true`, shows combined count badge, dual-section hover preview ("Pending alerts" + "Levels to review"), preserves the existing `alert-fired` event so `LevelsPanel` keeps refreshing on count increase. `app/dashboard/alerts/page.tsx` refactored into Suspense-wrapped client component (`useSearchParams` requires Suspense to avoid CSR-bailout at build). 6 filter pills (Pending default / Review / Acted / Ignored / Dismissed / All); Pending merges fired alerts + pending_review levels into one stream split by Today's-activity / Older-pending; Review filter retains the per-source-author grouping UX from the old standalone page. `app/dashboard/levels/review/page.tsx` is now a 3-line redirect to `/dashboard/alerts?view=review`. `AlertsBell.tsx` + `ReviewBell.tsx` deleted (no other importers). Verified: 1387/1387 tests passing; `next build` clean (alerts + levels/review both register as `○ Static`); redirect responds via `<meta http-equiv="refresh" content="1;url=/dashboard/alerts?view=review">`; header HTML emits exactly one bell SVG. DMG NOT rebuilt per rollout policy.
- ✅ **iPhone polish (4 commits on `redesign-rollout`)** — shipped 2026-04-30 night-late after live mobile testing surfaced issues. (1) `77ea0c7` mobile header trim — wrapped `TwsStatus` and `ThemeToggle` in `hidden md:flex` (mobile header was crammed with 8 elements causing "Portfolio Desk" title to wrap to 2 lines; trimmed to Title + Search + NotificationBell + PrivacyToggle + Settings = 6 elements per CLAUDE.md spec). Hidden the giant `font-serif text-2xl` "Today" h1 on mobile via `hidden md:block` (the date label above is enough orientation). Tightened section spacing `space-y-8` → `space-y-5 md:space-y-8`. (2) `4de767f` performance table overflow + first drawer attempt — `/dashboard/levels/performance` table wrapped in `overflow-x-auto` div with `min-w-[720px]` so all 8 columns render at full width and horizontal scroll is contained inside the rounded card; first attempt at drawer fix added inline `style={{ backgroundColor: "var(--panel)" }}`. (3) `edcb2e4` portal nav drawer to document.body — first inline-style attempt was applied (verified in served HTML) but drawer was still translucent on iOS Safari. Root cause: drawer rendered as descendant of `<header>` which has `bg-canvas/80 backdrop-blur-xl`. iOS Safari's treatment of `position: fixed` children inside a backdrop-filter parent caused page content to bleed through regardless of bg-color. Fix: hamburger button stays inline in the header for layout, but backdrop + drawer panel now portal to `document.body` via `React.createPortal` (matches `ChatDrawer` / `WelcomeOverlay` pattern). Z-indexes bumped backdrop `z-[60]`, drawer `z-[70]`. CSS variable fallback: `var(--panel, #ffffff)` so the drawer falls back to hard-coded white if the variable cascade ever fails. (4) `be610c9` swipe-left-to-close gesture — iOS-native finger-following dismiss on the mobile drawer. `onTouchMove` applies inline `translateX(dx)` with `transition: none` for 1:1 finger tracking (inline transform wins specificity over Tailwind translate-x-0/full classes). On touchend: dx ≤ -50px → `close()` (Tailwind animates dismiss); else `setDragX(0)` → snaps back. Guards: only treats as drag if `|dx| > |dy|` (lets vertical scrolls pass through) and `dx < 0` (rightward ignored); capped at -256px (drawer width). User confirmed all four fixes live on iPhone at `100.96.0.1:3098`.

New from this session (open):

Closed this session (2026-05-03 — briefing prompt escalation + email redesign):

- ✅ **§6 verbatim — pre-rendered cluster injection** — shipped 2026-05-03 (`6c7a766`, 4 files +514/-16). Last week's `f02517c` HARD RULE directive ("MUST equal the symbols in the Holdings exposure field") wasn't enough — 4/27 briefing still dropped CLH/GFL/PRIM/XMTR from ISM Manufacturing and added PWR + a "VIS basket" that wasn't in the deterministic field. Two structural fixes: (1) Pre-render the cluster line `**Holdings exposed:** SYM1, SYM2, ...` in TS as a "REQUIRED §6 cluster (paste this exact line)" field per macro event in `formatEventForPrompt`. Verbatim guarantee no longer relies on Opus's restraint. (2) Rewrite the §6 directive into 5 numbered HARD RULES (verbatim cluster, no multi-event lumping, cluster-after-prose, trust-the-data-over-memory, skip-if-no-mapping). **Live verified 5/04 regen: 8/8 PASS** on the verifier across all macro events with deterministic exposure (Housing Starts 8 names, ISM Services 33 names, JOLTS/ADP/Claims/NFP 11 each, UMich Sentiment 12). Updated regression test in `tests/calendar/briefing-macro-exposures.test.ts` to assert the new directive language + the cluster-injection field.
- ✅ **Email redesign — light Amber theme + reader-app spacing for all four outbound surfaces** — shipped 2026-05-03 (`b2201cc`, 2 files +318/-199). `lib/calendar/briefing-html.ts` is the single renderer for Sunday briefing + daily digest + earnings preview + earnings recap, so this redesign updates every outbound email in one shot. `workers/cron/src/html.ts` mirrors for cloud-fallback parity. **Design** — modeled on Research Feeds reader (`.prose-reader` / `.prose-newsletter` in `app/globals.css`): cream canvas (`#fafaf3`) as body bg (no white card — body sits directly on cream like reading on warm paper), 18px / 1.7 line-height in `--ink-dim` (`#2a2a26` softer than full black), 22px paragraph margins for airy spacing, 680px wrapper (~65ch), hero title in 28px amber gold, section `##` headers in ink-black with hairline edge underline, tiny `PORTFOLIO DESK` amber strip is the only chrome above body, date moved to footer. **Font stack** — `'IBM Plex Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif` and `'IBM Plex Mono', 'SF Mono', Menlo, Consolas, 'Courier New', monospace`. Plex first for users who have it, system fallback everywhere — Outlook → Segoe UI, Apple Mail → SF Pro, Gmail Android → Roboto. **NO webfont fetch**, so the user's brother on Outlook never breaks. Earnings scoreboard tables (locked 2026-04-28 for fill-by-hand printability) preserved as-is. **CRITICAL gotcha now in file header**: font-family names with spaces MUST use single quotes inside the double-quoted style attribute — mixing double quotes silently truncates the style attribute at the first inner quote, dropping every property after it (ate ~30 minutes today on "why isn't the color landing"). 229 calendar/digest/workers tests still passing post-change. Today's 3pm cron will still send the OLD dark theme (Electron DMG uses bundled pre-rebuild code); new design lands on next DMG rebuild via `/session-end`.
- ✅ **`scripts/verify-briefing-content.ts`** — new canonical verification harness for any future manual briefing regen. Runs 5 checks: §6 cluster verbatim diff per macro event, §3 earnings combined-position coverage, A1 price-hallucination scan (with smart filters for option strikes / EPS / dividends / comma-separated thousands), A5 self-admission regex sweep, A8 thesis-fingerprint check. Exits 0 on PASS, 1 on FAIL. Run via `npx tsx scripts/verify-briefing-content.ts --weekOf YYYY-MM-DD`. Companion `scripts/check-held-earnings.ts` rolls up held positions for a week's earnings event list (uses `issuerSiblings()` so dual-class siblings + options surface correctly).
- ✅ **5/04 briefing pre-generated + verified clean** (live, no commit). Pre-fired the briefing 2026-05-03 17:47:03 ($0.65 Opus call); verifier reports 8/8 §6 PASS, 19/20 §3 effective PASS (the one "FAIL" is a verifier false flag — Opus said "across 3 accounts" instead of naming each, which reads better), all stock-price citations match latest close to the cent (SPY $718.66, QQQ $667.74, INTC $94.48, etc.), no self-admission language. `isBriefingStale=false` so the 3pm cron will pick up this pre-gen. Briefing artifact saved to `~/Desktop/briefing-2026-05-04-FINAL.html`.

Closed this session (2026-05-05 night-end — Security Detail IA, research feed cleanup, background sync, earnings reaction + position fixes):

- ✅ **Security Detail page light theme below MarketDataPanel** — flipped 13 sections + 5 child components (`page.tsx`, `RecentAlertsPanel`, `TransactionsSection`, `ResearchDocumentsPanel`, `ResearchMentionsSection`, `CorporateActionsSection`) from terminal primitives (`TerminalSection`/`TerminalTH`/`TerminalTD`/`TerminalTag`) to the canonical light card pattern. New `app/dashboard/components/Section.tsx` primitive that mirrors `TerminalSection`'s API with light tokens (`rounded-xl border border-edge bg-panel`, Plex Mono uppercase title, `text-ink/dim/faint`). `MarketDataPanel` (chart + hero + LevelsPanel) intentionally stays dark — that boundary is the whole point. `<Chip>` covers all `TerminalTag` use cases (LT/ST term markers, grade letters, sentiment, impact, transaction-type pills). Browser-verified at NVDA: dark module on top, light cards below, zero console errors. Other terminal primitives untouched (`TerminalSection.tsx` kept for `MarketDataPanel`'s own use).

- ✅ **Newsletter HTML readability normalizer + backfill** — `lib/gmail/sanitize.ts` gains `normalizeNewsletterHtml(html)` as a 6-pass cleanup that runs after the existing security sanitizer: strip leading preheader (zero-width chars + nbsp + soft-hyphen runs that survive `style="display:none"` removal), strip publication chrome ("View this email in browser" / "Web version"), collapse whitespace-only block wrappers iteratively, collapse runs of `<hr>`/`<br>`, drop "Read more"/"View online" link-only CTA paragraphs, unwrap single-cell layout tables (negative-lookahead guards against unwrapping multi-row data tables), trim trailing structural orphans. Pure regex + char-code (no DOM library, no new deps). **Also fixed two pre-existing bugs in `sanitizeNewsletterHtml`** that surfaced under re-application: (a) `escapeAttr` was double-escaping `&amp;` to `&amp;amp;` on each pass — now uses negative lookahead `&(?!(?:amp|lt|gt|quot|apos|#\d+|#x[0-9a-fA-F]+);)`; (b) `trimEmailFooter`'s tail cleanup was eating closing tags, breaking balanced HTML — now narrows to whitespace + standalone `<br>`/`<hr>` + orphan opening tags only. Wired into both `lib/gmail/fetch.ts` write paths (initial fetch + `backfillArticleHtml`). Tightened `prose-newsletter` CSS (`hr { margin: 0.75em 0 }` was 1.5em, `p:empty { display: none }`, `img { max-width: min(100%, 600px) }`, `table { font-size: 14px }` was 13px). New `scripts/backfill-newsletter-html.ts` (idempotent — skip-if-unchanged + transaction-wrapped); ran against 543 articles and cleaned 400 with ~8% total byte reduction (individual articles 13–73%). 28 new tests in `tests/gmail/sanitize-normalize.test.ts`. Browser-verified two real articles (Purple Drink + BEP Research): zero hrs, zero empty `<p>`, zero layout tables, zero "View in browser" headers; real data tables intact.

- ✅ **Background research-feed sync — 90-min market hours + on-focus polling** — closes the "research feeds only refresh during the morning digest cron" gap. Three pieces. (a) **`POST /api/cron/research-sync`** — X-Cron-Secret-gated (mirrors `/api/cron/digest` pattern); runs `fetchNewArticles` + `processUnprocessedArticles` + `extractLevelsFromNewArticles` only, no email send, plain-JSON response. (b) **`scripts/run-research-sync.sh` + `~/Library/LaunchAgents/com.vanguard-skin.research-sync.plist`** — StartInterval=5400 (every 90 min), self-gates Mon-Fri 09:00-19:00 ET (the 19:00 ceiling pre-empts a planned 7pm digest cron — see new TODO below). Hits `:3099` (Electron) first, falls back to `:3000` (dev). plist loaded via `launchctl bootstrap` and confirmed in `launchctl list`. (c) **`lib/hooks/useResearchSync.ts`** — fires once on Research-tab mount + on `visibilitychange` after 10+ min idle; debounced once-per-5-min via `vgs:lastResearchSync` localStorage. Wired into `ResearchFeedsView.tsx` with the existing `setSyncStatus` "Refreshing in background…" pill. Combined effect: launchd keeps articles fresh during market hours when app is closed; in-app polling keeps iPhone-via-mesh fresh whenever user opens the app. AI cost increases ~$0.10–0.20/month (article count drives cost, not call frequency). Endpoint auth verified (401 without secret, 2xx with).

- ✅ **Mac-side Yahoo fallback for reaction capture** — closes a silent gap in `lib/calendar/enrichment-runner.ts` where the runner only attempted `captureReactionFromTws`; if TWS was unavailable, `reaction_snapshot` stayed NULL forever (the Worker was the only Yahoo path, and the Worker hasn't always been redeployed promptly). Now: Mac tries TWS first (better data quality), falls back to `captureReactionFromYahoo` from the same module the Worker + backfill script use. Diagnosed via DB query — six recent earnings events (5/5 ET/OCUL/PRIM/SHOP, 4/29 GFL, 4/24 NSC) had NULL `reaction_snapshot`; backfilled all 6 via existing `scripts/backfill-reaction-from-yahoo.ts` so SHOP recap will reference the actual reaction (-2.56% / SPY +0.22% / QQQ +0.47%). 181 calendar tests pass. Side benefit: removes urgency on the lingering Worker `wrangler deploy` since the Mac no longer depends on it for reaction capture.

- ✅ **`purgeClosedOptionHoldings` mutation — sister to `purgeExpiredOptionHoldings`** — `lib/mutations/closed-positions.ts`. Statement imports never zero-out positions that disappear from a later snapshot, and TWS auto-refresh only writes ACTIVE positions (TWS doesn't return zero-quantity entries) — so options closed via SELL_TO_CLOSE before expiry can linger in `holdings` indefinitely until expiration. New mutation deletes (account, security) holdings rows where the cumulative transaction history sums to zero (BTO+BTC vs STO+STC, with `COUNT(*) > 0` guard against empty histories). Wired as Step 1.6 of TWS auto-refresh (right after `purgeExpiredOptionHoldings`). Companion `scripts/purge-closed-options-once.ts` (mirrors `purge-expired-options-once.ts`) ran against live DB and purged 3 stale rows: SHOP $180c (Jan 2027 expiry, sold 3/30 — the original user-reported case), PSTG $105c, INTC $50c. 7 unit tests covering: long fully closed, partially open preserved, short-then-bought-back closed, still-open longs preserved, non-options preserved, scoping to (account, security) pair, no-history defensive guard. Tests 1513 → 1520.

- ✅ **Mandatory `## Guidance` section in earnings recap prompt** — `lib/digest/send-earnings-email.ts::renderRecapPrompt`. Was implicit before (buried in line-by-line table + a casual "for guidance change details — use web_search" mention). Now a dedicated, mandatory section with explicit web_search asks for full-year guide, next-quarter guide, segment guides, with structured shape (prior → new → change implies). AI must explicitly say "No updated guidance issued — prior guide stands" rather than silently omit. Will surface in the next live recap.

- ✅ **In-app generate-recap-now button + `/api/earnings/recap-modal`** — solves the "back-to-back 4:01/4:05/4:11 prints want immediate recap not 15-30 min wait" use case. Refactored `sendEarningsEmail` to extract a `composeEarningsEmail(db, eventId, phase, opts)` helper that returns `{ html, markdown, title, symbol, promptHash }` without sending email or writing audit row. New `POST /api/earnings/recap-modal` (in-app, no cron auth) optionally runs single-event enrichment (`runEnrichment(db, { eventId })` — TWS-less so Yahoo fallback covers reaction), then composes the recap, returns rendered HTML + markdown. New "gen" gold-tinted button on `EarningsRowChips` shows for every pending recap row (hidden when sent ✓ or skipped); click triggers the chained enrich+compose+display flow. `EarningsEmailViewer` extended with optional `inlineData` prop that bypasses the existing `/api/earnings/email-content` GET when the modal opens with fresh-composed content. Browser-verified: 34 buttons visible across pending recap rows, 0 console errors, layout clean. Same behavior as the email-send path — preview shape is byte-identical to "what the email would look like" — but no email, no audit row. AI cost per click: ~$0.05 + 30-60s wait.

- ✅ **Worker `wrangler deploy` re-shipped for resilience** — version `628571fb-76aa-441a-a519-19a75e79481a` (replaces `2dba8e8c` from 5/3). All 5 cron schedules re-registered (Sun 3pm summer + winter, weekday 8:45am summer + winter, every-15-min calendar enrich); bindings preserved (CRON_KV, ARCHIVE R2, EXPECTED_HOUR_*, PRIMARY_TIMEOUT_MS). Cloud Yahoo path (`workers/cron/src/yahoo.ts` with `&includePrePost=true`) now confirmed live. With the Mac-side Yahoo fallback in place, both Mac and Worker paths now have working pre-market / after-hours bar capture for BMO / AMC events.

New from this session (open):

- [ ] **Verify Mac-side Yahoo fallback fires correctly on next live earnings event** — added 2026-05-05. Verified via tests + dry-run, but not yet via a real reaction capture in the runner's normal flow. Watch the next event whose enrichment fires while TWS is offline; expect `reaction_snapshot.source = "yahoo"`. Should be self-evident from the next NULL-or-yahoo distribution check.
- [ ] **Verify generate-recap-now button works end-to-end on a live event** — added 2026-05-05. Browser shows the button placement; haven't actually clicked it on a real event yet (would cost ~$0.05 Anthropic + 30-60s). Try it on tomorrow's earnings night — gold "gen" button next to `pre`/`rec` chips on EarningsHub rows.
- [ ] **Verify `## Guidance` section actually fills in on next live recap** — added 2026-05-05. Prompt edit only, no data side. Watch the next 2-3 recap emails: section should appear with full-year + next-quarter + segment guides, or explicitly say "No updated guidance issued."
- [ ] **Second daily email at ~7pm ET (planned)** — user strongly considering. Architecture: new launchd plist mirroring `daily-digest.plist` schedule (weekday 19:00 ET) calling a new `/api/cron/digest-evening` (or extending existing with a `slot` body field). The 19:00 ceiling on the new background research-sync schedule pre-empts this so the evening email gets fresh AI-processed articles. Cross-cuts with the existing "Morning digest synthesis" + "Digest sort toggle" entries — pick a delivery surface for evening that matches the morning's chosen layout. ~2 hr including the cron route + plist + Worker mirror.

Closed this session (2026-05-01 — quick-wins sweep):

- ✅ **EarningsHub + Chat privacy leaks** — shipped `92f76d6` (2 files +25/-8). EarningsHub `NumCell` (desktop grid) and `MobileCard` (stacked rows) wrap `cons.eps` / `act.eps` / `cons.revenue` / `act.revenue` in `<PrivateText>`. Chat got a new `PrivateMarkdown` block-safe component for assistant messages (avoids `<p>`-inside-`<span>` invalid HTML); user messages wrap in `<PrivateText>`. agent-browser dev-server verification: 4/4 tests pass, 0 hydration warnings, 0 console errors. iPhone B1+B2 mobile-card verification deferred.
- ✅ **Caret affordance + bg-panel iOS audit** — shipped `790d317` (4 files +20/-6). `TabDropdown.tsx` swaps `▾` for `•••`, replaces rotation with gold open-state color (matches `feedback_no_carets.md` "standard '•••' overflow icon"). 4 header popovers (`DataConfidenceIndicator`, `TwsStatus`, `NotificationBell` hover, `TabDropdown` menu) get defensive inline `style={{ backgroundColor: "var(--panel)" }}` — same recipe as `4de767f`'s MobileNavDrawer fix, applied to every `bg-panel` surface that descends from the header's `backdrop-blur-xl` stacking context. iPhone B4 verified pass (the popovers themselves are desktop-only; visible mobile surfaces show no regression). 1387/1387 tests pass on both commits.

New from this session (open):

- [ ] **Phase 5 iPhone visual verify — A1 / A2 / A3 / A4 / A5 + privacy mobile-card B1 / B2 + dark-mode B5** — deferred from this session's quick-wins sweep (B4 popover-opacity passed). Walkthrough lives in `~/.claude/plans/can-you-pull-up-graceful-treehouse.md` Part A + B. Routes via Mesh `100.96.0.1:3099` (post-DMG-rebuild) or `:3000` (dev). ~20 min total when next at Mac + iPhone.

Closed this session (2026-04-30 night-end — visual rollout Phase 8 + merge to main):

- ✅ **Visual rollout — Phase 8 (brand sweep + font sweep + design-preview deletion + docs)** — shipped 2026-04-30 night-end (`6909e4c` on `redesign-rollout`, merged into `main` as `a800884`). 80 files changed (+28 / −7560 — design-preview deletion accounts for the deletion mass). **Brand sweep:** `lib/compute/tax-report.ts:274` TXF identifier, `app/dashboard/components/CanonicalCsvGuide.tsx:121,144` both prompt builders, `docs/canonical-csv-guide.md` — preserved the `A` TXF control prefix. **Font sweep:** 8 production `font-serif` references → `font-medium`; `Instrument_Serif` import + variable + className removed from `app/layout.tsx`; `--font-serif` token removed from `app/globals.css`. **Design-preview deletion:** entire `app/design-preview/` directory removed (74 files: dark, refined, light-paper, modern-brokerage, terminal, options, today-options, shared) — locked Amber tokens already live in production at `app/globals.css` + `/dashboard/today`. **Dead code:** orphan `app/dashboard/holdings/loading.tsx` deleted (page is now a redirect, no skeleton flash needed); other cut-tab redirect stubs (calendar, holdings, levels/review) kept as 5-line stubs to cover external bookmarks. **Docs:** `CLAUDE.md` Tab Structure + Mobile Responsive sections refreshed for the 6-tab IA + Notes mobile slot + unified Alerts inbox + theme toggle + NotesAmbient overlay + Cmd+K ticker-jump; `docs/DECISIONS.md` rollout entry; `memory/project_holistic_redesign.md` flipped status to "SHIPPED" with Phase 8 closeout section. **Inherited clean (no edits):** Import + Charts pages (server components delegating to children that already use Amber tokens); earnings preview/recap email composer (uses Phase 4's `briefingToHtml`); mobile `pb-safe`/`viewport-fit` baseline. Verified: 1387/1387 vitest passing on both branches; `next build` clean; merge-conflict on CLAUDE.md resolved by keeping main's richer "(newsletter articles + uploaded PDFs)" parenthetical and rollout's stale-Holdings-line removal. **DMG rebuild kicked off via `/session-end`** (electron:deploy in background; rebuilt from main post-merge).
- ✅ **Merge `redesign-rollout` → `main`** — 19 commits ahead, 8 behind; non-fast-forward merge commit `a800884`. Single CLAUDE.md conflict resolved (kept "(newsletter articles + uploaded PDFs)" + dropped stale Holdings line). Worktree at `~/code/vanguard-skin-redesign-worktree/` retained for now — user can `git worktree remove` + `git branch -d redesign-rollout` next session if desired (not auto-removed because pruning a worktree is a destructive operation requiring per-action confirmation).

New from this session (open):

- ✅ **In-app config UI for `FEATURE_MODELS`** — stale duplicate; CLOSED 2026-06-09 (see the ticked entry in the 2026-06-09 closed block above: `feature_model_overrides` + `AiModelsSection`). Drift caught by the 2026-06-11 session-end reconcile. The "audit other env-var-only configs" sub-idea remains future work.
- ✅ **Tailwind v4 `bg-panel` opaque-rendering investigation** — closed 2026-05-01 quick-wins sweep (`790d317`). Audit identified 4 latent siblings in `app/dashboard/components/` that descend from header's `backdrop-blur-xl` stacking context (DataConfidenceIndicator, TwsStatus, NotificationBell hover, TabDropdown menu); patched each with defensive inline `style={{ backgroundColor: "var(--panel)" }}`. iPhone B4 spot-check passed (no regression on the surfaces visible on portrait). Underlying Tailwind v4 token-resolution question remains unconfirmed — the fix is defense-in-depth (matches the pattern other surfaces will need if they end up in a similar stacking context), not a definitive root cause.

---

## Roadmap / future (flagged 2026-04-25 late night)

Larger multi-session items the user wants captured:

- ✅ **IA session — pre-redesign architecture lock** — completed 2026-04-29 (decisions at `~/.claude/plans/ia-decisions.md`). 30-min run-through resolved all 7 structural items: 9→6 desktop tabs (cut Overview / Holdings / Calendar), Performance + Trade Reviews → Analysis sub-views, Notes home + ambient overlay + mobile slot, Alerts unified single auto-promoted stream, chat = persistent right rail (desktop) + overlay (mobile), Cmd+K → global ticker-jump, mobile bottom nav = Today / Research / Chat / Notes / Analysis. Cross-cutting rules locked + app rename to **Portfolio Desk** (domain `myportfoliodesk.com` purchased same day). See "Closed this session (2026-04-29)" above. **Gate is open — visual rollout Phase 1 can proceed in next session.**
- [ ] **iPad parity** — can the app be used on iPad the same way as on iPhone? Unknown effort. Likely small if the mobile breakpoints already hold at ~1024px and Cloudflare Mesh routes the iPad, but there might be layout regressions at the in-between tablet width (768-1024px) where `md:` kicks in but the screen isn't desktop-sized. First step: load the app on an iPad via Mesh and identify what breaks before scoping work.
- [ ] **Design overhaul — light mode default, keep "Midnight Portfolio" as dark mode** — multi-session redesign. User has said repeatedly they don't love the midnight-blue dark palette. Plan: preserve the current dark palette under a `dark` class and introduce a new "light but not white" default palette (the `MarketDataPanel` + `TerminalSection` primitives are already scoped to stay dark regardless of outer theme, so the embedded data-module pattern becomes the anchor — see `memory/project_market_data_panel.md`). Requires: Tailwind v4 `@theme` duplication for both modes, `prefers-color-scheme` + settings toggle, a full audit of every hardcoded hex (chart colors, chip tones, Recharts palettes, PDF/email templates) to survive the mode flip. Scope in a design doc before any code.
- [ ] **OpenAI voice API integration** — scaffolding started in Claude Co-Work. Two directions: (a) **output/read-aloud** — weekly briefing email read aloud, per-company deep dives narrated on Security Detail, maybe alert triage by voice; (b) **input/voice bug reporting** — in-app record button that captures the user describing a bug or UI gripe, transcribes via Whisper/Realtime, and files the result somewhere structured (GitHub issue? `docs/plans/BUGS.md` append? a new `bug_reports` table?). Decide capture destination + whether to also capture a screenshot at the moment of recording (likely yes — bug reports without visual context are weak). Scope: design doc + capture flow + first surface (probably Today view "speak the briefing" button as the lowest-risk first shot).
- [ ] **Privacy mode as default + Face ID / biometric unlock** — currently `vgs:privacyMode` defaults off and the eye-icon toggle flips freely. Want: default ON everywhere (mobile first, possibly desktop Electron too), and unlocking real numbers requires Face ID (iOS WebAuthn `navigator.credentials` or native Electron biometric prompt). Pieces: (1) flip `PrivacyProvider` default to `true` + migrate existing localStorage to opt-in masking; (2) gate the toggle behind a WebAuthn challenge on mobile (iOS Safari supports platform authenticators); (3) Electron side uses `Touch ID` via `node-mac-auth` or equivalent; (4) decide the auto-lock policy (re-mask after N minutes idle? on every app focus?). Potential cross-cut with the voice-API "deep dive" feature — voice output reading real numbers aloud should respect the same unlock state. Scope in a design doc; biometric plumbing on both iOS-Safari-over-Mesh and Electron-on-macOS is the risk surface.

---

## AI Gateway phases

- ✅ **Phase 1** — AI Gateway routing (commit `8c82c1e`)
- ✅ **Phase 2** — Workers AI downroute: alertSuggestion → Llama 3.3, newsletterLevelExtraction → Kimi K2.6 (`264bc73`)
- ✅ **Phase 3** — R2 PDF archival (`264bc73`, migration 033)
- ✅ **Phase 4** — Workers Cron hybrid shipped 2026-04-21 (`16ba5a7` Sessions A+B, `3e2c7af` Session C). Live at vanguard-skin-cron.isaac-3d1.workers.dev. Design doc: [2026-04-21-workers-cron-hybrid.md](2026-04-21-workers-cron-hybrid.md).

---

## Backlog themes (from off-repo roadmap)

- **Theme D — Level source performance attribution** (blocked on data): hit-rate + P&L by `source_author`. Wait until ~30+ alerts have fired.
- ✅ **Theme E — Chat broader company data** complete: E1 press releases (`272566d`), E2 full 8-K body (`6328a99`), E3 EDGAR 10-K/10-Q, E4 analyst coverage (`1bf65d3`) all shipped 2026-04-22.
- ✅ **Theme F1 — Research PDF knowledge base** shipped 2026-04-22 (migration 035, commit `6624382`).
- ✅ **Theme G — Chart entry/exit signals v1 + narrative layer** complete: pivot S/R shipped 2026-04-22 (`51fcc51`), Haiku one-sentence narratives shipped 2026-04-23 (`25016cf`, migration 040).
- ✅ **Theme I — Chat history persistence**: shipped across 2026-04-21 + 2026-04-22.
- **Theme J — Cleanup track** (partial): J1 ✅ + J3 ✅ shipped 2026-04-21. J2 (E2E browser tests, ~15 hr) deferred.
- **Theme K — Options Phase 2 end-to-end verification**: blocked on the user holding more than 1 option position at once.

---

## Deferred

- **E2E browser tests** (Theme J2, ~15 hr) — dedicated session.
- **Electron code signing** — `xattr -cr` workaround accepted for local-only distribution.
- **Finnhub surprise history** — free tier returns `[]`; revisit only if briefings feel thin.

---

## Known issues

- **WSH API error 10276** — "News feed is not allowed" (2026-04-07). May need TWS subscription time.
- **Holdings cost basis column "–" in Accounts tab** — data gap, not a code bug (tooltip explains).
- **`next build` data collection** fails with an existing `data/vanguard.db` on disk. TypeScript compilation still succeeds. All 10 DB-loading pages are `force-dynamic`, so this only affects build-time collection, not runtime.

---

## Active design docs

- [2026-04-15-cloudflare-saas-rewrite.md](2026-04-15-cloudflare-saas-rewrite.md) — SaaS v3 scoping (D1, TWS bridge, multi-tenant, ~7 months)
- [2026-04-21-mobile-today-view-decision.md](2026-04-21-mobile-today-view-decision.md) — shipped
- [2026-04-21-workers-cron-hybrid.md](2026-04-21-workers-cron-hybrid.md) — Phase 4, shipped 2026-04-21
