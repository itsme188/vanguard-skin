# Vanguard Skin — TODO

> **In-repo shortlist.** Updated 2026-05-03 (briefing prompt escalation + email redesign session).
>
> - v2 build log (Mar–Apr 2026): [archive/TODO-v2-complete-2026-03-30.md](archive/TODO-v2-complete-2026-03-30.md)
> - Master roadmap (off-repo, session-driven): `~/.claude/plans/last-session-session-summary-eventual-ripple.md`
> - Live topic index: the Claude memory at `MEMORY.md` (per-project auto-memory)

---

## Open roadmap (themed)

> Consolidates open items as of 2026-04-30 night. The chronological session log below is the audit trail — entries here cross-reference it where useful.

### Bugs / Quality

- **Trade-vs-settlement date mismatch** (2026-04-30) — sold PSTG 3/30; cash didn't reflect until April. April Vanguard statement not yet imported. Decide how to represent in-flight trades: (a) visual chip on the position until next statement reconciles, (b) reconcile only against settled cash, (c) accept the drift as small + self-correcting. Cross-cuts with the IBKR April gap below.
- **Earnings transcripts panel — visual mess + mostly broken** (2026-04-30) — transcripts under Security Detail / Notes feed render poorly AND most API Ninjas pulls come back empty or broken. Audit which surface renders transcripts, fix layout, then investigate the upstream gap (API Ninjas free-tier? current-quarter blackout? a parser bug?).
- **Eli email landed in junk** (2026-04-30) — Resend deliverability follow-up for at least one recipient. The 2026-04-29 smoke test was clean for `isaac@wolfsonfamily.com` + `esafier@woodlotcapital.com` (`dkim=pass`, `spf=pass`), so this is either a different recipient or a regression on ongoing sends. Investigate: warming state on the `myportfoliodesk.com` IP, list-unsubscribe header presence, subject-line patterns flagging Bayesian filters.
- ✅ **EarningsHub privacy leak** — shipped 2026-05-01 quick-wins sweep (`92f76d6`). NumCell + MobileCard wrap Cons.EPS / Act.EPS / Cons.Rev / Act.Rev in `<PrivateText>`. Symbol / status chip / slot / delta stay visible (per public-vs-portfolio split).
- ✅ **Chat message renderer privacy leak** — shipped 2026-05-01 quick-wins sweep (`92f76d6`). New `PrivateMarkdown` component wraps assistant messages (block-safe — bare `PrivateText` would put `<p>`/`<table>` inside a `<span>`); `PrivateText` wraps user messages (plain text inside `<div>`). Verified on dev server: full message bodies mask to `•••` under privacy mode, no hydration warnings.
- **IBKR April 2026 transactions gap** (carried) — waiting on the April monthly statement; will import normally when it arrives.
- **Today view: IBKR holdings prices go stale, no manual refresh** (2026-05-02) — `/dashboard/today` is a server component that reads `prices` table via ROW_NUMBER CTE. Background `useAutoRefresh()` (30 min cadence) is wired into the Electron app shell, not the page, so navigating to Today before the timer fires shows stale data. Add a manual refresh button on the IBKR holdings block — pattern follows `EarningsHubRefreshButton.tsx`: client island, `POST /api/tws/auto-refresh { level: "quick" }`, `router.refresh()` on success. Live streaming (`/api/tws/stream`) is also implemented and can snapshot-to-DB on demand, but a button is the smaller and more predictable win — defer the live-stream-on-Today decision until we see whether the manual button feels enough.
- **Earnings recap data gap — `reaction_snapshot` NULL on shipped recaps** (2026-05-02, surfaced during read-throughs scoping) — LIN (event 2026-05-01) and HUN (event 2026-04-30) recap emails went out with `reaction_snapshot = NULL`, so the scoreboard's three reaction rows (Stock @ T+2h, SPY, QQQ) rendered as `—` and Sonnet's "## The reaction" section had no anchor data. Either (a) `captureReactionFromTws` failed silently, (b) the `MAX_AGE_MS_EARNINGS` 12h window expired before TWS was reachable, or (c) the runner skipped these rows for some other reason. Investigate `enriched_at` + `release_time` for both events; check if cloud-fallback Yahoo path could have filled them; backfill manually via `POST /api/calendar/enrich { eventId, upgradeReactionToTws: true }` if bars are still in TWS retention. **Note: this also blocks the read-throughs feature below** — read-throughs need the reaction signal to be present, so address before the read-through composer integration ships.
- **Stale TLT 4/24 $86.50 call still in `holdings`** (surfaced 2026-05-03 via 5/04 briefing inspection) — option expired 9 days ago but the position row is still present. The 5/04 briefing's §4 correctly flagged it (`"TLT 4/24/26 $86.50 call (already expired per the date)"`) but the underlying data hygiene gap means future emails will keep mentioning it. SQL fix: `DELETE FROM holdings WHERE security_id = (SELECT id FROM securities WHERE symbol = 'TLT   260424C00086500');` after confirming via `SELECT * FROM holdings WHERE ...`. Or broader: a launchd / cron sweep that auto-clears option positions whose `option_expiration_date < date('now')`. ~10 min for the one-row fix; ~30 min for the auto-sweep.
- **Verify new email design renders cleanly in real Outlook + Gmail clients** (added 2026-05-03 with `b2201cc`) — the design was tested in Safari only. After the next DMG rebuild, the first weekly briefing in the new theme ships to the user's brother (Outlook) and any Gmail recipients. Spot-check both: confirm font fallback to Segoe UI (Outlook) / SF Pro (Apple Mail) / Roboto (Gmail Android) reads cleanly, no broken styles, scoreboard tables still print white-on-cream as intended, no Outlook-specific layout regressions (e.g., 680px wrapper rendering at full width, inline `border-radius` rounded corners may degrade to square — that's expected and acceptable). Action: ask brother + check own Gmail web after first send. If anything broken, fix before locking in.

### Earnings emails

- **Per-ticker BMO/AMC time overrides** (2026-04-30) — user reports AAPL preview sent before 4:30 ET when AAPL reports at 4:30. Confirmed times: AAPL=16:30, AMZN=16:01, GOOGL=16:01, META=16:05, MSFT=16:05. Today's logic in `lib/calendar/release-times.ts` routes through `earningsHourToReleaseTime(BMO|AMC|DMH)` only. Add a new `EARNINGS_RELEASE_TIMES: Record<string, string>` symbol→time map, consult it BEFORE the BMO/AMC default. **Must mirror in `workers/cron/src/fallback-earnings.ts`** so cloud fallback doesn't regress to 16:15 — same class of bug as `WORKER_DIGEST_MODE` (see `memory/project_worker_digest_mode.md`).
- **Per-event preview/recap one-off skip** (2026-04-30) — current toggles are global enable + per-symbol mute (in `EarningsEmailsSection`). Add an in-app "skip this one" affordance on EarningsHub rows for events the user doesn't want noise from without permanently muting the symbol. Default stays on-send. Likely an additional column on `earnings_emails` or a separate `earnings_email_skips` table keyed on `(event_id, phase)`.
- **Morning digest synthesis** (2026-04-30) — replace N per-source summaries with one cross-source digest grouped by company / topic. Today: each newsletter gets its own block. Goal: single coalesced view (per-company section when multiple sources mention the same name; per-topic section for macro themes). Touches `lib/digest/send-digest.ts` prompt + likely a new pre-Claude bucketing pass.
- **Digest sort toggle — by publication vs by company** (2026-05-02) — user wants to flip the morning digest between the current per-source layout and a per-company layout. Email clients can't render JS toggles, so the "switch" has to live somewhere else. Three candidate deliveries: (a) a Settings preference (`digest_layout: "by_source" | "by_company"`) the next email respects, (b) an in-app digest viewer modal with client-side toggle (mirrors the `<EarningsEmailViewer>` pattern), (c) compose both views back-to-back in one email with anchor links. Cross-cuts with the existing "Morning digest synthesis" entry above — the by-company layout is essentially that ask shipped as one of two views. Pick a delivery surface in design before building.

### Page-level rework

- **Analysis page deep dive — continued** (2026-04-30) — Phase 5 closed the structural gaps but user wants to keep iterating: factors UX, classification pie chart polish, factor exposure surfacing. Coordinate with the ChatGPT collaboration thread (see Reminders).
- **Notes area rework — expanded context** (2026-04-30, Phase 4 follow-up) — journal should be reserved for **market & trading psychology** (how I feel about the market, how I'm trading), with a separate destination for **non-earnings stock-specific thoughts** (position notes, thesis updates, "why I'm watching this" jots). Earnings stays earnings. Likely a new note type (`stock_thought`?) or per-security notes panel. Open question parked from Phase 4: should NotesAmbient's "Save to Notes" route the user to a full notes page for the just-saved entry?

### New features

- **Stock-to-stock read-throughs** (2026-04-30; scope sharpened 2026-05-02) — when stock A reports or has news, surface implications for held stocks B/C/D. Likely surfaces: a new chat tool (`query_implied_reads`), a Today post-earnings sidebar block, or an "Implied reads" panel on the reporter's Security Detail page. Needs scoping in a design doc — model choice, how to source the relationships (sector/factor cosine? co-mention frequency in newsletters? user-curated pairs?), and what triggers the read-through computation.
  - **Concrete examples (added 2026-05-02 from user):** Two flavors of read-through:
    - **Directed pairs** (1 reporter → 1 target): PRTO (Proto Labs) earnings → updates XMTR (Xometry) preview; HUN (Huntsman) earnings → updates LIN (Linde) preview.
    - **Clusters** (any member's report updates every other member): the **ad-platform cluster** = {GOOGL, META, APP, RDDT, …}. When any one prints, the others' previews should reflect it.
  - **Trigger model the user wants:** beat-vs-consensus delta + price reaction, both already populated in `calendar_events` after enrichment (no new data sources needed).
  - **Schema recommendation:** hand-curated `read_through_pairs (id, reporter_symbol, target_symbol, hypothesis, weight, created_at)` directed table; clusters seed as N×(N−1) directed-pair rows via a small script (avoids a separate `clusters` table for MVP — re-evaluate if clusters get >6 members).
  - **Surface recommendation (highest-value):** extend the **earnings preview email composer** (`lib/digest/send-earnings-email.ts::renderPreviewPrompt`) so that for target B, if any reporter A in `read_through_pairs WHERE target_symbol = B` has a `calendar_events` row in `[B.event_date − 14d, B.event_date]` with non-null `actual_value` + `reaction_snapshot`, inject an "## Read-throughs from this earnings season" block into the prompt with A's beat-vs-consensus delta + reaction. Sonnet writes the implication. Smallest viable surface that delivers the user's literal example ("PRTO earnings change the XMTR preview").
  - **Auto-vs-manual:** Claude-mediated (Sonnet writes the implication paragraph), gated by the hand-curated pair list, surfaced *into* the preview email composer.
  - **Blocker:** the composer integration is blocked by the `reaction_snapshot` enrichment gap (see Bugs/Quality above) — read-throughs need the reaction signal to be present.
  - **Design doc:** `docs/plans/2026-05-02-stock-to-stock-read-throughs.md` (scoping deliverable for this session).
- **Slack integration** (2026-04-30) — scope TBD. Three directions: outbound (briefing/digest/alerts → channel), inbound (Slack threads as a research feed peer to Gmail), or both. Captured only this session; revisit when ready.
- **Background / server-side refresh policy** (2026-04-30) — audit what should run on a daily/weekly cadence and which side runs it. Current schedules (Mac launchd / Worker cron): Sunday 3pm ET briefing, weekday 8:45 ET digest, BMO+AMC earnings sweeps every 15min, calendar enrich every 15min, R2 state snapshot 2am, nightly QA 2am. Open candidates: factor-analysis cache, portfolio risk metrics roll-up, benchmark refresh on weekends, security-level scans without TWS, source performance attribution roll-up. Mac runs require TWS uptime; Worker runs require R2 snapshot freshness.

### Existing carries (still open, pre-2026-04-30)

- **Level source performance attribution (Theme D)** — wait until ~30 alerts have fired.
- ✅ **Caret affordance dislike** — shipped 2026-05-01 quick-wins sweep (`790d317`). `TabDropdown.tsx` swaps `▾` for `•••` overflow icon; rotation animation replaced by gold open-state color. Per `feedback_no_carets.md` ("standard '•••' overflow icon"). All caret affordances now retired across the app.
- **In-app config UI for `FEATURE_MODELS`** — let user swap models without editing `lib/ai/models.ts`. Multi-session (settings table override layer + resolver change + Settings UI). User noted "applies to other areas as well" — audit env-var/code-only configs at start.
- ✅ **`bg-panel` opaque-rendering investigation** — shipped 2026-05-01 quick-wins sweep (`790d317`). Audited every `bg-panel` usage in `app/dashboard/components/`; identified 4 latent siblings of the `4de767f` MobileNavDrawer bug (DataConfidenceIndicator, TwsStatus, NotificationBell hover, TabDropdown menu) — all descendants of header's `backdrop-blur-xl` stacking context. Patched each with defensive inline `style={{ backgroundColor: "var(--panel)" }}`. iPhone B4 verified pass (no header popovers visible on portrait, no regression on what is visible). Underlying root cause (Tailwind v4 token resolution under iOS Safari `backdrop-filter` parents) still unconfirmed — fix is defense-in-depth, not a definitive root-cause patch.
- **Wire `scripts/refresh-risk-free-rate.ts` into launchd** — Phase 5 D2 follow-up. Until done, rate stays at fallback 0.045.
- **Run `scripts/backfill-bond-durations.ts` against live DB** — Phase 5 D4 follow-up. Affects scenario rate-shock numbers.
- **Verify Phase 5 visual rendering on iPhone over Mesh** — partial 2026-05-01: B4 (bg-panel iOS popovers) verified pass on iPhone portrait. Still deferred: A1 Today MomentumPulse, A2 Performance sub-view, A3 trade-reviews redirect, A4 Factor Exposure tooltips, A5 Accounts IncomeYieldSection, B1 EarningsHub mobile-card mask, B2 Chat overlay mask, B5 dark-mode contrast. Routes to test via Mesh `100.96.0.1:3099` (Electron) or `:3000` (dev).
- **`~/.npm/_cacache` root ownership fix** — needs interactive `sudo chown -R Yitzi:staff ~/.npm`.
- **Polish — Security Detail Transactions sort URL params** — ~10 min table-header swap.

### Reminders (no code action)

- **Continue factor-analysis collaboration with ChatGPT** — give it the freshly refreshed GitHub link after this session's README + screenshot + CHANGELOG sweep so its context matches the post-redesign reality.

---

## Shipped since v2 (2026-03-30 → 2026-04-22)

Headline features not reflected in the v2 archive:

- **Security Levels + Alerts + MA-based levels** (migrations 029-031, `94379f1`, `62f98bc`)
- **Newsletter level extraction + review gate** (migration 032)
- **Mobile Today view** — phone-first single-column summary (`ad79afd`)
- **Privacy toggle** — `<Money>`/`<Pct>`/`<Shares>` masking (`68a2409`)
- **Pushover mobile push** for level alerts (`90c6589`)
- **Weekly briefing overhaul** — Opus 4.7 + Finnhub + weekend deep-reads
- **Data Integrity Overhaul** — IBKR cashBalance (migration 028), two-tier account cards
- **Trade review FIFO fixes** — expired options, short-sale P&L (migration 023)
- **AI Gateway + model-agnostic routing** — FEATURE_MODELS policy plane (`8c82c1e`)
- **Cloudflare Workers AI downroute** — Llama 3.3 + Kimi K2.6 (`264bc73`)
- **R2 PDF archival** — 39 Vanguard PDFs backfilled (`264bc73`, migration 033)
- **Chat history delete** (2026-04-22) — sidebar trash icon + `deleteConversation` mutation + `DELETE /api/chat/conversations/[id]`. Completes Theme I.
- **EDGAR 10-K / 10-Q section extraction** (2026-04-22, migration 034) — new chat tool `query_filing_section` summarizes Item 1A Risk Factors and MD&A via Sonnet, cached per `(symbol, accession, section)`. Feature key `filingSectionExtraction` in FEATURE_MODELS.
- **Research PDF knowledge base** (2026-04-22, migration 035) — upload analyst reports / research notes as PDFs; Claude extracts metadata + full body; SQLite FTS5 powers lexical search; chat tool `query_research_documents`; Documents tab in Research view. `researchDocumentExtraction` → Sonnet via native `document` content block.
- **Terminal redesign Phase 2 — MarketDataPanel KPI row** (2026-04-24) — 5-cell quote-strip between chart and LevelsPanel: Open / Day Range / 52w Range / Volume / ATR(14). Hidden on securities with no daily bars. New `<KpiCell>` primitive in `TerminalSection.tsx`; new queries `getLatestDailyBar` + `get52WeekRange` + `getKpisForSecurity`. Shipped alongside a `bare` option on `<Money>` to fix a `$$` double-dollar bug in the hero and new cells.
- **Stability burndown** (2026-04-24) — +43 tests. Fixed flaky transcript test (mocked EDGAR/Motley Fool/API Ninjas fetchers). Added 7 auto-refresh pipeline integration tests. Added 19 Cloudflare Worker cron tests (dst gates, KV dedup, primary/fallback paths, `/internal/*` handlers). Added 7 API route contract tests (sync-status, levels/extract, research/sync SSE).
- **Levels & alerts polish — Sessions 4+** (2026-04-20 → 2026-04-22, discovered during reconciliation pass) — items from master roadmap Theme A/L that shipped without being reflected in this file: A2 chat tools `query_levels` + `query_alerts` (`lib/chat/tools.ts:726,755`), L1 alerts inbox grouping into "Triggered today" / "Older pending" (`app/dashboard/alerts/page.tsx:277`), L4 `AlertsBell` hover preview, L5 alert row price-source chip (SMA/EMA), L8 `RecentAlertsPanel` on Security Detail.
- **Q1 2026 Vanguard Taxable income gap — closed** (2026-04-23) — all 11 Vanguard brokerage PDFs imported via Claude Co-Work. 167 dividends ($8,941) + 19 interest ($3,219) landed. VMFXX sweep-sign bug (201 rows) SQL-repaired in place; canonical CSV prompt patched (`176a064`) across all 3 synced sources so future imports preserve signed amounts.

---

## Next up (unblocked)

- ✅ **useCallback render-loop audit** — complete 2026-04-21, 0 candidates found. Pattern documented in `memory/feedback_usecallback_pattern.md`.
- ✅ **Next.js 16 async-params migration** — no-op as of 2026-04-22.
- ✅ **`trade-roundtrips.test.ts` TS errors** — fixed 2026-04-21 (`458ad59`).
- ✅ **Sortable column headers** — shipped 2026-04-23 (`882e620`). Shared `<SortableHeader>` + `<SortPicker>` + `useSortParam` hook. Applied across 7 tables (Holdings, 2× Transactions, 2× Tax Lots, Alerts, LevelsPanel).
- ✅ **Research mention extraction quality** — shipped 2026-04-23 (`64bdc39`). Two-layer gate (word-boundary + Haiku) at extraction time; 250 articles re-verified via `scripts/backfill-research-mentions.ts`. New FeatureKey `researchMentionVerification` → Haiku 4.5.
- ✅ **IBKR options metadata backfill** — shipped 2026-04-23 (`8707181`). 393 of 394 options now have structured `underlying_symbol` / `option_type` / `strike_price` / `expiration_date`; CALL/PUT chips now render on Security Detail.
- ✅ **Settings UI fallback for localhost** — shipped 2026-04-22 (`8ce0099`).
- ✅ **Q1 2026 Vanguard Taxable income gap** — closed 2026-04-23. See "Shipped since v2" above.

---

## Open items

Closed this session (2026-04-24 evening — Calendar Living Record Tier-3 sprint):

- ✅ **Phantom alert after full inbox clear** — closed 2026-04-24 afternoon (`c363e6d`). CustomEvent `alerts-updated` dispatch from inbox → AlertsBell listener. Sibling fix for ReviewBell via `reviews-updated`.
- ✅ **Global / in-page search** — closed 2026-04-24 afternoon (`e3085d6`). Cmd+K palette expanded to 7 source types (added research_article, research_document via FTS5, level, alert); AllHoldingsTable gained text filter.
- ✅ **Calendar / macro event post-release enrichment** — closed 2026-04-24 evening (migration 041, commits `58eef66` through `e353a79`). Full pipeline: scheduler + FRED/Finnhub actuals + TWS reaction capture + UI chips + briefing context block. Every-15-min Workers cron trigger covers off-Mac windows (primary-only for now). Plan: `docs/plans/2026-04-24-calendar-living-record.md`.
- ✅ **L9 — Calendar × Levels cross-reference** — verified shipped in earlier session (already in `CalendarView.tsx:276-281` via `getActiveLevelCountsForSecurityIds`).

Theme D capstone:

- ✅ **Theme D — Level source performance attribution v1** — shipped 2026-04-24 evening (`f906284`). New query `getSourcePerformance` + `/dashboard/levels/performance` page (empty-state tolerant) + chat tool `query_release_reactions` + Data Health "Unmapped sector ETFs" admin section.

Still open:

User-reported (2026-04-24):

_(All three user-reported items closed this session.)_

From master roadmap — reconciled 2026-04-24 evening after drift audit:

- ✅ **A3 — Daily-digest "Yesterday's triggered alerts"** — already shipped (`formatTriggeredAlertsSection` in `lib/digest/daily-digest.ts:22`); two bugs in it were also fixed this session (`6cb2cac`).
- ✅ **A5 — `expires_at` field in LevelsPanel form** — already shipped (`LevelsPanel.tsx:484 expiresAt state + :816 Field "Expires (auto-deactivate)"`).
- ✅ **L2 — LevelsPanel mobile quick-add form** — already shipped (`LevelsPanel.tsx:488 showAdvanced state, :778 "More options ↓" disclosure`).
- ✅ **L3 — Provenance filter in LevelsPanel** — already shipped (`LevelsPanel.tsx:861 authorFilter set + :898 filter application`).
- ✅ **L6 — Chart crosshair legend mobile density** — already shipped (`SecurityChart.tsx:736 hidden md:inline-flex for O/H/L + :757 mobile-only delta-from-open`).
- ✅ **H3 — Vanguard PDF direct import** — already shipped (`vanguard-pdf.ts:735 extractTransactionsFromPdf + :770 Promise.allSettled parallel holdings + transactions extraction`).
- ✅ **A13 — Cross-form validation audit** — completed 2026-04-25. Full walkthrough of 11 submit forms (LevelsPanel, SendDigestPanel, ManageSourcesModal, NotesView, TradeReviewView, ReconciliationTable, CorporateActionsSection, AnalysisView, ChatInterface, AddLevelPopover). Every `disabled={loading || !field}` guard is either correctly mode-aware (`disabled={loading || (priceSource === "static" && !price)}`) or a legitimate required-field gate with no mode-switching. No bugs found.

UX — tab navigation (flagged 2026-04-24):

- ✅ **Tab dropdowns for subviews** — shipped 2026-04-25 (`e6559d6`, follow-up `418bf21`). Research tab now shows a caret-dropdown with Notes / Trade Reviews / Feeds / Documents; Analysis shows Classification / Factor Exposure. Desktop-only — mobile keeps the in-page pill toggle. New `TabDropdown.tsx`; `nav-tabs.ts` extended with `subviews` + `matchParam`; `ResearchViewToggle` + `AnalysisView` mode-toggle gated to `md:hidden`. Keyboard: ArrowDown opens, Arrow keys/Home/End cycle, Enter selects, Escape/outside-click closes. **Non-obvious CSS fix:** the parent `<nav>` has `overflow-x-auto` which clips `position: absolute` descendants (overflow on one axis forces the other to clip too). Fix: menu uses `position: fixed` with `top`/`left` from `getBoundingClientRect()` so it escapes all ancestor clipping, plus scroll+resize listeners to keep the menu glued to the tab. Calendar has no subview toggle — the dropdown slots in if one is ever added. **Follow-up build fix (`418bf21`):** `TabDropdown` calls `useSearchParams()`, which blocks static pre-rendering for any dashboard page eligible for it (e.g., `/dashboard/levels/review`, `/alerts`, `/notes`). Wrapping the render in `<Suspense>` with a plain-`<Link>` fallback in `TabNav.tsx` isolates the CSR bailout to the dropdown itself.

New from Phase 9 (calendar-enrich Worker):

- ✅ **Phase 9b — Calendar-enrich cloud-fallback parity** — shipped 2026-04-25, code-only (flag-gated). Worker now reads the nightly R2 state snapshot (extended to cover trailing-24h events), filters in-window candidates, fetches actuals from FRED + Finnhub and reaction bars from **Yahoo Finance** (1-min chart endpoint, no auth, 10+ day retention), and writes per-event `cloud-enriched-{id}` payloads to KV (7d TTL). Mac's new `/api/calendar/reconcile-cloud-enrich` route reads those payloads on every wake (piggy-backed on every `/api/calendar/enrich` call), upserts into calendar_events with TWS-always-wins precedence on reaction_snapshot, and DELETEs the KV key. New `POST /api/calendar/enrich { upgradeReactionToTws: true }` body flag lets the UI re-capture TWS bars over a cloud-enriched row. Claude `nonfred` path is deferred on cloud (Worker writes `deferred: true` payload; Mac's next wake runs Claude itself). Gated by `CLOUD_ENRICH_ENABLED=true`; ship is safe at flag=false. **User actions before flip (3 secrets, $0):** `cd workers/cron && npx wrangler secret put FRED_API_KEY / FINNHUB_API_KEY / CLOUD_ENRICH_ENABLED` then `npx wrangler deploy`. **Provider choice:** Polygon Starter ($29/mo) rejected because its 15-min data delay creates a timing hole — events tick out of the 2h candidate window before Polygon's bars catch up. Polygon Advanced ($199/mo) is real-time but overpriced for our 10-20 candidate/day volume. Finnhub `/stock/candle` is paid-only since 2024 (verified 2026-04-25: 40-char free-tier key returns "You don't have access to this resource."). Yahoo's unofficial `query1.finance.yahoo.com/v8/finance/chart/{symbol}?interval=1m&period1&period2` endpoint is the free + real-time winner; verified returns 113+ 1-min bars 10 days back for SPY. Risk: unofficial, Yahoo could break; mitigation: graceful null fallback. New files: `workers/cron/src/yahoo.ts`, `workers/cron/src/enrich-actuals.ts`, `workers/cron/src/reaction-matcher.ts` (mirror of `lib/calendar/reaction-snapshot.ts` helpers), `app/api/calendar/reconcile-cloud-enrich/route.ts`. Test deltas: +7 cloud-enrich Worker tests, +7 Mac reconcile tests = +14, 1227 → 1241.

Other open (from MEMORY, not previously in this file):

- [ ] **IBKR April 2026 transactions gap** — waiting on April statement to arrive; will import normally when it lands. (Phase 5 plan note.)

Closed this session (follow-up):

- ✅ **UMich Consumer Sentiment calendar gap** — closed 2026-04-25 (`01bf248`). User flagged today's 04-24 Final release wasn't captured. Two bugs: hardcoded schedule only had prelim dates (2nd Friday, no 4th Friday finals), AND non-FRED events were inserted with null `event_time` → `resolveReleaseTime` had nothing to resolve → `release_time` stayed null → enrichment runner filtered them out (Consumer Confidence row id 67 was silently broken the same way). Fix: `NonFredEvent.releaseTime` required field, UMich split into prelim/final arrays with distinct shortNames, push site emits `event_time: src.releaseTime`. 2026 UMich dates cross-checked against `data.sca.isr.umich.edu/schedule.php`. Ran sync for weekOf 2026-04-20 → 2026-07-27 to upsert new rows. Today's 04-24 Final retroactively enriched via `/api/calendar/enrich {eventId:97}` (actual="49.8" via Claude+web_search; reaction absent because TWS is disconnected right now — acceptable for this one-off).

New from Phase 4 E2E scenario sweep (2026-04-25):

- [ ] **Security Detail Transactions table sort doesn't write URL params** (Scenario 2 #9). Clicking the DATE header (which shows a ↓ arrow) does not update `location.search` — other tables with the `useSortParam` pattern (Holdings, LevelsPanel) persist sort state to the URL. Likely the Security Detail Transactions section doesn't wrap its header in `<SortableHeader>` or isn't scope-keyed. 10-min fix.
- [ ] **Research Documents search may miss tag matches** (Scenario 6 #4). FTS5 search is body-only; a document tagged `ai risk / agentic ai` doesn't surface for query "ai". Design choice or gap? If the FTS index should include tags/title, extend the `INSERT INTO research_documents_fts` trigger. ~30 min.
- [ ] **Chat delete uses `window.confirm` instead of toast-confirm** (Scenario 8 #7). `ChatInterface.tsx:418` — swap for the `ToastProvider` confirm pattern used elsewhere in the app. Testable + consistent. ~15 min.
- [ ] **Today's data-quality chip is page-level, not per-row** (Scenario 1 #5). Spec-vs-code divergence: scenario expected per-row chips but `today/page.tsx` renders one `qualityChip` at the H1. Either update the SCENARIOS.md checklist OR ship per-row chips if the UX reads better. Defer unless user asks.

User-reported (2026-04-25 evening — all closed in this session):

- ✅ **Research document upload — "Metadata response was not valid JSON"** (JaguarAnalytics "Hidden Angles" PDF). Closed 2026-04-25. Root cause: `lib/research-documents/extract.ts:160` had `maxOutputTokens: 4000` ("metadata response is tiny") but the prompt allowed multi-paragraph `summary` with no length cap. Long-form research digests overflowed mid-string in `summary`. Fix: bumped to 8000 + added explicit "max 1500 chars" cap in `METADATA_PROMPT` summary section.
- ✅ **First BMO earnings (NSC) didn't enrich post-release** — closed 2026-04-25. Two pieces: (a) earnings window widened from 2h to 12h for `source='finnhub' OR event_type='earnings'` in `lib/calendar/enrichment-runner.ts` (markets don't open till 09:30 ET — pre-fix a 08:00 BMO earnings was useless to enrich); (b) "one-event-per-pass" theory disproved — runner correctly processes ALL in-window candidates per pass. NSC (id 102) wasn't picked at 08:13 because Finnhub sync didn't insert it until 12:00 ET (DB created_at confirmed). NSC retroactively enriched today via `/api/calendar/enrich {eventId:102}` — actual EPS 2.65, Rev $2.998B (reaction absent because TWS disconnected, acceptable). Regression test added.
- ✅ **Nightly QA cron now actually running** — closed 2026-04-25. Wrote `~/Library/LaunchAgents/com.vanguard-skin.nightly-qa.plist` (StartCalendarInterval Hour=2 Minute=0, daily) + `launchctl load`. Wrote the missing `memory/project_nightly_qa.md` topic file documenting what runs, what it tests, files, manual-run command, and add-new-check workflow.
- ✅ **Daily digest auto-send "skipped: No processed articles in selected range" (3-day silent failure)** — closed 2026-04-25. Real bug discovered after user's clarification about manual catch-up at 7 PM yesterday. Race condition: `sendDigestEmail` read `getLastDigestSentAt(db)` AFTER 60-90s of fetch+process awaits. A concurrent manual trigger (e.g., 8:46 phone-tap while 8:45 cron mid-process) updated `last_digest_sent_at` to "now"; cron then read poisoned timestamp → zero matches → skip. Fix: capture `sinceSnapshot` BEFORE any await in `lib/digest/send-digest.ts`. Regression test in `tests/digest/send-digest-race.test.ts` uses `vi.hoisted` to inject the timestamp update during the await.
- ✅ **DigestCatchup banner: "wasn't sent at 9 AM" + shows pre-trigger** — closed 2026-04-25. `app/dashboard/components/DigestCatchup.tsx` now uses `DIGEST_TIME_LABEL = "8:45 AM"` constant + only shows banner if `now >= 8:45 AM local` AND `lastSent < scheduled-trigger-today`. Pre-8:45 weekdays the banner is hidden ("expected, not late"). CLAUDE.md updated from "9 AM" to "8:45 AM".

Phase 4 E2E sweep cleanup (also closed this session):

- ✅ **E2E #1 — Security Detail Transactions sort URL params** — closed 2026-04-25. Real bug in `lib/hooks/useSortParam.ts`: `setSort` compared against URL params (always null on first click of default-sorted column) instead of the displayed sort, so first click on the DATE column wrote redundant `?...Sort=trade_date&Dir=desc` with no visual change ("feels broken"). Fix: compare against `sort.field`/`sort.dir` (which include defaults). Tri-state cycle now visibly engages from click 1.
- ✅ **E2E #2 — Research Documents FTS5 tag matches** — closed 2026-04-25 as verified-non-bug. Migration 036 already extends FTS5 to include `tags` column + triggers wire it correctly. Live DB confirms FTS5 query "ai" returns the doc tagged "agentic ai" / "ai risk". Added explicit regression test in `tests/queries/research-documents.test.ts::"FTS5 query 'ai' surfaces a doc tagged 'agentic ai' / 'ai risk'"`.
- ✅ **E2E #3 — Chat delete + Research doc delete swap window.confirm for ConfirmDialog** — closed 2026-04-25. `ChatInterface.tsx` and `ResearchDocumentsView.tsx` both swapped to the project's `ConfirmDialog` component (the same pattern `NotesView` uses). `deletePending` / `confirmingDelete` state holds the target; `<ConfirmDialog variant="danger">` renders the modal.
- ✅ **E2E #4 — Today data-quality chip placement** — closed 2026-04-25 as spec correction. Updated `tests/e2e/SCENARIOS.md` Scenario 1 #5 to reflect the page-level qualityChip currently shipped (per-row chips would add row-clutter for marginal info — user opted close).

Closed this session (2026-04-25 night — data-integrity burndown from 9-tab audit, `b60d71e`):

- ✅ **Import engine price-source mapping** — derived prices from holdings were hardcoded `source: "canonical"` (priority 4) regardless of parser. Vanguard-PDF-derived prices now inherit `vanguard-pdf` (priority 3) so a stale manual price (also priority 4) can no longer survive a fresh PDF import. New helper `holdingDerivedPriceSource()` in `lib/import/engine.ts`. Regression test added in `tests/import/engine.test.ts`.
- ✅ **Analysis scope leak in `getFactorCoverage.bySource`** — `lib/queries/analysis.ts:579-586` was querying all `security_factors` globally, breaking the Factor Exposure bar chart under single-account scope. Fixed via CTE-joined `scoped_security_ids` that covers both direct holdings AND option underlyings.
- ✅ **Missing `scope` param on 3 API routes** — `/api/compute/xirr`, `/api/compute/reconciliation`, `/api/compute/options-strategies` now accept `scope` via `resolveScopeToSingleId`. Peer routes (risk, factors) already had it. `options-strategies` also got a bonus fix: per-account `MAX(as_of_date)` subquery (global MAX could pick a different account's stale date).
- ✅ **Gmail date-header silent fallback** — `lib/gmail/fetch.ts:194` silently fell back to `new Date()` (now) on unparseable `Date:` headers, poisoning `received_at` with future timestamps and breaking digest filtering. Now drops the message with a `console.warn`.

**Audit meta:** 9 parallel Explore agents spawned (one per dashboard tab) surfaced 11 candidate findings. 6 were real bugs (shipped above); 7 were false positives verified-and-skipped (bond par on holdings page, AllHoldingsTable allocation %, getCashAnchors TWS filter, Money null rendering, reaction-snapshot DST year-rotation, calendar enrich POST force-dynamic, send-digest timestamp ordering). Grep-verify before acting saved ~4 bad fixes. 1245 → 1246 tests.

User-reported (2026-04-25 late night — open):

- [ ] **Mobile dropdown menu has clear/transparent background** — hard to see and navigate. Need to identify which menu (likely `MobileBottomNav` overflow or a tab-subview menu that somehow surfaces on mobile despite the `md:`-only `TabDropdown` gate — worth confirming first). Fix: solid `bg-raised` (or `bg-panel` with a 1px `border-edge`) + proper backdrop, same surface treatment as the mobile chat overlay.
- [ ] **Desktop tab-subview dropdown visibility + tab/subview rethink** — the caret-dropdowns shipped in `e6559d6` work but are low-contrast / easy to miss. Two-part: (a) visual pass on `TabDropdown` (stronger caret affordance, maybe hover underline, better menu chrome), and (b) a collaborative walk through the full tab + subview tree (Today / Overview / Accounts / Holdings / Analysis / Charts / Calendar / Research / Import, plus all subviews) to decide what actually belongs as a top-level tab vs a subview vs a settings-only surface. Brainstorm session before any code.
- [ ] **Research Feeds horizontal scroll is messy** — two parts: (1) find a better UI for the source chip / filter row that currently scrolls horizontally (wrap-to-lines? vertical list? dropdown? combobox?); (2) regardless of which alternative we pick, the horizontal scrollbar itself should be hidden (`scrollbar-hide` utility or `::-webkit-scrollbar { display: none }` on that container). Quick fix for (2), deeper brainstorm for (1).

Closed this session (2026-04-27 — email session: weekly briefing accuracy + digest race + Codex critical-bug-pass + calendar sync wiring):

- ✅ **Codex critical-bug-pass — calendar auth + derived-price provenance + release_time write-path** (`ce39140`, 17 files, +950/-264). Three coupled fixes from the 2026-04-26 Codex handoff: (a) `/api/calendar/enrich` and `/api/calendar/reconcile-cloud-enrich` now require `X-Cron-Secret` on every call — earlier "trusted local UI without secret" path was removed because the reconcile fetch built its target URL from the inbound `Host` header, leaking the secret to a caller-controlled origin. Reconcile logic extracted into `lib/calendar/cloud-reconcile.ts` (single source of truth) and called in-process — no internal HTTP hop. (b) `lib/valuation.ts::unitPriceFromMarketValue` centralizes `marketValue / quantity` with bond ÷100 + option × multiplier handling; `lib/import/engine.ts` no longer overwrites a higher-priority price already written earlier in the same import batch. Bonds and options now derive prices correctly. (c) `lib/mutations/calendar.ts` persists `release_time` on upsert (was relying on a separate backfill script); `lib/calendar/release-times.ts` normalizes earnings hours so finnhub-imported rows land enrichment-eligible immediately. CLAUDE.md updated to reflect the new auth model. +22 tests across 6 test files.
- ✅ **Briefing A1+A2+A3 — pass current prices, issuer-family, deterministic macro exposure** (`c4a9305`, 9 files, +1,426/-11). Closes the three regressions from 4/26's Sunday briefing. **A1**: `buildCurrentPrices(db, args)` returns a Map of held + option-underlying + earnings symbols; `formatCurrentPricesBlock` renders a Current Prices section + inline `last $X.XX (date)` per holding; prompt directive forbids inferring or fabricating prices. Live regen confirmed TER appears at $403.00 against the $180 strike — no more "well below $180" hallucination. **A2**: New `lib/securities/issuer-family.ts` maps GOOG/GOOGL, BRK A/B variants, FOX/FOXA, NWS/NWSA, UA/UAA, LBRDA/LBRDK, LSXMA/LSXMK, HEI/HEI.A; `buildCombinedPositionsForEvents` rolls up sibling-class positions per earnings event so the prompt sees one combined Alphabet roster (65 sh GOOG common + 1 GOOGL LEAP). Live regen confirmed "Long GOOG outright + GOOGL Jan-27 $220 calls" shipped correctly. **A3**: `macroEventToSectors(event)` maps event_type → sector whitelist; `buildMacroExposures(db, events)` queries holdings by sector and feeds the result to the prompt. +29 tests across briefing-current-prices, issuer-family, briefing-combined-positions, briefing-macro-exposures, plus 3 verify scripts under `scripts/verify-a*-*.ts`.
- ✅ **A3 directive strengthened — verbatim rule** (`f02517c`, 2 files, +25/-1). E1 Level 2 verification showed Opus still dropping XMTR + PRIM + CLH + GFL from the ISM Manufacturing exposure paragraph despite all four being in the deterministic Holdings exposure field. The original directive ("Use this list verbatim") wasn't authoritative enough — Opus pattern-matched to its training-prior idea of "industrial" and added PWR (DB-classified as Consumer) plus a "VIS basket" ETF. Strengthened to imperative HARD RULE language that frames the exposure list as data, forbids ETF additions / prose hand-waves / substitutions, and explicitly calls out the PWR-as-Consumer case. Regression test asserts the strengthened directive language stays in `briefing.ts`. **Verification deferred to next Sunday's automated 3pm cron** rather than burning another $0.65 in Opus tokens.
- ✅ **A6 — run calendar sync before briefing generation so PCE/GDP land** (`a15beb5`, 4 files, +431/-218). Sunday 4/26's briefing missed PCE + Q1 GDP advance (both released Thursday 4/30) because `sendBriefingEmail` ran `syncPortfolio` (TWS positions) but not calendar sync. The original plan called this "A6: Add PCE + GDP to FRED macro-events sync" — investigation showed that was a false reading: release IDs 53 (GDP) and 54 (Personal Income & Outlays / PCE) were already in `TRACKED_RELEASES`, and the plan's "release_id 21" actually maps to H.6 Money Stock. The real fix is structural: extracted the three-phase calendar sync (WSH → Claude macro → Finnhub) into `lib/calendar/sync.ts::syncCalendarForWeek(db, weekOf, opts)` as a reusable, non-SSE library function. Route file becomes a thin SSE bridge. `sendBriefingEmail` calls the library before generation. Errors in any phase are logged and never block — partial calendar data is still better than no briefing at all. +8 new tests in `tests/calendar/sync.test.ts`.
- ✅ **Digest race fix — close the 8:45→8:57 duplicate-send window** (`a98453b`, 12 files, +450/-22). Three interacting bugs caused today's daily digest to skip its 8:45 cron, let the user manual-send from the app, and then deliver a thinned-out duplicate from the Worker fallback at 8:57. **B1**: `sendDigestEmail` gains `skipMarkerUpdate`; `DigestCatchup` posts `{mode:"since_last", skipMarkerUpdate:true}` so a manual catch-up cannot poison cron's `last_digest_sent_at` while cron is in flight. **B2**: Worker primary timeout 120s → 300s in `wrangler.toml`. **B3**: New `mac-running` KV marker (10-min TTL) — Mac POSTs to Worker's new `/internal/running-marker` endpoint at the entry of `/api/cron/{briefing,digest}` with action=set, clears in finally. Worker re-reads markers right before firing fallback and skips when (a) mac marker is now present (slow but successful primary), (b) cloud marker is now present (race), or (c) macRunning is set. Fire-and-forget on Mac side; never blocks email delivery on Worker RTT. **B4**: `tests/digest/send-digest-race.test.ts` + `tests/workers/cron.test.ts` extended with post-cron-manual-trigger and slow-Mac-during-timeout scenarios. **Worker deployed live** — verified the 401-on-bad-secret response confirms the route is wired. Tomorrow's 8:45 AM digest is the production verification.
- ✅ **Codex CLI project configs landed** (`fedf783`, 11 files, +482/0). The `.agents/skills/` (data-auditor, db-query, run-tests, session-end, ship, test-writer) + `.codex/hooks/` (block-db-edits, **block-sqlite3-writes** — closes the .db loophole that surfaced when Codex routed around Edit/Write by using sqlite3 via Bash, check-todo-reconciled, post-edit-lint, stop-vitest) were created in the working tree by the 4/26 migration session but never committed. Now in git so any clone of the repo gets the same Codex behavior.

Operational follow-ups completed in-session (no commit):

- ✅ **Worker deployed** (`vanguard-skin-cron.isaac-3d1.workers.dev`) — `npm run deploy` from `workers/cron`. `PRIMARY_TIMEOUT_MS=300000` confirmed in deploy output; `/internal/running-marker` returns 401 on bad secret (route exists), `/health` returns 200.
- ✅ **release_time backfill** — 14 rows in 30-day window had null `release_time`. `VGS_DB_PATH=$(pwd)/data/vanguard.db npx tsx scripts/backfill-calendar-release-times.ts` resolved all 14, post-check shows 0 remaining.
- ✅ **Calendar sync run for week_of=2026-04-27** — 8 macro events landed including `fred:53:2026-04-30` (Q1 GDP Advance Estimate) and `fred:54:2026-04-30` (March Personal Income & Outlays / PCE).
- ✅ **E1 Level 2 — full briefing regen for week_of=2026-04-27** — 19,461 chars, 23 events. A1 ✅ (no fabricated TER price), A2 ✅ (GOOG + GOOGL LEAP combined), A6 ✅ (PCE + Q1 GDP both surface), A5 ✅ (zero self-admission hits), A3 ⚠️ partial (4 names dropped, PWR + VIS basket added). A3 directive strengthened in `f02517c`; next Sunday is the live re-test.

New from this session (open):

- ✅ **A3 — first-tier deterministic enforcement (cluster line injection)** — closed 2026-05-03 (`6c7a766`). Pre-renders the `**Holdings exposed:** SYM1, SYM2, ...` cluster line per macro event in `formatEventForPrompt` as a "REQUIRED §6 cluster (paste this exact line)" field. Verbatim guarantee no longer relies on Opus's restraint. §6 directive rewritten to 5 numbered HARD RULES (verbatim-paste, no-lumping, cluster-after-prose, trust-the-data, skip-if-no-mapping). Live verified 5/04 regen: 8/8 PASS on the verifier across all macro events with deterministic exposure (Housing Starts, ISM Services 33-name, JOLTS, ADP, Claims, NFP, UMich Sentiment). Full-paragraph TS rendering (the "level 2 escalation" in the original A3 entry) remains optional — only needed if cluster injection ever fails on a future week.
- [ ] **A5 — auto-regen on self-admission hit** — half-shipped 2026-05-03 (`6c7a766`). The regex SCAN now lives in `scripts/verify-briefing-content.ts` (post-gen, manual-run). Auto-regen path (re-call Opus with a forcing prompt when the regex hits in a fresh briefing) is still TODO. ~1 hr if the SCAN ever surfaces a real hit on an automated cron run; until then, no urgency.
- [ ] **A7 — long-only holdings filter → cross-account net** (deferred from plan). `briefing.ts` filters `WHERE h.quantity > 0`; should be `!= 0` plus per-symbol cross-account aggregation so MSFT shows as `net -3 (long 41.9 Vanguard, short 45 IBKR)` rather than two rows or one-side-only. Touches `getHoldings` + briefing prompt directive. ~1 hr.
- [ ] **A8 — HOOD-style level wording — pass thesis verbatim** (deferred from plan). Each level row's `thesis` text should flow into the prompt; Opus directive: "Quote the user's stored thesis when describing direction; do not infer 'breakdown' vs 'pullback' from price action alone." ~30 min.
- [ ] **C — In-app email schedule controls (Electron in-process scheduler)** (deferred from plan, full workstream). C1 AppSettings fields + migration, C2 SettingsModal "Email Schedule" section, C3 `electron/email-scheduler.ts` (two `setTimeout` chains aligned to ET wall-clock), C4 retire launchd plists after 7-day soak, C5 `/api/digest/status` returns `nextDailyAt`/`nextWeeklyAt`. ~1 day total. Quality-of-life, no urgency.
- [ ] **D — Email filtering (admin-mail short-circuit + relevance flag)** (deferred from plan, full workstream). D1 migration adding `is_relevant` / `excluded_category` / `excluded_reason` to `research_articles`; D2 pre-Claude regex short-circuit in `lib/gmail/fetch.ts` for welcomes / payment receipts / gift-rewards; D3 `is_portfolio_relevant` flag in `ANALYSIS_SCHEMA` for at-Claude judgment; D4 digest filter respects `is_relevant`; D5 audit UI in Research → Documents → "Filtered" tab. ~4-6 hr.
- [ ] **H — Per-source signal-extraction profiles** (deferred from plan, multi-session). Schema (`research_source_profiles` + `research_article_extracts`), 4-stage pipeline (format detect → section filter → structured extract → summary), 6 starter profiles (Vital Knowledge Catalyst Watch / Eliant / James Bulltard / Bloomberg Odd Lots / FundaAI / Helene Meisler), briefing prompt integration, discovery-loop UI. Multi-day; needs its own design doc. Plan section H1-H6 in `~/.claude/plans/i-want-to-plan-iterative-sedgewick.md`.
- [ ] **PWR sector reclassification investigation** (data-quality, surfaced this session). PWR (Quanta Services) is classified `sector="Consumer, Non-cyclical"` in the DB but is a real-world industrial-services name. Bloomberg's classification is the source. Two paths: (a) override at the application layer with a `securities.sector_override` column, (b) accept the DB classification and let A3's strengthened directive handle it. Investigate before next briefing cycle. ~30 min.

Closed this session (2026-04-27 evening — earnings digest MVP):

- ✅ **Earnings digest Tier 1 — preview + recap email composer** (`8e24802`, 6 files). Pre-earnings (2h before) + post-earnings (2h after) briefing emails for held names. New `lib/digest/send-earnings-email.ts` builds context from cross-account positions (incl. options via `underlying_symbol` traversal — caught the Vanguard TER LEAP that Tier-1 v1 missed), preferred-source newsletter feeds (Vital Knowledge, TMT Breakout, Eliant, Purple Drink, Meisler — last 7 days, fall back to any source last 30 days), analyst recommendations trend + press releases + prior-quarter transcript best-effort. Anthropic `web_search_20250305` fills gaps for missing consensus / sell-side commentary. Two new feature keys (`earningsPreview` + `earningsRecap`) → Sonnet 4.6 on Anthropic. New `POST /api/earnings/email { eventId, phase, to? }` for manual fire. Two scripts: `scripts/preflight-earnings-data.ts` (analyst + PR sync) and `scripts/fire-earnings-emails.ts <preview|recap> <date> [bmo|amc] [--dry-run]` (auto-finds held-stock earnings for a given date+slot, calls the API). 5 production emails fired (GLW, KO, TER, plus 2 GLW iterations) — user verified TER reads as "good proof of concept/first pass, links work" and flagged the LEAP miss which was fixed in-session. Plan: `~/.claude/plans/okay-let-s-see-if-joyful-feather.md`.
- ✅ **TER 2026-04-28 calendar row inserted manually** (id=259). Finnhub had only the Q2 row (2026-07-27); user-confirmed Q1 release tomorrow AMC is Teradyne. Inserted with `source='manual'`, `release_time='16:15'` so the new email composer treats it identically to Finnhub-sourced rows.
- ✅ **GLW + TER pre-flight syncs** ran at session start: 8 monthly analyst-recommendation snapshots (4 each — 21 SB+B / 5 H consistent for GLW, 15 SB+B / 7 H / 1 S for TER), 250 press releases (157 GLW + 93 TER over last 30 days). Price targets remain empty for both per Finnhub free-tier silent return; web_search fallback is the mitigation.
- ✅ **Option-position traversal bug** (`8e24802`, fix mid-session). `getCrossAccountPositions` joined on `s.symbol IN family` only; option holdings (OCC-format symbol like `TER   280121C00180000`) never matched. Extended WHERE to `(UPPER(s.symbol) IN family OR UPPER(COALESCE(s.underlying_symbol, '')) IN family)` and added option-formatting branch in the renderer that surfaces strike/expiry/multiplier and explicit notional (1 contract × 100 = 100 shares notional). Combined-exposure summary now lists shares + contracts separately with a directive to reason about delta-weighted asymmetry rather than summing raw counts. Re-fired TER preview confirmed both legs (Vanguard Taxable LEAP + IBKR stock) appear.

Closed this session (2026-04-28 — earnings digest Phase 1 + 2 + 3 + workflow):

- ✅ **`/session-end` is the authorization** (`583db88`, 4 files). Missed today's 06:00 ET earnings-preview window because a pending commit-confirmation went unseen. New rule in `~/.claude/CLAUDE.md` + all three project session-end checklists: invoking `/session-end` IS the green light for commit + push + rebuild; do not pause for confirmation. Destructive ops (force-push, branch delete, drop data) still require per-action confirmation.
- ✅ **Phase 1 — notes pull + event-symbol reaction + light-table scoreboard** (`07a1073`, 10 files, +478/-34). Three additions on top of last night's Tier 1 MVP. (a) New `getNotesForFamily(db, family, daysBack)` in `lib/queries/notes.ts` (joins via `securities.symbol` OR `underlying_symbol` so a note attached to a TER LEAP option surfaces under TER's earnings event). Composer threads `userNotes` through `PreviewContext` + `RecapContext` and renders `## Your prior notes on {symbol}` block FIRST in both prompts so the AI frames the briefing as conversation with the user's prior thesis. (b) `ReactionSnapshot` gains optional `symbol?: BenchmarkReaction & { symbol: string }`; `captureReactionFromTws` + `captureReactionFromYahoo` accept `opts.eventSymbol` and capture event-symbol bars. Recap can now say "GLW closed +4.2% vs SPY +0.1%". Mirrored in `workers/cron/src/{reaction-matcher,yahoo,calendar-enrich}.ts`. (c) `briefingToHtml` parses GitHub-flavored markdown tables and emits email-safe `<table>` with **light palette** (white bg, dark text, warm-gold-tint header, medium-gray borders) regardless of the surrounding dark email — user explicitly flagged a dark-bg scoreboard is unprintable. Empty/em-dash cells get 14px vertical padding for handwriting. New deterministic `renderHeadlineTable()` in the composer builds a 6-row scoreboard (EPS, Revenue, Guidance, stock @ T+2h, SPY @ T+2h, QQQ @ T+2h) from `consensus_estimate` + `actual_value` + `reaction_snapshot` so preview + recap share identical shape. Both prompts restructured to lead with AI-generated `## Line-by-line bogies` markdown table, then prose. +6 tests; 178/178 calendar+digest passing. Verified live: GLW recap fired three times during iteration (v1 prose, v2 dark scoreboard, v3 light scoreboard) — v3 locked.
- ✅ **Phase 2 — Earnings Hub on `/dashboard/today` + manual-add CRUD + audit table** (`481681d`, 12 files, +1,145). Visible-from-the-app surface for the earnings digest. Migration 042 adds `earnings_emails` audit table (UNIQUE(event_id, phase) + ON DELETE CASCADE + 2 indexes); `sendEarningsEmail` writes audit row on success (ON CONFLICT updates in place). New `getSymbolStatus(db, symbols[])` query in `lib/queries/briefing-symbols.ts` returns held/watchlist/neither in one round-trip. New `getEarningsForWeekDeduped(db, weekOf)` in `lib/queries/calendar.ts` uses ROW_NUMBER() to prefer Finnhub when manual+finnhub rows exist for same (symbol, date, type). Extended `app/api/calendar/events/route.ts` with POST/PATCH/DELETE — only `source='manual'` rows are user-editable (sync-owned rows return 403). Three new mutations in `lib/mutations/calendar.ts` (`insertCalendarEvent` / `updateCalendarEvent` / `deleteCalendarEvent`). New `mondayOf()` helper in `lib/calendar/date-utils.ts`. New `EarningsHub.tsx` (server) + `EarningsHubAddForm.tsx` + `EarningsHubRefreshButton.tsx` (client) plug in between TodayReleases and Alerts. Day-grouped list with status chips (held/watchlist/no-position + preview-sent/recap-sent/pending). +12 tests in `tests/queries/earnings-hub.test.ts`; 190/190 passing.
- ✅ **Phase 3 — Cron automation, self-gating sweep, live verified** (`38ac2d3`, 7 files, +555/-21). Three cron-authenticated routes (`/api/cron/earnings-{preview,recap,sweep}`) all using new `lib/cron/wrappers.ts::withCronAuth` for the X-Cron-Secret pattern. Sweep route does work in-process: `findEmailCandidates(db)` returns preview candidates (release_time in [now+105m, now+135m]) + recap candidates (enriched_at within 4h) for held|watchlist symbols with no audit row, dispatches each to the composer. Composer's UNIQUE-protected audit-row insert is dedup floor — next 15-min tick sees the row and skips. `scripts/enrich-calendar-events.sh` restructured to run BOTH enrich AND sweep per launchd tick (was exit-on-first-success, now sequential). New `scripts/sweep-earnings-emails.ts` for tsx fallback. **Live verification at 14:06 ET 2026-04-28**: sweep returned `{swept:3, sent:3, failed:0}` for HOOD + V + TER previews inside the 16:15 AMC window. First time the system fired earnings emails without manual intervention. Audit rows confirmed for event_id 249/258/259 with `phase=preview` and `sent_at` within minutes of fire.
- ✅ **Phase 5 + weekly Finnhub auto-refresh** (`e49303c`, 7 files, +425/-10). Self-contained settings UI for the earnings emails. `lib/queries/earnings-settings.ts` stores `earnings_emails_enabled` + `earnings_emails_muted_symbols` in the existing key-value `settings` table — changes apply to the next 15-min cron sweep without an app restart (no env-var threading). New `app/api/settings/earnings/route.ts` (GET + PATCH); new `EarningsEmailsSection` client component slotted into SettingsModal with master toggle + chip list of muted symbols. `findEmailCandidates` reads settings at top, returns [] when disabled, filters muted symbols at candidate level. +9 settings tests passing. Weekly Finnhub refresh: `lib/digest/send-briefing.ts` now syncs current week + next week so Finnhub-newly-published earnings dates surface on the Earnings Hub without manual click.
- ✅ **Session-end docs reconciliation** (`99f79eb`). MEMORY.md gains a Recent Work entry covering today's nine commits + meta-findings; CLAUDE.md adds an Earnings Digest architecture bullet + four new API route entries (`/api/cron/earnings-{preview,recap,sweep}`, `/api/calendar/events` POST/PATCH/DELETE, `/api/settings/earnings`).
- ✅ **Phase 4 — Worker cloud fallback + Mac/Worker marker handshake** (`880d84f`, 10 files, +1,126/-27). Lean cloud-fallback shipped + Worker deployed to `vanguard-skin-cron.isaac-3d1.workers.dev`. New `workers/cron/src/fallback-earnings.ts` produces a compact email (scoreboard + cross-account positions + actuals + reaction) when Mac primary is unreachable, with a footer disclosing limited context — newsletter bogies, analyst data, prior transcripts, and user notes are intentionally NOT mirrored to avoid divergence as we iterate on Mac content. Snapshot bumped to `schemaVersion: 2` (adds holdings, securities, accounts, earnings_emails, earnings settings). Worker `html.ts` ported today's markdown-table parser + light-palette table rendering for visual parity with Mac primary. Marker plumbing keyed on (phase, eventId): `mac-sent-earnings-*`, `cloud-sent-earnings-*`, `mac-running-earnings-*`. Three new Worker internal endpoints (`/internal/earnings-marker`, `/internal/earnings-running-marker`, `/internal/earnings-sent-marker`) all X-Cron-Secret-gated. Mac cron routes (`/api/cron/earnings-{preview,recap}/route.ts`) updated with the marker dance: pre-check cloud-sent → set running on entry → write mac-sent on success → clear running in finally. Wider gate `shouldRunEarningsFallback` (Mon-Fri 05:00-20:00 ET) covers BMO previews + AMC recap window which the calendar-enrich 09:30-18:00 gate would miss. **Worker deployed live**: `https://vanguard-skin-cron.isaac-3d1.workers.dev` — the new earnings endpoints respond on the new schedule (`*/15 * * * *` cron trigger). Smoke test: `/internal/earnings-marker` returns `{sentBy: null}` for event 247 (no marker yet — expected since Phase 4 just shipped). Bad-secret returns 401. **Caveat**: cloud fallback is gated on snapshot v2 — last night's R2 snapshot is still v1 so the fallback short-circuits until tonight's 2am snapshot writes v2. Marker writes from the Mac side are immediately active.

Closed this session (2026-04-28 evening — earnings polish + Today/Hub redesign):

- ✅ **Track A — `formatLargeUSD` + AI prompt directive** (`d891a62`). New utilities `formatLargeUSD` / `formatLargeNumber` / `parseLargeUSD` in `lib/format.ts` (boundaries: ≥$1B → `$N.NNB` 2dp, ≥$1M → `$N.NM` 1dp, ≥$1k → comma-grouped, sub-$1k → 2dp EPS-scale). Email scoreboard's inline `formatRevenue` swapped to the shared util. Both `earningsPreview` + `earningsRecap` system prompts now carry a strict number-format directive forbidding raw-digit revenues like `4,345,870,107`. +13 boundary tests in `tests/lib/format.test.ts`.
- ✅ **Track B — SymbolLink consistency** (`d891a62`). `EarningsHub` ticker is now a real `<SymbolLink>` (was a plain span); `TodayReleases` prefixes earnings rows with the clickable symbol. Other components (`UpcomingEventsCard`, `MorningBriefing`, `CalendarView`) already had the conditional pattern.
- ✅ **Track C — In-app email viewer** (`d891a62`). `earnings_emails.ai_output_md` already stored markdown; `briefingToHtml` was already pure → no migration. New `EarningsEmailViewer.tsx` modal iframe-renders the full sent email at fidelity (light scoreboard rebuilt deterministically + AI prose verbatim). New `GET /api/earnings/email-content?eventId=&phase=` returns `{title, sentAt, sentTo, fullHtml}`. New `lib/queries/earnings-emails.ts` exposes `getEmailAudit` + `getSentPhasesForEvents`. `renderHeadlineTable` + `formatDateLong` exported from the composer for re-use. `EarningsHub` row chips converted to clickable `EarningsRowChips.tsx` button-style; `CalendarView` expanded panel exposes "View preview email" / "View recap email" links via the new query passed from the page.
- ✅ **Track D — Bogeys upload + manual entry + actuals override + composer integration** (`d891a62`, the meaty one). Migration 043 `earnings_bogeys` (event_id, source pdf_upload/manual/newsletter, source_label, eps/rev consensus + whisper, segments JSON, guidance_notes, R2 key, research_document_id, research_article_id) with UNIQUE on (event_id, source, source_label). New AI feature key `earningsBogeysExtraction` → Sonnet 4.6 with native PDF document content block. Multi-symbol fan-out: `lib/earnings/extract-bogeys.ts` extracts per-symbol from one PDF, fans out via `issuerSiblings()` to matching `calendar_events` for the target week. R2 archival via `buildStatementKey({sourceType:"earnings_bogeys_pdf"})` (graceful no-op without R2 env vars). Three new routes: `POST /api/earnings/bogeys/upload` (multipart), `POST /api/earnings/bogeys` (manual entry CRUD), `POST /api/earnings/actuals` (manual override of reported EPS/Revenue when enrichment misses — writes back to `calendar_events.actual_value` in Finnhub-shape so all downstream readers Just Work). Composer integration: `getBogeysForEvent` + new `renderBogeysBlock` sits directly after user notes in both prompts; recap explicitly anchors beat/miss on whisper when present. UI: `+ Upload bogeys PDF` button on Earnings Hub footer, per-row `+ bog` / `✎ bog` chip opens `BogeysEditModal.tsx` with three sections (existing list + delete, **actuals override**, manual entry). All currency inputs accept `$4.34B` / `4340M` / raw via `parseLargeUSD`. +13 tests across `parseExtractionResponse` tolerance + `upsertBogey` idempotency.
- ✅ **Track E + redesign — Bloomberg-style Earnings Hub data desk + dual-class fixes + finnhub-figure formatter** (`d891a62`). Today page no longer caps at `max-w-2xl` (672px) — uses dashboard's `max-w-[1600px]` (verified at 1392px on a 1440 viewport). Alerts + Levels grid collapses to single col when nearby-levels card is empty (was leaving a 684px dead column). EarningsHub rewritten as Bloomberg-style data desk: 10-col explicit grid on desktop (`When · Pos · Ticker · Cons EPS · Act EPS · Cons Rev · Act Rev · Δ · Bogeys · Email`), gold day-of-week separators with mono uppercase tracking, hex-spec `#10b981` / `#ef4444` for delta (no Tailwind theme drift). Mobile: stacked 4-row card per event, no horizontal overflow. New shared `lib/format/finnhub-figure.ts` (`parseFinnhubFigure` + `formatFinnhubFigure[Compact]`) — applied EVERYWHERE Finnhub strings reach the user: Hub rows, `TodayReleases` est line + `EnrichmentRowSummary` + `EnrichmentDetail`, email scoreboard. No more raw-digit revenue leaks anywhere. Dual-class fixed: `getSymbolStatus` walks `issuerSiblings()` so GOOGL events show HELD when user holds GOOG; new `getSecurityIdForSymbolWithSiblings` + post-process in `getEarningsForWeekDeduped` resolves null `security_id` via family for SymbolLink. Font sweep on Today: 12-13px row body → 14px to match Security Detail's TerminalTD baseline. +8 tests in `tests/lib/finnhub-figure.test.ts`. Verified visually via two `agent-browser` runs at 1440×900 + 375×812: table renders all 10 columns with compact numbers, day headers gold, GOOGL HELD chip, mobile cards stack cleanly, no console errors, alerts spans full width.

New from this session (open):

- [ ] **Earnings preview — source hierarchy work** (next phase, scope TBD per user 2026-04-27). Currently the composer treats all preferred sources equally and falls back to a wider net only when zero preferred articles match. Future: per-source weighting by historical hit-rate, per-source content-extraction profiles (TMT Breakout's "bogies" sections vs. VK's free-form prose vs. Eliant's options-heavy framing), and a deduplication step when multiple sources cover the same name. Multi-session — needs design doc.
- [ ] **Worker fallback content parity with Mac** (deferred — sequel to Phase 4). Once Mac email content stabilizes after iteration, mirror the rich context (newsletter bogies, analyst recs, prior transcripts, user notes) into the Worker fallback. Today's lean fallback is intentional: trying to mirror now would create a divergence problem the moment we touch the Mac prompt.

Closed this session (2026-04-28 night — design preview mockups for holistic redesign):

- ✅ **Holistic app redesign — mockup phase** (`a1b5067`, 58 files, 6,504 LOC). Six aesthetic directions built under `app/design-preview/*` for browseable comparison. **Direction chosen for light mode:** Sage & Linen + IBM Plex (warm cream `#f7f4ed`, deep moss brand, sage gain / burned sienna loss, Plex Sans body + Plex Mono numbers). **Direction chosen for dark mode:** Bloomberg-pro (soft-black `#0a0a0a`, amber brand `#ffb84d`, Plex Mono everywhere, semantic green/red). All five reference views (Today, Overview, Security Detail, Holdings, Calendar) live in both chosen directions at `/design-preview/refined` and `/design-preview/dark`. **Rejected:** Light Paper, Pure Terminal as default (kept as basis for Bloomberg dark), Modern Brokerage v1, Source Serif headers. Mobile-responsive baked in (≤390px clean) — three CSS lessons captured in topic file: `min-width: 0` on grid children prevents track inflation, `overflow-x: clip` on body+main is the safety net, Sparkline uses viewBox + 100% width never hardcoded SVG width attribute. Body text contrast bumped to 8.7:1 on cream (was 4.5:1) after user feedback. Smallest font tier raised 20% (10→12px floor) after user feedback. **Topic file overhauled:** `memory/project_holistic_redesign.md` now captures chosen palettes + tokens + primitives + mobile lessons + rejected-direction notes + 5-7 phase rollout plan.

New from 2026-04-28 night (open — gating future work):

- [ ] **UX audit before visual rollout** (~60-90 min, supervised). Run `~/.claude/plans/ux-audit.md` in a fresh session. Structured walkthrough of 22 surfaces (9 tabs + chat drawer + 4 email surfaces + 5 sub-pages + cross-cutting flows). Output: prioritized wishlist at `~/.claude/plans/ux-wishlist.md` with severity tags (STRUCTURAL / FLOW / POLISH / CUT / MISSING). Recommendation in wishlist drives whether visual rollout proceeds as-planned (0 STRUCTURAL items) or stalls for UX restructure (1+ STRUCTURAL items). **Must run before visual rollout.**
- ✅ **Visual rollout — Phase 1 (Foundation)** — shipped 2026-04-29 evening (`92ceaf4` on branch `redesign-rollout`, NOT yet merged to main). 7 files changed (+321 / −177): dual-theme token system in `app/globals.css` (`:root` Sage & Linen light + `[data-theme="dark"]` Bloomberg-pro), font swap to IBM Plex Sans + Mono with Instrument Serif kept transitionally, anti-FOUC inline script in `app/layout.tsx`, new `ThemeToggle.tsx` (sun/moon icon, localStorage `vgs:theme`), header rename "Vanguard Skin" → "Portfolio Desk" in `app/dashboard/layout.tsx` + `MobileNavDrawer.tsx`, `DataConfidenceIndicator` chrome shrink to dot + %, `EarningsHub.tsx` theme-adaptive refactor (replaced ~40 hardcoded dark hex with token classes). Tests at baseline (1376 pass / 4 pre-existing fail in `tests/security-levels.test.ts`); next build clean. Two iterations on user feedback during review: EarningsHub theme-adapt + ink-faint contrast bump 3.7:1 → 5.7:1 → 7.0:1. Worktree at `~/code/vanguard-skin-redesign-worktree/` on branch `redesign-rollout`. Plan: `~/.claude/plans/redesign-rollout-handoff.md` (8 phases total).
- ✅ **Visual rollout — Phase 2 (Chrome lock-in)** — shipped 2026-04-29 (`1e049e6` on branch `redesign-rollout`). See "Closed this session (2026-04-29 — visual rollout Phase 2)" above.
- ✅ **Visual rollout — Phase 3 (Today re-skin + Week-Ahead sub-view + Hero absorption)** — shipped 2026-04-30 (`becd42f` on branch `redesign-rollout`, 22 commits). Phase 3 base landed in the first ~30 min as `0f1b8c0`; remaining 21 commits were a multi-hour design exploration that **pivoted the light-mode palette from Sage & Linen → Amber (Bloomberg-light)** and locked specific layout/shadow/density decisions. Locked design lives at `/design-preview/today-options/amber-roomy-final`: strip-header (no Hero card), medium density (p-4 sm:p-5), 3-layer moss-tinted shadow (ring 20% + close drop 40% + ambient 30%), big section gaps (space-y-8). Production state at end of Phase 3: still on Sage & Linen tokens — locked design isolated to `/design-preview/*` routes. Memory file: `memory/project_holistic_redesign.md` (REVISED 2026-04-30 section). Production port = Phase 3.5 below.
- ✅ **Visual rollout — Phase 3.5 (Production port of locked Today design)** — shipped 2026-04-30 evening (`537f915` on branch `redesign-rollout`, 5 files +127 / −112). Token swap in `app/globals.css :root` (canvas/panel/raised/muted/edge → Amber values, gold #3a4a3f → #b8860b, up/down → emerald/ruby, shadow-card → 3-layer moss-tinted), borderless light cards via new rule scoped to `:root:not([data-theme="dark"])` so dark mode keeps its hairlines (its shadow has no ring layer). `app/dashboard/today/page.tsx` Hero card → slim horizontal strip with `border-b border-edge` and smaller font clamp. Card paddings tightened to medium tier (`p-4 sm:p-5`), section spacing bumped (`space-y-6 → space-y-8`). `WeekAheadView.tsx`, `TodayReleases.tsx`, `NearbyLevelsCard.tsx` mirrored to medium density. **Verified**: 1376 vitest passing (4 pre-existing B1 failures, baseline), `next build` clean, browser agent visual diff confirmed production matches reference, Accounts + Research auto-adapted via global token swap, dark mode preserved card borders, mobile reflowed without horizontal scroll, privacy mode masks portfolio strip + holdings correctly. Pre-existing follow-up surfaced (NOT a Phase 3.5 regression): EarningsHub "Cons. Rev / Act. Rev / Act EPS" columns leak dollar amounts under privacy mode — see new open item below.
- ✅ **Visual rollout — all 9 phases SHIPPED** — closed 2026-04-30 night-end. Phases 0-3.5 + 4-7 landed across the day (`redesign-rollout`); Phase 8 closed at `6909e4c`; merged to `main` as `a800884` during `/session-end`. Holistic redesign rollout complete.

Operational follow-ups completed in-session (no commit):

- ✅ **TER 2026-04-28 calendar row inserted manually** (id=259, last night). Finnhub had only Q2 (2026-07-27); user-confirmed Q1 release tomorrow AMC. Inserted with `source='manual'`, `release_time='16:15'`.
- ✅ **GLW + TER pre-flight syncs** (last night): 8 monthly analyst-recommendation snapshots + 250 press releases over last 30 days. Price targets remain empty per Finnhub free-tier silent return.
- ✅ **Live AMC preview fire via cron** (14:06 ET today): HOOD + V + TER previews delivered to production recipient list via the new `/api/cron/earnings-sweep` endpoint. Audit rows landed; next 15-min tick will skip them cleanly. **Recap window for these three opens at ~18:15 ET when the post-release enrichment runner populates `actual_value`.**

Closed this session (2026-04-29 — IA session + Portfolio Desk rebrand + Resend email swap):

- ✅ **IA session — decisions locked** (no commit; output at `~/.claude/plans/ia-decisions.md`). 30-min run-through of the 10 decision threads from `ia-session-plan.md` plus the 4 audit-gap pre-session questions. Final shape: 9 desktop tabs → **6** (Today | Accounts | Analysis | Research | Charts | Import); cut Overview (→ Today), Holdings (→ Cmd+K ticker-jump + Accounts table), Calendar (→ Today sub-view); Performance + Trade Reviews → Analysis sub-views; Notes ambient + Research home + mobile slot; Alerts unified single auto-promoted stream; chat = persistent right rail (desktop) + overlay (mobile); Cmd+K repurposed to global ticker-jump; mobile bottom nav = Today / Research / Chat / Notes / Analysis; cross-cutting rules locked (no horizontal scroll, progressive disclosure, reader-app long-form, dot+% data confidence, **rename "Vanguard Skin" → "Portfolio Desk"**). Visual rollout phases sketched at the bottom of the decisions file. Memory entries created for both the IA decisions and the Portfolio Desk branding (domain `myportfoliodesk.com` purchased same day). **Visual rollout is gated on `ia-decisions.md` existing — gate is now open.** One functional bug surfaced for Phase 7 prep: newsletter level extraction is broken (Alerts auto-promoted stream would render empty for the wrong reason without the fix).
- ✅ **Resend email swap — outbound migrated, inbound preserved** (`dc9577d`, 15 files, +197/-142). Outbound (Sunday briefing, daily digest, earnings preview/recap) now sends from `{briefing,digest,earnings}@myportfoliodesk.com` via Resend SMTP (Mac) and Resend REST (Worker fallback). Inbound stays on Gmail intentionally (Vital Knowledge IMAP on Mac, OAuth REST for newsletter listing in `workers/cron/src/gmail.ts` — kept read-only after stripping `sendMessage` + `buildMime` + dead base64 encoders). Cleaner call sites: 3 send-* files lose redundant env-var checks; new shared sender modules accept `fromLocalPart` so adding a new outbound surface is a one-arg change. Settings UI fields added through `electron/settings-store.ts` + `electron/main.ts` + `lib/settings/file-store.ts` + `app/dashboard/components/SettingsModal.tsx` (with inbound/outbound labels). Worker `Env` swapped `FROM_EMAIL` → `RESEND_API_KEY` + `RESEND_FROM_DOMAIN`; live `wrangler secret put` + `wrangler deploy` shipped Worker version `cb266b46-68be-4c48-b381-62103d4d61a2`. **Live verified end-to-end**: smoke test from `noreply@myportfoliodesk.com` reached both `isaac@wolfsonfamily.com` + `esafier@woodlotcapital.com` with `dkim=pass header.i=@myportfoliodesk.com` + `spf=pass smtp.mailfrom=...@send.myportfoliodesk.com` + DMARC implicitly aligned. Cloudflare Email Routing catch-all (`*@myportfoliodesk.com` → `isaac@wolfsonfamily.com`) configured for inbound replies. **Tomorrow 8:45 AM ET digest is the first cron-driven test**; verification routine `trig_01P1u1jtRdvvwyDSseowDbEU` scheduled via `/schedule` to fire 9:00 AM ET tomorrow and report via Gmail MCP. Memory file: `memory/project_resend_email_swap.md`.

Operational follow-ups completed in-session (no commit):

- ✅ **`myportfoliodesk.com` domain purchased on Cloudflare** (12:43 ET) — DNS already on Cloudflare; "Auto configure" Resend → CF DNS path verified domain in <2 min.
- ✅ **Worker secrets pushed live** — `RESEND_API_KEY`, `RESEND_FROM_DOMAIN` set; `FROM_EMAIL` deleted; `WORKER_GMAIL_*` preserved for inbound.
- ✅ **Cloudflare Email Routing enabled on `myportfoliodesk.com`** — apex MX/SPF/DKIM Locked; catch-all rule active to `isaac@wolfsonfamily.com`; redundant `briefings@` custom rule deleted (catch-all covered it).
- ✅ **Verification routine scheduled** — one-shot remote agent `trig_01P1u1jtRdvvwyDSseowDbEU`, fires 2026-04-30T13:00:00Z (9:00 AM EDT), uses Gmail MCP to confirm digest landed with correct From-address + auth headers + INBOX label. Result transcript at https://claude.ai/code/routines/trig_01P1u1jtRdvvwyDSseowDbEU.

Closed this session (2026-04-29 evening — visual rollout Phase 0 + Phase 1):

- ✅ **Rollout handoff plan written** — `~/.claude/plans/redesign-rollout-handoff.md`. 8 phases (0-8) with per-phase goal, surfaces touched, dependencies, pre-flight, acceptance checklist, rollback unit, supervised-vs-autonomous flag, risks. Includes pre-rollout blockers table (B1 broken newsletter level extraction → Phase 7; B2 Analysis data-quality session → Phase 5; B3 worktree setup → Phase 0; B4 optional earnings-mute), enforcement checklist (5 cross-cutting rules from `ia-decisions.md` re-verified at every phase), full-rollout risk register. Memory file `project_holistic_redesign.md` updated to reflect gate cleared.
- ✅ **Phase 0 — worktree setup** (this session, no commit). `git worktree add ~/code/vanguard-skin-redesign-worktree -b redesign-rollout` with isolated `node_modules` (712 packages), `data/vanguard.db` + `-shm` + `-wal` copied for consistent SQLite snapshot, dev server boots on `:3098` without colliding with main's `:3099`. **Discovery:** 4 pre-existing test failures in `tests/security-levels.test.ts` on the `findCrossedLevels` + `review_status='auto_approved'` whitelist path — verified to reproduce in main with no code changes. These are the same code path as B1 (broken newsletter level extraction) and have been documented as B1's reproducer in the handoff doc. **Workaround applied:** `~/.npm/_cacache/content-v2/sha512/61` is owned by `root` (some past `sudo npm install`), so the worktree install used `--cache /tmp/npm-cache-redesign-rollout` to bypass it.
- ✅ **Phase 1 — Foundation** — see "New from 2026-04-28 night" line above; full details there.

Closed this session (2026-04-29 — visual rollout Phase 2):

- ✅ **Visual rollout — Phase 2 (Chrome lock-in)** — shipped 2026-04-29 (`1e049e6` on branch `redesign-rollout`, NOT yet merged to main). 12 files changed (+231/-427 — net-negative because three redirect pages dropped 60-150 LOC of DB queries each for a 6-line `redirect()` call). **6-tab desktop nav:** `nav-tabs.ts` reduced 9 → 6 (Today / Accounts / Analysis / Research / Charts / Import); cut Overview, Holdings, Calendar; Trade Reviews subview dropped from Research (relocates to Analysis in Phase 5). **Mobile bottom nav:** Calendar slot replaced with Notes (`/dashboard/research?view=notes`); active-state now uses `useSearchParams` so Research highlights when view≠notes and Notes highlights when view=notes — both can route to the same path without highlighting together. Wrapped in `<Suspense>` for the searchParams CSR-bailout. **Chat split into three layout modes** via new `lib/hooks/useIsLargeDesktop.ts` (`(min-width: 1280px)` matchMedia): mobile (<768px) keeps fullscreen overlay; desktop (768-1279px) keeps slide-in drawer + Cmd+J toggle; xl (≥1280px) is always-visible 480px right rail. Layout reserves `xl:pr-[480px]` so content doesn't underlap. ChatToggleButton hidden at xl. Cmd+J at xl dispatches new `focus-chat-input` event (ChatInterface focuses textarea) instead of toggling. **CommandPalette repurposed as global ticker-jump:** `/api/search` gained `?type=security` short-circuit (returns just securities, skips notes/articles/etc); palette UI is symbol-first with placeholder "Jump to ticker (AAPL, NVDA, ...)" + ticker rendered in gold mono on left. **Old routes redirect** (files retained through Phase 8 cleanup so iPhone home-screen shortcuts don't 404): `/dashboard` → `/dashboard/today`, `/dashboard/holdings` → `/dashboard/today`, `/dashboard/calendar` → `/dashboard/today?view=week-ahead`. Acceptance: tests 1376/1380 (4 pre-existing security-levels failures, B1 reproducer — no Phase 2 regression); next build clean (redirect routes now Static ○ since they no longer hit DB; force-dynamic baseline preserved on the 7 surviving DB pages); 9-item agent-browser checklist all PASS at 1440x900, 1024x768, 390x844 (no horizontal scroll, rail always-visible at xl, Cmd+K returns AAPL in gold mono, all three redirects land on Today). DMG NOT rebuilt per handoff policy (rollout work stays in worktree; DMG ships from main only after Phase 8 merges).

New from this session (open):

- [ ] **`~/.npm/_cacache/content-v2/sha512/61` is root-owned** — needs `sudo chown -R Yitzi:staff ~/.npm` (user-approved 2026-04-29 but Claude can't sudo from non-interactive session). Run as `! sudo chown -R Yitzi:staff ~/.npm` from a Claude prompt OR run manually in any terminal. Until fixed, future `npm install` in worktree must pass `--cache /tmp/npm-cache-redesign-rollout` (or any non-default cache path).
- ✅ **B1 — newsletter level extraction + 4 test failures** — closed 2026-04-30 night-late (`c819c55` on `main`, cherry-picked to `redesign-rollout` as `fa61b55`). Two halves: (a) `seedPrice` test default date `"2026-04-20"` had aged out 10 days past the 4-day stale-price guard — 4 tests failing on `findCrossedLevels` were calendar-bitrot, fixed by switching the default to `new Date().toISOString().slice(0, 10)`. (b) `newsletterLevelExtraction` was on Kimi K2.6 (Workers AI) which produced 0 levels in 30 days; direct curl probe confirmed Kimi's `reasoning_content` consumed the full 2048 `max_tokens` budget before any visible JSON (`finish_reason: length`, `content: null`). Reverted to `anthropic/${SONNET_MODEL}`; smoke on 10 articles produced first real `pending_review` row (TER entry @ $280, Irrational Analysis). 1380/1380 on main, 1387/1387 on rollout. Phase 7 unblocked.
- ✅ **Phase 8 brand-prose sweep** — closed 2026-04-30 night-end (`6909e4c` on `redesign-rollout`, merged via `a800884` on `main`). All 3 references swapped: `lib/compute/tax-report.ts:274` (TXF identifier — preserved the `A` TXF control prefix), `app/dashboard/components/CanonicalCsvGuide.tsx:121,144` (both prompt builders), and `docs/canonical-csv-guide.md`. `tests/import/canonical-csv.test.ts` had no brand string — three-way sync was effectively two-way here.
- ✅ **Phase 8 font-serif sweep** — closed 2026-04-30 night-end (`6909e4c`). All 8 production `font-serif` references replaced with `font-medium` weight bumps (WelcomeOverlay, DataHealthView, ChatInterface, alerts, today, levels/performance, WeekAheadView ×2). `Instrument_Serif` import + variable + className removed from `app/layout.tsx`; `--font-serif` token removed from `app/globals.css`. Acceptance gate met: `grep -rn "font-serif" app/dashboard lib` returns zero. WelcomeOverlay's "Vanguard Dashboard" h1 text intentionally preserved — that's the Electron app display name, separate from the Phase 1 chrome rename.

Closed this session (2026-04-30 — visual rollout Phase 3 + Phase 3.5):

- ✅ **Visual rollout — Phase 3 (Today re-skin + design exploration + light-mode pivot)** — shipped 2026-04-30 across 22 commits ending `becd42f` on branch `redesign-rollout`. Phase 3 base (`0f1b8c0`) landed in ~30 min: Today re-skin + Week-Ahead sub-view at `?view=week-ahead` + Hero portfolio card absorption from old Overview tab. Remaining 21 commits were a multi-hour design exploration that **pivoted the light-mode palette from Sage & Linen → Amber (Bloomberg-light)** and locked specific layout/shadow/density decisions. Direction iterated through Sage & Linen tints → uniform Quiet/Zones/Newsprint/Editorial → Forest/Sienna/Amber → Amber border experiments → frame thickness → border-vs-glow → final direction. Locked at `/design-preview/today-options/amber-roomy-final`: strip-header (no Hero card), medium density, 3-layer moss-tinted shadow (ring 20% + close drop 40% + ambient 30%), big section gaps (space-y-8). Detail in `memory/project_holistic_redesign.md` (REVISED 2026-04-30 section). **Iteration cost note:** "vibe-driven" feedback (too vanilla, more color, more grounded) was hard to translate to specific CSS without comparison previews. Lesson: for substantial design exploration, build the `/design-preview/<feature>-options` comparison page IN PARALLEL with the production work, not after.
- ✅ **Visual rollout — Phase 3.5 (Production port of locked Today design)** — shipped 2026-04-30 evening (`537f915`, 5 files +127/-112). Token swap in `app/globals.css :root`: canvas/panel/raised/muted/edge → Amber values, gold #3a4a3f → vivid amber #b8860b (Bloomberg-light brand), up/down → emerald/ruby for white-card contrast, blue → deep navy #1a3b8a, ink hierarchy 21:1 / 14:1 / 7:1, shadow-card → 3-layer moss-tinted (kept moss tint after Sage→Amber pivot because moss reads as a believable cast shadow on warm canvas; pure-black would feel harsh). Borderless light cards via new rule scoped to `:root:not([data-theme="dark"]) .rounded-xl.bg-panel|.bg-panel-sage|.bg-panel-warm|.card-elev { border: 0 }` — dark mode keeps its hairlines (its shadow has no ring layer + #0d0d0d-on-#0a0a0a brightness delta is too subtle). `app/dashboard/today/page.tsx` Hero card → slim horizontal strip (border-b border-edge, smaller font clamp 22-28px). Card paddings tightened to medium tier (`p-4 sm:p-5`), section spacing bumped (`space-y-6 → space-y-8`), Holdings list `-mx-4 / px-4 / py-2`. `WeekAheadView.tsx` mirrored to medium density (DayCard p-4 sm:p-5, day grid gap-4, event tile p-3, day header mb-3, event list space-y-2). `TodayReleases.tsx` + `NearbyLevelsCard.tsx` padding tweaks. **Verified**: `npx vitest run` 1376 passing (4 pre-existing B1 failures, baseline); `npx next build` clean; browser-agent visual diff confirmed production `/dashboard/today` matches reference `/design-preview/today-options/amber-roomy-final`; Accounts + Research auto-adapted via global token swap with no breakage; dark mode preserved card borders; mobile reflowed without horizontal scroll (scrollWidth=541 ≤ innerWidth=546 at 546px); privacy mode masks portfolio strip + IBKR holdings + change pills correctly. Handoff doc updated to mark Phase 3.5 DONE; Phase 4 (Research) is now the next slot. DMG NOT rebuilt per handoff policy (rollout work stays in worktree; DMG ships from main only after Phase 8 merges).

New from this session (open):

- [ ] **EarningsHub privacy-mode leak** (surfaced 2026-04-30 by browser-agent visual check during Phase 3.5). When `vgs:privacyMode=on`, the EarningsHub "Cons. Rev / Act. Rev / Act EPS" columns still show real dollar amounts (e.g. `$0.85 → $269.7M`, `$0.70 → $4.348`). Other EarningsHub elements + portfolio strip + IBKR holdings DO mask correctly. Pre-existing — NOT introduced by Phase 3.5; the table just hadn't been routed through `<Money>` / `usePrivateFormatter` when EarningsHub was first built. Fix: wrap the cons/actual cells in `<Money>` (or `<PrivateText>` for the Finnhub-shape strings). ~20 min standalone bug fix; consider folding into Phase 4 if it touches the same component.

Closed this session (2026-04-30 evening — visual rollout Phase 4):

- ✅ **Visual rollout — Phase 4 (Research restructure + ambient Notes + reader-app Feeds + email reader treatment)** — shipped 2026-04-30 evening (`3a4d014` on branch `redesign-rollout`, 7 files +268/-66). **ResearchViewToggle** drops the Trade Reviews entry; legacy `?view=reviews` still resolves so bookmarks keep working until Phase 5 moves Trade Reviews to Analysis. **NotesAmbient** (new component): floating pen-icon FAB on every dashboard page + Cmd+; keyboard shortcut, drafts persist to `localStorage:vgs:notes-ambient` on debounced keystroke, "Save to Notes" POSTs a `journal` note via `/api/notes`, gold dot affordance when an unsaved draft exists, `xl:right-[500px]` offset keeps the FAB + open panel clear of the persistent chat rail at ≥1280px. **ResearchFeedsView** source filter unified to a single native `<select>` on every viewport (per user feedback the desktop pill cluster was "a jumble"); article summaries + portfolio_relevance bumped to 17px / 1.7 line-height; `.prose-reader` / `.prose-newsletter` to 18px / 1.7. **briefingToHtml** body 14px → 16px and 1.65 → 1.7 line-height; paragraph / list-item / blockquote vertical rhythm relaxed for reader-app feel. Verified: 1376 vitest passing (4 pre-existing B1 failures, baseline); `next build` clean. DMG NOT rebuilt per rollout policy. Visual sign-off via iPhone over LAN at `192.168.1.154:3098` — desktop browser verification was blocked because the worktree has no Playwright install.

New from this session (open):

- [ ] **Notes area rework** (Phase 4 follow-up, 2026-04-30) — current note types `journal / earnings / trade_thesis` leave no natural home for a "stock-related but not earnings" thought. Rework so a per-security non-earnings note has a clear destination. Open question parked: should NotesAmbient's "Save to Notes" route the user to a full notes page for the just-saved entry? (User: "I'm not totally sure on that. You could leave it for now.") Revisit during the rework — likely interleaves with Phase 5 (Analysis) since Trade Reviews relocates then.
- ✅ **Caret affordance dislike** (2026-04-30) — closed 2026-05-01 quick-wins sweep (`790d317`). `TabDropdown.tsx` swaps `▾` for `•••` overflow icon; gold open-state color replaces rotation animation. All caret affordances retired.
- [ ] **Wire `scripts/refresh-risk-free-rate.ts` into launchd OR daily-digest cron** (Phase 5 D2 follow-up, 2026-04-30 late night) — script committed in Phase 5 fetches DGS3MO from FRED and writes to settings cache, but doesn't auto-run. Two options: (a) new launchd plist `~/Library/LaunchAgents/com.vanguard-skin.risk-free-rate.plist` with daily 9am ET trigger (simplest), OR (b) inline `refreshRiskFreeRateFromFred(db)` call into the existing daily-digest path which already runs FRED-adjacent code (zero new launchd surface). Until wired, the rate stays at fallback 0.045 → Sharpe ratios drift slightly. ~10 min for either option.
- [ ] **Run `scripts/backfill-bond-durations.ts` against live DB** (Phase 5 D4 follow-up, 2026-04-30 late night) — script committed but not yet executed against the production DB. `npx tsx scripts/backfill-bond-durations.ts --dry-run` first to preview, then without flag to apply. One-time op; affects scenario rate-shock numbers for held bonds with null `duration_years`.
- [ ] **Verify Phase 5 visual rendering on iPhone** (2026-04-30 late night) — Phase 5 work done in worktree but not visually tested via iPhone-over-Mesh on `:3098` because session ended at commit time. Check: Momentum Pulse tile renders correctly when status is non-neutral (likely currently neutral so won't show on Today — that's expected behavior); Performance sub-view layout at 390px; Income/Yield section at the bottom of Analysis; benchmark picker dropdown on Factor Analysis card; sector beta tooltip expand/collapse on Scenarios card; EmptySection rendering on Options sub-cards (no options held in any account). Worktree dev server `:3098` is the verification target.
- ✅ **EarningsHub privacy leak still open** (carried from Phase 3.5) — closed 2026-05-01 quick-wins sweep (`92f76d6`). NumCell + MobileCard wrap pre-formatted Finnhub-shape strings in `<PrivateText>`.

Closed this session (2026-04-30 late night — visual rollout Phase 5):

- ✅ **Visual rollout — Phase 5 (Analysis re-skin + Performance + Trade Reviews + Momentum Pulse + B2 fixes)** — shipped 2026-04-30 late night (`3550b62` on branch `redesign-rollout`, 26 files +1460/-82). **Streams A-C (additive sub-views):** (A) Momentum Pulse — added MTUM/SPMO/USMV to `BENCHMARK_SYMBOLS`, new `lib/compute/momentum-spread.ts` with `computeMomentumPulse` returning `{leading | neutral | weakening | sell_off | insufficient_data}`, +7 tests, new `MomentumPulse.tsx` Today tile that renders only on non-neutral status. All public market data → `formatPercent()` not `<Pct>` (per `feedback_privacy_market_data.md`). (B) Trade Reviews relocated to `/dashboard/analysis?view=trade-reviews`; `/dashboard/research?view=reviews` now redirects; legacy data-loading branch removed from research page. (C) Performance sub-view at `/dashboard/analysis?view=performance` — TWR + XIRR + period selector (YTD/1Y/3Y/5Y/All) + scope picker + per-account breakdown when >1 account. **Stream D (B2 data-quality):** (D1) `daily-valuation.ts:117` cashBalance=0 audit claim was wrong — Phase 2 already infers cash from monthly snapshot anchors; added clarifying comment so future readers don't trip on it. (D2) FRED DGS3MO risk-free-rate plumbing replaces hardcoded 0.045 in `risk.ts` + `options-greeks.ts`; new `lib/queries/risk-free-rate.ts` with lazy 48h cache in settings table, defensive `try/catch` for in-memory test DBs without settings; new `scripts/refresh-risk-free-rate.ts` ready for launchd. (D3) Per-scope benchmark default + dropdown picker on FactorAnalysis card — Vanguard→VTI, IBKR→QQQ, Roth→SPY, ALL→SPY; factors API already accepted `benchmark` param. (D4) `scripts/backfill-bond-durations.ts` (years_to_maturity × 0.85) replaces the 5y flat fallback for held bonds; ScenarioModeling card gained an expandable details section listing all sector/style/size beta multipliers. (D5) New `IncomeYieldSection` at the bottom of Analysis — TTM dividends + interest + fees + net income + top 10 payers + per-account breakdown; new `lib/queries/income.ts`. (D6) `EmptySection` primitive replaces silent `return null` in OptionsGreeksCard / OptionsStrategies / ExpirationCalendar / FixedIncomeCard with titled empty card + "Why is this empty?" reason. **Re-skin:** all 10 Analysis card components swapped to Phase 3.5 Amber chrome (`bg-panel rounded-xl p-4 sm:p-5 card-elev`); borders auto-drop in light mode via `globals.css`. **Verified:** 1383/1387 tests passing (4 pre-existing B1 failures, baseline; +7 from new momentum tests, no regressions); `next build` clean; type-check clean. **B2 closed** — Phase 5 unblocked + shipped. DMG NOT rebuilt per rollout policy.

Closed this session (2026-04-30 night — visual rollout Phase 6):

- ✅ **Visual rollout — Phase 6 (Accounts re-skin + Holdings table absorption)** — shipped 2026-04-30 night (`82fa8fd` on branch `redesign-rollout`, 6 files +185/-79). New `lib/queries/holdings.ts::getAllHoldings(db)` hoists the cross-account holdings SQL out of the old (now-redirect) `/dashboard/holdings/page.tsx` into the canonical `lib/queries/` location; `AllHoldingsTable` re-exports its row type from there to eliminate the duplicate definition. New `app/dashboard/components/AccountSelector.tsx` (client) renders a pill row at the top of `/dashboard/accounts` with an "All Accounts" entry plus per-account pills; `?id=all` triggers the cross-account view (renders `<AllHoldingsTable>` inside `<section id="holdings">` so `#holdings` deep-links land correctly), per-account remains the default. `AccountDetail` stripped of its internal pill row (now provided by the page so both branches share the same selector). **De-caret pass:** `ReconciliationSection`'s rotating chevron `<polyline>` swapped for literal "Show"/"Hide" text on the right side of the section header — `aria-expanded` retained for a11y. **Holdings redirect retargeted:** `/dashboard/holdings` now → `/dashboard/accounts?id=all#holdings` (was `/dashboard/today` from Phase 2; matches Phase 6 spec). Verified: 1376 vitest passing (4 pre-existing B1 failures, baseline); `next build` clean; agent-browser visual at desktop (1280px) + mobile (390px) confirmed all 6 acceptance criteria PASS — pill row renders + active-state correct, `?id=all` shows 146-position table with working sort + filter, `/dashboard/holdings` redirect lands at `accounts?id=all#holdings`, ReconciliationSection has zero `<svg>` inside the toggle button, mobile `documentElement.scrollWidth === clientWidth === 390` (no page-level horizontal overflow; table scroll lives inside `overflow-x-auto` wrapper), `<SymbolLink>` navigates to Security Detail. Pre-existing dev-mode `Performance.measure` console error on `HoldingsRedirect` was present before Phase 6 (only the redirect target string changed) — not a regression. DMG NOT rebuilt per rollout policy.

Closed this session (2026-04-30 night-late — B1 fix on main + visual rollout Phase 7 + iPhone polish):

- ✅ **B1 — newsletter level extraction + 4 test failures** — closed 2026-04-30 night-late (`c819c55` on `main`, cherry-picked to `redesign-rollout` as `fa61b55`). See ticked entry in "New from this session (open)" block above for full detail. 1380/1380 on main, 1387/1387 on rollout.
- ✅ **Visual rollout — Phase 7 (Alerts auto-promoted stream + Levels review merger)** — shipped 2026-04-30 night-late (`9ad23f8` on branch `redesign-rollout`, 6 files +762/-568). New `<NotificationBell>` replaces the prior split `<AlertsBell>` + `<ReviewBell>` — single bell that polls both `/api/alerts?countOnly=true` and `/api/levels/review?countOnly=true`, shows combined count badge, dual-section hover preview ("Pending alerts" + "Levels to review"), preserves the existing `alert-fired` event so `LevelsPanel` keeps refreshing on count increase. `app/dashboard/alerts/page.tsx` refactored into Suspense-wrapped client component (`useSearchParams` requires Suspense to avoid CSR-bailout at build). 6 filter pills (Pending default / Review / Acted / Ignored / Dismissed / All); Pending merges fired alerts + pending_review levels into one stream split by Today's-activity / Older-pending; Review filter retains the per-source-author grouping UX from the old standalone page. `app/dashboard/levels/review/page.tsx` is now a 3-line redirect to `/dashboard/alerts?view=review`. `AlertsBell.tsx` + `ReviewBell.tsx` deleted (no other importers). Verified: 1387/1387 tests passing; `next build` clean (alerts + levels/review both register as `○ Static`); redirect responds via `<meta http-equiv="refresh" content="1;url=/dashboard/alerts?view=review">`; header HTML emits exactly one bell SVG. DMG NOT rebuilt per rollout policy.
- ✅ **iPhone polish (4 commits on `redesign-rollout`)** — shipped 2026-04-30 night-late after live mobile testing surfaced issues. (1) `77ea0c7` mobile header trim — wrapped `TwsStatus` and `ThemeToggle` in `hidden md:flex` (mobile header was crammed with 8 elements causing "Portfolio Desk" title to wrap to 2 lines; trimmed to Title + Search + NotificationBell + PrivacyToggle + Settings = 6 elements per CLAUDE.md spec). Hidden the giant `font-serif text-2xl` "Today" h1 on mobile via `hidden md:block` (the date label above is enough orientation). Tightened section spacing `space-y-8` → `space-y-5 md:space-y-8`. (2) `4de767f` performance table overflow + first drawer attempt — `/dashboard/levels/performance` table wrapped in `overflow-x-auto` div with `min-w-[720px]` so all 8 columns render at full width and horizontal scroll is contained inside the rounded card; first attempt at drawer fix added inline `style={{ backgroundColor: "var(--panel)" }}`. (3) `edcb2e4` portal nav drawer to document.body — first inline-style attempt was applied (verified in served HTML) but drawer was still translucent on iOS Safari. Root cause: drawer rendered as descendant of `<header>` which has `bg-canvas/80 backdrop-blur-xl`. iOS Safari's treatment of `position: fixed` children inside a backdrop-filter parent caused page content to bleed through regardless of bg-color. Fix: hamburger button stays inline in the header for layout, but backdrop + drawer panel now portal to `document.body` via `React.createPortal` (matches `ChatDrawer` / `WelcomeOverlay` pattern). Z-indexes bumped backdrop `z-[60]`, drawer `z-[70]`. CSS variable fallback: `var(--panel, #ffffff)` so the drawer falls back to hard-coded white if the variable cascade ever fails. (4) `be610c9` swipe-left-to-close gesture — iOS-native finger-following dismiss on the mobile drawer. `onTouchMove` applies inline `translateX(dx)` with `transition: none` for 1:1 finger tracking (inline transform wins specificity over Tailwind translate-x-0/full classes). On touchend: dx ≤ -50px → `close()` (Tailwind animates dismiss); else `setDragX(0)` → snaps back. Guards: only treats as drag if `|dx| > |dy|` (lets vertical scrolls pass through) and `dx < 0` (rightward ignored); capped at -256px (drawer width). User confirmed all four fixes live on iPhone at `100.96.0.1:3098`.

New from this session (open):

Closed this session (2026-05-03 — briefing prompt escalation + email redesign):

- ✅ **§6 verbatim — pre-rendered cluster injection** — shipped 2026-05-03 (`6c7a766`, 4 files +514/-16). Last week's `f02517c` HARD RULE directive ("MUST equal the symbols in the Holdings exposure field") wasn't enough — 4/27 briefing still dropped CLH/GFL/PRIM/XMTR from ISM Manufacturing and added PWR + a "VIS basket" that wasn't in the deterministic field. Two structural fixes: (1) Pre-render the cluster line `**Holdings exposed:** SYM1, SYM2, ...` in TS as a "REQUIRED §6 cluster (paste this exact line)" field per macro event in `formatEventForPrompt`. Verbatim guarantee no longer relies on Opus's restraint. (2) Rewrite the §6 directive into 5 numbered HARD RULES (verbatim cluster, no multi-event lumping, cluster-after-prose, trust-the-data-over-memory, skip-if-no-mapping). **Live verified 5/04 regen: 8/8 PASS** on the verifier across all macro events with deterministic exposure (Housing Starts 8 names, ISM Services 33 names, JOLTS/ADP/Claims/NFP 11 each, UMich Sentiment 12). Updated regression test in `tests/calendar/briefing-macro-exposures.test.ts` to assert the new directive language + the cluster-injection field.
- ✅ **Email redesign — light Amber theme + reader-app spacing for all four outbound surfaces** — shipped 2026-05-03 (`b2201cc`, 2 files +318/-199). `lib/calendar/briefing-html.ts` is the single renderer for Sunday briefing + daily digest + earnings preview + earnings recap, so this redesign updates every outbound email in one shot. `workers/cron/src/html.ts` mirrors for cloud-fallback parity. **Design** — modeled on Research Feeds reader (`.prose-reader` / `.prose-newsletter` in `app/globals.css`): cream canvas (`#fafaf3`) as body bg (no white card — body sits directly on cream like reading on warm paper), 18px / 1.7 line-height in `--ink-dim` (`#2a2a26` softer than full black), 22px paragraph margins for airy spacing, 680px wrapper (~65ch), hero title in 28px amber gold, section `##` headers in ink-black with hairline edge underline, tiny `PORTFOLIO DESK` amber strip is the only chrome above body, date moved to footer. **Font stack** — `'IBM Plex Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif` and `'IBM Plex Mono', 'SF Mono', Menlo, Consolas, 'Courier New', monospace`. Plex first for users who have it, system fallback everywhere — Outlook → Segoe UI, Apple Mail → SF Pro, Gmail Android → Roboto. **NO webfont fetch**, so the user's brother on Outlook never breaks. Earnings scoreboard tables (locked 2026-04-28 for fill-by-hand printability) preserved as-is. **CRITICAL gotcha now in file header**: font-family names with spaces MUST use single quotes inside the double-quoted style attribute — mixing double quotes silently truncates the style attribute at the first inner quote, dropping every property after it (ate ~30 minutes today on "why isn't the color landing"). 229 calendar/digest/workers tests still passing post-change. Today's 3pm cron will still send the OLD dark theme (Electron DMG uses bundled pre-rebuild code); new design lands on next DMG rebuild via `/session-end`.
- ✅ **`scripts/verify-briefing-content.ts`** — new canonical verification harness for any future manual briefing regen. Runs 5 checks: §6 cluster verbatim diff per macro event, §3 earnings combined-position coverage, A1 price-hallucination scan (with smart filters for option strikes / EPS / dividends / comma-separated thousands), A5 self-admission regex sweep, A8 thesis-fingerprint check. Exits 0 on PASS, 1 on FAIL. Run via `npx tsx scripts/verify-briefing-content.ts --weekOf YYYY-MM-DD`. Companion `scripts/check-held-earnings.ts` rolls up held positions for a week's earnings event list (uses `issuerSiblings()` so dual-class siblings + options surface correctly).
- ✅ **5/04 briefing pre-generated + verified clean** (live, no commit). Pre-fired the briefing 2026-05-03 17:47:03 ($0.65 Opus call); verifier reports 8/8 §6 PASS, 19/20 §3 effective PASS (the one "FAIL" is a verifier false flag — Opus said "across 3 accounts" instead of naming each, which reads better), all stock-price citations match latest close to the cent (SPY $718.66, QQQ $667.74, INTC $94.48, etc.), no self-admission language. `isBriefingStale=false` so the 3pm cron will pick up this pre-gen. Briefing artifact saved to `~/Desktop/briefing-2026-05-04-FINAL.html`.

Closed this session (2026-05-01 — quick-wins sweep):

- ✅ **EarningsHub + Chat privacy leaks** — shipped `92f76d6` (2 files +25/-8). EarningsHub `NumCell` (desktop grid) and `MobileCard` (stacked rows) wrap `cons.eps` / `act.eps` / `cons.revenue` / `act.revenue` in `<PrivateText>`. Chat got a new `PrivateMarkdown` block-safe component for assistant messages (avoids `<p>`-inside-`<span>` invalid HTML); user messages wrap in `<PrivateText>`. agent-browser dev-server verification: 4/4 tests pass, 0 hydration warnings, 0 console errors. iPhone B1+B2 mobile-card verification deferred.
- ✅ **Caret affordance + bg-panel iOS audit** — shipped `790d317` (4 files +20/-6). `TabDropdown.tsx` swaps `▾` for `•••`, replaces rotation with gold open-state color (matches `feedback_no_carets.md` "standard '•••' overflow icon"). 4 header popovers (`DataConfidenceIndicator`, `TwsStatus`, `NotificationBell` hover, `TabDropdown` menu) get defensive inline `style={{ backgroundColor: "var(--panel)" }}` — same recipe as `4de767f`'s MobileNavDrawer fix, applied to every `bg-panel` surface that descends from the header's `backdrop-blur-xl` stacking context. iPhone B4 verified pass (the popovers themselves are desktop-only; visible mobile surfaces show no regression). 1387/1387 tests pass on both commits.

New from this session (open):

- [ ] **Phase 5 iPhone visual verify — A1 / A2 / A3 / A4 / A5 + privacy mobile-card B1 / B2 + dark-mode B5** — deferred from this session's quick-wins sweep (B4 popover-opacity passed). Walkthrough lives in `~/.claude/plans/can-you-pull-up-graceful-treehouse.md` Part A + B. Routes via Mesh `100.96.0.1:3099` (post-DMG-rebuild) or `:3000` (dev). ~20 min total when next at Mac + iPhone.

Closed this session (2026-04-30 night-end — visual rollout Phase 8 + merge to main):

- ✅ **Visual rollout — Phase 8 (brand sweep + font sweep + design-preview deletion + docs)** — shipped 2026-04-30 night-end (`6909e4c` on `redesign-rollout`, merged into `main` as `a800884`). 80 files changed (+28 / −7560 — design-preview deletion accounts for the deletion mass). **Brand sweep:** `lib/compute/tax-report.ts:274` TXF identifier, `app/dashboard/components/CanonicalCsvGuide.tsx:121,144` both prompt builders, `docs/canonical-csv-guide.md` — preserved the `A` TXF control prefix. **Font sweep:** 8 production `font-serif` references → `font-medium`; `Instrument_Serif` import + variable + className removed from `app/layout.tsx`; `--font-serif` token removed from `app/globals.css`. **Design-preview deletion:** entire `app/design-preview/` directory removed (74 files: dark, refined, light-paper, modern-brokerage, terminal, options, today-options, shared) — locked Amber tokens already live in production at `app/globals.css` + `/dashboard/today`. **Dead code:** orphan `app/dashboard/holdings/loading.tsx` deleted (page is now a redirect, no skeleton flash needed); other cut-tab redirect stubs (calendar, holdings, levels/review) kept as 5-line stubs to cover external bookmarks. **Docs:** `CLAUDE.md` Tab Structure + Mobile Responsive sections refreshed for the 6-tab IA + Notes mobile slot + unified Alerts inbox + theme toggle + NotesAmbient overlay + Cmd+K ticker-jump; `docs/DECISIONS.md` rollout entry; `memory/project_holistic_redesign.md` flipped status to "SHIPPED" with Phase 8 closeout section. **Inherited clean (no edits):** Import + Charts pages (server components delegating to children that already use Amber tokens); earnings preview/recap email composer (uses Phase 4's `briefingToHtml`); mobile `pb-safe`/`viewport-fit` baseline. Verified: 1387/1387 vitest passing on both branches; `next build` clean; merge-conflict on CLAUDE.md resolved by keeping main's richer "(newsletter articles + uploaded PDFs)" parenthetical and rollout's stale-Holdings-line removal. **DMG rebuild kicked off via `/session-end`** (electron:deploy in background; rebuilt from main post-merge).
- ✅ **Merge `redesign-rollout` → `main`** — 19 commits ahead, 8 behind; non-fast-forward merge commit `a800884`. Single CLAUDE.md conflict resolved (kept "(newsletter articles + uploaded PDFs)" + dropped stale Holdings line). Worktree at `~/code/vanguard-skin-redesign-worktree/` retained for now — user can `git worktree remove` + `git branch -d redesign-rollout` next session if desired (not auto-removed because pruning a worktree is a destructive operation requiring per-action confirmation).

New from this session (open):

- [ ] **In-app config UI for `FEATURE_MODELS` + other env-var-only configs** — user request 2026-04-30 night-late. Make per-feature model selection editable in Settings without code edits, so the user (and future distributors of the app) can swap Sonnet ↔ Haiku ↔ OpenAI etc. for cost tuning. Path: `lib/ai/models.ts::FEATURE_MODELS` is the single source of truth today; surface each feature key with a dropdown of supported models. User noted "applies to other areas as well" — when picking this up, audit which other env-var / code-only configs deserve UI surfacing (Pushover keys are already done; risk-free-rate cron schedule? AI Gateway routing? muted earnings symbols?). Likely a multi-session feature given the schema (settings-table backed override layer), the resolver change in `lib/ai/provider.ts`, and the Settings UI section. Tracked in MEMORY.md TODO too.
- ✅ **Tailwind v4 `bg-panel` opaque-rendering investigation** — closed 2026-05-01 quick-wins sweep (`790d317`). Audit identified 4 latent siblings in `app/dashboard/components/` that descend from header's `backdrop-blur-xl` stacking context (DataConfidenceIndicator, TwsStatus, NotificationBell hover, TabDropdown menu); patched each with defensive inline `style={{ backgroundColor: "var(--panel)" }}`. iPhone B4 spot-check passed (no regression on the surfaces visible on portrait). Underlying Tailwind v4 token-resolution question remains unconfirmed — the fix is defense-in-depth (matches the pattern other surfaces will need if they end up in a similar stacking context), not a definitive root cause.

---

## Roadmap / future (flagged 2026-04-25 late night)

Larger multi-session items the user wants captured:

- ✅ **IA session — pre-redesign architecture lock** — completed 2026-04-29 (decisions at `~/.claude/plans/ia-decisions.md`). 30-min run-through resolved all 7 structural items: 9→6 desktop tabs (cut Overview / Holdings / Calendar), Performance + Trade Reviews → Analysis sub-views, Notes home + ambient overlay + mobile slot, Alerts unified single auto-promoted stream, chat = persistent right rail (desktop) + overlay (mobile), Cmd+K → global ticker-jump, mobile bottom nav = Today / Research / Chat / Notes / Analysis. Cross-cutting rules locked + app rename to **Portfolio Desk** (domain `myportfoliodesk.com` purchased same day). See "Closed this session (2026-04-29)" above. **Gate is open — visual rollout Phase 1 can proceed in next session.**
- [ ] **iPad parity** — can the app be used on iPad the same way as on iPhone? Unknown effort. Likely small if the mobile breakpoints already hold at ~1024px and Cloudflare Mesh routes the iPad, but there might be layout regressions at the in-between tablet width (768-1024px) where `md:` kicks in but the screen isn't desktop-sized. First step: load the app on an iPad via Mesh and identify what breaks before scoping work.
- [ ] **Design overhaul — light mode default, keep "Midnight Portfolio" as dark mode** — multi-session redesign. User has said repeatedly they don't love the midnight-blue dark palette. Plan: preserve the current dark palette under a `dark` class and introduce a new "light but not white" default palette (the `MarketDataPanel` + `TerminalSection` primitives are already scoped to stay dark regardless of outer theme, so the embedded data-module pattern becomes the anchor — see `memory/project_market_data_panel.md`). Requires: Tailwind v4 `@theme` duplication for both modes, `prefers-color-scheme` + settings toggle, a full audit of every hardcoded hex (chart colors, chip tones, Recharts palettes, PDF/email templates) to survive the mode flip. Scope in a design doc before any code.
- [ ] **OpenAI voice API integration** — scaffolding started in Claude Co-Work. Two directions: (a) **output/read-aloud** — weekly briefing email read aloud, per-company deep dives narrated on Security Detail, maybe alert triage by voice; (b) **input/voice bug reporting** — in-app record button that captures the user describing a bug or UI gripe, transcribes via Whisper/Realtime, and files the result somewhere structured (GitHub issue? `docs/plans/BUGS.md` append? a new `bug_reports` table?). Decide capture destination + whether to also capture a screenshot at the moment of recording (likely yes — bug reports without visual context are weak). Scope: design doc + capture flow + first surface (probably Today view "speak the briefing" button as the lowest-risk first shot).
- [ ] **Privacy mode as default + Face ID / biometric unlock** — currently `vgs:privacyMode` defaults off and the eye-icon toggle flips freely. Want: default ON everywhere (mobile first, possibly desktop Electron too), and unlocking real numbers requires Face ID (iOS WebAuthn `navigator.credentials` or native Electron biometric prompt). Pieces: (1) flip `PrivacyProvider` default to `true` + migrate existing localStorage to opt-in masking; (2) gate the toggle behind a WebAuthn challenge on mobile (iOS Safari supports platform authenticators); (3) Electron side uses `Touch ID` via `node-mac-auth` or equivalent; (4) decide the auto-lock policy (re-mask after N minutes idle? on every app focus?). Potential cross-cut with the voice-API "deep dive" feature — voice output reading real numbers aloud should respect the same unlock state. Scope in a design doc; biometric plumbing on both iOS-Safari-over-Mesh and Electron-on-macOS is the risk surface.

---

## AI Gateway phases

- ✅ **Phase 1** — AI Gateway routing (commit `8c82c1e`)
- ✅ **Phase 2** — Workers AI downroute: alertSuggestion → Llama 3.3, newsletterLevelExtraction → Kimi K2.6 (`264bc73`)
- ✅ **Phase 3** — R2 PDF archival (`264bc73`, migration 033)
- ✅ **Phase 4** — Workers Cron hybrid shipped 2026-04-21 (`16ba5a7` Sessions A+B, `3e2c7af` Session C). Live at vanguard-skin-cron.isaac-3d1.workers.dev. Design doc: [2026-04-21-workers-cron-hybrid.md](2026-04-21-workers-cron-hybrid.md).

---

## Backlog themes (from off-repo roadmap)

- **Theme D — Level source performance attribution** (blocked on data): hit-rate + P&L by `source_author`. Wait until ~30+ alerts have fired.
- ✅ **Theme E — Chat broader company data** complete: E1 press releases (`272566d`), E2 full 8-K body (`6328a99`), E3 EDGAR 10-K/10-Q, E4 analyst coverage (`1bf65d3`) all shipped 2026-04-22.
- ✅ **Theme F1 — Research PDF knowledge base** shipped 2026-04-22 (migration 035, commit `6624382`).
- ✅ **Theme G — Chart entry/exit signals v1 + narrative layer** complete: pivot S/R shipped 2026-04-22 (`51fcc51`), Haiku one-sentence narratives shipped 2026-04-23 (`25016cf`, migration 040).
- ✅ **Theme I — Chat history persistence**: shipped across 2026-04-21 + 2026-04-22.
- **Theme J — Cleanup track** (partial): J1 ✅ + J3 ✅ shipped 2026-04-21. J2 (E2E browser tests, ~15 hr) deferred.
- **Theme K — Options Phase 2 end-to-end verification**: blocked on the user holding more than 1 option position at once.

---

## Deferred

- **E2E browser tests** (Theme J2, ~15 hr) — dedicated session.
- **Electron code signing** — `xattr -cr` workaround accepted for local-only distribution.
- **Finnhub surprise history** — free tier returns `[]`; revisit only if briefings feel thin.

---

## Known issues

- **WSH API error 10276** — "News feed is not allowed" (2026-04-07). May need TWS subscription time.
- **Holdings cost basis column "–" in Accounts tab** — data gap, not a code bug (tooltip explains).
- **`next build` data collection** fails with an existing `data/vanguard.db` on disk. TypeScript compilation still succeeds. All 10 DB-loading pages are `force-dynamic`, so this only affects build-time collection, not runtime.

---

## Active design docs

- [2026-04-15-cloudflare-saas-rewrite.md](2026-04-15-cloudflare-saas-rewrite.md) — SaaS v3 scoping (D1, TWS bridge, multi-tenant, ~7 months)
- [2026-04-21-mobile-today-view-decision.md](2026-04-21-mobile-today-view-decision.md) — shipped
- [2026-04-21-workers-cron-hybrid.md](2026-04-21-workers-cron-hybrid.md) — Phase 4, shipped 2026-04-21
