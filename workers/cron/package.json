{
  "name": "vanguard-skin-cron",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker cron — primary-path + fallback for Mac briefing/digest emails (Phase 4)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^2.0.0",
    "ai": "^6.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.0",
    "vitest": "^1.0.0",
    "wrangler": "^4.0.0"
  }
}
